---
phase: 09-postinstall-hooks-mechanism
plan: 02
type: execute
wave: 2
cross_ai: true
depends_on:
  - "09-01"
files_modified:
  - .claude/architecture.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REQ-codegraph-mcp-postinstall

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A real, disposable container proves the host-presence-aware CSV composition against genuine PATH detection — fake `claude`/`cursor-agent` executables planted on PATH before a fresh install, `codex`/`opencode` deliberately absent — exercised through the real production entry point `installer.engine.install_tool` against the real, committed `codegraph` registry entry, not a hand-built `Tool`/`Method` or a stubbed dispatch (ROADMAP Phase 9 SC#5, REQ-codegraph-mcp-postinstall, ONESHOT-RULES Rule 14)."
    - "The container run captures the exact composed argv the postinstall hook handed the real `Runner` (`[\"codegraph\", \"install\", \"--target\", \"claude,cursor\", \"--location\", \"global\", \"--yes\"]`) and confirms `codegraph`'s own fresh `github_release` install — a genuinely new install-adjacent mechanism this milestone introduces — succeeds end to end against a real filesystem inside the container, not merely a unit-test fake."
    - "The container run inspects the REAL resulting MCP config file(s) `codegraph install` wrote inside the container's own throwaway `$HOME` (e.g. `~/.claude.json`'s `mcpServers.codegraph` key) and asserts the real entry is actually present — proving the mechanism's real machine-visible effect, not merely that a subprocess exited 0 (cycle-1 cross-AI review HIGH 4). A second, separate container run with ALL FOUR fake host stubs absent proves no such config file is created at all — the real-filesystem half of the documented no-op."
    - "`logging_runner` wraps `installer.run.run_command` (raising `CommandError` on failure, exactly like every other executor's `Runner`), never a raw `subprocess.run(..., check=True)` call — so a captured failure inside the container exercises the SAME error path `_codegraph_mcp_register`'s `except CommandError` branch handles in production, not a different exception type that would silently bypass it (cycle-1 cross-AI review HIGH 4)."
    - "`.claude/architecture.md` documents the postinstall mechanism as a named, discoverable convention — mirroring the existing `smoke`-param section — so a future registry author finds this pattern before reinventing a templated command string or a new state-tracking database."
    - "`.planning/PROJECT.md`'s Key Decisions table records this phase's real decisions (the closed-dispatch-name field shape, the `cursor-agent` -> `cursor` id-mapping fix, the `--target auto` rejection, D-01's structural no-re-trigger limitation, and the Tier-3 container proof) in the same consolidated end-of-phase pattern Phases 5, 6, 7, and 8 already use."
  artifacts:
    - .claude/architecture.md
    - .planning/PROJECT.md
  key_links:
    - "The container's `install_tool(codegraph_tool, platform, runner=logging_runner)` call is the SAME production entry point `installer/app.py::run_wizard` uses in real life — no stubbed runner, no hand-built `Method`/`ExecContext` — closing ONESHOT-RULES Rule 14's requirement for real disposable-container evidence of a genuinely new install-adjacent mechanism this milestone introduces."
---

<objective>
Close Phase 9. Task 1 proves 09-01's postinstall mechanism against a real disposable
container (ONESHOT-RULES Rule 14) rather than trusting unit-test fakes alone — the
mechanism runs `codegraph install --target <csv> --location global --yes` for real,
against a real freshly-downloaded `codegraph` binary, with fake `claude`/`cursor-agent`
stubs planted on PATH before the install so the composed CSV can be verified against
genuine PATH detection rather than a monkeypatched `shutil.which`. Task 2 documents the
mechanism in `.claude/architecture.md` (mirroring the existing `smoke`-param section) and
consolidates this phase's decisions into `.planning/PROJECT.md`'s Key Decisions table,
mirroring the established end-of-phase pattern from Phases 5, 6, 7, and 8.

Purpose: REQ-codegraph-mcp-postinstall's Tier-3 real-machine-effect half (09-01 shipped
the mechanism and its unit-level proof; this plan supplies the real-container evidence
ONESHOT-RULES Rule 14 requires for any genuinely new install-adjacent behavior).

Scope boundary: this plan makes no production or test code changes. Task 1 is pure
verification (its evidence is recorded in the SUMMARY, per `human_verify_mode:
end-of-phase`'s consolidation design); Task 2 touches only `.claude/architecture.md` and
`.planning/PROJECT.md`.

Output: a recorded, verbatim Tier-3 container transcript proving the postinstall
mechanism's host-presence-aware composition and the fresh `codegraph` install both work
end to end against a real filesystem; a new "postinstall hooks" section in
`.claude/architecture.md`; four new Key Decisions rows in `.planning/PROJECT.md` with
`Outcome = Phase 9`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/ONESHOT-RULES.md
@.planning/phases/09-postinstall-hooks-mechanism/09-CONTEXT.md
@.planning/phases/09-postinstall-hooks-mechanism/09-RESEARCH.md
@.planning/phases/09-postinstall-hooks-mechanism/09-01-PLAN.md
@.planning/phases/08-ai-tier-catalog-expansion-uv-tool-executor/08-04-PLAN.md
@CLAUDE.md
@.claude/architecture.md
@installer/engine.py
@installer/postinstall.py
@installer/model.py
@installer/registry.toml

`.codegraph/` exists at the repo root. Use `codegraph_explore` (MCP tool) or
`codegraph explore "<symbols>"` (shell) BEFORE Grep/Read when locating code. Run
`codegraph index || codegraph init -i` once before starting.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier-3 verification — real install of `codegraph` plus its postinstall MCP-registration hook, against fake `claude`/`cursor-agent` stubs, in a disposable container</name>
  <files>None (verification-only; no production or test files change)</files>
  <precondition>colima is running (or started per ONESHOT-RULES Rule 14: `brew install docker` if missing, then `colima start`) before this task's `docker run` command executes.</precondition>
  <read_first>
    - ONESHOT-RULES.md Rule 14 in full — the Tier-3 disposable-container requirement for
      any genuinely new install-adjacent behavior this milestone introduces, and this
      project's own committed container recipe pattern (`colima` + the `docker` CLI, `curl
      -LsSf https://astral.sh/uv/install.sh | sh` for `uv`, never `pip install uv`).
    - `.planning/phases/08-ai-tier-catalog-expansion-uv-tool-executor/08-04-PLAN.md`
      Task 2 in full — the exact prior-art container recipe this task adapts: go through
      the real `installer.engine.install_tool` entry point (never a hand-built
      `Method`/`ExecContext`, never a stubbed executor), install `curl`/`tar`/
      `ca-certificates` before anything else, put `uv`'s installed location on `PATH`, and
      record every `*_OUTCOME` print line verbatim in the SUMMARY.
    - `installer/engine.py::install_tool` and `installer/postinstall.py` as 09-01 left
      them — confirm the real, unmodified dispatch call site and the real
      `_codegraph_mcp_register` hook this container run exercises.
    - `installer/registry.toml`'s real `codegraph` entry (with 09-01's `postinstall =
      "codegraph-mcp-register"` field and dated comment) — the entry this container
      installs, unmodified.
  </read_first>
  <action>
    Use this exact recipe, adapted from 08-04-PLAN.md Task 2's committed pattern:

    ```bash
    docker run --rm -v "$(pwd)":/repo:ro -w /repo python:3.13-slim bash -c '
      set -eux
      apt-get update -qq && apt-get install -y -qq curl tar ca-certificates
      curl -LsSf https://astral.sh/uv/install.sh | sh
      export PATH="$HOME/.local/bin:$PATH"
      uv --version

      # Plant fake claude/cursor-agent executables BEFORE installing codegraph, so the
      # postinstall hook this milestone ships sees them present at the exact moment
      # codegraph'"'"'s OWN successful install fires it (D-01: single-direction trigger --
      # only codegraph'"'"'s own fresh install dispatches this, never a later event).
      # codex/opencode are deliberately left absent to prove the composed CSV excludes
      # them, not merely that it includes present hosts.
      mkdir -p /usr/local/bin "$HOME/.cursor"
      printf "#!/bin/sh\necho fake-claude \"\$@\"\n" > /usr/local/bin/claude
      printf "#!/bin/sh\necho fake-cursor-agent \"\$@\"\n" > /usr/local/bin/cursor-agent
      chmod +x /usr/local/bin/claude /usr/local/bin/cursor-agent
      which claude cursor-agent
      ! which codex >/dev/null 2>&1
      ! which opencode >/dev/null 2>&1

      python3 - <<PY
import json
import os
import sys

sys.path.insert(0, "/repo")
from installer.engine import install_tool
from installer.model import load_tools
from installer.platform import Platform
from installer.run import run_command
from installer.run import CommandError

CAPTURED = []


def logging_runner(cmd):
    CAPTURED.append(cmd)
    run_command(cmd)  # the SAME Runner seam every other executor uses; raises CommandError


tools = {t.id: t for t in load_tools("/repo/installer/registry.toml")}
codegraph = tools["codegraph"]
platform = Platform(os="debian", arch="amd64", immutable=False, has_brew=False)
outcome = install_tool(codegraph, platform, runner=logging_runner, tools=tools)
print("CODEGRAPH_OUTCOME", outcome.status, outcome.method_kind, outcome.verified)
assert outcome.status.value == "installed", outcome.errors
assert outcome.method_kind == "github_release"

postinstall_calls = [c for c in CAPTURED if c[:2] == ["codegraph", "install"]]
print("POSTINSTALL_ARGV", postinstall_calls)
assert len(postinstall_calls) == 1, postinstall_calls
assert postinstall_calls[0] == [
    "codegraph",
    "install",
    "--target",
    "claude,cursor",
    "--location",
    "global",
    "--yes",
], postinstall_calls[0]
print("POSTINSTALL_WARNING", outcome.postinstall_warning)
assert outcome.postinstall_warning is None, outcome.postinstall_warning

# HIGH 4 fix: inspect the REAL resulting MCP config file, not merely the captured argv.
claude_config_path = os.path.expanduser("~/.claude.json")
with open(claude_config_path) as f:
    claude_config = json.load(f)
print("CLAUDE_MCP_ENTRY", claude_config.get("mcpServers", {}).get("codegraph"))
assert "codegraph" in claude_config.get("mcpServers", {}), claude_config
PY
      export PATH="$HOME/.local/opt/codegraph/bin:$PATH"
      codegraph --version || true
      echo TIER3_OK
    '
    ```

    Run a SECOND, separate container invocation of the identical recipe with the
    `claude`/`cursor-agent` stub-planting block removed entirely (no fake executables on
    PATH at all, `codex`/`opencode` still absent) — the all-hosts-absent case. Its
    `python3` block only needs: run `install_tool` the same way, assert
    `outcome.postinstall_warning is None` and `CAPTURED == []` (the hook never called the
    runner at all), and assert `not os.path.exists(os.path.expanduser("~/.claude.json"))`
    — proving the real-filesystem half of the documented no-op (cycle-1 review HIGH 4):
    zero hosts present means codegraph's own `install` subcommand is never invoked, so no
    MCP config file is ever created, not merely that the mocked argv was empty in a unit
    test.

    This exercises the real `installer.app`-facing path: registry load ->
    `resolve_methods` -> the real `github_release` executor -> `InstallOutcome` ->
    `installer/postinstall.py::_codegraph_mcp_register` -> the real `codegraph` binary's
    own `install` subcommand, writing into this THROWAWAY container's own `$HOME` (never
    the real host machine, per ONESHOT-RULES Rule 5 — the container is `--rm` and the
    volume mount is `:ro`).

    Record in the SUMMARY, verbatim: the full container transcript for BOTH runs,
    especially the `CODEGRAPH_OUTCOME`, `POSTINSTALL_ARGV`, `POSTINSTALL_WARNING`, and
    `CLAUDE_MCP_ENTRY` lines, and the `codegraph --version` output.

    If `outcome.postinstall_warning` is non-`None` (meaning the real `codegraph install`
    subcommand itself failed inside the container — e.g. because a target's config
    directory does not exist yet), this is real evidence to investigate, not to force a
    pass on: inspect the printed warning detail, create any missing directory the error
    names (the recipe already pre-creates `$HOME/.cursor`; extend it if another target
    needs one), and retry once. A genuine checksum mismatch, an `outcome.status` other
    than `installed`, a missing `mcpServers.codegraph` entry, or a failure that persists
    after one retry and is unrelated to a transient network issue IS the destructive-
    anomaly-shaped finding ONESHOT-RULES Rule 9 allows a stop for — record it and report
    rather than writing a SUMMARY that contradicts the evidence.
  </action>
  <verify>
    <automated>bash -c '
      set -eux
      docker run --rm -v "$(pwd)":/repo:ro -w /repo python:3.13-slim bash -c "
        set -eux
        apt-get update -qq && apt-get install -y -qq curl tar ca-certificates
        curl -LsSf https://astral.sh/uv/install.sh | sh
        export PATH=\"\$HOME/.local/bin:\$PATH\"
        mkdir -p /usr/local/bin \"\$HOME/.cursor\"
        printf \"#!/bin/sh\necho fake-claude \\\"\\\$@\\\"\n\" > /usr/local/bin/claude
        printf \"#!/bin/sh\necho fake-cursor-agent \\\"\\\$@\\\"\n\" > /usr/local/bin/cursor-agent
        chmod +x /usr/local/bin/claude /usr/local/bin/cursor-agent
        python3 -c \"
import sys; sys.path.insert(0, \\\"/repo\\\")
from installer.engine import install_tool
from installer.model import load_tools
from installer.platform import Platform
from installer.run import run_command
tools = {t.id: t for t in load_tools(\\\"/repo/installer/registry.toml\\\")}
outcome = install_tool(tools[\\\"codegraph\\\"], Platform(os=\\\"debian\\\", arch=\\\"amd64\\\", immutable=False, has_brew=False), runner=run_command, tools=tools)
assert outcome.status.value == \\\"installed\\\" and outcome.postinstall_warning is None
import json, os
with open(os.path.expanduser(\\\"~/.claude.json\\\")) as f: cfg = json.load(f)
assert \\\"codegraph\\\" in cfg.get(\\\"mcpServers\\\", {})
print(\\\"TIER3_GATE_OK\\\")
\"
      "
    ' 2>&1 | tail -5</automated>
    <fails_when>the container cannot install/build `uv`; `install_tool`'s assertions fail
    (`status != "installed"`, wrong `method_kind`, more or fewer than one captured
    `codegraph install` call, or an argv that does not exactly match the expected CSV); the
    real `~/.claude.json` inside the container lacks a `mcpServers.codegraph` entry; the
    all-hosts-absent run leaves a `~/.claude.json` file behind at all; `TIER3_GATE_OK` is
    never printed; or a non-`None` `postinstall_warning` persists after one retry. This is a
    REAL automated gate (cycle-1 review HIGH 4), not the unconditional `echo` this plan
    originally shipped.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the full container transcript verbatim for BOTH runs, including
      `CODEGRAPH_OUTCOME`, `POSTINSTALL_ARGV` (exactly matching the expected
      `claude,cursor` CSV), `POSTINSTALL_WARNING` (`None`), and `CLAUDE_MCP_ENTRY` (the
      real `mcpServers.codegraph` config value) for the two-hosts-present run; and the
      all-hosts-absent run's evidence that no `codegraph install` call happened and no
      `~/.claude.json` file was created.
    - The `<verify>` automated gate exits 0 and prints `TIER3_GATE_OK`.
    - No production or test file changes in this task's commit (or no commit at all, if
      the evidence is folded into Task 2's commit) — this task is pure verification.
  </acceptance_criteria>
  <done>The postinstall mechanism's host-presence-aware CSV composition, and `codegraph`'s
  own fresh `github_release` install, are both proven to actually work end to end in a
  real disposable container against a real filesystem — including the real resulting MCP
  config-file entry, and a real no-config-file-created zero-hosts case — not merely proven
  structurally correct via unit tests with a monkeypatched `installer.status.is_installed`
  — closing ONESHOT-RULES Rule 14's requirement for this milestone's one genuinely new
  install-adjacent mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Document the postinstall mechanism in `.claude/architecture.md` and consolidate Phase 9's decisions into `.planning/PROJECT.md`</name>
  <files>.claude/architecture.md, .planning/PROJECT.md</files>
  <read_first>
    - `.claude/architecture.md`'s existing sections on `smoke` (inside "Tier is a browsing
      label") and "Phase 3: install, uninstall, and tweak lifecycle" — the exact
      documentation shape (what the mechanism is, why it is closed-dispatch, what it
      deliberately does NOT do) this task's new section mirrors.
    - `.planning/PROJECT.md`'s Key Decisions table in full — the existing three-column
      shape and its most recent Phase 8 rows, the pattern this task extends (mirroring
      08-04-PLAN.md's own Task 3).
    - This phase's two plans (09-01, 09-02 Task 1, both already executed by the time this
      task runs) — the concrete decisions and evidence each one produced, which this task
      records in the ledger without re-deriving them.
  </read_first>
  <action>
    Add a new section to `.claude/architecture.md`, after the existing "Phase 3: install,
    uninstall, and tweak lifecycle" section (or after whichever is the LAST phase-specific
    section currently in the file — append after it, do not insert in the middle), titled
    `## Phase 9: postinstall hooks`. State, in flowing prose: `Tool.postinstall: str |
    None` names a hook in `installer/postinstall.py::POSTINSTALL_HOOKS`, a closed,
    code-owned dispatch table mirroring the existing `smoke` param
    (`installer/model.py::POSTINSTALL_HOOK_NAMES` validates the name at load time exactly
    like `SMOKE_CHECK_NAMES`); the registry can never carry a literal postinstall command,
    only a hook NAME from a closed set, so a registry edit alone can never introduce
    arbitrary post-install execution — a deliberate departure from
    REQ-postinstall-field's literal "inline command string" framing, chosen because the
    one proving case (`codegraph`'s MCP registration) cannot be expressed as a static
    string: its `--target` value depends on which agent hosts are live on the machine at
    install time. State that `installer/engine.py::install_tool` dispatches a declared
    `postinstall` hook exactly once, immediately after the specific `Method` that just
    reported success, never on the `ALREADY_INSTALLED` early-return path — the structural
    reason D-01's "single-direction trigger, no re-trigger on a later unrelated event" is
    a property of the code, not a guard that could rot. State that a hook's failure is
    carried on `InstallOutcome.postinstall_warning` and never turns a successful install
    into a failed one. State that idempotency is a live check only
    (`status.is_installed`, `guard_status`, `has_managed_block`'s existing convention):
    `codegraph`'s own hook calls `installer.status.is_installed` directly on the four
    agent-host catalog `Tool` objects (threaded through via `install_tool`'s `tools`
    parameter, never a hook-local `shutil.which` re-implementation of that seam), mapping
    `cursor-agent` to codegraph's own `cursor` target id — a real, live-verified id
    mismatch, not a naming choice — and NEVER passes `--target auto`, because codegraph's
    own `--target auto` resolution silently falls back to registering `claude` when it
    detects zero installed hosts. State that the dispatch call is Method-aware (receives
    the succeeded `Method`) and isolated in its own `try/except Exception` at the call
    site, structurally separate from the method ladder's own exception handling, so an
    unexpected hook exception can never be misattributed to the method's own execution or
    turn a completed install into `FAILED`.

    Then add four new rows to `.planning/PROJECT.md`'s Key Decisions table (after the most
    recent Phase 8 rows, before the closing `---`), following the exact three-column shape
    the table already uses, each with `Outcome` = `Phase 9` (the literal string, no
    suffix):

    1. Decision: `Tool.postinstall` is a closed dispatch-hook NAME (mirroring the existing
       `smoke` param), not a literal inline command string or a `postinstall_script`
       file — a deliberate, formally-reconciled deviation from REQ-postinstall-field's
       literal inline/script wording (cycle-1 cross-AI review HIGH 1). Dispatch is
       Method-aware in fact (the succeeded `Method` is passed into `run_postinstall`/every
       hook), not merely by call-site scope, satisfying REQ-postinstall-execution-timing's
       literal wording. Rationale: `codegraph`'s one proving case needs a `--target` value
       computed from live host presence, which cannot be expressed as a static registry
       string without inventing a new TOML templating micro-language for a single tool's
       one-off need; a closed, code-owned dispatch table also closes the supply-chain-shaped
       risk of registry data becoming shell text. See 09-01-PLAN.md/09-01-SUMMARY.md.
    2. Decision: codegraph's postinstall hook maps this project's catalog id
       `cursor-agent` to codegraph's own `--target` id `cursor`, and never passes
       `--target auto`. Host presence is checked via `installer.status.is_installed` on
       real, loaded catalog `Tool` objects — never a hook-local `shutil.which`
       re-implementation of that same seam (cycle-1 cross-AI review HIGH 2). Rationale:
       live reading of codegraph v1.2.0's own
       `installer/targets/registry.js::resolveTargetFlag` found `--target auto` silently
       falls back to registering `claude` when it detects zero installed hosts — a real
       correctness gap 09-RESEARCH.md found that this mechanism must never trigger. See
       09-01-PLAN.md/09-01-SUMMARY.md and 09-RESEARCH.md.
    3. Decision: postinstall dispatch sits only inside `install_tool`'s per-method success
       branch, never on the `ALREADY_INSTALLED` early return, and is additionally wrapped
       in its own isolated `try/except Exception` at the call site — structurally separate
       from the method ladder's own `except CommandError | ExecutorError | VersionError`
       block — so an unexpected hook exception can never be misattributed to the method's
       own execution or turn a completed install into `FAILED` (cycle-1 cross-AI review
       HIGH 3). Rationale: this is what makes D-01's accepted limitation (codegraph's MCP
       registration does not re-trigger when a NEW agent host is installed later) true by
       construction rather than a special case that could rot; a known, accepted gap
       recorded rather than silently worked around. See 09-CONTEXT.md D-01 and
       09-01-SUMMARY.md.
    4. Decision: the postinstall mechanism was proven in a real disposable container
       (ONESHOT-RULES Rule 14), with fake `claude`/`cursor-agent` executables planted on
       PATH before installing the real, committed `codegraph` registry entry through the
       actual production `install_tool` entry point — including inspecting the REAL
       resulting `mcpServers.codegraph` config-file entry, and a second all-hosts-absent
       run proving no such file is created at all (cycle-1 cross-AI review HIGH 4).
       Rationale: unit-test fakes alone cannot prove the composed `codegraph install
       --target claude,cursor --location global --yes` argv is what a real install
       actually executes end to end against a real filesystem, nor that it actually writes
       the config entry it claims to — the same evidentiary bar Phase 8's `graphify`/`rtk`
       container proof set. See 09-02-PLAN.md/09-02-SUMMARY.md.

    Do not alter any existing row or the table's column order in either file.

    Finally, bump `.planning/PROJECT.md`'s closing `*Last updated: ...*` footer to the
    actual date this task runs (`date +%Y-%m-%d`), keeping the file's existing three-part
    shape, e.g. `*Last updated: {date} after Phase 9 Postinstall Hooks Mechanism
    (09-01..09-02)*`.
  </action>
  <verify>
    <automated>grep -q "## Phase 9: postinstall hooks" .claude/architecture.md && [ "$(grep -cE '\| Phase 9 \|$' .planning/PROJECT.md)" -eq 4 ] && grep -Eq '^\*Last updated:.*Phase 9' .planning/PROJECT.md</automated>
    <fails_when>`.claude/architecture.md` lacks the new section heading; `.planning/PROJECT.md`
    does not contain exactly four table rows whose Outcome cell is the literal `Phase 9`
    (matched precisely as a row ending in `| Phase 9 |`); or the `*Last updated: ...*`
    footer still lacks a `Phase 9` citation.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `<automated>` command above exits 0.
    - `.planning/PROJECT.md`'s Key Decisions table gains exactly four new rows, each with `Outcome` = `Phase 9` (unsuffixed).
    - `git diff -- .planning/PROJECT.md` shows only the four new table rows plus the `*Last updated: ...*` footer line changed — no existing decision row altered, reordered, or removed.
    - `git diff -- .claude/architecture.md` shows only the new appended `## Phase 9: postinstall hooks` section — no existing section altered, reordered, or removed.
    - `make validate && make test` exits 0 on this task's own commit.
  </acceptance_criteria>
  <done>`.claude/architecture.md` documents the postinstall mechanism as a discoverable
  architectural convention; `.planning/PROJECT.md`'s Key Decisions table records all four
  of this phase's real decisions, each condensed to a short statement plus a
  cross-reference; its `Last updated` footer reflects Phase 9's completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A disposable Tier-3 container's downloaded, executed real `codegraph` binary -> the container's own throwaway `$HOME` | Real subprocess execution and real file writes, entirely confined to a `--rm` container torn down immediately after the check (`:ro` source mount); never touches this host machine's real files or shell config (ONESHOT-RULES Rule 5/Rule 14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06 | Tampering | Fake `claude`/`cursor-agent` stub executables planted on PATH inside the Tier-3 container | low | accept | The stubs exist only inside the disposable, `--rm` container, torn down immediately after the check; they are never written to, or executed on, the real host machine. |
| T-09-07 | Denial of Service | A genuinely new install-adjacent mechanism (postinstall dispatch) shipping with only unit-test-fake evidence, never proven against a real filesystem | high (if unverified) | mitigate | Closed by this plan's Task 1: a real container run through the actual `installer.engine.install_tool` production entry point, against the real committed `codegraph` registry entry, per ONESHOT-RULES Rule 14. |

</threat_model>

<verification>
Phase-level checks, run after both tasks — this is Phase 9's final plan, so also a full
phase close-out check:

- Task 1's container evidence is recorded verbatim in its SUMMARY (`CODEGRAPH_OUTCOME`,
  `POSTINSTALL_ARGV`, `POSTINSTALL_WARNING`).
- `grep -q "## Phase 9: postinstall hooks" .claude/architecture.md` exits 0.
- `[ "$(grep -cE '\| Phase 9 \|$' .planning/PROJECT.md)" -eq 4 ]` exits 0.
- `make validate && make test` exits 0 on the committed tree.
- `uv run pytest -q` (full suite) exits 0 — no regression from either task (Task 1 makes
  no code changes; Task 2 touches only markdown).
- The SUMMARY confirms every requirement this phase owns (REQ-postinstall-field,
  REQ-postinstall-execution-timing, REQ-postinstall-idempotency-live-check,
  REQ-postinstall-noninteractive-only, REQ-codegraph-mcp-postinstall) is satisfied by a
  specific plan/task, per `human_verify_mode: end-of-phase`'s consolidation design
  (ONESHOT-RULES Rule 14).
</verification>

<success_criteria>
Completes ROADMAP Phase 9's numbered success criteria:

5. (SC#5, real-machine half) After installing `codegraph` in a real disposable container,
   its MCP server registers for every already-installed agent host (proven here with fake
   `claude`/`cursor-agent` stubs) and the composed invocation excludes `codex`/`opencode`
   when they are genuinely absent — closing the real-container half of SC#5 that 09-01's
   unit-level proof could not supply alone.

This plan closes Phase 9: SC#1-SC#4 and the mechanism half of SC#5 (09-01) plus the real-
container half of SC#5 (this plan) are all satisfied, each by a specific plan whose
SUMMARY carries the evidence.
</success_criteria>

<output>
Create `.planning/phases/09-postinstall-hooks-mechanism/09-02-SUMMARY.md` when done.
</output>
