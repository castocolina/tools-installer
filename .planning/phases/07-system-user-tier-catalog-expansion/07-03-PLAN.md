---
phase: 07-system-user-tier-catalog-expansion
plan: 03
type: execute
wave: 3
cross_ai: true
depends_on:
  - "07-02"
files_modified:
  - installer/registry.toml
  - tests/test_registry.py
  - installer/enums.py
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REQ-terminal-emulator-entries

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`kitty` and `wezterm` both appear as `tier=\"user\"` catalog entries and install cleanly on macOS via a Homebrew **cask** (corrected from REQUIREMENTS.md's own speculative 'both in homebrew-core' parenthetical, which 07-RESEARCH.md's live API check disproved: both are casks, no formula exists for either) (ROADMAP Phase 7 SC#2)."
    - "Both tools have a verified Linux path: `kitty` via native distro packages (dnf/apt/pacman) on non-atomic Linux; `wezterm` via `pacman` on Arch and a checksum-verified `github_release` AppImage download on Debian/Fedora, using the exact `{asset}.sha256` sidecar naming this plan confirmed live against the real GitHub Releases API — resolving 07-RESEARCH.md's Open Question 2, not left as a guess (ROADMAP SC#2)."
    - "`wezterm`'s AppImage `github_release` path is proven installable and runnable, not merely structurally correct: a Tier-3 disposable container actually resolves the live release tag, downloads the AppImage and its `.sha256` sidecar through this project's own, unmodified `installer/download.py::install_download`, verifies the checksum, and successfully runs `wezterm --version` — closing cross-AI review's MEDIUM finding (cycle 2) that method-kind/param assertions alone don't prove the AppImage is installable and runnable."
    - "`kitty` has no method at all on immutable Linux (Bazzite) — a real, pre-existing gap in `installer/download.py`'s archive extraction (`.txz`/XZ assets, hardcoded `tar -xzf`/gzip-only) — and this is accepted and recorded rather than silently absent or papered over with a broken method, per 07-RESEARCH.md Open Question 3's recommendation and because REQ-linux-bazzite-shell-parity names only `zsh`/`oh-my-zsh`, not `kitty`/`wezterm`."
    - "`wezterm`'s AppImage `github_release` method, by contrast, DOES resolve on immutable Linux (Bazzite is `os=\"fedora\", immutable=True`, and `github_release`-kind methods are never skipped by `installer/resolve.py`'s immutable-Linux native-manager check) — a real, tested, unplanned bonus this plan documents rather than assumes."
    - "`wezterm`'s Linux-arm64 scope is an explicit, narrowly-reasoned decision, not a silent gap: Arch arm64 is ALREADY covered by the existing, arch-unrestricted `pacman` method; only Debian/Fedora arm64 lack a path, and whether that gap can be closed depends on a live GitHub Releases API check this planning session could not perform (no network access in the planning sandbox) — the executor performs that one live check and either adds a fourth `github_release` method (if a real Linux-arm64 AppImage asset exists) or narrows and corrects the registry comment's phrasing (if not), per cross-AI review's MEDIUM finding (cycle 2)."
    - "A new `terminal` catalog category exists (`Category` enum, `installer/enums.py`, plus its `[[category]]` blurb in `installer/registry.toml`) rather than mislabeling terminal emulators under an existing, semantically-wrong category (`editor`/`shell`) — the smallest correct addition, mirroring the sixteen already-shipped categories' exact shape."
  artifacts:
    - installer/registry.toml
    - tests/test_registry.py
    - installer/enums.py
    - .planning/PROJECT.md
  key_links:
    - "`wezterm`'s `github_release` method's `checksum = \"{asset}.sha256\"` is read by the existing, unmodified `installer/download.py::_resolve_target`/`expected_sha256` — the multi-line `<hash>  <filename>` sidecar format this plan verified live (`WezTerm-{tag}-Ubuntu20.04.AppImage.sha256`) is already handled by `checksums.py::expected_sha256`'s existing `_names_match` branch, zero new parsing code."
    - "`wezterm`'s `raw = true` on its `github_release` method routes through `installer/download.py`'s existing raw-asset branch (`_install_unverified`/`_place_verified`'s `if method.params.get(\"raw\") is True` short-circuit), which never calls the hardcoded `tar -xzf` this plan's `kitty` comment documents as broken for `.txz` — this is WHY the AppImage path was chosen for `wezterm`'s Linux fallback instead of a plain archive download."
    - "The new `terminal` `Category` enum member is read by the existing, unmodified `installer/model.py::load_categories`'s `_parse_enum(Category, cat_id, ...)` validation — the same mechanism every other category already goes through."
---

<objective>
Add `kitty` and `wezterm` as verified `tier="user"` terminal-emulator entries — ROADMAP
Phase 7 SC#2 — using a Homebrew **cask** on macOS (correcting REQUIREMENTS.md's own
"both in homebrew-core" parenthetical, which this session's live brew API check
disproved: `formula/kitty.json`/`formula/wezterm.json` both `404`, `cask/*.json` both
`200`) and a per-tool researched Linux path: native distro packages for `kitty`,
`pacman`-plus-checksummed-AppImage for `wezterm`. Close 07-RESEARCH.md's two remaining
Open Questions with live verification performed this session (not left as guesses):
Open Question 2 (the exact `.sha256` sidecar naming for `wezterm`'s AppImage — confirmed
live against the real GitHub Releases API and its actual sidecar content) and Open
Question 3 (whether `kitty`'s immutable-Bazzite gap should be accepted-and-documented
or should prompt a `download.py` change — accepted, per the research's own
recommendation and because no locked requirement names `kitty` for Bazzite parity).

Purpose: REQ-terminal-emulator-entries — ROADMAP Phase 7 SC#2. This plan also closes
out the phase: it consolidates every decision made across 07-01/07-02/07-03 into
`.planning/PROJECT.md`'s Key Decisions table in one pass, mirroring Phase 6's
end-of-phase consolidation pattern.

Decisions implemented:
- 07-RESEARCH.md's cask-not-formula correction (Common Pitfall: "Assuming
  kitty/wezterm are 'in homebrew-core'") — both use `kind="cask"`, not `kind="brew"`,
  on macOS.
- 07-RESEARCH.md's Alternatives Considered: `wezterm`'s Linux fallback uses the
  AppImage `github_release` with `raw=true` (not a plain archive `github_release`,
  which would need `installer/download.py`'s hardcoded `tar -xzf` — gzip-only, and
  `wezterm`'s per-distro Linux assets are `.tar.xz`, so a non-raw archive method would
  hit the exact same extraction gap `kitty`'s comment documents). `raw=true` sidesteps
  archive extraction entirely.
- Open Question 3's recommendation, adopted explicitly: `kitty` has NO method on
  immutable Linux this phase — native packages exist but are skipped there by design,
  and no `github_release` fallback is added because `installer/download.py`'s
  extraction cannot open `.txz`/XZ assets (hardcoded `tar -xzf`, gzip-only) — this is
  recorded as an accepted, tested gap, not a silent omission, and not a scope-widening
  fix to `download.py` (no locked requirement names `kitty` for Bazzite parity;
  07-RESEARCH.md's own recommendation is "accept and document" over a speculative
  `-xzf` -> `-xf` change with no requirement demanding it).

Cross-AI review (codex, cycle 2) found four further gaps in this revision, plus
confirmed the phase-closeout claim below needed rewriting once 07-02's D-01/macOS-
version work is genuinely complete (HIGH — see the `<success_criteria>` rewrite at
the end of this plan). MEDIUM: the AppImage path was proven only structurally
(method-kind and param assertions), never actually downloaded, checksum-verified,
and executed. MEDIUM: Linux arm64 has no WezTerm path despite arm64 being a
supported project architecture, with no explicit scope decision recorded. LOW: the
required `kitty` registry comment self-contradicted on why no Linux cask fallback
exists (blaming a `404` — the ABSENCE of a formula — for a gap that is actually
caused by this project's own `_applies` policy blocking every cask on non-macOS
regardless of what Homebrew ships upstream). LOW: the six new `PROJECT.md` decision
rows duplicated research/plan/summary content at excessive length. This revision
adds a real Tier-3 container check that downloads, checksum-verifies, and runs
`wezterm --version` (Task 1, new PART G); records an explicit, narrowly-scoped
arm64 decision — Arch arm64 is already covered by the unrestricted `pacman` method,
and the executor performs one live GitHub Releases API check (which this planning
session could not — no network access in the sandbox) to decide whether Debian/
Fedora arm64 gets a real method or a corrected, narrower accepted-gap comment;
rewrites the `kitty` comment to state its two independent, non-contradictory
reasons without conflating them; and condenses all six `PROJECT.md` decision rows
to short, cross-referenced statements (Task 2).

Scope boundary: this plan does not modify `installer/download.py`, `installer/model.py`,
`installer/resolve.py`, or `installer/checksums.py` — `wezterm`'s AppImage path and
`kitty`'s accepted gap both rely entirely on existing, already-tested mechanisms
(`raw=true`, `expected_sha256`'s multi-line sidecar parsing, the immutable-Linux native-
manager skip) — Task 1's new Tier-3 check EXERCISES these mechanisms for real, it does
not modify them. The one genuinely new piece of production code is a single `Category`
enum member (`installer/enums.py`) — the smallest correct fix for labeling terminal
emulators, not a mislabel into an existing, semantically-wrong category.

Output: a new `terminal` category (`installer/enums.py` + `installer/registry.toml`
`[[category]]` blurb); `kitty` and `wezterm` `[[tool]]` blocks, each with a dated
`# Verified` comment (the `kitty` comment's self-contradiction corrected); new tests
in `tests/test_registry.py`; a Tier-3 container transcript proving `wezterm`'s
AppImage path installs and runs for real; an explicit, narrowly-scoped Linux-arm64
decision (a fourth method if a live check finds a real asset, or a corrected comment
if not); the `user` tier tripwire count updated to 38; a consolidated, condensed Key
Decisions update in `.planning/PROJECT.md` covering the whole phase, including the
now-fully-resolved D-01/macOS-version outcome.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@.planning/phases/07-system-user-tier-catalog-expansion/07-CONTEXT.md
@.planning/phases/07-system-user-tier-catalog-expansion/07-RESEARCH.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@installer/registry.toml
@installer/enums.py
@installer/download.py
@installer/checksums.py
@tests/test_registry.py

`.codegraph/` exists at the repo root. Use `codegraph_explore` (MCP tool) or
`codegraph explore "<symbols>"` (shell) BEFORE Grep/Read when locating code. Run
`codegraph index || codegraph init -i` once before starting.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `terminal` category, `kitty` and `wezterm` registry entries</name>
  <precondition>colima is running and `docker info` succeeds (Tier-3 container runtime per ONESHOT-RULES Rule 14, needed for PART G's real AppImage download/checksum/execute check) — run `colima start` first if not; already verified working on this machine per Phase 6's SUMMARY, not a Rule 5 violation. Also requires live network access to `api.github.com` and `github.com` release asset URLs (for PART C's arm64 check and PART G's real download).</precondition>
  <files>installer/registry.toml, installer/enums.py, tests/test_registry.py</files>
  <read_first>
    - 07-RESEARCH.md's "Standard Stack -> Core" rows for `kitty`/`wezterm`, "Pattern 3:
      `raw=true` single-file GitHub release", and "Common Pitfall 2: `kitty`'s Linux
      release assets are `.txz`" in full — the exact verified facts (cask tokens/
      versions, no Linux/macOS formula for either, `kitty`'s `.txz` Linux assets vs.
      `wezterm`'s AppImage) and the reasoning for each method choice.
    - `installer/enums.py` in full (81 lines) — the `Category` `StrEnum`'s exact
      shape (`PACKAGE_MANAGER = "pkg-mgr"`, ... `DIAGRAM = "diagram"`) — this task adds
      one member, `TERMINAL = "terminal"`, after `DIAGRAM`, in the same style.
    - `installer/registry.toml` lines 1-58 (the `[[category]]` table) — the exact
      `id`/`desc` shape every category blurb already uses; this task appends one more
      block after the `diagram` category, before the first `[[tool]]`.
    - `installer/registry.toml` lines 1706-1750 (`vscode`/`jetbrains-toolbox` — the
      real, already-shipped `cask`+`app` shape, including the arch-split `app`-kind
      methods `vscode` uses for its non-cask fallback, which `kitty`/`wezterm` do NOT
      need since both ship one universal cask each) and the `podman`/`gitleaks` entries
      for the native-Linux-methods-plus-no-fallback shape `kitty` mirrors.
    - `installer/download.py` lines 96-150, 190-209 (`install_download`,
      `_install_unverified`, `_place_verified`) — confirms `raw=true` never reaches the
      `tar -xzf` calls at lines 146/207 (it goes straight to `curl -o <link> && chmod
      +x`), which is exactly why `wezterm`'s Linux fallback uses `raw=true` on a
      single-file AppImage rather than a plain archive method.
    - `installer/checksums.py` in full (59 lines) — `expected_sha256`'s multi-line
      `<hash>  <name>` branch (`_names_match`) is what makes `checksum =
      "{asset}.sha256"` work against the real sidecar content this plan verified live
      (`34010a07...  WezTerm-20240203-110809-5046fc22-Ubuntu20.04.AppImage`) — no new
      parsing code needed, confirming 07-RESEARCH.md's Open Question 2.
    - `tests/test_registry.py` lines 942-953 (`test_every_used_category_has_a_blurb`,
      `test_every_category_blurb_is_used_by_a_tool`) — these two existing tests
      self-verify the new `terminal` category needs no dedicated test of its own, as
      long as this task adds both the `[[category]]` blurb AND at least one tool using
      it in the same commit.
    - `installer/resolve.py` lines 42-44 (`_applies`'s `cask` branch: `return
      platform.os == "macos" and platform.has_brew`) — re-confirm this project blocks
      EVERY `kind="cask"` method on any non-macOS platform unconditionally, regardless
      of what Homebrew's own cask metadata ships for that platform upstream — the real
      reason `kitty`'s Linux path here is native distro packages, not a stale "no
      Linux cask exists" claim cross-AI review (codex, cycle 1) flagged as
      contradicted by `formulae.brew.sh/api/cask/kitty.json`'s own `variations` object
      (re-fetched this session: it declares real `x86_64_linux`/`arm64_linux`
      artifacts).
    - `tests/test_registry.py` line 841 (`SIDECAR_VERIFIED = {"rg", "starship", "ruff",
      "deno", "tealdeer"}`) and `test_sidecar_verified_tools_declare_checksums`
      (immediately below it) — the existing generic "every `github_release` method for
      a tool in this set must carry a `{asset}`-templated `checksum`" invariant this
      task extends by adding `"wezterm"` to the set, per cross-AI review's suggestion.
    - `installer/download.py`'s `ExecContext`, `install_download`, `_resolve_target`
      (lines 21-116) and `installer/versions.py`'s `TagResolver`/`resolve_github_tag`
      — the real, unmodified functions PART G's Tier-3 check calls directly (not a
      lookalike shell reproduction, per cross-AI review, cycle 2, MEDIUM): confirmed
      this session that `installer.download`'s full transitive import graph
      (`checksums`, `executors`, `locations`, `model`, `platform`, `run`, `versions`,
      `assets`, `guards`, `enums`) is 100% Python-stdlib, so no third-party package
      install is needed inside the Tier-3 container to exercise it for real.
  </read_first>
  <action>
    Write the tests first (RED), confirm they fail, then add the registry/enum
    content (GREEN).

    PART A — add the category. In `installer/enums.py`, add `TERMINAL = "terminal"`
    to the `Category` `StrEnum`, directly after `DIAGRAM = "diagram"`. In
    `installer/registry.toml`, add a `[[category]]` block after the existing `diagram`
    category (before the first `[[tool]]`): `id = "terminal"`, `desc = "Terminal
    emulator apps for daily interactive shell use"`.

    PART B — add the `kitty` entry:

    `id = "kitty"`, `name = "kitty"`, `category = "terminal"`, `cmd = "kitty"`,
    `priority = "P3"`, `audience = "human"`, `tier = "user"`, `desc =
    "GPU-accelerated terminal emulator"`, four methods: `kind="dnf" package="kitty"`,
    `kind="apt" package="kitty"`, `kind="pacman" package="kitty"`, `kind="cask"
    os=["macos"] cask="kitty" app="kitty.app"`.

    Directly above the `[[tool]]` block, add a `# Verified 2026-09-06: ...` comment
    stating: confirmed live this session via `formulae.brew.sh/api/cask/kitty.json`
    (cask token `kitty`, version `0.48.2`) and `formulae.brew.sh/api/formula/kitty.json`
    (`404` — no Linux/macOS formula exists, cask-only); `kitty` is natively packaged as
    `kitty` on Debian, Fedora, and Arch. Correct a stale claim cross-AI review (codex,
    cycle 1) caught in 07-RESEARCH.md's own Anti-Patterns section ("there is no Linux
    Homebrew path for either" kitty/wezterm): re-fetching `formulae.brew.sh/api/cask/
    kitty.json` this session shows the kitty cask's `variations` object DOES declare
    real `x86_64_linux`/`arm64_linux` artifacts upstream — so "no Linux cask exists" is
    false for `kitty` specifically (it is true for `wezterm`, whose cask has an empty
    `variations` object and macOS-only `depends_on`). State the REAL reason `kitty`'s
    Linux path here is native distro packages, not a `kind="cask"` method: this
    project's own `installer/resolve.py::_applies`'s cask branch is hardcoded
    `platform.os == "macos" and platform.has_brew`, unconditionally excluding every
    cask-kind method on any non-macOS platform regardless of what Homebrew ships
    upstream — a generic, tool-agnostic project policy, not an absence of upstream
    Linux packaging. State the accepted Bazzite gap explicitly and why it is not fixed
    this phase: `kitty`'s GitHub release Linux assets
    (`kitty-{ver}-{arch}.txz`, confirmed via `api.github.com/repos/kovidgoyal/kitty/releases/latest`
    this session) are XZ-compressed (`.txz`), but `installer/download.py`'s extraction
    (`_install_unverified`/`_place_verified`, lines 146 and 207) hardcodes `tar -xzf`
    (gzip-only, `-z` flag) — a `github_release` method for `kitty` on Linux would fail
    at extraction time. No native manager runs on immutable Bazzite by design
    (`installer/resolve.py::_applies`'s existing immutable-Linux skip), so `kitty`
    genuinely has NO applicable method on immutable Bazzite — accepted and recorded,
    not silently absent. State the resolver-applicable-Linux-fallback conclusion
    precisely, per cross-AI review (codex, cycle 2, LOW), which found the prior
    draft's phrasing self-contradictory (crediting the formula's `404` for the
    absence of a cask fallback, when the cask branch's own exclusion — stated two
    sentences above in this same comment — is the real, independent reason): state
    both reasons side by side, without conflating them — "No resolver-applicable
    Linux fallback: the formula is absent (`404`, confirmed above), and project
    policy excludes casks on Linux (`_applies`'s cask branch, confirmed above) —
    two independent facts, neither implying the other." State that
    `REQ-linux-bazzite-shell-parity` names only `zsh`/`oh-my-zsh`, so this gap
    violates no locked requirement, and that fixing `download.py`'s archive
    extraction (e.g. `tar -xzf` -> `tar -xf`, which auto-detects compression) is out
    of this registry-only phase's scope per 07-RESEARCH.md Open Question 3's own
    recommendation — flag for a future phase if ever revisited, do not silently
    attempt it here.

    PART C — add the `wezterm` entry:

    `id = "wezterm"`, `name = "WezTerm"`, `category = "terminal"`, `cmd = "wezterm"`,
    `priority = "P3"`, `audience = "human"`, `tier = "user"`, `desc = "GPU-accelerated,
    cross-platform terminal emulator with multiplexing"`, three methods: `kind="pacman"
    package="wezterm"`; `kind="github_release" os=["debian","fedora"] arch=["amd64"]
    repo="wezterm/wezterm" asset="WezTerm-{ver}-Ubuntu20.04.AppImage"
    checksum="{asset}.sha256" member="wezterm" raw=true`; `kind="cask" os=["macos"]
    cask="wezterm" app="WezTerm.app"`.

    Directly above the `[[tool]]` block, add a `# Verified 2026-09-06: ...` comment
    stating: confirmed live via `formulae.brew.sh/api/cask/wezterm.json` (cask token
    `wezterm`, version `20240203-110809,5046fc22`) and `formulae.brew.sh/api/formula/wezterm.json`
    (`404` — cask-only, same as `kitty`). State the Linux story per-distro, verified
    live via `api.github.com/repos/wezterm/wezterm/releases/latest` this session: Arch
    has `wezterm` natively packaged in the official `[extra]` repo (plain `pacman -S
    wezterm` works, no custom repo needed) — no fallback method is declared for it;
    Debian/Fedora have no plain native package (Debian needs WezTerm's own apt repo
    added, Fedora needs `dnf copr enable wez/wezterm` — neither is a stock package-
    manager install this project can automate without adding an untrusted third-party
    repo, a worse trust posture than a checksum-verified GitHub release), so those two
    use the `github_release` AppImage instead. State the checksum finding precisely,
    since 07-RESEARCH.md left it as Open Question 2 and this comment closes it: the
    release publishes `WezTerm-{tag}-Ubuntu20.04.AppImage.sha256` as a sibling asset,
    and its content (fetched and read this session) is the multi-line `<hash>
    <filename>` format (`34010a07...  WezTerm-20240203-110809-5046fc22-Ubuntu20.04.AppImage`),
    which `installer/checksums.py::expected_sha256`'s existing `_names_match` branch
    already parses correctly — `checksum = "{asset}.sha256"` needs no new code.

    Resolve the Linux-arm64 scope with a live check, per cross-AI review (codex,
    cycle 2, MEDIUM): the planning session that wrote this revision had no network
    access to re-verify the prior draft's arm64 claim, so this is deliberately left
    as a live check for the executor, not asserted from memory. Fetch
    `api.github.com/repos/wezterm/wezterm/releases/latest` and inspect the full
    asset list for a Linux arm64/aarch64 AppImage (a name containing `arm64` or
    `aarch64` alongside `AppImage`). Two outcomes, both acceptable, neither silent:
    (a) if a real Linux-arm64 AppImage asset exists, add a fourth method —
    `kind="github_release" os=["debian","fedora"] arch=["arm64"] repo="wezterm/
    wezterm" asset="<the real asset name>" checksum="{asset}.sha256" member="wezterm"
    raw=true` — mirroring the amd64 method exactly, add the matching test coverage
    (mirror `test_wezterm_appimage_covers_bazzite_where_kitty_cannot`'s assertions
    for the new arch), and state in the comment that arm64 IS covered, citing the
    real asset name; (b) if no such asset exists (only `.deb`/`.rpm`/other
    non-AppImage arm64 variants, or no arm64 assets at all), keep `arch=["amd64"]`
    on this method and state in the comment, precisely and narrowly (per cross-AI
    review's ask — do not repeat a possibly-stale claim): the exact asset list found
    this session naming what non-AppImage arm64 variants exist (if any), and the
    scope decision explicitly: Arch Linux arm64 is ALREADY covered by this entry's
    OWN `pacman` method (undeclared `arch`, so it applies on every Arch architecture,
    including arm64 — `installer/resolve.py::_applies`'s arch filter is a no-op when
    a method declares no `arch` list); only Debian/Fedora arm64 lack a path, a gap
    bounded by upstream asset availability (outside this project's control), not a
    project scoping choice, and REQ-terminal-emulator-entries does not name a
    specific architecture. Either way, cite `puppeteer`'s existing Linux-arm64 gate
    as this registry's precedent for the reasoning shape.
    State the unplanned Bazzite bonus explicitly, so it reads as understood rather than
    accidental: unlike `kitty`, this AppImage method DOES resolve on immutable Fedora
    (Bazzite) — `installer/resolve.py::_applies`'s immutable-Linux skip applies only to
    native package-manager kinds (`dnf`/`apt`/`pacman`), never to `github_release`, and
    `raw=true` means `_install_unverified`/`_place_verified` never reach the `tar -xzf`
    call that breaks on `kitty`'s `.txz` assets — `wezterm` gets a real Linux/Bazzite
    path as a side effect of this design choice, though `REQ-linux-bazzite-shell-parity`
    does not require it.

    Both comments MUST literally contain: `kitty` comment — `.txz`, `tar -xzf`,
    `download.py`, `x86_64_linux`, `platform.os == "macos"`; `wezterm` comment —
    `.sha256`, `arm64`, `Bazzite`.

    PART D — add the tests:

    - `test_kitty_has_no_linux_download_fallback` — load `kitty`; assert
      `[m.kind for m in resolve_methods(kitty, Platform(os="fedora", arch="amd64",
      immutable=False, has_brew=True))] == ["dnf"]`; assert
      `resolve_methods(kitty, Platform(os="fedora", arch="amd64", immutable=True,
      has_brew=True))` is empty (the accepted Bazzite gap); assert
      `[m.kind for m in resolve_methods(kitty, Platform(os="macos", arch="arm64",
      immutable=False, has_brew=True))] == ["cask"]`.
    - `test_wezterm_appimage_covers_bazzite_where_kitty_cannot` — load `wezterm`;
      assert `[m.kind for m in resolve_methods(wezterm, Platform(os="arch",
      arch="amd64", immutable=False, has_brew=True))] == ["pacman"]`; assert
      `[m.kind for m in resolve_methods(wezterm, Platform(os="arch", arch="arm64",
      immutable=False, has_brew=True))] == ["pacman"]` (Arch arm64 is ALREADY
      covered by the arch-unrestricted `pacman` method — proves the narrowed scope
      of any remaining arm64 gap, per the arm64 decision above); assert
      `[m.kind for m in resolve_methods(wezterm, Platform(os="debian", arch="amd64",
      immutable=False, has_brew=True))] == ["github_release"]`; assert
      `[m.kind for m in resolve_methods(wezterm, Platform(os="fedora", arch="amd64",
      immutable=True, has_brew=True))] == ["github_release"]` (the Bazzite bonus,
      proved not assumed); assert `[m.kind for m in resolve_methods(wezterm,
      Platform(os="macos", arch="arm64", immutable=False, has_brew=True))] ==
      ["cask"]`. Then, conditioned on which arm64 outcome PART C's live check found:
      if NO Linux-arm64 AppImage method was added, assert
      `resolve_methods(wezterm, Platform(os="fedora", arch="arm64", immutable=False,
      has_brew=True))` is empty; if a fourth arm64 method WAS added, assert instead
      that it resolves to `["github_release"]` on that same fixture, with `params`
      naming the real asset PART C's live check found. Per cross-AI review's MEDIUM
      finding — method-kind lists alone don't prove the AppImage is actually
      installed safely, only that the RIGHT kind was picked — also assert on the
      `debian`/`fedora` `github_release` method's own `params` directly (not just
      its `kind`): `params["asset"] == "WezTerm-{ver}-Ubuntu20.04.AppImage"`,
      `params["checksum"] == "{asset}.sha256"`, `params["raw"] is True`,
      `params["member"] == "wezterm"`, and `method.arch == ("amd64",)`. Without this,
      a future edit dropping `raw=true` (sending the AppImage to the hardcoded
      `tar -xzf` it is specifically designed to bypass) or dropping `checksum` would
      still pass a kind-list-only assertion.
    - `test_kitty_entry_records_the_txz_extraction_gap` and
      `test_wezterm_entry_records_the_checksum_sidecar_verification` — the same
      "read committed text, assert substrings within the line-window immediately
      preceding `id = \"...\"`" locality pattern used throughout this phase, asserting
      the exact substrings listed above.

    PART E — update `test_registry_tier_distribution_is_pinned`'s `"user"` count from
    `36` to `38` in the same commit.

    PART F — add `"wezterm"` to `tests/test_registry.py`'s existing `SIDECAR_VERIFIED`
    set (line 841), per cross-AI review's suggestion: this makes the existing generic
    `test_sidecar_verified_tools_declare_checksums` also assert, independently of
    Part D's own test, that every `github_release` method on `wezterm` carries a
    `checksum` param containing `{asset}` — a second, already-existing safety net over
    the same load-bearing field, at the cost of one set-literal edit.

    PART G — Tier-3 verification: actually download, checksum-verify, and run the
    `wezterm` AppImage, per cross-AI review (codex, cycle 2, MEDIUM): PART D's tests
    prove the RIGHT method/params were picked, never that the real pipeline
    (`installer/download.py::install_download` -> `installer/checksums.py::
    expected_sha256` -> a live GitHub Releases resolve -> a real download -> a real
    checksum comparison -> `chmod +x` -> the binary actually running) works
    end-to-end. `installer/download.py`'s own transitive import graph (`installer.
    checksums`, `installer.executors`, `installer.locations`, `installer.model`,
    `installer.platform`, `installer.run`, `installer.versions`, `installer.assets`,
    `installer.guards`, `installer.enums`) is 100% Python-stdlib — no third-party
    package is needed to exercise it, only `curl`/`ca-certificates` for the actual
    network calls the real code shells out to.

    In a disposable `python:3.13-slim`-or-newer container (removed after use, per
    ONESHOT-RULES Rule 14), mount this repository read-only (e.g. `docker run --rm
    -v "$(pwd):/repo:ro" -w /repo python:3.13-slim sh -c '...'`), install `curl
    ca-certificates` inside it, then run a one-liner (`PYTHONPATH=/repo python3 -c
    "..."`) that: loads the real `wezterm` tool from the real, committed
    `installer/registry.toml` (`installer.model.load_tools`); selects its
    `debian`/`fedora` `github_release` method; builds a real
    `installer.download.ExecContext(runner=installer.run.run_command,
    platform=installer.platform.Platform(os="debian", arch="amd64",
    immutable=False, has_brew=False), resolve_tag=installer.versions.
    resolve_github_tag)` — a REAL runner and a REAL, live tag resolver, not stubs;
    calls `installer.download.install_download(method, ctx)` for real (this
    resolves the live release tag, downloads the AppImage AND its `.sha256`
    sidecar, verifies the checksum via `expected_sha256`, and places the
    executable at `~/.local/bin/wezterm`, i.e. `/root/.local/bin/wezterm` as root
    in the container); asserts the function returns `True` (checksum-verified, not
    the unverified fallback). Then run `~/.local/bin/wezterm --version` (or add
    `~/.local/bin` to `PATH` first) and confirm it exits 0 and prints a version
    string. Record in the SUMMARY, verbatim: the resolved tag, the download/
    checksum outcome (`install_download`'s return value), and the full
    `wezterm --version` output plus its exit code. If checksum verification raises
    `ChecksumMismatch` or the binary fails to execute, this is a destructive-
    anomaly-shaped finding per ONESHOT-RULES Rule 9 — stop, do not write comments
    that contradict the evidence, and report exactly what happened (a transient
    network failure is not this anomaly — retry once first).

    Do not modify `installer/download.py`, `installer/model.py`,
    `installer/resolve.py`, or `installer/checksums.py` — every mechanism `kitty`/
    `wezterm` need already exists and PART G exercises it for real without changing
    it; the only new production code in this task is the one `Category` enum
    member.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py -q</automated>
    <fails_when>pytest exits non-zero; any of this task's four new resolve/comment
    tests fails; the wezterm resolve test's `raw`/`checksum`/`member`/`arch` field
    assertions fail; `test_sidecar_verified_tools_declare_checksums` fails once
    `"wezterm"` is added to `SIDECAR_VERIFIED`; `test_every_used_category_has_a_blurb`/
    `test_every_category_blurb_is_used_by_a_tool` regress (would mean the category
    blurb and its tool usage are out of sync); the tier tripwire still shows `36`;
    or PART G's Tier-3 container run fails to download, checksum-verify, and
    execute the real `wezterm` AppImage.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry.py -q` exits 0.
    - `uv run python3 -c "from pathlib import Path; from installer.model import load_tools; ts={t.id:t for t in load_tools(Path('installer/registry.toml'))}; assert ts['kitty'].category=='terminal'; assert ts['wezterm'].category=='terminal'"` exits 0.
    - `grep -c '\.txz' installer/registry.toml` is at least 1.
    - `grep -c '{asset}.sha256"' installer/registry.toml` is at least 1 (the literal method param, not just the comment).
    - `grep -c 'x86_64_linux' installer/registry.toml` is at least 1 (the corrected kitty-cask-Linux finding).
    - `"wezterm"` is present in `tests/test_registry.py`'s `SIDECAR_VERIFIED` set.
    - The SUMMARY records PART G's Tier-3 transcript verbatim: the resolved tag, `install_download`'s `True` (checksum-verified) return, and the full `wezterm --version` output with its exit code.
    - `make validate && make test` exits 0 on this task's own commit.
  </acceptance_criteria>
  <done>A new `terminal` category exists in both `installer/enums.py` and `installer/registry.toml`; `kitty` and `wezterm` are verified `tier="user"` entries with cask-corrected macOS methods and researched Linux paths, including the corrected (not self-contradictory) reason `kitty`'s Linux path is native packages rather than a cask (this project's own `resolve.py` blocks all casks on non-macOS AND no formula exists — two independent facts, stated as such); `kitty`'s accepted immutable-Bazzite gap and `wezterm`'s AppImage `raw`/checksum verification are both recorded and tested with exact-field assertions (not kind-lists alone); a real Tier-3 container run proves the AppImage actually downloads, checksum-verifies, and runs, closing cross-AI review's MEDIUM finding (cycle 2); the Linux-arm64 scope decision is explicit and live-checked, not asserted from a possibly-stale claim; `wezterm` is added to the existing `SIDECAR_VERIFIED` regression net; the tier tripwire reflects both new user-tier entries.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Phase 7's decisions into `.planning/PROJECT.md`</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md`'s Key Decisions table in full — the existing three-column
      (`Decision`/`Rationale`/`Outcome`) shape and its most recent Phase 6 rows, the
      pattern this task extends (mirroring 06-01-PLAN.md's own Task 2). Note the exact
      row format on disk (confirmed via `od -c` this session): each row ends
      `... | Phase 6 (06-01) |` with a space before the closing pipe and no trailing
      space after it — the `<verify>` command below depends on matching this format
      precisely.
    - This phase's three plans (07-01, 07-02's Tasks 1-4, and this plan's own Task 1,
      all already executed by the time this task runs) — the concrete decisions each
      one made, which this task records in the ledger without re-deriving them.
      07-02's Task 1 implemented the real macOS-version gate (`Platform.os_version`/
      `Method.min_os_version`) and Task 3 implemented D-01's disabled-state rendering
      for real (`platform_could_support`, threaded through `CatalogScreen`, reusing
      the Uninstall view's dim/non-selectable-row mechanism) — decision 4 below
      records THAT complete outcome, not the original NO_METHOD-only premise, and not
      the cycle-1/cycle-2 "accepted residual gap" framing cross-AI review rejected
      twice.
    - Cross-AI review (codex, cycle 2, LOW) found these six rows, in the prior draft,
      duplicated research/plan/summary content at excessive length. This revision
      condenses each to a short statement plus a cross-reference to the plan/summary
      that carries the full detail, per the reviewer's own suggested pattern.
  </read_first>
  <action>
    Add six new rows to `.planning/PROJECT.md`'s Key Decisions table (after the most
    recent existing rows, before the closing `---`), following the exact three-column
    shape the table already uses, each with `Outcome` = `Phase 7` (the literal string,
    no suffix — do not append `(07-0N)` to the Outcome cell itself; the plan
    provenance lives in the Rationale text instead, so the `<verify>` command's exact
    `| Phase 7 |` row-ending match keeps working). Keep each Rationale cell to one or
    two sentences plus a `See {file}` pointer — the full investigation lives in the
    cited plan/SUMMARY, not here:

    1. Decision: `oh-my-zsh` ships with explicit `env = { RUNZSH = "no", CHSH = "no",
       KEEP_ZSHRC = "yes" }` and `requires = ["zsh", "git"]`. Rationale: `install.sh`'s
       non-TTY defaults don't preserve `.zshrc` unless set explicitly, and `git` is a
       hard, undeclared prerequisite `install.sh` itself requires. See
       07-01-PLAN.md/07-01-SUMMARY.md.
    2. Decision: `zsh`/`oh-my-zsh`'s Linux/Bazzite parity reuses the existing `podman`
       entry unchanged; no new container-runtime entry was added. Rationale:
       `podman`'s immutable-Linux-falls-to-brew shape already resolves correctly
       there, proven directly. See 07-01-SUMMARY.md.
    3. Decision: Apple Containers ships as a real `kind="brew" formula="container"`
       action, not a doc/version-gate entry. Rationale: Homebrew's own formula
       metadata declares a real `requirements` floor (`macos>=26`, `arch=arm64`),
       correcting REQUIREMENTS.md's speculative framing. See 07-02-SUMMARY.md.
    4. Decision: D-01's "shows disabled when unavailable" is satisfied by a
       `Platform`-derived `platform_could_support` signal threaded from `setup.py`
       through `CatalogScreen`, reusing the Uninstall view's dim/non-selectable-row
       mechanism — not `install_tool`'s `NO_METHOD` alone, and not a bare
       `resolve_methods` check. `Platform.os_version`/`Method.min_os_version` close
       the macOS-version case too (Apple Containers declares `min_os_version="26"`).
       Rationale: cross-AI review found `NO_METHOD` architecturally unreachable from
       the catalog-browsing path (cycle 1), a bare availability check would wrongly
       dim brew-dependent tools on a fresh Mac (cycle 2), and the macOS-version gap
       was rejected as "documented but unimplemented" in BOTH cycle 1 and cycle 2 —
       this is the resolution that closed all three, with no residual gap remaining.
       See 07-02-SUMMARY.md and `.claude/architecture.md`'s Registry-authoring
       guidelines.
    5. Decision: `gnu-bash` uses `cmd = "gnu-bash"` (not the literal `"bash"`) plus two
       arch-split `detect_path` methods. Rationale: macOS's own `/bin/bash` is always
       on PATH, so `cmd = "bash"` would make `is_installed` report the entry
       permanently satisfied and the install action would never run, on any Mac. See
       07-02-SUMMARY.md.
    6. Decision: `kitty` ships with no Linux `github_release` fallback (no method at
       all on immutable Bazzite); `wezterm`'s Linux fallback is a checksum-verified
       AppImage `github_release` with `raw=true`, proven by a real Tier-3
       download/checksum/execute run. Rationale: `kitty`'s Linux release assets are
       `.txz`, which this project's download extraction cannot open; `raw=true`
       bypasses extraction entirely. `kitty`'s Linux-cask absence is two independent
       facts (no formula exists; this project's own resolver blocks every cask on
       non-macOS regardless of what Homebrew ships upstream), not one causing the
       other. See 07-03-SUMMARY.md.

    Do not alter any existing row or the table's column order.

    Finally, bump `.planning/PROJECT.md`'s closing `*Last updated: ...*` footer to the
    actual date this task runs (`date +%Y-%m-%d`), keeping the file's existing
    three-part shape (date, trigger phrase, brief description), e.g. `*Last updated:
    {date} after Phase 7 system & user tier catalog expansion (07-01..07-03)*`.
  </action>
  <verify>
    <automated>[ "$(grep -cE '\| Phase 7 \|$' .planning/PROJECT.md)" -eq 6 ] && grep -Eq '^\*Last updated:.*Phase 7' .planning/PROJECT.md</automated>
    <fails_when>`.planning/PROJECT.md` does not contain exactly six table rows whose
    Outcome cell is the literal `Phase 7` (matched precisely as a row ending in
    `| Phase 7 |` — a decision row is missing, duplicated, or uses a different/suffixed
    outcome string), or the `*Last updated: ...*` footer still lacks a `Phase 7`
    citation. Cross-AI review (codex, cycle 1, HIGH) found the ORIGINAL
    `grep -c 'Phase 7' -eq 6` command cannot pass: it counts every line containing the
    substring `Phase 7` anywhere, which is 6 (the decision rows) PLUS 1 (the footer
    line) = 7, not 6 — the row-anchored regex above counts only the six table rows and
    checks the footer as a separate, independent condition.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `<automated>` command above exits 0.
    - `.planning/PROJECT.md`'s Key Decisions table gains exactly six new rows, each with `Outcome` = `Phase 7` (unsuffixed).
    - `git diff -- .planning/PROJECT.md` shows only the six new table rows plus the `*Last updated: ...*` footer line changed — no existing decision row altered, reordered, or removed.
    - `make validate && make test` exits 0 on this task's own commit.
  </acceptance_criteria>
  <done>`.planning/PROJECT.md`'s Key Decisions table records all six decisions this phase made across its three plans, each condensed to a short statement plus a cross-reference — including D-01's now-fully-closed resolution (07-02's real `platform_could_support`/`min_os_version` implementation, not the original `NO_METHOD`-only premise nor either cycle's "accepted residual gap" framing) and the corrected kitty-cask-Linux rationale (two independent facts, not one causing the other) — and its `Last updated` footer reflects Phase 7's completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `installer/registry.toml` (project-authored data) -> `installer/executors.py::_cask`'s `brew install --cask` invocation | `cask` values are plain, non-interpolated argv elements sourced only from this project's own committed registry data |
| GitHub Releases (public, untrusted network) -> `wezterm`'s AppImage download | Mitigated by the existing, unmodified checksum-verification path (`installer/download.py::_install_verified`) — a mismatch halts the ladder (`checksum_policy="fail"` default), never silently falls through |
| A future registry editor -> `kitty`'s Linux Bazzite gap | The gap is non-obvious (native packages exist and work everywhere else); a future edit adding a `github_release` method for `kitty` on Linux without also fixing `installer/download.py`'s `.txz` handling would silently ship a broken install |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Tampering | `installer/executors.py::_cask` — `brew install --cask kitty/wezterm` | low | accept | Already the accepted baseline for every `cask`-kind entry in this registry (Homebrew's own review/signing process); no new mitigation needed. |
| T-07-08 | Tampering | `wezterm`'s AppImage download over HTTPS from `github.com/wezterm/wezterm/releases` | low | mitigate | Already mitigated by the existing, unmodified `checksum = "{asset}.sha256"` verification path — a mismatch is `ChecksumMismatch`, which halts the ladder rather than silently falling through, consistent with every other checksum-verified `github_release` entry in this registry. |
| T-07-09 | Denial of Service (silently broken install) | A future edit adding a `kitty` Linux `github_release` method without also fixing `installer/download.py`'s `.txz`/gzip-only extraction | medium | mitigate | `test_kitty_entry_records_the_txz_extraction_gap` pins the comment naming the exact broken lines (`installer/download.py:146,207`, `tar -xzf`, `.txz`) so a future editor sees the reasoning before adding a method that would fail at extraction time; `test_kitty_has_no_linux_download_fallback` asserts the current, intentional empty-on-Bazzite resolution so a silent regression (or silent "fix") is caught either way. |
| T-07-13 | Denial of Service (silently broken AppImage path) | A future edit to `wezterm`'s `asset`/`checksum`/`raw`/`member` params that breaks the real download-verify-execute path without any test noticing (method-kind/param assertions alone can pass while the real pipeline is broken) | medium | mitigate | This revision's Tier-3 check (Task 1, PART G) actually downloads, checksum-verifies, and runs the real AppImage through `installer.download.install_download` end-to-end, closing cross-AI review's MEDIUM finding (cycle 2) that structural assertions alone were insufficient; the recorded SUMMARY transcript is the evidence a future regression review can compare against. |

</threat_model>

<verification>
Phase-level checks, run after both tasks — and, since this is the phase's final plan, a full Phase 7 close-out check:

- `uv run pytest tests/test_registry.py -q` exits 0 (all new tests pass, tier tripwire at `system: 26, ai: 10, user: 38`).
- `uv run pytest tests/test_platform.py tests/test_resolve.py tests/test_status.py tests/test_selection.py tests/test_catalog_tui.py tests/test_setup.py tests/test_wizard_app.py -q` exits 0 — the full cross-plan test surface this phase touched (07-01/07-02/07-03 combined) is green, not just this plan's own file.
- `make validate && make test` exits 0 on the committed tree.
- `uv run python3 -c "from pathlib import Path; from installer.model import load_tools, load_categories; load_tools(Path('installer/registry.toml')); load_categories(Path('installer/registry.toml'))"` exits 0 — the full registry, including the new category and six new tools across all three of this phase's plans, still parses cleanly end to end.
- `grep -c "Verified 2026-09-06" installer/registry.toml` is at least 6 (all six of this phase's new entries).
- `grep -c "min_os_version" installer/registry.toml` is at least 2 — Apple Containers' real macOS-version gate is present in the committed registry, not merely described in a plan.
- `.planning/PROJECT.md` shows only additions (six new, condensed Key Decisions rows) plus the `*Last updated: ...*` footer changed.
- The SUMMARY records a real, resolver-level assertion that a too-old-macOS Apple-Silicon `Platform` fixture resolves ZERO methods for Apple Containers (not a documentation-only claim) — the HIGH finding cross-AI review raised in both cycle 1 and cycle 2 is closed by evidence, not by a third round of acceptance language.
</verification>

<success_criteria>
Completes ROADMAP Phase 7's numbered success criteria:

2. (SC#2) `kitty`/`wezterm` install cleanly on macOS via a Homebrew cask (corrected from a formula) with a verified Linux path per tool (native distro packages for `kitty`; `pacman` plus a checksum-verified AppImage `github_release` for `wezterm`, now proven by a real Tier-3 download/checksum/execute run, not method-kind assertions alone), closing 07-RESEARCH.md's Open Questions 2 and 3 with live verification rather than leaving either as a guess, and recording an explicit, live-checked Linux-arm64 scope decision.

Cross-AI review (codex, cycle 1, MEDIUM; cycle 2, HIGH) found this plan's phase-closeout
claim premature across two cycles: cycle 1 flagged it for asserting all four ROADMAP
success criteria satisfied while 07-02 still left D-01 unimplemented; cycle 2 flagged
it again because 07-02's cycle-2 revision still only ACCEPTED and DOCUMENTED the
macOS-version gap rather than closing it — the same HIGH finding, twice. This
revision's closeout claim is honest about what actually shipped this cycle, not a
repeat of either prior framing:

SC#1 (zsh/oh-my-zsh/gnu-bash/Apple Containers install cleanly, oh-my-zsh's rewrite
behavior confirmed safe via a three-phase Tier-3 container run including a fresh-user
case, `git` prerequisite fixed, a fresh-Bazzite-without-brew fixture proving the
resolver's honest boundary) — satisfied. SC#2 (this plan, kitty/wezterm) — satisfied,
including the corrected kitty-cask-Linux rationale and a real Tier-3 AppImage
install/execute proof. SC#3 (zsh/oh-my-zsh Linux/Bazzite parity via the existing
`podman` entry, including the honest has_brew=False boundary and the real two-run
bootstrap path) — satisfied. SC#4 (Apple Containers resolved as a real `brew install
container` action; D-01's disabled-state now GENUINELY and COMPLETELY implemented) —
satisfied WITH NO RESIDUAL GAP: `installer/platform.py::Platform.os_version` and
`installer/resolve.py`'s `min_os_version` gate (07-02, Task 1) close the macOS-version
case a cross-AI reviewer rejected as "documented but unimplemented" in BOTH cycle 1 and
cycle 2; `installer/resolve.py::platform_could_support` (07-02, Task 1, consumed by
Task 3's `CatalogScreen` wiring) correctly distinguishes a genuine incompatibility
(wrong `os`/`arch`/macOS-version — shown disabled) from a merely-not-yet-bootstrapped
Homebrew (left selectable, preserving the installer's own bare-machine bootstrap path);
and the staging-bypass MEDIUM finding is closed across `unstaged_recommends`,
`action_accept_recommends`, and `on_tool_browser_accepted`. Every wrong-`os`/
wrong-`arch`/too-old-macOS-version machine is now correctly shown disabled while
browsing — not merely the common case. This phase closes with no explicitly-accepted
residual gap for D-01; a future review finding the version gate insufficient would be
a genuine new defect, not the same one accepted a third time.
</success_criteria>

<output>
Create `.planning/phases/07-system-user-tier-catalog-expansion/07-03-SUMMARY.md` when done.
</output>

