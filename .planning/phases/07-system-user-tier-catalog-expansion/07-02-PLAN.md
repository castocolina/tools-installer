---
phase: 07-system-user-tier-catalog-expansion
plan: 02
type: execute
wave: 2
cross_ai: true
depends_on:
  - "07-01"
files_modified:
  - installer/registry.toml
  - tests/test_registry.py
  - .claude/architecture.md
autonomous: true
requirements:
  - REQ-system-tier-shell-container-entries

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`gnu-bash` and Apple Containers (`container`) both appear as `tier=\"system\"` catalog entries and install cleanly via verified live methods on the platforms they declare (ROADMAP Phase 7 SC#1, remainder)."
    - "`gnu-bash`'s `is_installed` check cannot be permanently satisfied by macOS's own pre-existing `/bin/bash` — a real correctness gap this plan found and fixes, not something 07-RESEARCH.md flagged, because that false positive would make the entry's install action a permanent no-op on every Mac, violating SC#1's 'installs cleanly' bar by construction."
    - "Whether Apple Containers needs an actual install step on current macOS, or is a pure version-gate/doc entry, is resolved (it needs a real `brew install container` step) and recorded — correcting REQUIREMENTS.md's own speculative 'may be a version-gate/doc entry' framing (ROADMAP SC#4)."
    - "Apple Containers always appears in the catalog (never omitted) and, on a machine where it cannot install (wrong arch, non-macOS), resolves to `NO_METHOD` — the exact same signal every other platform/arch-gated tool in this registry already produces — satisfying CONTEXT D-01's 'reuse the existing unavailable-dependency pattern, do not invent a new one' instruction with zero new UI code, a decision this plan makes explicit rather than leaving as 07-RESEARCH.md's Open Question 1."
    - "Both new entries carry a `# Verified 2026-09-06: ...` comment per the registry-authoring verification checklist, and the D-01 disabled-state resolution is additionally recorded as a durable, generalizable convention in `.claude/architecture.md` (not just a one-off comment), so a future platform/arch-gated entry has a citable precedent."
    - "`tests/test_registry.py::test_registry_tier_distribution_is_pinned`'s `system` count is updated from 24 to 26 in the same commit."
  artifacts:
    - installer/registry.toml
    - tests/test_registry.py
    - .claude/architecture.md
  key_links:
    - "`gnu-bash`'s two `arch`-split `brew` methods rely on the existing, unmodified `installer/resolve.py::_applies` arch gate for INSTALL selection, and on the existing, unmodified `installer/status.py::is_installed` detect_path loop (which does not filter by arch) for detecting either absolute install location — the same two-step `which()`-then-`detect_path` mechanism `sdkman` already uses, applied here to solve a same-named-binary collision `sdkman` never had to."
    - "Apple Containers' `os=[\"macos\"], arch=[\"arm64\"]` on its single `brew` method is read by the existing, unmodified `installer/resolve.py::_applies` — the identical mechanism already proven for `codegraph`'s Linux/macOS split and `puppeteer`'s Linux arm64 gate; `install_tool` returns `NO_METHOD` on any other platform with zero new code."
---

<objective>
Add `gnu-bash` and Apple Containers (`container`) as verified `tier="system"` registry
entries — the remaining half of ROADMAP Phase 7 SC#1 — and resolve, explicitly and
durably, the two open decisions 07-CONTEXT.md/07-RESEARCH.md left for the planner:
whether Apple Containers is a real install action (it is: `brew install container`,
not a doc/version-gate entry), and what "shows disabled when unavailable" (D-01)
concretely means given no persistent grayed-out catalog row exists anywhere in
`catalog_tui.py` today.

This plan also fixes a real correctness gap found while grounding `gnu-bash`'s design
against this codebase (not flagged by 07-RESEARCH.md): macOS ships `/bin/bash` (ancient
3.2, SIP-protected) on every Mac, always. If `gnu-bash`'s `cmd` were the literal
string `"bash"`, `installer/status.py::is_installed`'s first line
(`shutil.which(tool.cmd)`) would return that system binary and report the entry
"already installed" on every Mac, forever — before Homebrew's newer build is ever
installed. The install action would then never run, on any machine, permanently,
which would make SC#1's "installs cleanly" claim false by construction for this one
entry. This is distinct from `zsh`'s case (07-01-PLAN.md): macOS's bundled `zsh` is a
real, modern build that genuinely satisfies `zsh`'s stated purpose, but macOS's bundled
bash 3.2 is explicitly what `gnu-bash`'s own purpose ("coexists with macOS's ancient
system bash") exists to supplement, so "any bash present" cannot be treated as
satisfying it the way "any zsh present" can.

Purpose: REQ-system-tier-shell-container-entries (`gnu-bash`, Apple Containers) —
ROADMAP Phase 7 SC#1 (remainder) and SC#4.

Decisions implemented:
- 07-RESEARCH.md's Apple Containers finding: Homebrew's own formula metadata
  (`requirements: macos>=26, arch=arm64`) proves a real install step exists
  (`brew install container`) — REQUIREMENTS.md's "may be a version-gate/doc entry"
  framing was speculative and is corrected here (SC#4).
- Open Question 1's recommendation, adopted as the explicit resolution rather than left
  open: D-01's "shown disabled" is satisfied by the entry always appearing in its tier
  view (never omitted) plus `Method.arch=["arm64"]`/`os=["macos"]` producing the same
  `NO_METHOD` outcome every other platform-unavailable tool in this registry already
  produces on an unsupported machine — no new row-level render, per D-01's own "reuse,
  don't invent" instruction, once the specific existing mechanism is named precisely.
- Pitfall 4's guidance, followed: no `min_os_version`/similar field is added to
  `Method` — the macOS-26 floor is enforced only by Homebrew's own `brew install
  container` at install time, a clear failure there rather than a silent success, and
  building a new OS-version-detection mechanism is out of a registry-only phase's scope.

Scope boundary: this plan adds registry data, tests, and one durable architecture note
only. It does not modify `installer/model.py`, `installer/resolve.py`,
`installer/status.py`, or any `catalog_tui.py`/`ui_common.py` rendering code — D-01's
resolution is that no such change is needed.

Output: `gnu-bash` and `container` `[[tool]]` blocks in `installer/registry.toml`, each
with a dated `# Verified` comment; new tests in `tests/test_registry.py`; a new
"Platform-gated entries need no separate 'disabled' render" subsection under
`.claude/architecture.md`'s existing "## Registry-authoring guidelines" heading; the
`system` tier tripwire count updated to 26.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@.planning/phases/07-system-user-tier-catalog-expansion/07-CONTEXT.md
@.planning/phases/07-system-user-tier-catalog-expansion/07-RESEARCH.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@.claude/python-tooling.md
@installer/registry.toml
@installer/status.py
@installer/resolve.py
@installer/uninstall.py
@tests/test_registry.py

`.codegraph/` exists at the repo root. Use `codegraph_explore` (MCP tool) or
`codegraph explore "<symbols>"` (shell) BEFORE Grep/Read when locating code. Run
`codegraph index || codegraph init -i` once before starting.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `gnu-bash` (with the same-named-binary detection fix) and Apple Containers registry entries</name>
  <files>installer/registry.toml, tests/test_registry.py</files>
  <read_first>
    - 07-RESEARCH.md's "Standard Stack -> Core" table rows for `gnu-bash` and Apple
      Containers, and the "Verified `Method.arch`/`Method.os` gating" code example —
      the live-verified brew formula facts (`bash` stable `5.3.15`, `keg_only` unset;
      `container` formula's declared `requirements` array: `macos>=26`, `arch=arm64`).
    - `installer/status.py` lines 16-40 (`is_installed`) — re-confirm the exact
      two-step order: `shutil.which(tool.cmd)` first (unconditional), `detect_path`
      loop over ALL of `tool.methods` only as a fallback, and that the detect_path loop
      does NOT filter by the checking method's own `os`/`arch` — it is a plain
      file-existence check, which is why two arch-specific methods are both needed
      here (one per real Homebrew prefix), not one.
    - `installer/uninstall.py` lines 129-153 (`_manager_name`, `_manager_hint`,
      `_managed_hint`) — confirms a synthetic `cmd` value degrades gracefully here: the
      uninstall hint's manager name comes from `method.params["formula"]` (which this
      task sets to `"bash"`), not from `tool.cmd`; `tool.cmd` is only used for the
      optional "found at {path}" suffix via `which(tool.cmd)`, which is simply omitted
      when `cmd` cannot resolve — no crash, no incorrect classification.
    - `installer/registry.toml` lines 1225-1241 (the `codegraph` `# Verified
      2026-09-05 ...` comment) and the `puppeteer` entry's Linux-arm64-gate comment
      (`installer/registry.toml`, the block directly above `id = "puppeteer"`) — the
      exact style and the precedent for "a method's `arch` restriction is the whole
      unavailability mechanism, no separate UI needed" this task's `container` comment
      extends explicitly to a new case.
    - `tests/test_registry.py` lines 706-728 for the "load real tool, assert
      `resolve_methods` per `Platform` fixture" pattern this task's new tests follow.
  </read_first>
  <action>
    Write the tests first (RED — the entries do not exist yet), confirm they fail,
    then add the registry content (GREEN).

    PART A — add the `gnu-bash` entry, placed near `zsh`/`oh-my-zsh` (`category =
    "shell"`):

    `id = "gnu-bash"`, `name = "GNU Bash"`, `category = "shell"`, `cmd = "gnu-bash"`
    (deliberately NOT `"bash"` — see the comment below), `priority = "P2"`, `audience =
    "both"`, `tier = "system"`, `desc = "Modern GNU Bash (5.x) installed alongside
    macOS's SIP-protected system bash 3.2, never replacing it."`, two methods:
    `kind="brew" formula="bash" os=["macos"] arch=["arm64"] detect_path="/opt/homebrew/bin/bash"`
    and
    `kind="brew" formula="bash" os=["macos"] arch=["amd64"] detect_path="/usr/local/bin/bash"`.

    Directly above the `[[tool]]` block, add a `# Verified 2026-09-06: ...` comment
    stating: the real Homebrew formula name is `bash`, not `gnu-bash` (confirmed live,
    `formulae.brew.sh/api/formula/bash.json`, stable `5.3.15`, `keg_only` unset, the
    only caveat is `DEFAULT_LOADABLE_BUILTINS_PATH` — no PATH-shadowing warning, because
    Homebrew's bash installs to its own prefix (`/opt/homebrew/bin/bash` on Apple
    Silicon, `/usr/local/bin/bash` on Intel), coexisting with macOS's SIP-protected
    `/bin/bash` rather than replacing it). Then state the detection design and WHY, as
    a load-bearing finding, not a stylistic choice: macOS ships `/bin/bash` (ancient,
    always present) on every Mac; if `cmd` were the literal string `"bash"`,
    `is_installed`'s unconditional `shutil.which("bash")` check would find that system
    binary and report this entry `ALREADY_INSTALLED` on every Mac, permanently, before
    Homebrew's build is ever installed — `install_tool` would never even attempt
    `brew install bash`, on any machine, ever. `cmd = "gnu-bash"` is a probe name no
    installer ever creates, so `shutil.which` correctly returns `None` and
    `is_installed` falls through to each method's `detect_path`, which checks
    Homebrew's real, absolute, prefix-specific install location — the same two-step
    `which()`-then-`detect_path` mechanism `sdkman`'s entry already relies on for an
    analogous "the real command name is not reliably `which`-able" reason, applied here
    to solve a same-named-binary collision instead. State that two `brew` methods are
    declared, split by `arch`, because `resolve_methods`'s arch gate must select only
    the one applicable install method for THIS machine, while `is_installed`'s
    `detect_path` loop (which does not filter by arch) must still recognize either
    absolute path as a valid "already installed" signal regardless of which method
    would resolve here.

    PART B — add the `container` entry (Apple Containers), placed near `podman`/
    `colima` (`category = "docker"`, matching those existing container-runtime
    entries):

    `id = "container"`, `name = "Apple Containers"`, `category = "docker"`, `cmd =
    "container"`, `priority = "P2"`, `audience = "both"`, `tier = "system"`, `desc =
    "Apple's native container runtime; requires macOS 26+ and Apple Silicon, enforced
    by Homebrew's own formula requirements."`, one method: `kind="brew"
    formula="container" os=["macos"] arch=["arm64"]`.

    Directly above the `[[tool]]` block, add a `# Verified 2026-09-06: ...` comment
    with two parts. Part one (SC#4): states this is a REAL install action, correcting
    REQUIREMENTS.md's own speculative framing — confirmed live via
    `formulae.brew.sh/api/formula/container.json`: a real formula named `container`
    exists (homepage `apple.github.io/container`), with a declared `requirements`
    array of `{"name":"macos","version":"26"}` and `{"name":"arch","version":"arm64"}`
    — a present version-floor declaration, not an absence; `brew install container` is
    the install action, not a doc/version-gate no-op. Part two (D-01's disabled-state
    resolution — this is the load-bearing part a future reader needs): states plainly
    that `os=["macos"]`/`arch=["arm64"]` on this single method is the entire mechanism
    behind CONTEXT D-01's "shows disabled when unavailable" requirement — on any other
    platform or Intel Mac, `installer/resolve.py::resolve_methods` returns an empty
    list and `installer/engine.py::install_tool` reports `NO_METHOD`, the exact same
    outcome every other platform/arch-gated tool in this registry already produces
    (e.g. `puppeteer` on Linux arm64, `codegraph`'s Linux/macOS split) — the entry still
    always appears in the System tier view (never omitted, satisfying D-01's other
    half), and no new "disabled row" render exists anywhere in `catalog_tui.py`, nor is
    one added here, because `NO_METHOD` already IS this project's one, pre-existing
    signal for "cannot install here." States explicitly that no OS-version-floor field
    is added to `Method` — Homebrew's own `brew install container` enforces the
    macOS-26 requirement at install time (a clear failure there, not a silent
    success), and building a new version-detection mechanism is out of this
    registry-only phase's scope (07-RESEARCH.md Pitfall 4).

    Both comments MUST literally contain: `gnu-bash` comment — `5.3.15`,
    `DEFAULT_LOADABLE_BUILTINS_PATH`, `sdkman`; `container` comment — `NO_METHOD`,
    `macOS 26` or `macos", "version":"26`, `arm64`, `brew install container`. Write the
    comments to genuinely contain these substrings.

    PART C — add the tests:

    - `test_gnu_bash_avoids_system_bash_false_positive_via_detect_path` — load
      `gnu-bash`; assert `.cmd != "bash"`; assert both methods have `kind == "brew"`
      and `params["formula"] == "bash"`; assert the method with `arch == ("arm64",)`
      has `params["detect_path"] == "/opt/homebrew/bin/bash"` and the method with
      `arch == ("amd64",)` has `params["detect_path"] == "/usr/local/bin/bash"`.
    - `test_gnu_bash_only_resolves_on_macos` — assert `resolve_methods(gnu_bash,
      Platform(os="debian", arch="amd64", immutable=False, has_brew=True))` is empty,
      and `resolve_methods(gnu_bash, Platform(os="macos", arch="arm64",
      immutable=False, has_brew=True))` equals exactly one `brew` method.
    - `test_apple_containers_resolves_only_on_macos_arm64` — assert
      `resolve_methods(container, Platform(os="macos", arch="arm64", immutable=False,
      has_brew=True))` kinds equal `["brew"]`; assert
      `resolve_methods(container, Platform(os="macos", arch="amd64", immutable=False,
      has_brew=True))` is empty (Intel Mac — D-01's disabled case); assert
      `resolve_methods(container, Platform(os="fedora", arch="arm64", immutable=False,
      has_brew=True))` is empty (non-macOS).
    - `test_apple_containers_always_appears_in_the_registry_regardless_of_platform` —
      assert `"container" in {t.id for t in load_tools(REGISTRY)}` — a direct,
      literal proof of D-01's "never omitted" half, independent of any platform check.
    - `test_gnu_bash_entry_records_the_detection_fix` and
      `test_apple_containers_entry_records_the_disabled_state_resolution` — the same
      "read committed text, assert substrings within the line-window immediately
      preceding `id = \"...\"`" locality pattern as 07-01-PLAN.md's Task 1, asserting
      the exact substrings listed above.

    PART D — update `test_registry_tier_distribution_is_pinned`'s `"system"` count
    from `24` to `26` in the same commit.

    Do not modify `installer/model.py`, `installer/resolve.py`,
    `installer/status.py`, `installer/uninstall.py`, or any `catalog_tui.py`/
    `ui_common.py` file — this task is registry data and tests only; the D-01
    resolution is that the existing `resolve_methods`/`is_installed`/`NO_METHOD`
    mechanism already suffices.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py -q</automated>
    <fails_when>pytest exits non-zero; any of this task's six new tests fails; the tier
    tripwire still shows `24`; or a required comment substring lands outside the
    asserted line-window above the entry it documents.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry.py -q` exits 0.
    - `uv run python3 -c "from pathlib import Path; from installer.model import load_tools; ts={t.id:t for t in load_tools(Path('installer/registry.toml'))}; gb=ts['gnu-bash']; assert gb.cmd != 'bash'; assert {m.arch for m in gb.methods} == {('arm64',), ('amd64',)}"` exits 0.
    - `grep -c 'NO_METHOD' installer/registry.toml` is at least 1.
    - `grep -c 'brew install container' installer/registry.toml` is at least 1.
    - `make validate && make test` exits 0 on this task's own commit.
  </acceptance_criteria>
  <done>`gnu-bash` and `container` exist as verified `tier="system"` registry entries; `gnu-bash`'s detection design provably avoids the macOS system-bash false-positive; Apple Containers is confirmed as a real `brew install container` action (SC#4) and always appears in the registry regardless of platform, resolving to `NO_METHOD` on any machine it cannot install on (D-01); six new tests pin all of the above plus both dated comments.</done>
</task>

<task type="auto">
  <name>Task 2: Record D-01's disabled-state resolution as a durable architecture.md convention</name>
  <files>.claude/architecture.md</files>
  <read_first>
    - `.claude/architecture.md` in full — the existing `## Registry-authoring
      guidelines` section (Phase 6) with its two subsections, `### Per-tool, per-OS
      verification checklist` and `### Prefer brew` — this task appends a third
      subsection in the same prose style, citing real code and this phase's own
      `container` entry as the worked example.
    - This plan's own Task 1 (already executed, same plan) — the `container` entry
      and its comment this task cites.
    - 07-RESEARCH.md's Open Question 1 in full — the exact reasoning ("no persistent
      per-row 'disabled' render exists... the cheapest correct answer is probably 'no
      new render'...") this task turns into a permanent, generalized statement rather
      than a one-off registry comment a future reader might mistake for
      tool-specific trivia.
  </read_first>
  <action>
    Append a third subsection, `### Platform-gated entries need no separate "disabled"
    render`, to `.claude/architecture.md`'s existing `## Registry-authoring guidelines`
    section (after `### Prefer brew`), in the same prose style as the file's other
    sections.

    State the general convention, not just this one entry: a registry entry whose
    `Method.os`/`Method.arch` restrictions leave it with no applicable method on the
    current machine needs no separate "shown disabled" UI — `installer/resolve.py`'s
    existing `resolve_methods` already returns an empty list in that case, and
    `installer/engine.py::install_tool` already reports `NO_METHOD`, the one signal
    this project already uses for "cannot install here," identical for every tool
    regardless of why it cannot install. Cite `.claude/architecture.md`'s own existing
    "Tier is a browsing label" reasoning — "a screen with no `Platform` must not make a
    judgement it cannot make correctly" — as the same principle applied one level
    lower: `CatalogScreen`'s row rendering has no `Platform` context either, so it
    correctly shows every tool identically regardless of platform availability, and the
    availability verdict is deferred to the same place it already lives for every other
    tool: `resolve_methods`/`install_tool`, evaluated against the real `Platform` only
    at install time. State the concrete precedent this convention is not new: `codegraph`'s
    Linux/macOS split, `puppeteer`'s Linux-arm64 gate, and — the worked example this
    phase adds — Apple Containers' `os=["macos"], arch=["arm64"]` single method (D-01,
    Phase 7), which was the phase's explicit ask: the tool always appears in its tier
    view (never omitted) and, where unavailable, produces the exact same `NO_METHOD`
    outcome as every other platform-gated tool, with zero new rendering code. State
    explicitly, as the closing line of the subsection, that this resolves 07-RESEARCH.md's
    Open Question 1: D-01's "reuse the existing unmet-requires/unavailable-dependency
    display pattern" language pointed at this exact mechanism once named precisely, not
    at inventing a new persistent row-level render.

    The subsection must literally contain the strings `D-01`, `NO_METHOD`, and
    `container`.
  </action>
  <verify>
    <automated>grep -q '### Platform-gated entries need no separate "disabled" render' .claude/architecture.md && grep -q "D-01" .claude/architecture.md && grep -q "NO_METHOD" .claude/architecture.md && grep -q "container" .claude/architecture.md</automated>
    <fails_when>any `grep -q` in the chain matches zero times — the new subsection heading, or a required citation (`D-01`, `NO_METHOD`, `container`), is missing from `.claude/architecture.md`.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `<automated>` command above exits 0.
    - `.claude/architecture.md`'s `## Registry-authoring guidelines` section gains a third subsection, `### Platform-gated entries need no separate "disabled" render`, appended after the existing `### Prefer brew` subsection — no existing content altered or reordered.
    - The new subsection cites `resolve_methods`/`install_tool`/`NO_METHOD` by name, cites the pre-existing "Tier is a browsing label" reasoning ("a screen with no `Platform` must not make a judgement it cannot make correctly"), names `codegraph`/`puppeteer` as prior precedent and `container` (Apple Containers, D-01, Phase 7) as this phase's worked example, and states explicitly that this resolves 07-RESEARCH.md's Open Question 1.
    - `git diff -- .claude/architecture.md` shows only the new subsection appended — no existing line altered.
    - `make validate && make test` exits 0 on this task's own commit.
  </acceptance_criteria>
  <done>`.claude/architecture.md` documents, as a durable and generalized registry-authoring convention (not tool-specific trivia), that a platform/arch-gated entry needs no separate "disabled" render — `NO_METHOD` already is that signal — citing Apple Containers as the concrete Phase 7 worked example and closing 07-RESEARCH.md's Open Question 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `installer/registry.toml` (project-authored data) -> `installer/executors.py::_brew`'s `brew install <formula>` invocation | `formula` values are plain, non-interpolated argv elements sourced only from this project's own committed registry data, never runtime/user input |
| Homebrew's own package/formula trust boundary -> this project | `brew install bash`/`brew install container` rely entirely on Homebrew's own supply-chain trust (signed bottles, formula review) — outside this project's threat model, consistent with every existing `brew`/`cask` entry |
| A future registry editor -> `gnu-bash`'s detection design | The `cmd = "gnu-bash"` + arch-split `detect_path` design is non-obvious; a future edit that "simplifies" it back to `cmd = "bash"` would silently reintroduce the permanent false-positive this task fixes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | `installer/executors.py::_brew` — `brew install bash`/`brew install container` | low | accept | Already the accepted baseline for every `brew`-kind entry in this registry (Homebrew's own review/bottle-signing process); no new mitigation needed. |
| T-07-05 | Denial of Service (feature dead-on-arrival) | A future edit reverting `gnu-bash`'s `cmd` to the literal string `"bash"`, silently reintroducing the permanent macOS system-bash false positive this task fixes | medium | mitigate | `test_gnu_bash_avoids_system_bash_false_positive_via_detect_path` asserts `cmd != "bash"` directly; `test_gnu_bash_entry_records_the_detection_fix` pins the comment explaining why, so a future editor sees the reasoning before "simplifying" it away. |
| T-07-06 | Denial of Service (unenforceable version floor) | Apple Containers installed via `brew install container` on macOS < 26, where Homebrew's own formula requirement rejects the install | low | accept | Homebrew's own `requirements` metadata enforces the macOS-26/arm64 floor at install time with a clear failure, not a silent success (07-RESEARCH.md Pitfall 4); building a redundant version-detection mechanism in this project is out of scope for a registry-only phase and would duplicate a check Homebrew already performs correctly. |

</threat_model>

<verification>
Phase-level checks, run after both tasks:

- `uv run pytest tests/test_registry.py -q` exits 0 (all new tests pass, no regression, tier tripwire at 26).
- `make validate && make test` exits 0 on the committed tree.
- `grep -c "Verified 2026-09-06" installer/registry.toml` is at least 4 (this plan's two new comments plus 07-01's two).
- `.claude/architecture.md` shows only the new third subsection added in `git diff` — no existing content altered or removed.
</verification>

<success_criteria>
Contributes to ROADMAP Phase 7's numbered success criteria:

1. (SC#1, remainder) `gnu-bash` and Apple Containers install cleanly via verified live methods — `gnu-bash`'s detection design is provably correct (no permanent false positive), completing SC#1 alongside 07-01-PLAN.md's `zsh`/`oh-my-zsh`.
4. (SC#4) Apple Containers needs a real install step on current macOS (`brew install container`) — resolved and recorded, correcting REQUIREMENTS.md's speculative "may be a version-gate/doc entry" framing. D-01's "shows disabled when unavailable" is resolved as `NO_METHOD` (no new UI) and documented as a durable, generalizable convention.
</success_criteria>

<output>
Create `.planning/phases/07-system-user-tier-catalog-expansion/07-02-SUMMARY.md` when done.
</output>

