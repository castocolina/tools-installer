---
phase: 08-ai-tier-catalog-expansion-uv-tool-executor
plan: 04
type: execute
wave: 4
cross_ai: true
depends_on:
  - "08-03"
files_modified:
  - installer/registry.toml
  - tests/test_registry.py
  - tests/test_catalog_tui.py
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REQ-recommends-wiring-agent-hosts

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Selecting `claude`, `opencode`, `codex`, or `cursor-agent` surfaces `recommends = (\"codegraph\", \"graphify\", \"rtk\")` via the existing, unmodified Phase 2 mechanism (`installer/selection.py::unstaged_recommends` + `installer/catalog_tui.py`'s selection-time prompt) — never auto-installing anything (ROADMAP Phase 8 SC#4)."
    - "This is per-host research, not a uniform assumption applied blindly: 08-RESEARCH.md's per-host table shows each of `codegraph` (MCP server, host-agnostic by design), `graphify` (explicit README/PyPI support for all four hosts), and `rtk` (a native hook integration for three of the four hosts, and a weaker `AGENTS.md`-instructions-based integration for `codex`) has a real, vendor-documented integration path for all four hosts — the uniform outcome is the honest result of D-01's directive to verify per host, not a default the directive asked to avoid."
    - "`antigravity` declares NO `recommends` at all (not an empty list added defensively, but the field simply absent, matching its current entry) — deferred per CONTEXT.md D-01, not dropped; this is the one host explicitly excluded from this phase's wiring."
    - "Claude/opencode's Phase 2 illustrative placeholder (`recommends = [\"rg\", \"fd\", \"jq\"]`, explicitly commented as provisional pending this phase) is replaced with the real researched companion set, closing the exact gap ROADMAP Phase 2 SC#4's own amendment note named this phase as owning."
  artifacts:
    - installer/registry.toml
    - tests/test_registry.py
    - tests/test_catalog_tui.py
    - .planning/PROJECT.md
  key_links:
    - "Every id named in the four hosts' `recommends` lists (`codegraph`, `graphify`, `rtk`) already exists as a real catalog tool by the time this plan runs (`codegraph`: Phase 5; `graphify`: 08-01; `rtk`: 08-03) — verified by the existing, unmodified `test_shipped_registry_recommends_all_resolve` integrity test, which fails loudly if any recommended id is unknown, exactly the same mechanism that already guards every other tool's `recommends` list in this registry."
---

<objective>
Instantiate batch 1's `REQ-recommends-soft-dependency` mechanism (Phase 2, unchanged
here) with the real per-host companion data ROADMAP Phase 2 SC#4's own amendment note
named this phase as owning: `claude`, `opencode`, `codex`, and `cursor-agent` each gain
`recommends = ["codegraph", "graphify", "rtk"]`. `antigravity` gets none, per CONTEXT.md
D-01 — explicitly deferred, not dropped, until its own companion-tool ecosystem is
better understood.

Per D-01's directive — "research which of codegraph/graphify/rtk actually applies to
which host CLI — no all apply for every host agent cli" — this is not the ROADMAP's
original illustrative uniform-list assumption applied blindly. 08-RESEARCH.md's
per-host table shows the honest outcome of actually reading each tool's own
documentation: `codegraph` is an MCP server, host-agnostic by design; `graphify`'s own
README/PyPI description explicitly lists all four hosts among its 16+ supported
integrations; `rtk`'s README documents a native, runtime-enforced hook for
`claude`/`opencode`/`cursor-agent` (`--agent claude`, `--opencode`,
`--agent cursor`), and a materially weaker, convention-based `AGENTS.md`+`RTK.md`
instructions integration for `codex` — real and vendor-documented, but not a
runtime-enforced hook the way the other three are. The uniform recommends list across
all four hosts is therefore the RESULT of verification, not a shortcut around it — with
`rtk`'s one asymmetry (its weaker Codex integration) recorded as a comment rather than
silently smoothed over.

This is the phase's last plan and also closes out Phase 8: it consolidates the four
decisions made across 08-01 through 08-04 into `.planning/PROJECT.md`'s Key Decisions
table in one pass, mirroring Phase 6's and Phase 7's own end-of-phase consolidation
pattern.

Purpose: REQ-recommends-wiring-agent-hosts — ROADMAP Phase 8 SC#4.

Decisions implemented:
- CONTEXT.md D-01: per-host `recommends` membership is a research question, not a
  fixed constant — verified, not assumed, per host, in 08-RESEARCH.md's Architecture
  Patterns Pattern 3; `antigravity` is excluded from this phase's wiring per the same
  decision.
- 08-RESEARCH.md's Anti-Patterns: the seam's honest finding (uniform fit across the
  four hosts) is recorded as such, with `rtk`'s Codex-integration asymmetry called out
  rather than smoothed into an unqualified "these four tools recommend the same set."

Scope boundary: this plan edits only the `recommends` field on four already-existing
`[[tool]]` blocks (`claude`, `opencode`, `codex`, `cursor-agent`) plus their preceding
comments, and the exact-membership/coverage tests that pin the result. It does not
touch `antigravity`'s entry, `installer/selection.py`, or `installer/catalog_tui.py` —
the Phase 2 `recommends` mechanism itself is unchanged; this plan supplies data only.

Output: updated `recommends` fields and dated comments on `claude`/`opencode`/`codex`/
`cursor-agent` in `installer/registry.toml`; new/extended tests in
`tests/test_registry.py` pinning the exact per-host membership and `antigravity`'s
exclusion; a consolidated four-row Key Decisions update in `.planning/PROJECT.md`
covering the whole phase.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@.planning/phases/08-ai-tier-catalog-expansion-uv-tool-executor/08-CONTEXT.md
@.planning/phases/08-ai-tier-catalog-expansion-uv-tool-executor/08-RESEARCH.md
@.planning/phases/07-system-user-tier-catalog-expansion/07-03-PLAN.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@installer/registry.toml
@installer/selection.py
@tests/test_registry.py

`.codegraph/` exists at the repo root. Use `codegraph_explore` (MCP tool) or
`codegraph explore "<symbols>"` (shell) BEFORE Grep/Read when locating code. Run
`codegraph index || codegraph init -i` once before starting.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the real per-host `recommends` data onto `claude`/`opencode`/`codex`/`cursor-agent`</name>
  <files>installer/registry.toml, tests/test_registry.py, tests/test_catalog_tui.py</files>
  <read_first>
    - 08-RESEARCH.md's "Architecture Patterns -> Pattern 3: `recommends` wiring — real
      per-host data" in full, including the per-host evidence table and the exact
      registry comment example — the source of truth for both the data and the
      comment wording this task adds.
    - `.claude/architecture.md`'s "Tier is a browsing label" section, specifically the
      paragraphs on `recommends`: "never resolved, never ordered, never expanded
      transitively, and never installed on its own"; "the only code that reads
      `recommends` is `installer/selection.py::unstaged_recommends`... and
      `installer/catalog_tui.py`'s selection-time prompt" — confirms this plan needs
      no changes to either file, only registry data.
    - `installer/registry.toml` lines 1313-1373 (`codex`, `claude`, `opencode`, in
      full, including claude/opencode's existing Phase 2 illustrative three-item
      placeholder list — `rg`, `fd`, and `jq` — and its two-line deferral comment) —
      the exact blocks this task edits.
    - This phase's own 08-02-PLAN.md (already executed) — `cursor-agent`'s entry,
      created there with no `recommends` field, which this task now adds to.
    - `tests/test_registry.py` lines 1254-1276
      (`test_shipped_registry_recommends_all_resolve`,
      `test_agent_hosts_recommend_existing_catalog_tools`) — the existing generic
      integrity test (unchanged by this task, it will simply now also validate the
      three new ids trivially since they all resolve) and the host-loop test this
      task extends; note this test's own comment already anticipates this moment:
      "Phase 8 D-01 specifies per-host membership, so an equality check would be
      mandatory churn when the real companion data lands" — that data has now
      landed, so an exact-equality test is added ALONGSIDE (not instead of) this
      existing subset-style test, per the pattern `test_java_tools_install_
      exclusively_through_sdkman` already establishes for other locked-exact-value
      facts.
  </read_first>
  <behavior>
    - `load_tools(REGISTRY)`'s `claude`, `opencode`, `codex`, and `cursor-agent`
      entries each have `recommends == ("codegraph", "graphify", "rtk")`.
    - `load_tools(REGISTRY)`'s `antigravity` entry has `recommends == ()` — unchanged
      from 08-02.
    - `test_shipped_registry_recommends_all_resolve` continues to pass with zero
      changes to that test's own code (a pure consequence of `codegraph`, `graphify`,
      and `rtk` all existing as real catalog tools by this point in the phase).
  </behavior>
  <action>
    Write the tests first (RED — the real data is not yet wired), confirm they fail
    for the right reason (old placeholder / absent field, not a typo), then edit the
    registry (GREEN).

    Replace `claude`'s existing two-line deferral comment and its
    illustrative three-item placeholder `recommends` line (`rg`, `fd`, `jq`) with:

    ```
    # real companion set for this ai-tier catalog (Phase 8, REQ-recommends-wiring-agent-hosts):
    # codegraph (MCP server, host-agnostic), graphify (explicitly supports this host per its
    # own README), rtk (native hook integration for this host -- see 08-RESEARCH.md's
    # per-host table for the one exception, codex, where rtk's integration is
    # instructions-based rather than a runtime hook).
    recommends = ["codegraph", "graphify", "rtk"]
    ```

    Apply the identical edit to `opencode`'s existing placeholder.

    Add the identical comment-plus-`recommends` pair to `codex`'s block (which
    currently declares no `recommends` field at all), inserted directly after its
    `desc = "..."` line and before its `[[tool.method]]` block — matching where
    `claude`/`opencode` already place theirs relative to their own `desc` line.

    Add the identical comment-plus-`recommends` pair to `cursor-agent`'s block
    (created in this phase's 08-02 plan with no `recommends` field), inserted
    directly after its `desc = "..."` line and before its `[[tool.method]]` block.

    Do NOT add any `recommends` field to `antigravity` — its entry stays exactly as
    08-02 created it, per CONTEXT.md D-01's explicit exclusion.

    Add the tests to `tests/test_registry.py`:

    - Extend `test_agent_hosts_recommend_existing_catalog_tools`'s loop from
      `("claude", "opencode")` to `("claude", "opencode", "codex", "cursor-agent")` —
      the existing subset-style assertions (`recommends` non-empty, every id a known
      catalog tool) now apply meaningfully to all four real hosts.
    - `test_agent_host_recommends_match_the_researched_per_host_set` — the new
      exact-membership test: assert `claude.recommends == opencode.recommends ==
      codex.recommends == tools["cursor-agent"].recommends == ("codegraph",
      "graphify", "rtk")`, and assert `tools["antigravity"].recommends == ()`. This
      pins the ACTUAL researched outcome as a locked fact a future edit cannot
      silently drift from, per D-01 now being resolved rather than open.
    - `test_agent_host_recommends_entries_record_the_rtk_codex_caveat` — NOTE: unlike
      this registry's usual dated `# Verified` comments (which sit ABOVE `[[tool]]`),
      the `recommends` comment for `claude`/`opencode` is already shipped INSIDE the
      block, after `desc = "..."` and therefore after `id = "claude"`/`id =
      "opencode"` themselves (confirmed by reading the current
      `installer/registry.toml` directly) — this task's edit keeps that existing
      placement for all four hosts, it does not invent it. So this test must search
      the window AFTER each `id = "..."` line (through the tool's `[[tool.method]]`
      block or roughly 10 lines, whichever comes first), NOT before it — the
      "preceding" locality pattern used elsewhere in this registry does not apply
      here. Assert `"codegraph"`, `"graphify"`, and `"rtk"` each appear in that
      after-`id` window for `id = "claude"`, `id = "opencode"`, `id = "codex"`, and
      `id = "cursor-agent"`; additionally assert the literal phrase "instructions-
      based rather than a runtime hook" (the rtk/Codex caveat this test is named
      for) appears specifically in `codex`'s own window — a future edit dropping
      just that phrase while keeping the three tool names would otherwise still
      pass.

    Update the existing, now-stale
    `tests/test_catalog_tui.py::test_marking_claude_in_the_ai_view_offers_its_
    recommends_and_r_stages_them` — it loads the REAL registry and hardcodes the
    pre-Phase-8 placeholder recommends text (`"claude pairs well with rg, fd, jq -
    press r"`) and the resulting staged set (`{"claude", "rg", "fd", "jq"}`); after
    this task's registry edit, `claude.recommends` is `("codegraph", "graphify",
    "rtk")`, so both hardcoded strings would fail. Update them to the new researched
    values (`"claude pairs well with codegraph, graphify, rtk - press r"` and
    `{"claude", "codegraph", "graphify", "rtk"}`). Add `tests/test_catalog_tui.py`
    to this plan's `files_modified`.

    Do not modify `installer/selection.py`, `installer/catalog_tui.py`, or
    `installer/model.py` — the `recommends` mechanism itself (Phase 2) is unchanged;
    this task supplies data only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py tests/test_catalog_tui.py -q</automated>
    <fails_when>pytest exits non-zero; either new test fails; any of the four hosts'
    `recommends` is not exactly `("codegraph", "graphify", "rtk")`; or
    `antigravity.recommends` is anything other than `()`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry.py -q` exits 0.
    - `uv run python3 -c "from pathlib import Path; from installer.model import load_tools; ts={t.id:t for t in load_tools(Path('installer/registry.toml'))}; want=('codegraph','graphify','rtk'); assert ts['claude'].recommends==want; assert ts['opencode'].recommends==want; assert ts['codex'].recommends==want; assert ts['cursor-agent'].recommends==want; assert ts['antigravity'].recommends==()"` exits 0.
    - `grep -c 'rg", "fd", "jq"' installer/registry.toml` is 0 (the Phase 2 illustrative placeholder is fully retired, not left alongside the real data).
    - `make validate && make test` exits 0 on this task's own commit.
  </acceptance_criteria>
  <done>`claude`, `opencode`, `codex`, and `cursor-agent` each declare the real,
  researched `recommends = ("codegraph", "graphify", "rtk")`; `antigravity` remains
  unwired per D-01; the Phase 2 illustrative placeholder is fully retired; new tests
  pin the exact per-host membership and the recorded per-host rationale, closing
  REQ-recommends-wiring-agent-hosts.</done>
</task>

<task type="auto">
  <name>Task 2: Tier-3 verification — real install of `graphify` (uv-tool) and `rtk` (github_release) in a disposable container</name>
  <files>None (verification-only; no production or test files change)</files>
  <read_first>
    - ONESHOT-RULES.md Rule 14 (Tier-3 disposable-container verification requirement
      for any new install method this milestone introduces) — cross-AI review
      (cycle 1, HIGH) found this phase's plans (08-01's `graphify`/`uv-tool`, 08-03's
      `rtk`/`github_release`) proved their install methods only via unit tests and
      runner-recording assertions, never a real end-to-end install against a real
      filesystem — closing the same class of gap Phase 7's 07-03-PLAN.md PART G
      closed for `wezterm`'s AppImage path.
    - `installer/engine.py::install_tool` — the real, unmodified production entry
      point (`installer/app.py`'s only route into installing a catalog tool). Cycle 2
      review (HIGH) found the prior draft of this task called `_uv_tool`/
      `install_download` directly, bypassing `install_tool`'s resolver/routing ladder
      and `InstallOutcome` reporting — the actual thing this milestone ships. This
      task now goes through `install_tool` for both tools, exactly as `app.py` does.
    - `installer/model.py::load_tools` — loads `Tool` objects straight from
      `installer/registry.toml`; use it to fetch the real, committed `graphify` and
      `rtk` entries rather than constructing `Tool`/`Method` objects by hand.
  </read_first>
  <action>
    Cycle 2 review (HIGH) also found the prior container recipe under-specified:
    no `curl`/`tar`/CA-certificates install, no `uv`'s `~/.local/bin` on `PATH`, a
    `pip install uv` fallback that violates this repo's uv-only rule, and no
    concrete command to capture the resolved RTK tag. Use this exact recipe instead:

    ```bash
    docker run --rm -v "$(pwd)":/repo:ro -w /repo python:3.13-slim bash -c '
      set -eux
      apt-get update -qq && apt-get install -y -qq curl tar ca-certificates
      curl -LsSf https://astral.sh/uv/install.sh | sh
      export PATH="$HOME/.local/bin:$PATH"
      uv --version

      # Graphify: through the real production entry point.
      python3 - <<PY
import sys
sys.path.insert(0, "/repo")
from installer.engine import install_tool
from installer.model import load_tools
from installer.platform import Platform

tools = {t.id: t for t in load_tools("/repo/installer/registry.toml")}
graphify = tools["graphify"]
platform = Platform(os="debian", arch="amd64", immutable=False, has_brew=False)
outcome = install_tool(graphify, platform)
print("GRAPHIFY_OUTCOME", outcome.status, outcome.method_kind, outcome.verified)
assert outcome.status.value == "installed", outcome.errors
assert outcome.method_kind == "uv-tool"
PY
      export PATH="$HOME/.local/share/uv/tools/graphifyy/bin:$PATH"
      graphify --version || graphify --help

      # RTK: same production entry point, real live release.
      python3 - <<PY
import sys
sys.path.insert(0, "/repo")
from installer.engine import install_tool
from installer.model import load_tools
from installer.platform import Platform

tools = {t.id: t for t in load_tools("/repo/installer/registry.toml")}
rtk = tools["rtk"]
platform = Platform(os="debian", arch="amd64", immutable=False, has_brew=False)
outcome = install_tool(rtk, platform)
print("RTK_OUTCOME", outcome.status, outcome.method_kind, outcome.verified)
assert outcome.status.value == "installed", outcome.errors
assert outcome.method_kind == "github_release"
assert outcome.verified is True
PY
      export PATH="$HOME/.local/bin:$PATH"
      rtk --version
    '
    ```

    This exercises the real `installer.app`-facing path for both tools: registry
    load → `resolve_methods` → the actual `uv-tool`/`github_release` executor →
    `InstallOutcome`. No stubbed runner, no hand-built `Method`/`ExecContext`.

    Record in the SUMMARY, verbatim: the full container output (both `*_OUTCOME`
    print lines, the resolved RTK release tag visible in the download step, and the
    `graphify`/`rtk` version-command output). A genuine checksum mismatch, an
    `outcome.status` other than `installed`, or an install/download failure
    unrelated to a transient network issue IS a destructive-anomaly-shaped finding
    per ONESHOT-RULES Rule 9 — stop and report rather than writing a SUMMARY that
    contradicts the evidence (retry once first if the failure looks transient).
  </action>
  <verify>
    <automated>echo "Tier-3 container verification — no automated pytest gate; evidence recorded in SUMMARY per action above"</automated>
    <fails_when>the container run cannot install/build `uv`; either `install_tool`
    call's assertions fail (`status != "installed"`, wrong `method_kind`, or
    `verified is not True` for `rtk`); or either tool's version/help command does
    not exit 0.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records both tools' real container-verified install-and-run
      evidence verbatim, per the `<action>` above.
    - No production or test file changes in this task's commit (or no commit at all,
      if evidence is folded into Task 3's SUMMARY-writing commit) — this task is
      pure verification.
  </acceptance_criteria>
  <done>Both of this phase's genuinely new install mechanisms (`uv-tool` via
  `graphify`, and `rtk`'s `github_release`/checksum ladder) are proven to actually
  install and run a working binary in a real disposable container, not merely
  proven structurally correct via unit tests — closing cross-AI review's HIGH
  finding (cycle 1) that the phase close-out lacked ONESHOT Rule 14's required
  Tier-3 evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate Phase 8's decisions into `.planning/PROJECT.md`</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md`'s Key Decisions table in full — the existing three-column
      (`Decision`/`Rationale`/`Outcome`) shape and its most recent Phase 7 rows, the
      pattern this task extends (mirroring 07-03-PLAN.md's own Task 2).
    - This phase's four plans (08-01 through 08-04, all already executed by the time
      this task runs) — the concrete decisions each one made, which this task records
      in the ledger without re-deriving them.
  </read_first>
  <action>
    Add four new rows to `.planning/PROJECT.md`'s Key Decisions table (after the most
    recent existing Phase 7 rows, before the closing `---`), following the exact
    three-column shape the table already uses, each with `Outcome` = `Phase 8` (the
    literal string, no suffix). Keep each Rationale cell to one or two sentences plus
    a `See {file}` pointer:

    1. Decision: `installer/resolve.py`'s `_applies`/`_RANK` dispatch tables gained an
       explicit `"uv-tool"` case (unconditional-True, rung 20) alongside the new
       executor and `METHOD_KINDS` entry. Rationale: 08-RESEARCH.md's own change list
       named only `model.py`/`executors.py`; without this fix, resolving any
       `uv-tool` method would raise `KeyError` from `resolve.py`'s closed native-
       package-manager fallthrough. See 08-01-PLAN.md/08-01-SUMMARY.md.
    2. Decision: `graphifyy`'s automated `[SUS]` legitimacy flag was discharged with a
       live, fail-closed PyPI + GitHub identity/age check before its registry entry
       was written, mirroring Phase 5's established automated-gate pattern rather
       than a `gate="blocking-human"` stop. Rationale: ONESHOT-RULES Rules 9/14 (the
       Tier-3 container tier, not a mid-run human stop, is this project's escalation
       path; package identity here is checkable from live metadata with an exit
       code). See 08-01-PLAN.md/08-01-SUMMARY.md.
    3. Decision: `cursor-agent`'s registry `cmd` uses the legacy `"cursor-agent"`
       symlink name, not the script's newer primary `"agent"` name. Rationale: the
       newer name risks colliding with an unrelated `agent` binary from a different
       installed CLI on a multi-agent-tool machine; `cursor-agent` is unambiguous and
       matches every other reference to this tool already in this project. See
       08-02-PLAN.md/08-02-SUMMARY.md.
    4. Decision: `rtk`'s Linux install path uses two separate arch-gated
       `github_release` methods (musl for amd64, gnu for arm64), not one unscoped
       template, plus a same-project-confirmed `brew` fallback after the locked
       checksum-verified method. Rationale: upstream's own release assets are
       asymmetric per architecture — a single template would 404 on one of them; the
       `recommends` wiring on `claude`/`opencode`/`codex`/`cursor-agent`
       (`codegraph`/`graphify`/`rtk`) is the honest RESULT of per-host verification
       (D-01), not a uniform default applied blindly, with `rtk`'s weaker
       instructions-based Codex integration recorded rather than smoothed over;
       `antigravity` stays unwired per D-01, deferred not dropped. See
       08-03-PLAN.md/08-03-SUMMARY.md and 08-04-PLAN.md/08-04-SUMMARY.md.

    Do not alter any existing row or the table's column order.

    Finally, bump `.planning/PROJECT.md`'s closing `*Last updated: ...*` footer to the
    actual date this task runs (`date +%Y-%m-%d`), keeping the file's existing
    three-part shape (date, trigger phrase, brief description), e.g. `*Last updated:
    {date} after Phase 8 AI tier catalog expansion & uv-tool executor
    (08-01..08-04)*`.
  </action>
  <verify>
    <automated>[ "$(grep -cE '\| Phase 8 \|$' .planning/PROJECT.md)" -eq 4 ] && grep -Eq '^\*Last updated:.*Phase 8' .planning/PROJECT.md</automated>
    <fails_when>`.planning/PROJECT.md` does not contain exactly four table rows whose
    Outcome cell is the literal `Phase 8` (matched precisely as a row ending in
    `| Phase 8 |` — a decision row is missing, duplicated, or uses a
    different/suffixed outcome string), or the `*Last updated: ...*` footer still
    lacks a `Phase 8` citation.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `<automated>` command above exits 0.
    - `.planning/PROJECT.md`'s Key Decisions table gains exactly four new rows, each with `Outcome` = `Phase 8` (unsuffixed).
    - `git diff -- .planning/PROJECT.md` shows only the four new table rows plus the `*Last updated: ...*` footer line changed — no existing decision row altered, reordered, or removed.
    - `make validate && make test` exits 0 on this task's own commit.
  </acceptance_criteria>
  <done>`.planning/PROJECT.md`'s Key Decisions table records all four decisions this
  phase made across its four plans, each condensed to a short statement plus a
  cross-reference; its `Last updated` footer reflects Phase 8's completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `installer/registry.toml` (project-authored data) -> `installer/selection.py::unstaged_recommends` -> `installer/catalog_tui.py`'s selection-time prompt | Unchanged this plan; the existing, unmodified Phase 2 mechanism never auto-installs a recommendation — a user must press the accept key, which performs exactly the same mutation a manual space-mark performs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | This plan installs no package and adds no dependency to `pyproject.toml`; it edits four existing `recommends` fields only. The package-legitimacy gate does not apply here — it was already discharged for this phase's one flagged package (`graphifyy`) in 08-01-PLAN.md Task 1. |
| T-08-10 | Information Disclosure | Surfacing `rtk` (a token-usage-monitoring CLI) as a recommendation could be read as this project endorsing data collection it does not itself perform | low | accept | The `recommends` prompt is purely informational and non-blocking (Phase 2's existing mechanism); the registry comment states plainly what each tool does, and nothing is ever installed without an explicit user keypress. |
| T-08-11 | Denial of Service (stale companion data) | A future `codegraph`/`graphify`/`rtk` behavior change makes one host's integration path (especially `rtk`'s already-weaker Codex integration) obsolete without this registry's comment being revisited | low | accept | The registry comment explicitly names the one known asymmetry (rtk's Codex integration is instructions-based, not a runtime hook) so a future reader has the caveat already surfaced rather than discovering it independently; no automated staleness check exists for vendor-side integration claims, consistent with this project's existing registry-authoring convention (a dated comment, not a live re-check, per `.claude/architecture.md`'s Registry-authoring guidelines). |

</threat_model>

<verification>
Phase-level checks, run after both tasks — and, since this is the phase's final plan, a full Phase 8 close-out check:

- `uv run pytest tests/test_registry.py -q` exits 0 (all new tests pass, tier tripwire at `ai: 14`, unchanged from 08-03 since this plan adds no new tool).
- `uv run pytest tests/test_executors.py tests/test_resolve.py tests/test_model.py tests/test_registry.py -q` exits 0 — the full cross-plan test surface this phase touched (08-01 through 08-04 combined) is green, not just this plan's own file.
- `make validate && make test` exits 0 on the committed tree.
- `uv run python3 -c "from pathlib import Path; from installer.model import load_tools, load_categories; load_tools(Path('installer/registry.toml')); load_categories(Path('installer/registry.toml'))"` exits 0 — the full registry, including all four of this phase's new tools, still parses cleanly end to end.
- `.planning/PROJECT.md` shows only additions (four new, condensed Key Decisions rows) plus the `*Last updated: ...*` footer changed.
- The SUMMARY records the final `ai` tier count (14) and confirms every requirement this phase owns (REQ-uv-tool-executor, REQ-agent-host-entries, REQ-rtk-github-release, REQ-recommends-wiring-agent-hosts) is satisfied by a specific plan/task, per `human_verify_mode: end-of-phase`'s consolidation design (ONESHOT-RULES Rule 14).
</verification>

<success_criteria>
Completes ROADMAP Phase 8's numbered success criteria:

4. (SC#4) Selecting `claude`/`opencode`/`codex`/`cursor-agent` surfaces its
   `recommends` list (`codegraph`, `graphify`, `rtk`) via the existing Phase 2
   mechanism, without auto-installing anything — the real, per-host-verified
   companion set (D-01), not the ROADMAP's original illustrative uniform list applied
   blindly. `antigravity` remains unwired per D-01, deferred not dropped.

This plan closes Phase 8: SC#1 (08-01, the `uv-tool` executor and `graphify`), SC#2
(08-02, `antigravity`/`cursor-agent`), SC#3 (08-03, `rtk`), and SC#4 (this plan) are
all satisfied, each by a specific plan whose SUMMARY carries the evidence.
</success_criteria>

<output>
Create `.planning/phases/08-ai-tier-catalog-expansion-uv-tool-executor/08-04-SUMMARY.md` when done.
</output>
