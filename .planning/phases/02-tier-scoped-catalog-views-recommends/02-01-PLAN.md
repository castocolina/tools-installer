---
phase: 02-tier-scoped-catalog-views-recommends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - installer/ui_common.py
  - installer/tool_browser.py
  - installer/catalog_tui.py
  - installer/wizard_app.py
  - installer/deps.py
  - setup.py
  - tests/test_ui_common.py
  - tests/test_catalog_tui.py
  - tests/test_wizard_app.py
  - tests/test_setup.py
  - tests/test_deps.py
  - tests/test_tool_browser.py
  - .claude/architecture.md
autonomous: true
cross_ai: true
requirements:
  - REQ-catalog-tier-views

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The top nav offers six views in the order System / User / AI / Doctor / Uninstall / Policies; number keys 1-6 reach them and GLOBAL_NAV reads \"1-6 views\", both derived from the VIEWS table rather than hand-assigned (ROADMAP Phase 2 SC#1, D-01)."
    - "Each of the three tier views lists exactly the tools whose Tool.tier equals that view's name -- the three views partition the catalog with nothing lost and nothing duplicated -- and each still switches between the five existing groupings (Category/Priority/Audience/Status/Table) with left/right or the tabs (ROADMAP Phase 2 SC#1)."
    - "Marking a tool in one tier view and another tool in a different tier view, then pressing enter from either, commits ONE batch containing both ids in full-catalog order; `a`/`i` act only on the active tier view and never clear marks staged in another view."
    - "Marking a tool whose `requires` names a tool in a different tier names that dependency in the active view's status line at mark time, with no prior visit to the other tier view (ROADMAP Phase 2 SC#2/SC#3)."
    - "The ids a tier view returns still resolve against the whole catalog: resolve_dependencies over the full tools list drags in an unstaged system-tier dependency of an ai-tier selection, with zero changes to resolve_dependencies itself (ROADMAP Phase 2 SC#2/SC#3)."
    - "No test module still names the pre-Phase-2 flat catalog view: the string sweep over tests/test_wizard_app.py, tests/test_ui_common.py and tests/test_setup.py leaves zero occurrences, so no test can reach show_view with a name VIEWS no longer registers."
    - "The wizard renders the six-view nav and the cross-tier dependency notice in a REAL terminal, captured by tmux capture-pane text matching -- not only under the headless Textual pilot (ONESHOT Rule 8 / Rule 14 Tier 2; this is a gating check, not manual sanity)."
  artifacts:
    - installer/ui_common.py
    - installer/tool_browser.py
    - installer/catalog_tui.py
    - installer/wizard_app.py
    - installer/deps.py
    - setup.py
    - tests/test_ui_common.py
    - tests/test_catalog_tui.py
    - tests/test_wizard_app.py
    - tests/test_setup.py
    - tests/test_deps.py
    - tests/test_tool_browser.py
    - .claude/architecture.md
  key_links:
    - "installer/enums.py::Tier member values == the first three VIEW_ORDER names -> UnifiedApp.__init__ builds one CatalogScreen per Tier with view=tier.value, so no second name->tier mapping table exists anywhere"
    - "UnifiedApp._staged (one set[str]) -> each CatalogScreen._staged -> each ToolBrowser.selected (the same object, mutated in place) -> select_tools(catalog, list(staged)) -> CatalogScreen.Decided.result -> UnifiedApp.exit"
    - "ToolBrowser.SelectionChanged.item_id/.selected -> CatalogScreen.on_tool_browser_selection_changed -> deps.missing_requires(full catalog) -> StatusLine.set"
    - "ui_common.BASE_VIEW (= VIEW_ORDER[0]) -> UnifiedApp.get_default_screen / show_view / _navigable / action_back and setup.py::_build_app's initial_view default -- one constant replaces the scattered \"catalog\" sentinel"
---

<objective>
Split the single flat Catalog view into three tier-scoped top-level views (System / User / AI)
that land FIRST in the nav per D-01, backed by ONE shared staged selection so a batch assembled
across tiers commits as a single result -- and make a cross-tier `requires` drag-in visible from
whichever tier view the user happens to be standing in.

Purpose: ROADMAP Phase 2 SC#1-SC#3. Browsing must match how the user walks a fresh machine
(system prerequisites, then personal picks, then agent tooling), and a tier-filtered view must
never hide the fact that an ai-tier pick needs a system-tier prerequisite. This plan is also
the load-bearing half of the phase: plan 02-02's `recommends` prompt adds tools into the
shared staged batch this plan creates.

Output: three `View` rows replacing the single `catalog` row; a derived `BASE_VIEW` constant and
a derived `GLOBAL_NAV` string; `ToolBrowser` accepting an injected, shared selection set and
mutating it in place; `CatalogScreen` parameterised by view name / full catalog / shared staged
set; `UnifiedApp` hosting three tier `CatalogScreen`s; a new pure `deps.missing_requires`
preview; and the migrated test suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tier-scoped-catalog-views-recommends/02-CONTEXT.md
@.planning/phases/02-tier-scoped-catalog-views-recommends/02-PATTERNS.md
@installer/ui_common.py
@installer/tool_browser.py
@installer/catalog_tui.py
@installer/wizard_app.py
@installer/deps.py
@installer/enums.py
@installer/selection.py
@setup.py
@tests/test_ui_common.py
@tests/test_catalog_tui.py
@tests/test_wizard_app.py
@.claude/architecture.md
</context>

<artifacts_produced>
New symbols this plan introduces (cite these, not hallucinated ones, in review):

- `installer/ui_common.py::VIEWS` -- three new `View` rows replacing the single `catalog` row:
  `name="system"` / `label="System"`, `name="user"` / `label="User"`, `name="ai"` / `label="AI"`,
  all three keeping `mode="STAGED"`, `glyph="o"`, `style="cyan"` and the existing
  `actions="space toggle | enter install | a all | i invert"` string. `VIEWS` becomes a 6-row
  tuple; `doctor`/`uninstall`/`policies` keep their rows verbatim and shift to positions 4/5/6.
- `installer/ui_common.py::BASE_VIEW` -- new module constant, `BASE_VIEW: str = VIEW_ORDER[0]`.
  The single name for "the app's base screen", replacing the hard-coded `"catalog"` sentinel in
  four places in `wizard_app.py` plus `setup.py`'s `initial_view` default.
- `installer/ui_common.py::GLOBAL_NAV` -- changed from a hand-maintained literal to
  `f"1-{len(VIEWS)} views | ^p nav | esc back | q quit"` (renders `1-6 views`).
- `installer/tool_browser.py::ToolBrowser.__init__` -- new keyword-only parameter
  `selected: set[str] | None = None`; when given, that exact set object becomes
  `self.selected`, so several browsers can share one selection.
- `installer/tool_browser.py::ToolBrowser.refresh_marks` -- the existing private
  `_refresh_marks` renamed to a public method (3 internal call sites updated). Plan 02-02's
  recommends-accept path calls it; nothing else about it changes.
- `installer/tool_browser.py::ToolBrowser.SelectionChanged` -- gains two optional attributes,
  `item_id: str | None = None` and `selected: bool = False`, set by `action_toggle_selected`
  and left at their defaults by `action_select_all` / `action_invert`.
- `installer/catalog_tui.py::CatalogScreen.__init__` -- three new keyword-only parameters:
  `view: str` (which tier view this instance IS), `catalog: list[Tool]` (the WHOLE catalog, for
  full-catalog ordering and cross-tier `requires` lookups -- distinct from the tier-filtered
  `tools` it displays), and `staged: set[str]` (the selection shared by all three instances).
- `installer/catalog_tui.py::CatalogScreen._by_id` -- `{tool.id: tool}` over the WHOLE catalog (not
  the tier-filtered display list), so a cross-tier lookup resolves. Added in Task 3, its first
  consumer, to keep it from being an orphan helper in Tasks 1-2.
- `installer/deps.py::missing_requires` -- new pure function returning the transitive `requires`
  ids of one tool that are neither staged nor installed. A read-only preview, NOT a second
  resolver: no ordering, no availability verdict, no cycle error, no drag-in.
- `installer/wizard_app.py::UnifiedApp._catalogs` -- new `dict[str, CatalogScreen]` keyed by
  tier-view name, replacing the single `self._catalog` attribute.
- `installer/wizard_app.py::UnifiedApp._staged` -- new `set[str]`, the one staged batch shared
  by the three tier screens.
- `installer/wizard_app.py::UnifiedApp.catalog_for` -- new public accessor
  `catalog_for(view: str) -> CatalogScreen`, the headless-test seam for the two non-base tier
  screens (the existing `catalog` property is kept, now returning the base/system screen).
- `tests/test_catalog_tui.py::_tool` -- gains keyword-only `tier: str = "system"` and
  `requires: tuple[str, ...] = ()` parameters.
- New tests listed per task below.

No new modules and no new files are created: full-catalog ordering reuses the existing
`installer/selection.py::select_tools`, so no `StagedSelection`-style class is introduced.
No changes to `installer/model.py`, `installer/registry.toml`, or
`installer/deps.py::resolve_dependencies` (only an addition beside it) in this plan.
`Tool.recommends` and the recommends prompt belong to plan 02-02.
</artifacts_produced>

<source_audit>
| Source | Item | Covered by |
|---|---|---|
| GOAL | Three top-level tier views in walk-a-fresh-machine order | Task 2 |
| GOAL | Picking an AI tool surfaces complementary tools, never auto-installed | plan 02-02 |
| REQ | REQ-catalog-tier-views (three tier-scoped views, groupings kept, cross-tier drag-in visible from the dependent's own tier view) | Task 1 (shared-selection seam), Task 2 (views + groupings), Task 3 (cross-tier drag-in visibility) |
| REQ | REQ-recommends-soft-dependency | plan 02-02 |
| CONTEXT | D-01 tier views land first, existing views shift +2 | Task 2 |
| CONTEXT | D-02 demo pair uses tools already in registry.toml | plan 02-02 |
| CONTEXT | D-03 real `recommends` data deferred to Phase 8 | plan 02-02 (not implemented here, by design) |
| CONTEXT | D-04 prompt fires on space-mark | plan 02-02 |
| CONTEXT | D-05 accept joins the SAME staged batch | Task 1 builds the shared batch; Task 2 shares it across three screens; plan 02-02 writes into it |
| CONTEXT | Discretion: how tier CatalogScreens share code | Task 1 (parameterised screen) + Task 2 (one class, three instances, pre-filtered `tools` + shared `staged`) |
| CONTEXT | Discretion: how three `Decided` messages reduce to one result | Task 1 (see `<design_decisions>`) |

No unplanned items. Deferred-Ideas exclusion: wiring `claude`/`opencode`/`codex`/`cursor-agent`/
`antigravity`'s real `recommends` data is Phase 8 (REQ-recommends-wiring-agent-hosts) and is
deliberately absent from both plans.
</source_audit>

<design_decisions>
02-PATTERNS.md flags two open design points with no existing precedent. This plan resolves the
first; plan 02-02 resolves the second.

**Open point 1 -- how three `CatalogScreen` instances reduce their `Decided` messages to one
result.** They do not need reducing, because they never hold three separate selections. All
three share ONE `set[str]` owned by `UnifiedApp` (`self._staged`), handed down to each
`CatalogScreen` and on to each `ToolBrowser` as its `selected` attribute. Only the ACTIVE screen
receives key events, so only one `Decided` is ever posted; whichever screen posts it reports the
whole shared batch, ordered against the full catalog via the existing
`installer/selection.py::select_tools`. The existing single `on_catalog_screen_decided` handler
is unchanged.

Two consequences make this safe rather than clever:
- `ToolBrowser.action_select_all` / `action_invert` currently REBIND `self.selected` to a fresh
  set built from that browser's own items, which would silently discard the other two tiers'
  marks. Both are changed to mutate in place (`|=` and `^=` over this browser's selectable ids).
  For a browser that owns its whole set -- the Uninstall view, and any single-view catalog --
  the result is bit-for-bit identical, because `selected` is then always a subset of that
  browser's selectable ids; for a shared set it becomes exactly the wanted "act on the active
  tier view only" semantic.
- `ToolBrowser.selected_ids()` and `_refresh_marks`/`refresh_marks` already filter by this
  browser's own adapter items, so a shared set carrying foreign tier ids is inert in both.

**Why the ROADMAP's `claude` -> `pnpm` example is proved with `mmdc` -> `pnpm` instead.**
ROADMAP SC#2 originally named "`claude` (ai tier) ... surfaces the `pnpm` (system tier) drag-in
notice". Cycle-1 review flagged that this criterion stays literally false after the plan lands, so
**SC#2 has been rewritten in `.planning/ROADMAP.md`** to name the boundary-crossing behaviour rather
than a fixture that does not exist, with an inline note recording the rewrite and pointing at
Phase 8. The mechanism this plan delivers is unchanged; only the criterion's example is now true.
The reasoning behind the substitution follows.
`claude` in `installer/registry.toml` installs via `kind="script"` (`https://claude.ai/install.sh`)
and `kind="cask"` (`claude-code`) and declares no `requires` at all -- it does not depend on
`pnpm`, and inventing that edge would put a false fact in the catalog. The only real cross-tier-
VIEW `requires` edge in the catalog today is `mmdc` (tier `user`) -> `pnpm` (tier `system`);
`java`/`gradle`/`maven`/`groovy`/`springbootcli` -> `sdkman` are all system->system, so they
cross no view boundary. This is the same documented substitution STATE.md already records for
Phase 1 ("REQ-dependency-chain-requires' illustrative examples ... name tools not yet in
registry.toml"), and Phase 8 is where agent hosts gain real cross-tier relationships. Task 2
therefore proves SC#2/SC#3 with the real `mmdc` -> `pnpm` edge plus a synthetic ai->system
fixture that stays valid even if Phase 5 retargets `mmdc.requires`.

**Why the in-view notice is a preview, not a second resolver.** `deps.missing_requires` names
what will be dragged in; it deliberately makes no availability judgement (a `CatalogScreen` has
no `Platform`). The authoritative verdict -- including the "not available on this platform --
skipped" wording SC#3 mentions as the alternative -- stays with
`resolve_dependencies` + `render_dependency_notice` on the post-TUI path in
`installer/app.py::run_wizard`, unchanged. Cycle-1 review noted this leaves SC#3's *parenthetical*
alternative ("or an unavailable-dependency notice") outside the view. SC#3's primary branch -- the
drag-in being obvious with no required visit order -- IS delivered in-view, and the criterion reads
"drag-in **or** unavailable notice", so the criterion is met. The residual honesty gap is that the
notice would name a dependency that `resolve_dependencies` may later skip as unavailable. Task 3
closes that with wording rather than architecture: the notice says the ids are added automatically
at install time **and that any unavailable on this platform are reported when the installer runs**.
Giving `CatalogScreen` a `Platform` and an availability callable to make the verdict in-view is
explicitly rejected here -- it would duplicate `resolve_dependencies`' judgement in a second place,
which is exactly the drift `.claude/architecture.md` forbids.
</design_decisions>

<review_dispositions>
Cycle-1 cross-AI review (`02-REVIEWS.md`, `opencode-plan-review` / `xai/grok-4.6`). Every current
actionable finding against plan 02-01, and where it now lives.

| Finding | Severity | Disposition | Where |
|---|---|---|---|
| `tests/test_wizard_app.py` `"catalog"` string sentinels under-specified (`_app` default :94, `test_esc_on_catalog_is_inert` :885, ~10 `current_view ==` asserts) | HIGH | **Incorporated** | Task 2 `<action>` enumerates every line by number across all three test modules; `<behavior>` adds the sweep as a RED gate; `<verify>` adds a negative grep over the three files; `must_haves.truths` gained the zero-occurrences truth |
| ROADMAP SC#2 stays literally false (`claude` has no `requires`) | MEDIUM | **Incorporated (criterion rewritten)** | `.planning/ROADMAP.md` SC#2 rewritten to name the boundary-crossing behaviour + `mmdc`->`pnpm` + an ai->system fixture, with an inline note recording the rewrite; `<design_decisions>` records the reconciliation |
| SC#3's unavailable-dependency notice is not visible in-view | MEDIUM | **Partially incorporated, remainder explicitly rejected** | SC#3's primary branch is delivered in-view; Task 3 widens the notice wording to name the platform caveat and pins it with an assertion. Putting the availability *verdict* in-view is rejected: it needs a `Platform` on the screen and would be a second copy of `resolve_dependencies`' judgement. Rationale in `<design_decisions>` |
| Task 1 bundles nav + shared selection + three screens + setup.py + four test modules in one low-confidence tracer | MEDIUM | **Incorporated (task split)** | Split into three tasks: Task 1 is a behaviour-preserving seam refactor (one screen, green suite, no view rename), Task 2 is the user-visible three-view slice plus the sentinel sweep, Task 3 is the notice. Each task boundary is a green tree, per `CLAUDE.md`'s validate-before-commit rule -- which is why the split is by *commit-safe layer*, not by leaving a red tree between tasks |
| ONESHOT Rule 8 (real terminal session) marked "optional, not gating" despite `UI hint: yes` | MEDIUM | **Incorporated (made gating)** | Task 3 gains a gating `tmux` `capture-pane` structural check as an automated verify step; the plan-level verification section's final step is now step 6 and explicitly GATING; `must_haves.truths` gained the real-terminal truth. Per ONESHOT Rule 14 Tier 2 this is a text match the executor runs itself -- no human gate, navigation-only per Rule 5 |
| `missing_requires` should use `installed.get(dep_id, False)`, not indexing | LOW | **Incorporated** | Task 3 `<action>` mandates `.get`, `<behavior>` adds a partial-installed-map RED test, `<acceptance_criteria>` pins it |
| `SelectionChanged.__init__` must call `super().__init__()` | LOW | **Incorporated** | Task 1 `<action>` mandates copying `Accepted`'s shape; `<verify>` asserts message delivery still works |
| `tests/test_tool_browser.py` absent from Task 1's targeted pytest | LOW | **Incorporated** | Added to `files_modified`, to Task 1's `<files>` and `<verify>`, and to `<verification>` step 2 |
</review_dispositions>

<tasks>

<task_ordering_note>
Cycle-1 review flagged the original single tracer as too large (nav table + shared selection + three
screens + `setup.py` + four test modules, at `confidence: low`). It is now three tasks. The split is
by COMMIT-SAFE LAYER rather than by vertical slice, because `CLAUDE.md` requires
`make validate && make test` to pass on the exact tree of every commit -- so no task may end on a red
tree, and "views renamed but tests not yet migrated" is not an available boundary.

Task 1 is therefore a behaviour-preserving refactor: it builds the whole shared-selection seam and
the derived base-view constant while `VIEWS` still holds the single flat catalog row, so the app
behaves identically and the existing suite passes untouched. Task 2 is the tracer proper -- the first
task after which a user can do something new (three tier views, one batch across them) -- and its
only mechanical surface is the table plus the test sweep, because every structural change it would
otherwise have carried already landed green in Task 1. Task 3 adds the cross-tier notice.
</task_ordering_note>

<task type="auto" tdd="true">
  <name>Task 1: Shared-selection seam and a derived base-view constant, with no user-visible change</name>
  <files>installer/ui_common.py, installer/tool_browser.py, installer/catalog_tui.py, installer/wizard_app.py, setup.py, tests/test_ui_common.py, tests/test_tool_browser.py, tests/test_catalog_tui.py</files>
  <read_first>
    installer/ui_common.py:101-142 (the `VIEWS` tuple -- read it but do NOT edit it in this task),
    :144-145 (`VIEW_ORDER`/`VIEW_BY_NAME`, both derived -- the anchor `BASE_VIEW` is defined under);
    installer/tool_browser.py:70-78 (`Accepted` -- note `super().__init__()` then attribute
    assignment; this is the exact shape `SelectionChanged` must copy), :80-82 (`SelectionChanged`,
    today a bare `Message` subclass with no `__init__`), :100-111 (`__init__`), :126-149 (`_rebuild`
    and its `call_after_refresh(self._refresh_marks)` at :149), :176-220
    (`_highlighted_item`/`_first_selectable_row`/`_refresh_marks`/`action_toggle_selected`), :222-238
    (`action_select_all`/`action_invert` -- the two set REBINDS this task converts to in-place
    mutation), :240-249 (`selected_ids`/`action_accept`);
    installer/catalog_tui.py:132-176 (`CatalogScreen` docstring, `Decided`, `__init__`, `_adapter`),
    :245-251 (`on_tool_browser_accepted` -- the empty-selection warning moves from this browser's ids
    to the shared batch);
    installer/selection.py:81-84 (`select_tools` -- catalog-ordered tools for a set of ids; the
    ordering helper being reused instead of writing a new one);
    installer/wizard_app.py:674-698 (`__init__` -- the single `CatalogScreen` at :690), :708-720
    (`on_mount` and its install_screen comment), :722-730 (`catalog` property +
    `get_default_screen`), :732-742 (`show_view`), :744-750 (`_navigable`), :762-767 (`action_back`);
    setup.py:128-135 and :210-222 (`_build_app`'s `initial_view` default and the `UnifiedApp(...)`
    construction);
    tests/test_tool_browser.py (the whole module -- the widget's isolation tests, which must stay
    green through the selection-seam change and are this task's primary regression net);
    tests/test_catalog_tui.py:199-218 (`test_select_all_and_invert` /
    `test_enter_returns_selection_in_catalog_order` -- the two tests that pin today's bulk-action and
    ordering semantics and must pass unchanged).
  </read_first>
  <behavior>
    - RED (tests/test_ui_common.py) `test_base_view_is_the_first_registered_view`:
      `BASE_VIEW == VIEW_ORDER[0]` and `BASE_VIEW in VIEW_BY_NAME`. Deliberately asserts the
      DERIVATION, not a literal name, so it survives Task 2 untouched.
    - RED (tests/test_tool_browser.py) `test_browser_adopts_an_injected_selection_set`: constructing
      `ToolBrowser(adapter, selected=shared)` makes `browser.selected` the SAME object as `shared`
      (`is`, not `==`), and a `space` toggle is visible in `shared` from outside the widget.
    - RED (tests/test_tool_browser.py) `test_bulk_actions_never_rebind_the_selection_set`: capture
      `id(browser.selected)` before, press `a`, then `i`; the identity is unchanged after both, and
      a foreign id seeded into the shared set before either press is still there afterwards.
    - RED (tests/test_tool_browser.py) `test_selection_changed_carries_the_toggled_row`: a `space`
      toggle delivers a `SelectionChanged` whose `item_id` is the toggled id and whose `selected`
      reflects the new state; `a` and `i` deliver one whose `item_id` is `None`. Delivery itself is
      the assertion that matters -- a `SelectionChanged.__init__` that forgets `super().__init__()`
      never arrives, so this test fails by timeout/absence rather than by a wrong value.
    - GREEN afterwards: `tests/test_tool_browser.py`, `tests/test_catalog_tui.py`,
      `tests/test_wizard_app.py`, `tests/test_ui_common.py`, `tests/test_setup.py`,
      `tests/test_uninstall_e2e.py` and `tests/test_policies_e2e.py` all pass with NO edits to their
      existing assertions. That is the point of this task: the app is byte-for-byte the same to a
      user, so nothing in the suite should need migrating yet.
  </behavior>
  <action>
    Start in installer/ui_common.py, and change nothing except an addition. Directly under
    `VIEW_BY_NAME`, add `BASE_VIEW: str = VIEW_ORDER[0]` with a one-line comment explaining that the
    first registered view is the app's base screen (a Textual app has exactly one) and that every
    other view is installed and pushed on top of it -- so reordering `VIEWS` moves the base screen
    with it and no call site needs editing. Do NOT touch the `VIEWS` rows or `GLOBAL_NAV` in this
    task; Task 2 owns both. Because `VIEWS` is unchanged here, `BASE_VIEW` evaluates to the name the
    single flat catalog row already carries, which is precisely why the substitutions below are
    behaviour-preserving.

    Next installer/tool_browser.py. Give `__init__` a keyword-only `selected: set[str] | None = None`
    parameter and assign `self.selected = set() if selected is None else selected` -- when a set is
    passed the browser adopts THAT object rather than copying it, which is how several browsers will
    later mutate one shared batch. Document that on the attribute with a comment: several browsers
    may share one set, so the two bulk actions must never rebind it. Convert `action_select_all` to
    build the selectable-id set into a local and then `self.selected |= that_set`, and
    `action_invert` to `self.selected ^= that_set`, in both cases leaving `self.selected` bound to
    the same object. State in a comment why this is behaviour-preserving for a browser that owns its
    whole set: `selected` is then always a subset of that browser's selectable ids, so union
    collapses to the old assignment and symmetric-difference collapses to the old set-difference.
    Rename `_refresh_marks` to `refresh_marks` (public) and update its three call sites
    (`call_after_refresh` in `_rebuild`, and the calls at the end of `action_select_all` and
    `action_invert`); the body, the `NoMatches` guard, and its comment are unchanged.

    Then give `SelectionChanged` a real payload, copying `Accepted`'s shape exactly:
    `def __init__(self, item_id: str | None = None, selected: bool = False) -> None:` whose FIRST
    statement is `super().__init__()`, followed by the two attribute assignments. The `super()` call
    is not optional bookkeeping -- `Message.__init__` is what makes a Textual message deliverable,
    and omitting it silently breaks dispatch of every `SelectionChanged` in the app, including the
    Uninstall view's existing handler. Give it a docstring saying `item_id` names the single row a
    `space` toggle changed and `selected` whether it is now marked, and that the bulk actions leave
    both at their defaults because they change many rows at once. Post
    `self.SelectionChanged(item_id, item_id in self.selected)` from `action_toggle_selected`; leave
    the two bulk posts as bare `self.SelectionChanged()`. The Uninstall view's handler
    (`installer/wizard_app.py::UninstallScreen.on_tool_browser_selection_changed`) ignores the
    payload and needs no edit.

    Next installer/catalog_tui.py. Give `CatalogScreen.__init__` three keyword-only parameters after
    the existing positional three: `view: str`, `catalog: list[Tool]`, `staged: set[str]`. Pass
    `view` straight to `super().__init__(view=view)` in place of the hard-coded name. Store
    `self.tools = list(tools)` as today (this instance's display list), plus
    `self._catalog = list(catalog)` (the WHOLE catalog -- needed for full-catalog result ordering now
    and for cross-tier lookups in Task 3 and plan 02-02) and `self._staged = staged`. Build the
    browser as `ToolBrowser(self._adapter(), selected=staged)`. Rewrite `on_tool_browser_accepted` so
    the empty-selection check and the `Decided` payload both read the shared batch instead of this
    browser's ids: compute `ids = [tool.id for tool in select_tools(self._catalog,
    list(self._staged))]` (importing `select_tools` from `installer.selection`), warn with the
    existing "Select at least one tool, or press q to quit." message and return when it is empty,
    otherwise clear the status and post `self.Decided(ids)`. With one screen this is provably
    identical to today -- `_staged` IS that browser's set and `select_tools` already returns catalog
    order, which `test_enter_returns_selection_in_catalog_order` pins -- and it is what makes enter
    from a non-owning tier view correct in Task 2. Update the class docstring to say `Decided`
    carries the whole staged batch in full-catalog order regardless of which instance posted it.
    Leave `_adapter`, `_row_cells`, `_groups`, `_detail_text`, `_sort`, the five public seams,
    `on_tool_browser_selection_changed` and `on_data_table_header_selected` untouched.

    Next installer/wizard_app.py. Import `BASE_VIEW` from `installer.ui_common`. In `__init__`, add
    `self._staged: set[str] = set()` above the catalog screen and construct that still-single screen
    as `CatalogScreen(tools, installed, blurbs, view=BASE_VIEW, catalog=list(tools),
    staged=self._staged)`. Default `initial_view` to `BASE_VIEW` and set
    `self.current_view = BASE_VIEW`. Replace the four remaining base-screen name literals with
    `BASE_VIEW`: the `on_mount` guard, the two comparisons in `show_view`, and the one in
    `action_back`. Do not restructure `_views`, `on_mount`'s install loop, `get_default_screen`, the
    `catalog` property, `_navigable`, `BINDINGS`, `on_catalog_screen_decided`, `NavScreen`,
    `action_abort` or `action_hard_abort` -- Task 2 owns every one of those.

    Then setup.py: import `BASE_VIEW` from `installer.ui_common` and use it as `_build_app`'s
    `initial_view` default. The three keyword call sites that pass `"doctor"`, `"uninstall"` and
    `"policies"` are unaffected, and `tests/test_setup.py`'s fake keeps its own literal default,
    which still matches.

    Finally add the four RED tests listed in `<behavior>` -- one in tests/test_ui_common.py, three in
    tests/test_tool_browser.py -- and add NOTHING else to the test suite. If any pre-existing test in
    any module needs its assertions edited to pass, stop: that means a change in this task was not
    behaviour-preserving, and the fix belongs in the source, not the test.
  </action>
  <reversibility rating="costly">
    The shared-selection seam is the load-bearing decision of the phase: unwinding it later means
    re-privatising `ToolBrowser.selected`, reverting `select_all`/`invert` to rebinding, and
    collapsing `CatalogScreen`'s catalog/staged parameters -- after Task 2, plan 02-02 and Phase 8
    have all built on it. `BASE_VIEW` on its own is trivially reversible.
  </reversibility>
  <verify>
    <automated>uv run python3 -c "import inspect; from installer.ui_common import BASE_VIEW, VIEW_ORDER; from installer.tool_browser import ToolBrowser; assert BASE_VIEW == VIEW_ORDER[0], BASE_VIEW; p = inspect.signature(ToolBrowser.__init__).parameters['selected']; assert p.kind is inspect.Parameter.KEYWORD_ONLY, p.kind; assert p.default is None; assert hasattr(ToolBrowser, 'refresh_marks'), 'refresh_marks was not made public'; src = inspect.getsource(ToolBrowser.SelectionChanged.__init__); assert 'super().__init__()' in src, 'SelectionChanged.__init__ does not call super().__init__() -- Textual will not deliver it'"</automated>
    <fails_when>Non-zero exit. An `ImportError` means `BASE_VIEW` was never added; a `KeyError` on
    `selected` means the injected-set parameter is missing or was made positional; an
    `AttributeError` on `refresh_marks` means the rename was skipped, which strands plan 02-02's
    accept path; the last assertion fails when `SelectionChanged.__init__` was written without
    chaining to `Message.__init__`, the failure mode the cycle-1 review called out as silently
    breaking message delivery.</fails_when>
    <automated>uv run pytest tests/test_tool_browser.py tests/test_catalog_tui.py -q</automated>
    <fails_when>Non-zero exit. A failing `test_select_all_and_invert` means `|=`/`^=` were not
    equivalent to the old rebind for a browser that owns its whole set -- most likely the local set
    was built from all rows instead of only SELECTABLE rows. A failing
    `test_enter_returns_selection_in_catalog_order` means `Decided` is no longer catalog-ordered, so
    `select_tools` was handed the display list instead of `self._catalog`. A test that hangs or
    reports a never-delivered message points at the missing `super().__init__()`.</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>Non-zero exit anywhere. This task is behaviour-preserving by construction, so ANY
    pre-existing test failing here is a real regression in the seam -- do not migrate a test to make
    it pass, fix the source.</fails_when>
    <automated>make validate</automated>
    <fails_when>Non-zero exit. A vulture hit on `refresh_marks` means the three internal call sites
    were not all updated to the public name; pyright errors point at the keyword-only signatures.</fails_when>
  </verify>
  <acceptance_criteria>
    `installer/ui_common.py` exports `BASE_VIEW` derived as `VIEW_ORDER[0]`, and its `VIEWS` tuple
    and `GLOBAL_NAV` are byte-identical to before this task. `ToolBrowser.__init__` accepts a
    keyword-only `selected` set and adopts that exact object; `action_select_all` and `action_invert`
    mutate it in place and never rebind it; `refresh_marks` is public with all three call sites
    updated; `SelectionChanged.__init__` calls `super().__init__()` first and stores `item_id` and
    `selected`. `CatalogScreen.__init__` takes keyword-only `view`, `catalog` and `staged`, stores
    `_catalog` and `_staged`, injects `staged` into its browser, and posts `Decided` built from
    `select_tools` over the full catalog. `UnifiedApp` owns one `_staged` set and passes it to its
    single catalog screen; the four base-screen name literals in `wizard_app.py` and `setup.py`'s
    `_build_app` default are all `BASE_VIEW`. Exactly four tests were added and zero existing tests
    were edited, skipped, xfailed, deleted or weakened. `make validate && make test` pass.
  </acceptance_criteria>
  <done>The whole shared-selection machinery exists and is proven by the widget's own isolation
  tests, the base screen is named by one derived constant instead of a literal, and the app is
  indistinguishable from before to any user or any existing test -- so Task 2's diff is the view
  table plus a string sweep, and nothing else.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Three tier-scoped catalog views over the shared staged selection, end to end</name>
  <files>installer/ui_common.py, installer/catalog_tui.py, installer/wizard_app.py, tests/test_ui_common.py, tests/test_catalog_tui.py, tests/test_wizard_app.py, tests/test_setup.py</files>
  <read_first>
    installer/ui_common.py:79-96 (the `View` frozen dataclass -- the row shape the three new rows
    must fill), :101-142 (the `VIEWS` tuple; the single flat catalog row at :102-111 is the template
    for all three new rows and is the row being replaced), :144-146 (`VIEW_ORDER`/`VIEW_BY_NAME`
    plus the `BASE_VIEW` Task 1 added -- all three derived, none needs an edit), :148-154
    (`_view_key`, position-derived), :160 (`GLOBAL_NAV`, the one hand-maintained literal),
    :163-183 (`FooterBar`), :185-224 (`WayfindingHeader` -- both iterate `VIEWS` generically, so
    neither needs per-view branching);
    installer/enums.py:23-28 (`Tier`: SYSTEM/USER/AI declared in exactly the nav order D-01 asks
    for, with values equal to the three new view names -- this is the coupling that removes any
    need for a second mapping table);
    installer/catalog_tui.py:132-176 (`CatalogScreen` docstring and `__init__` as Task 1 left it,
    already taking keyword-only `view`/`catalog`/`staged`), :193-196 (`_groups`), :217-232 (the
    public seams `view`/`selected`/`detail_text`/`status_text`);
    installer/wizard_app.py:34-43 (the `installer.ui_common` import block), :652-672 (`UnifiedApp`
    docstring + `BINDINGS`, note the `enumerate(VIEW_ORDER)` number-key generator at :668-671
    that needs no change), :674-698 (`__init__` as Task 1 left it -- the single `CatalogScreen`
    and the `_views` dict), :708-720 (`on_mount` and its install_screen comment -- read the whole
    comment, it explains why every non-base screen MUST be installed), :722-730 (`catalog`
    property + `get_default_screen`), :744-750 (`_navigable`), :783-784
    (`on_catalog_screen_decided`);
    tests/test_ui_common.py:84-99 (the wayfinding-header test asserting `\[2] Doctor` and the
    `\[1]` flat-catalog token), :101-107, :137-160, :168-183, :205-215 (every place a view name, a
    view index, or the `1-4 views` literal is asserted -- specifically :104, :144, :154, :178
    and :207, enumerated in `<action>`);
    tests/test_catalog_tui.py:17-69 (`_unified_app`, `_tool`, `_catalog` fixtures), :154-359
    (every headless test -- they all drive the base screen through `app.catalog`);
    tests/test_wizard_app.py:24-34 (`_tool`), :86-118 (`_app`, whose `initial_view` default at :94
    is a string sentinel, plus the `VIEW_ORDER` assertion at :118), :120-145 and :245-270 (the nav
    tests whose number keys all shift by +2), :330-420 (the uninstall tests that `press("3")` from
    the default view at :336, :352, :376, :393, :414), :870-955 (`test_esc_on_catalog_is_inert` at
    :885 and the remaining `current_view` assertions at :879, :888, :925, plus the comment at :929
    and the view-name tuple at :951);
    tests/test_setup.py:35-65 (the `_build_app` fake whose `initial_view` default at :43 is a
    string sentinel Task 1 deliberately left alone).
  </read_first>
  <behavior>
    - RED (tests/test_ui_common.py) `test_tier_views_lead_the_nav_and_match_the_tier_enum`:
      `VIEW_ORDER == ("system", "user", "ai", "doctor", "uninstall", "policies")`;
      `VIEW_ORDER[:3] == tuple(tier.value for tier in Tier)` (so a rename on either side fails
      loudly instead of drifting); `BASE_VIEW == "system"`; `set(VIEW_BY_NAME) == set(VIEW_ORDER)`.
    - RED (tests/test_ui_common.py) `test_global_nav_names_every_view`:
      `GLOBAL_NAV == "1-6 views | ^p nav | esc back | q quit"`.
    - RED (tests/test_catalog_tui.py) `test_each_tier_view_lists_only_its_own_tier`: over a
      3-tier fixture catalog, `[t.id for t in app.catalog_for(name).tools]` is `["pnpm"]` for
      `"system"`, `["jq"]` for `"user"`, `["claude"]` for `"ai"`; and the union of the three
      `.tools` lists equals the full catalog with no duplicates (the three views partition it).
    - RED (tests/test_catalog_tui.py)
      `test_staging_spans_tier_views_and_commits_from_any_of_them`: press `space` on the base
      System view (marks `pnpm`), press `3` (AI view), press `space` (marks `claude`), press
      `enter`; `app.return_value == ["pnpm", "claude"]` -- both tiers, full-catalog order, from
      a screen that is not the base screen.
    - RED (tests/test_catalog_tui.py) `test_select_all_is_scoped_to_the_active_tier_view`: mark
      `pnpm` on System, press `2` (User view), press `a` -> `app.catalog.selected ==
      {"pnpm", "jq"}` (the System mark survived a select-all in another view); press `i` ->
      `{"pnpm"}`.
    - RED (tests/test_catalog_tui.py) `test_tier_view_keeps_the_five_grouping_tabs`: on the User
      view, `screen.view == "category"`, then `right` -> `"priority"`, `right` -> `"audience"`,
      `right` -> `"status"`, `right` -> `"table"`, `right` -> `"category"` (wraps) -- ROADMAP
      SC#1's "each still groupable/sortable by Category/Priority/Audience/Status/Table".
    - RED (tests/test_wizard_app.py) `test_number_key_navigates_to_each_view`: rewritten to walk
      `2`->`user`, `3`->`ai`, `4`->`doctor`, `5`->`uninstall`, `6`->`policies`, `1`->`system`,
      asserting `isinstance(app.screen, DoctorScreen)` still holds on `4`.
    - RED (sweep gate, not a pytest case) the old flat-catalog view name survives nowhere in
      tests/test_wizard_app.py, tests/test_ui_common.py or tests/test_setup.py -- pinned by the
      negative grep in `<verify>`. Cycle-1 review found this is the phase's highest-probability
      defect: `_app`'s default at tests/test_wizard_app.py:94 feeds `show_view`, so a single missed
      sentinel is a `KeyError` on `_views` in ~10 tests at once, and `test_esc_on_catalog_is_inert`
      (:885) fails the same way.
    - GREEN afterwards: the whole existing suite, unchanged in intent -- every currently-passing
      test in tests/test_ui_common.py, tests/test_catalog_tui.py, tests/test_wizard_app.py,
      tests/test_setup.py, tests/test_tool_browser.py, tests/test_uninstall_e2e.py and
      tests/test_policies_e2e.py passes again once its view name / number key is migrated.
  </behavior>
  <action>
    Work in installer/ui_common.py first. Replace the single `catalog` `View` row with three
    rows, keeping every other row byte-identical and keeping them in their current relative
    order after the new three. Row 1: `name="system"`, `label="System"`,
    `palette="System - prerequisites a fresh machine needs first"`. Row 2: `name="user"`,
    `label="User"`, `palette="User - your personal picks"`. Row 3: `name="ai"`, `label="AI"`,
    `palette="AI - tooling for coding agents"`. All three carry `mode="STAGED"`, `glyph="o"`,
    `style="cyan"`, `actions="space toggle | enter install | a all | i invert"` (identical to
    today's catalog row -- the interaction model is unchanged, only the tool list differs) and
    `hint="space marks a tool; enter installs everything you staged"` (the hint is widened from
    today's "your selection" because enter now commits marks made in any tier view). The three
    `name` values are exactly the three `Tier` member values and that is load-bearing: it is what
    lets `UnifiedApp` build one screen per tier without a second lookup table. Add a short comment
    above the three rows saying so.

    `BASE_VIEW` already exists (Task 1) and is derived, so replacing the row is all it takes to move
    the base screen; do not add a second constant and do not hand-assign a number key anywhere.

    Change `GLOBAL_NAV` from its literal to `f"1-{len(VIEWS)} views | ^p nav | esc back | q quit"`
    so the count can no longer drift from the table. Update the two stale docstrings that name a
    fixed view count: `_view_key`'s "([1] ... [4])" and `WayfindingHeader`'s "breadcrumb of the
    four views" -- both should describe the count as derived from `VIEWS`. Do not touch
    `FooterBar`, `WayfindingTab`, `ModeBadge`, `AppScreen`, or `_view_key`'s body: they already
    iterate or index the table generically.

    Next, installer/catalog_tui.py, which needs only a docstring correction now that Task 1 already
    parameterised the screen. Update the class docstring: it is no longer "mounted as the unified
    app's base screen" but "one tier-scoped instance per `Tier`; the base screen is whichever tier is
    first in `VIEW_ORDER`". Nothing else in this module changes in this task.

    Next, installer/wizard_app.py. Import `Tier` from `installer.enums` (`BASE_VIEW` is already
    imported from Task 1). In `__init__`, replace Task 1's single `self._catalog = CatalogScreen(...)`
    line with a dict comprehension over `Tier` building `self._catalogs: dict[str, CatalogScreen]`,
    one entry per `tier.value`, each constructed with
    `[tool for tool in tools if tool.tier == tier]` as its display list and
    `view=tier.value, catalog=list(tools), staged=self._staged` (`self._staged` already exists from
    Task 1 and keeps its place above the comprehension). Then build `self._views` as the
    two non-base tier screens (`{name: screen for name, screen in self._catalogs.items() if name
    != BASE_VIEW}`) updated with the existing `doctor`/`uninstall`/`policies` entries, so
    `on_mount`'s existing install loop installs all five without any change to that loop or its
    comment -- this is the point the memory-noted Textual pitfall turns on, and it is satisfied by
    routing the two extra tier screens through the SAME `_views` dict that already gets
    `install_screen`. Default `initial_view` to `BASE_VIEW` and set `self.current_view =
    BASE_VIEW`. Replace the four remaining base-screen sentinels with `BASE_VIEW`: the `on_mount`
    guard, the two comparisons in `show_view`, and the one in `action_back`. Point
    `get_default_screen` and the `catalog` property at `self._catalogs[BASE_VIEW]`, updating the
    property's docstring to say it is the base (first-registered tier) screen, and add
    `def catalog_for(self, view: str) -> CatalogScreen: return self._catalogs[view]` documented as
    the headless-test seam for the two non-base tier screens. Update `_navigable` to test
    `self.screen is self._catalogs[BASE_VIEW] or self.screen in self._views.values()`. Update the
    module docstring's "Catalog, doctor, uninstall, and policies are all functional views" and its
    "[catalog] / [catalog, <one other view>]" stack invariant to name the base view generically.
    `BINDINGS`, `on_catalog_screen_decided`, `NavScreen`, `action_abort` and `action_hard_abort`
    all stay exactly as they are.

    setup.py needs no edit in this task -- Task 1 already pointed `_build_app`'s `initial_view`
    default at `BASE_VIEW`, so it follows the table automatically.

    Finally migrate the tests. Cycle-1 review found the original recipe under-specified the string
    sentinels here, so this is enumerated line by line; treat the line numbers as of the current tree
    and re-locate by content if a preceding edit shifts them. Work file by file and re-run the
    negative grep in `<verify>` before moving on, so a miss surfaces at the file that caused it. That
    grep counts every occurrence, including comments, so do not leave the retired view name in a
    comment, a docstring or a test name in any of the three files -- say "the previous single flat
    view" if a comment needs to refer to it at all.

    tests/test_ui_common.py -- five sentinel sites plus two literals. Replace the old view name at
    :104 (`WayfindingHeader(active=...)`), :144 and :154 (both `VIEW_ORDER ==` tuples, which become
    the six-name tuple), :178 (`FooterBar(...)`) and :207 (`VIEW_BY_NAME[...]`) with `"system"`.
    Point `test_wayfinding_header_highlights_active_view` at the new numbering: Doctor is `\[4]` and
    the non-active token it checks becomes `\[1] System`. Update the three `1-4 views` occurrences to
    `1-6 views`, rename `test_global_nav_names_four_views` to `test_global_nav_names_every_view`, and
    add the two new RED tests. Task 1's `test_base_view_is_the_first_registered_view` asserts the
    derivation, not a name, so it must still pass here untouched.

    tests/test_wizard_app.py -- the file the review singled out, and the one that fails hardest if a
    site is missed, because `_app`'s default flows into `show_view`. There are eleven sentinel sites:
    the `_app` helper's `initial_view: str = ...` default at :94; the `VIEW_ORDER ==` assertion at
    :118 (becomes the six-name tuple); the `current_view ==` assertions at :124, :138, :256, :283,
    :305, :879, :888 and :925; the explicit `_app(initial_view=...)` argument at :885 inside
    `test_esc_on_catalog_is_inert`; the prose comment at :929; and the view-name tuple at :951. Every
    one becomes `"system"` (or, for :94, `BASE_VIEW` imported from `installer.ui_common`, which is
    the more durable form). Rename `test_esc_on_catalog_is_inert` and
    `test_starts_on_the_catalog_view` to name the system view instead of the old flat one. Give its
    `_tool` helper `tier="system"`. Then shift every navigation number key by +2, INCLUDING the five
    uninstall tests that reach the Uninstall view from the default screen at :336, :352, :376, :393
    and :414, which all become `press("5")`. Do the shift in descending order -- `press("4")` to
    `press("6")` first, then `press("3")` to `press("5")`, then `press("2")` to `press("4")` --
    because doing it ascending would double-shift the keys you just rewrote. The two `press("1")`
    sites keep their key and only their comments/assertions change.

    tests/test_setup.py -- one site: the `_build_app` fake's `initial_view` default at :43 becomes
    `"system"`. Task 1 deliberately left it alone because it still matched then; it does not now.

    tests/test_catalog_tui.py -- no sentinel sweep (it drives screens through `app.catalog`, not by
    name), but two structural additions. Give `_tool` a keyword-only `tier: str = "system"` parameter
    passed through to `Tool(...)`; defaulting to system keeps every existing test driving the base
    screen, so their assertions hold unchanged, and without it `Tool.__init__`'s own `tier=Tier.USER`
    default would leave the System view empty and every `space`/`a`/`i` assertion in the module
    returning an empty set. Add a `_tiered_catalog()` helper returning one tool per tier (`pnpm`
    system/`pkg-mgr`/P0, `jq` user/`data`/P1, `claude` ai/`ai`/P0 -- all three are real `Category`
    members) and its installed map, then add the four new RED tests.

    tests/test_tool_browser.py, tests/test_uninstall_e2e.py and tests/test_policies_e2e.py need no
    edit: the first tests the widget in isolation with no view names, and the other two pass
    `initial_view="uninstall"` / `"policies"` explicitly. They are in this task's verify anyway,
    because they are where an unrouted `install_screen` or a broken message payload shows up.
  </action>
  <!-- planner-discipline-allow: "catalog" -- the negative grep below targets the three test
       modules, never this plan; the literal is unavoidable when enumerating the sentinels to
       remove, and the action explicitly forbids leaving it in a comment in those files. -->
  <reversibility rating="costly">
    D-01's nav reorder is reversible on its own (one table, rows move back). What is not cheaply
    reversible is the three-instance topology built on Task 1's seam: unwinding it means collapsing
    three `CatalogScreen` instances back into one after plan 02-02 and Phase 8 have both built on the
    shared batch, plus re-migrating the whole test sweep in the other direction.
  </reversibility>
  <verify>
    <automated>uv run python3 -c "from installer.enums import Tier; from installer.ui_common import BASE_VIEW, GLOBAL_NAV, VIEW_BY_NAME, VIEW_ORDER; assert VIEW_ORDER == ('system','user','ai','doctor','uninstall','policies'), VIEW_ORDER; assert VIEW_ORDER[:3] == tuple(t.value for t in Tier), VIEW_ORDER[:3]; assert BASE_VIEW == 'system', BASE_VIEW; assert set(VIEW_BY_NAME) == set(VIEW_ORDER); assert GLOBAL_NAV == '1-6 views | ^p nav | esc back | q quit', GLOBAL_NAV"</automated>
    <fails_when>Non-zero exit with an AssertionError printing the actual value: the three tier
    rows are missing, mis-ordered, or named something other than the three `Tier` values; or
    `BASE_VIEW` is absent/not derived from `VIEW_ORDER[0]`; or `GLOBAL_NAV` still hard-codes a
    view count. An ImportError instead means `BASE_VIEW` was never added to
    installer/ui_common.py.</fails_when>
    <automated>uv run pytest tests/test_ui_common.py tests/test_catalog_tui.py -q</automated>
    <fails_when>Non-zero exit. The six new tests fail if a tier view shows the wrong tools, if
    the three views do not partition the catalog, if enter from the AI view returns only the AI
    tier's ids (the shared batch was not shared, or `Decided` still reported
    `ToolBrowser.Accepted.ids`), if `a`/`i` in one tier view cleared another tier's marks (a
    surviving set rebind in `action_select_all`/`action_invert`), or if the five grouping tabs no
    longer cycle. Existing tests fail if `_tool`'s tier default was not set to `"system"`, which
    leaves the base screen empty and every `space`/`a`/`i` assertion returning an empty
    set.</fails_when>
    <automated>uv run pytest tests/test_wizard_app.py tests/test_setup.py tests/test_tool_browser.py tests/test_uninstall_e2e.py tests/test_policies_e2e.py -q</automated>
    <fails_when>Non-zero exit. A `current_view` assertion mismatch means a number key was shifted
    the wrong way (ascending replacement double-shifts); a `KeyError` out of `show_view` or
    `_navigable` means a base-screen sentinel was left as the old literal while `VIEWS` moved on;
    a Textual `ScreenError`/blank pushed screen means a tier screen reached `push_screen` without
    having been installed in `on_mount`; a `tests/test_tool_browser.py` failure means Task 1's
    widget seam regressed under the three-instance wiring.</fails_when>
    <automated>test "$(grep -o '"catalog"' tests/test_wizard_app.py tests/test_ui_common.py tests/test_setup.py | wc -l | tr -d ' ')" = 0</automated>
    <fails_when>Non-zero exit means at least one occurrence of the retired flat view name survives in
    those three modules. This is the cycle-1 HIGH finding's gate. Run `grep -n '"catalog"'
    tests/test_wizard_app.py tests/test_ui_common.py tests/test_setup.py` to list the survivors; each
    is either a `show_view` input that will raise `KeyError` on `_views`, or a stale assertion that
    passes today only because it is never reached. A hit inside a comment or a test name counts and
    must also be reworded -- the gate deliberately does not exempt comments.</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>Non-zero exit anywhere in the suite. Nothing outside the three swept modules should
    have needed an edit, so a failure in another module means a source change leaked behaviour the
    tier split was supposed to preserve.</fails_when>
  </verify>
  <acceptance_criteria>
    installer/ui_common.py's VIEWS tuple has exactly six rows whose names are, in order, system,
    user, ai, doctor, uninstall, policies; the first three equal the three Tier member values.
    BASE_VIEW still equals VIEW_ORDER[0] and is now the system view. GLOBAL_NAV is computed from
    len(VIEWS) and renders "1-6 views". UnifiedApp holds one CatalogScreen per Tier in _catalogs,
    reuses the single shared _staged set from Task 1, installs the two non-base tier screens through
    the existing _views install loop, and exposes catalog (base screen) plus catalog_for(view). The
    retired flat view name appears zero times in tests/test_wizard_app.py, tests/test_ui_common.py
    and tests/test_setup.py, including in comments and test names. Every navigation number key in
    tests/test_wizard_app.py is shifted by +2, the five uninstall tests included.
    `uv run pytest -q` passes with no test skipped, xfailed, deleted, or weakened.
  </acceptance_criteria>
  <done>Six nav views in D-01's order with derived number keys and a derived GLOBAL_NAV; three
  tier-scoped CatalogScreen instances partitioning the catalog by Tool.tier, each keeping all
  five groupings; one shared staged selection that survives view switches, is scoped correctly by
  a/i, and commits as one full-catalog-ordered result from whichever tier view the user pressed
  enter in; the whole existing suite migrated and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make a cross-tier requires drag-in visible from the dependent tool's own tier view</name>
  <files>installer/deps.py, installer/catalog_tui.py, tests/test_deps.py, tests/test_catalog_tui.py, .claude/architecture.md</files>
  <read_first>
    installer/deps.py:21-29 (`Resolution`), :31-137 (`resolve_dependencies` -- read it to confirm
    the new function must NOT duplicate its ordering, blocking, or cycle handling), :140-150
    (`requires_integrity_errors` -- the shape and placement to mirror for a small pure helper
    beside the resolver), :11-14 (the module's imports: `Callable` from collections.abc);
    installer/catalog_tui.py:240-243 (`on_tool_browser_selection_changed` -- currently only
    clears the status line; this is the hook point), :198-209 (`_detail_text` and its existing
    `requires` clause -- the wording convention to stay consistent with);
    installer/ui_common.py:59-76 (`StatusLine.set`/`clear` and `SEVERITY_STYLE` -- the only three
    severities available);
    installer/app.py:116-127 (`run_wizard`'s post-selection `resolve_dependencies` +
    `render_dependency_notice` -- the authoritative path this task previews but must not
    duplicate or bypass);
    installer/render.py:20-30 (`render_dependency_notice`'s existing wording, for consistency);
    tests/test_deps.py:1-50 (module imports and the `_tool`/`_resolve` helpers), :52-75
    (`test_missing_dependency_is_dragged_in` -- the assertion style to mirror), :130 onwards
    (`test_resolver_drags_in_dependency_across_a_tier_boundary`, Phase 1's cross-tier proof, so
    the new tests complement rather than duplicate it);
    tests/test_app.py:864-908 (`test_run_wizard_installs_dependencies_before_dependents` -- Phase
    1 already proves `select_catalog` returning `["mmdc"]` installs `pnpm` first, so this task
    adds the tier-view half, not another copy of that);
    .claude/architecture.md:1-34 (the five rules and the existing "Tier is a browsing label"
    section this task extends).
  </read_first>
  <behavior>
    - RED (tests/test_deps.py) `test_missing_requires_names_unstaged_uninstalled_dependencies`:
      for a tool requiring `pnpm` where `pnpm` is neither staged nor installed,
      `missing_requires` returns `("pnpm",)`.
    - RED (tests/test_deps.py) `test_missing_requires_skips_staged_and_installed_dependencies`:
      the same tool returns `()` when `pnpm` is in the staged set, and `()` when the installed
      map reports it present.
    - RED (tests/test_deps.py) `test_missing_requires_tolerates_a_partial_installed_map`: an
      `installed` mapping that omits the dependency's id entirely still returns `("pnpm",)` instead
      of raising `KeyError`. Cycle-1 review flagged this: a `KeyError` raised inside a Textual
      message handler wedges the message loop, and the sibling lookup in plan 02-02 already uses
      `.get` for exactly this reason.
    - RED (tests/test_deps.py) `test_missing_requires_is_transitive_and_cycle_safe`: an
      `a -> b -> c` chain from `a` returns `("b", "c")` in first-seen order, and a deliberate
      `a -> b -> a` cycle terminates and returns `("b",)` rather than raising or hanging (the
      contrast with `resolve_dependencies`, which raises `DependencyCycleError`, is the point).
    - RED (tests/test_deps.py) `test_missing_requires_ignores_ids_absent_from_the_catalog`: a
      `requires` id with no catalog entry is omitted, because reporting it is
      `resolve_dependencies`' job.
    - RED (tests/test_deps.py) `test_missing_requires_matches_the_real_registry_cross_tier_edge`:
      loading `installer/registry.toml`, `missing_requires(mmdc, catalog, staged=set(),
      installed={every id: False})` returns `("pnpm",)`, and `mmdc.tier != pnpm.tier` -- the real
      user->system edge, asserted against real data.
    - RED (tests/test_catalog_tui.py)
      `test_system_tier_dependency_is_announced_from_the_ai_view`: over a synthetic catalog of
      `pnpm` (system) and `agent` (ai, `requires=("pnpm",)`), press `3` to land on the AI view
      WITHOUT ever visiting System, press `space`; `"pnpm"` appears in
      `app.catalog_for("ai").status_text` (ROADMAP SC#3, no required visit order).
    - RED (tests/test_catalog_tui.py) `test_unmarking_a_tool_clears_the_dependency_notice`:
      pressing `space` a second time on the same row leaves `status_text` empty (the notice
      describes a mark, not a row).
    - RED (tests/test_catalog_tui.py) `test_already_staged_dependency_is_not_re-announced`: with
      `pnpm` marked first from the System view, marking `agent` from the AI view leaves
      `status_text` empty.
    - RED (tests/test_catalog_tui.py) `test_dependency_notice_does_not_promise_availability`: the
      notice raised by marking `agent` mentions the platform caveat -- the ids are added at install
      time and any unavailable on this platform are reported when the installer runs. Cycle-1 review
      flagged that a notice claiming an unconditional drag-in over-promises, because
      `resolve_dependencies` may skip an unavailable dependency; this pins the honest wording without
      putting an availability verdict on a screen that has no `Platform`.
    - RED (tests/test_catalog_tui.py)
      `test_selection_made_in_one_tier_view_resolves_against_the_whole_catalog`: mark `agent`
      from the AI view and press `enter`; `app.return_value == ["agent"]`, and feeding that
      through `select_tools(catalog, ids)` into `resolve_dependencies(..., catalog, ...)` yields
      `dragged_in == ("pnpm",)` and an order of `["pnpm", "agent"]` -- the tier filter narrowed
      the VIEW, never the resolve (ROADMAP SC#2).
  </behavior>
  <action>
    Add `missing_requires` to installer/deps.py immediately after `requires_integrity_errors`,
    importing `Container` and `Mapping` from `collections.abc` alongside the existing `Callable`.
    Signature: `missing_requires(tool: Tool, catalog: list[Tool], *, staged: Container[str],
    installed: Mapping[str, bool]) -> tuple[str, ...]`. Build `by_id` from the catalog, seed a
    `seen` set with `tool.id` so a self-referencing chain cannot loop, then breadth-first walk
    `tool.requires`: skip an id already in `seen`; add it to `seen`; look it up and skip it
    entirely when the catalog has no such entry; otherwise append it to the result only when it is
    neither in `staged` nor reported installed, and either way extend the queue with that
    dependency's own `requires` so a staged intermediate does not hide its own unstaged
    dependencies. Read the installed flag as `installed.get(dep_id, False)`, never
    `installed[dep_id]` -- a partial map must be treated as "not known to be installed", because this
    function runs inside a Textual message handler where a `KeyError` wedges the message loop rather
    than surfacing as a clean failure. Return the accumulated ids as a tuple in first-seen order. The docstring must
    say plainly what this is and is not: it names what `resolve_dependencies` will drag in, so a
    tier-scoped view can show a cross-tier dependency at mark time; it performs no ordering, makes
    no availability judgement, raises no cycle error, and drags nothing in --
    `resolve_dependencies` remains the single mechanism that decides what installs and in what
    order, per `.claude/architecture.md`'s tier rule. Do not modify `resolve_dependencies`,
    `Resolution`, or `requires_integrity_errors`.

    In installer/catalog_tui.py, import `missing_requires` from `installer.deps` and add
    `self._by_id = {tool.id: tool for tool in self._catalog}` to `__init__` (keyed over the whole
    catalog so a lookup resolves regardless of which tier the toggled row belongs to; it is added
    here rather than in Task 1 because this task is its first consumer and an unused attribute would
    trip the no-orphan-helpers rule). Extend `on_tool_browser_selection_changed`: after the existing
    `event.stop()` and `self.status.clear()`, return early when `event.item_id` is `None` (a bulk
    select-all/invert, which names no single row) or when `event.selected` is false (the row was
    just unmarked); look the id up in `self._by_id` and return when it is absent; then call
    `missing_requires(tool, self._catalog, staged=self._staged, installed=self._installed)`. When
    the result is non-empty, set the status line to a sentence naming the tool and the joined
    dependency ids, stating they are added automatically at install time AND that any not available
    on this platform are reported when the installer runs, with severity `"ok"` -- a drag-in is a
    normal successful outcome, not a warning, and `SEVERITY_STYLE` offers only ok/warn/error. That
    second clause is the whole of this screen's availability story and is deliberate: the screen
    holds no `Platform`, so it states what it knows (these ids will be pulled in) and defers the
    verdict it cannot make to the authoritative post-TUI path, instead of either staying silent
    about the caveat or duplicating `resolve_dependencies`' judgement here. Note that by the time this handler runs the toggled id is already in
    `self._staged`, which is exactly why `missing_requires` seeds `seen` with `tool.id`. Keep the
    existing unconditional `self.status.clear()` so the notice is transient: it belongs to the
    mark that produced it and must not survive the next selection change. Do not add a `Platform`,
    an availability callable, or any part of `resolve_dependencies` to this screen; the
    availability verdict and the "not available on this platform" wording stay on the post-TUI
    path in `installer/app.py::run_wizard`.

    Extend the "Tier is a browsing label" section of .claude/architecture.md (do not renumber or
    edit the five rules) with what Phase 2 wired: `tier` selects which of the three top-level
    catalog views a tool is listed under; the three views are three `CatalogScreen` instances over
    one shared staged selection, so a batch assembled across tiers commits once; the tier filter
    narrows what is DISPLAYED and never what is resolved, because the ids a view returns are
    resolved against the whole catalog by `resolve_dependencies`; and `deps.missing_requires` is a
    read-only preview of that resolution, never a second ordering mechanism. Add a sentence
    recording that ROADMAP SC#2's original illustrative `claude` -> `pnpm` pair was rewritten during
    plan-review convergence and is proved with the real `mmdc` -> `pnpm` user->system edge, because
    `claude` installs via its own script/cask and declares no dependency on `pnpm`. Add one more
    sentence on the availability split: the in-view notice states that unavailable dependencies are
    reported when the installer runs, and the availability VERDICT stays with `resolve_dependencies`
    + `render_dependency_notice` on the post-TUI path, because a screen with no `Platform` must not
    make a judgement it cannot make correctly.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deps.py -q</automated>
    <fails_when>Non-zero exit. A hang or a `DependencyCycleError` from the cycle test means the
    walk reuses `resolve_dependencies`' recursion instead of the `seen`-guarded queue; a wrong
    tuple from the transitive test means a staged or installed intermediate stopped the walk
    instead of only being omitted from the result; a failure in the real-registry test means the
    `mmdc` -> `pnpm` edge moved (Phase 5 may retarget it) and the test needs retargeting, not
    deleting.</fails_when>
    <automated>uv run pytest tests/test_catalog_tui.py -q</automated>
    <fails_when>Non-zero exit. An empty `status_text` after marking the ai-tier tool means the
    notice never fired -- either `SelectionChanged` carries no payload, or `_by_id` was built over
    the tier-filtered display list so the cross-tier lookup missed; a non-empty `status_text`
    after unmarking means the early return on `event.selected` is missing; a `dragged_in` other
    than `("pnpm",)` in the resolve test means the tier view narrowed the catalog it hands to
    `resolve_dependencies`.</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>Non-zero exit anywhere in the suite -- the changed `SelectionChanged` signature
    or the new status-line write regressed the Uninstall view or a Task 1 assertion.</fails_when>
    <automated>test "$(grep -v '^#' .claude/architecture.md | grep -c 'missing_requires')" -gt 0</automated>
    <fails_when>A count of 0, meaning the architecture note never recorded that the in-view preview
    is not a second ordering mechanism. Markdown headings are stripped before counting so a heading
    that merely repeats the name cannot satisfy the gate on its own.</fails_when>
    <automated>S=gsd-p2-nav; P=$(mktemp); tmux kill-session -t $S 2>/dev/null; tmux new-session -d -s $S -x 200 -y 50 "uv run setup.py"; sleep 8; tmux capture-pane -p -t $S > "$P"; tmux send-keys -t $S 2; sleep 2; tmux capture-pane -p -t $S >> "$P"; tmux send-keys -t $S q; sleep 2; tmux kill-session -t $S 2>/dev/null; cat "$P"; grep -q '\[1\] System' "$P" && grep -q '\[2\] User' "$P" && grep -q '\[3\] AI' "$P" && grep -q '\[4\] Doctor' "$P" && grep -q '\[5\] Uninstall' "$P" && grep -q '\[6\] Policies' "$P" && grep -q '1-6 views' "$P"</automated>
    <fails_when>Non-zero exit, or a captured pane missing any of the seven tokens. This is ONESHOT
    Rule 8 / Rule 14 Tier 2 and it is GATING, not manual sanity: the headless Textual pilot has been
    found to mask burst/timing bugs a real terminal surfaces, so the six-view nav and the derived
    footer count must be proved in an actual `uv run setup.py` process. It is a text match, so the
    executor runs it directly -- no human and no container. Keep it navigation-only per Rule 5: press
    only view-number keys and `q`, and never `enter`, which would commit an install against this real
    machine. A blank capture usually means the app needed longer to paint -- raise the first `sleep`
    before concluding the nav is wrong. If `tmux` is not installed, install it and rerun; do not
    downgrade this step to the pilot.</fails_when>
  </verify>
  <acceptance_criteria>
    installer/deps.py exports missing_requires with the documented signature; it is transitive,
    terminates on a cycle, omits ids absent from the catalog, omits staged and installed ids while
    still walking through them, reads the installed flag with .get so a partial map cannot raise,
    and returns first-seen order. resolve_dependencies, Resolution and requires_integrity_errors are
    byte-identical to before. CatalogScreen writes a status-line notice naming the unstaged
    cross-tier dependencies of a newly marked tool AND naming the platform caveat, writes nothing on
    an unmark or on a bulk select-all/invert, and holds no Platform or availability callable. A
    selection made from the AI view without ever opening the System view still yields
    dragged_in == ("pnpm",) when resolved against the full catalog. .claude/architecture.md
    records the three-views/one-staged-batch wiring, the preview-not-a-resolver rule, the
    mmdc->pnpm substitution behind the rewritten ROADMAP SC#2, and the availability split. The
    tmux structural check passes against a real `uv run setup.py` process, navigation-only.
    `make validate && make test` both pass.
  </acceptance_criteria>
  <done>A cross-tier `requires` edge is named in the status line the moment its dependent is
  marked, from whichever tier view the user is in and with no required visit order; the
  authoritative resolve still runs once over the whole catalog after the TUI exits, unchanged;
  architecture.md records the design; `make validate && make test` pass on the committed tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal keypress -> `ToolBrowser` selection state | The only writer of the staged batch. Every id that reaches the installer originates from a keypress on a selectable row; nothing else may add one. |
| Shared `UnifiedApp._staged` set -> three `CatalogScreen` instances | One mutable object now crosses three screens. A defect here stages a tool the user never marked, which later becomes a real install. |
| Staged ids -> `select_tools` -> `resolve_dependencies` -> install engine | The tier filter must narrow what is displayed and never what is resolved, or a needed system-tier prerequisite is silently dropped from a real install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `UnifiedApp._staged` shared across three `CatalogScreen`s | medium | mitigate | The set has exactly three writers, all explicit user actions (`action_toggle_selected`, `action_select_all`, `action_invert`); the two bulk writers are converted to in-place, scope-limited operations over only the active browser's selectable ids (Task 1), pinned at the widget level by Task 1's `test_bulk_actions_never_rebind_the_selection_set` and at the app level by Task 2's `test_select_all_is_scoped_to_the_active_tier_view`, which asserts a bulk action in one tier view neither adds to nor clears another tier's marks. |
| T-02-02 | Denial of Service (availability of a catalog entry) | tier filtering in `UnifiedApp.__init__` | medium | mitigate | A tool whose tier no longer matches any view would become unreachable in every view and silently un-installable. `Tool.tier` is already a hard-validated `Tier` at load (Phase 1), the three view names are asserted equal to the three `Tier` values, and `test_each_tier_view_lists_only_its_own_tier` asserts the three views partition the catalog with no tool lost or duplicated. |
| T-02-03 | Information disclosure (of a required prerequisite) | tier-scoped view hiding a cross-tier `requires` edge | medium | mitigate | `deps.missing_requires` names unstaged cross-tier dependencies in the status line at mark time, and `test_selection_made_in_one_tier_view_resolves_against_the_whole_catalog` proves the returned ids still resolve against the full catalog. The authoritative post-TUI `resolve_dependencies` + `render_dependency_notice` path is untouched. |
| T-02-04 | Elevation of privilege | install engine / `Runner` | info | accept | This plan adds no executor, no subprocess argument, and no network call. View names and tier values are attribute reads and screen-dict keys; they never reach `Runner`, argv, or a URL. |
| T-02-05 | Repudiation | `CatalogScreen.Decided` result | low | accept | The committed batch is exactly what the shared set holds, reported in full-catalog order, and the existing `render_audit` / `render_summary` output continues to enumerate every tool before and after install, so the user always sees what was committed. |
| T-02-SC | Tampering (supply chain) | package installs | n/a | accept | This plan installs no npm/pip/cargo package and adds no dependency to `pyproject.toml`; the package-legitimacy gate does not apply and no `[ASSUMED]`/`[SUS]` checkpoint is required. |
</threat_model>

<verification>
Run, in order:
1. `uv run python3 -c "from installer.enums import Tier; from installer.ui_common import BASE_VIEW, GLOBAL_NAV, VIEW_ORDER; print(VIEW_ORDER, BASE_VIEW, GLOBAL_NAV)"` -- prints the six
   view names in D-01's order, `system`, and `1-6 views | ^p nav | esc back | q quit`.
2. `uv run pytest tests/test_ui_common.py tests/test_catalog_tui.py tests/test_wizard_app.py tests/test_setup.py tests/test_deps.py tests/test_tool_browser.py -q` -- all pass, including the 19 tests this plan adds.
3. `test "$(grep -o '"catalog"' tests/test_wizard_app.py tests/test_ui_common.py tests/test_setup.py | wc -l | tr -d ' ')" = 0` -- the retired flat view name survives nowhere in the swept modules.
4. `uv run pytest -q` -- the whole suite, with nothing skipped, xfailed, deleted or weakened.
5. `make validate && make test` -- ruff, ruff format, pyright, bandit, vulture, shellcheck, then
   pytest with the 90% coverage floor, per CLAUDE.md's "Validate before you commit" rule.
6. **Real-terminal structural check -- GATING, not optional** (ONESHOT Rule 8, Rule 14 Tier 2;
   this phase carries `UI hint: yes`). Run the wizard under `tmux` as specified in Task 3's verify
   block and confirm by `capture-pane` text that the header reads
   `[1] System  [2] User  [3] AI  [4] Doctor  [5] Uninstall  [6] Policies`, that the footer reads
   `1-6 views`, and that marking `mmdc` in the User view announces `pnpm`. The executor performs
   this itself -- it is a string match on a monospace TUI, so it needs no human and no container.
   Navigation-only per Rule 5: press view-number keys, `space` and `q` only, never `enter`.
</verification>

<success_criteria>
- The top nav offers three tier-scoped catalog views (System/User/AI) in place of the single flat
  Catalog, first in the order, with `doctor`/`uninstall`/`policies` shifted to 4/5/6 and every
  number key still derived from `VIEW_ORDER` position (ROADMAP Phase 2 SC#1, D-01).
- Each tier view still switches between Category/Priority/Audience/Status/Table, and the three
  views partition the catalog by `Tool.tier` with nothing lost or duplicated (SC#1).
- Selecting a tool from the AI view whose `requires` names a system-tier tool surfaces that
  dependency in-view at mark time, with no prior visit to the System view, and the returned ids
  still resolve against the whole catalog (SC#2, SC#3).
- A batch staged across two tier views commits as one full-catalog-ordered result from whichever
  view the user pressed enter in.
- `installer/deps.py::resolve_dependencies` is unchanged; the only addition beside it is a pure,
  read-only preview function that tolerates a partial `installed` map.
- The retired flat view name survives in no test module, so no test can reach `show_view` with a
  name `VIEWS` no longer registers.
- The six-view nav and the `1-6 views` footer are confirmed in a REAL terminal via `tmux`
  `capture-pane` text matching, as a gating step (ONESHOT Rule 8 / Rule 14 Tier 2).
- `make validate && make test` pass on the committed tree.
</success_criteria>

<output>
Create `.planning/phases/02-tier-scoped-catalog-views-recommends/02-01-SUMMARY.md` when done.
</output>
