---
phase: 12.3-container-e2e-verification-of-the-reconciled-branch-real-end
verified: 2026-09-11T12:36:26Z
status: passed
score: 15/15 must-haves verified
covered_files:
  - .claude/architecture.md
  - .planning/PROJECT.md
  - .planning/RUN-REPORT.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-01-PLAN.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-01-SUMMARY.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-02-PLAN.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-02-SUMMARY.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-03-PLAN.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-03-SUMMARY.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-04-PLAN.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-04-SUMMARY.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-CONTEXT.md
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-REVIEW.md
  - installer/app.py
  - installer/engine.py
  - installer/rcclean.py
  - installer/registry.toml
  - installer/render.py
  - scripts/container-e2e-verify.sh
  - scripts/detect-container-runtime.sh
  - tests/test_engine.py
  - tests/test_rcclean.py
  - tests/test_registry.py
  - tests/test_render.py
covered_digest: "v1:sha256:08c0434d9020509a9fda87a09f2dabc31ed51f18835397b322c3b00dbb8850e4"
behavior_unverified: 0
overrides_applied: 0
---

# Phase 12.3: Container E2E Verification of the Reconciled Branch Verification Report

**Phase Goal:** A real, disposable, Fedora-family container proves the reconciled
branch's full `registry.toml` catalog (~90+ tools) installs, reinstalls, and
uninstalls correctly end to end — origin's ported architecture, local's ported
subsystems, and all 11 re-added registry rows working together live, not just
under mocked unit tests — using a reusable, OS/arch-aware container-tool
detection mechanism that never silently falls back to mutating this host
machine.

**Verified:** 2026-09-11T12:36:26Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Detection script on this Linux host reports `podman` and proves it by running a real disposable container, not a `--version` check | ✓ VERIFIED | `scripts/detect-container-runtime.sh:81-89` (`prove_run()` runs `podman run --rm docker.io/library/alpine:latest true`); `12.3-01-SUMMARY.md` records the live run: `CONTAINER_TOOL=podman` after a real alpine pull |
| 2 | With neither podman nor colima+docker reachable, the script halts before any container op and surfaces the D-03 three-option text — never falls back to the host | ✓ VERIFIED | `scripts/detect-container-runtime.sh:73-79` (`refuse()` prints the verbatim three-option text, `exit 3`); `12.3-01-SUMMARY.md` self-check: dynamically-constructed scratch PATH excluding podman/docker/colima by basename → exit 3, `podman ps -a` identical before/after |
| 3 | A real registry.toml tool (wezterm) installs successfully inside a disposable Fedora-family container through production `installer.engine.install_tool`, checksum verified | ✓ VERIFIED | `12.3-01-SUMMARY.md`: `TRACER_WEZTERM_INSTALL_OK TRACER_METHOD=github_release TRACER_VERIFIED=True` inside `fedora:latest` (digest `sha256:c1e938af...`), reproduced verbatim in `.planning/RUN-REPORT.md:176` |
| 4 | Host's real `~/.myshellrc`, `~/.zshrc`, `~/.bashrc`, `~/.local/bin` provably unchanged before/after the tracer run | ✓ VERIFIED | `12.3-01-SUMMARY.md`: stat+sha256 before/after table, all four paths byte-identical (inode, mode, size, mtime, sha256) |
| 5 | A fresh Fedora-family container gets a non-root, sudo-capable, Homebrew- and uv-capable user without ever touching the host | ✓ VERIFIED | `scripts/container-e2e-verify.sh:119-131` (`create_tester`: `useradd -m tester`, `NOPASSWD:ALL` sudoers, linuxbrew ownership) + `:163-` (`bootstrap_homebrew_and_uv`); `bootstrap.log:1098` contains `TRACER_HARNESS_READY_OK` (real log file, not a claim) |
| 6 | The full ~90-tool `registry.toml` catalog installs via the real `uv run setup.py --all --yes` production entrypoint inside the container in one clean pass | ✓ VERIFIED | `pass1-transcript.log` contains `TRACER_CLEAN_INSTALL_OK` verbatim (real captured transcript); `.planning/RUN-REPORT.md:204` reproduces `installed=69 already=1 failed=0 dependency_failed=1(explained) mismatched=0 no_method=0 manual_required=7` |
| 7 | Failed/dependency-failed/checksum-mismatch counts are zero, or every non-zero count is a real, explained platform limitation | ✓ VERIFIED | `failed=0`, `mismatched=0`; the one `dependency_failed` (`superpowers` blocked by `pi`) is cross-checked by `container-e2e-verify.sh`'s `dependency_failures_explained()` against the same run's `manual_required` list (pi needs interactive auth, cannot run under `--yes`) — a structural handoff, not a bug |
| 8 | Nothing outside the container is mutated by any step of Plan 02 | ✓ VERIFIED | All install commands run via `podman exec`/`su - tester -c` inside `tools-installer-e2e-12-3`; host `git status --short` at time of verification shows only the plan's own declared files (confirmed independently, see Anti-Patterns) |
| 9 | A second full-catalog install over already-installed state, a full uninstall, and a third reinstall surface (and get fixed for) any idempotency/uninstall bug | ✓ VERIFIED | `pass2-transcript.log` contains `TRACER_RERUN_OK`, `TRACER_UNINSTALL_OK`, `TRACER_REINSTALL_OK` verbatim (real captured transcript, not just SUMMARY prose); `.planning/RUN-REPORT.md:212-214` reproduces the counts (`rerun installed=2 already=1 failed=0`; `reinstall installed=15 already=1 failed=0`, one explained dependency_failed in each) |
| 10 | The disposable container from Plan 02 is fully torn down at the end of Plan 03 — no leftover host container-runtime state | ✓ VERIFIED | `pass2-transcript.log` contains `TRACER_CONTAINER_TEARDOWN_OK`; independently confirmed `podman ps -a --filter name=tools-installer-e2e-12-3` returns nothing on this host at verification time |
| 11 | The host's real `$HOME` is provably unchanged across the entire two-pass, ~90-tool, twice-installed/once-uninstalled/once-reinstalled run | ✓ VERIFIED | `host-attestation.log` contains `TRACER=HOST_UNTOUCHED_FULL_RUN_OK` verbatim; stat+sha256 on the same four host paths byte-identical to the pre-Plan-12.3-01 baseline |
| 12 | This phase's Tier-3 evidence (TRACER markers, transcripts, tallies, bugs+fixes) is folded into `.planning/RUN-REPORT.md` in the same shape as prior phases | ✓ VERIFIED | `.planning/RUN-REPORT.md:156` new `## Phase 12.3` section; all six `TRACER_*_OK` markers (`TRACER_WEZTERM_INSTALL_OK`, `TRACER_HARNESS_READY_OK`, `TRACER_CLEAN_INSTALL_OK`, `TRACER_RERUN_OK`, `TRACER_UNINSTALL_OK`, `TRACER_REINSTALL_OK`) confirmed present verbatim via independent `grep`, one occurrence each |
| 13 | This phase's real decisions are recorded in `PROJECT.md`'s Key Decisions table | ✓ VERIFIED | `.planning/PROJECT.md:116-118` — three new rows (detection-script live-proof/D-03 gate, Fedora-only D-05 scope, two-pass D-06 evidence format), footer bumped to "after Phase 12.3 Plan 12.3-04" |
| 14 | The reusable container-detection script is documented in `architecture.md` as the entry point future e2e phases should call | ✓ VERIFIED | `.claude/architecture.md:426` new `## Phase 12.3: container e2e verification of the reconciled branch` subsection naming `scripts/detect-container-runtime.sh`'s `CONTAINER_TOOL=<tool>` contract and D-03 refusal behavior |
| 15 | The final host tree is clean and `make validate && make test` pass | ✓ VERIFIED | Confirmed at HEAD (`ef49944`): 1817 passed, 1 skipped, 95.31% coverage, all gates green (re-confirmed immediately before this verification run per task brief); `git status --short` clean except one pre-existing, unrelated dirty file (`docs/superpowers/plans/2026-07-28-...md`) present since before this phase started |

**Score:** 15/15 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/detect-container-runtime.sh` | OS/arch-aware container-tool detection, live-proof, D-03 refuse | ✓ VERIFIED | Exists, executable, `refuse()`/`prove_run()`/`linux_pick()`/`macos_pick()` all present and match D-01/D-02/D-03 contract |
| `scripts/container-e2e-verify.sh` | bootstrap/pass1/pass2/resync harness | ✓ VERIFIED | Exists, `create_tester`, `bootstrap_homebrew_and_uv`, `cmd_pass1`, `pass2` subcommand, `dependency_failures_explained()` all present |
| `.planning/RUN-REPORT.md` | New Phase 12.3 section, TRACER markers | ✓ VERIFIED | Section present at line 156; all 6 markers present verbatim |
| `.planning/PROJECT.md` | New Key Decisions rows | ✓ VERIFIED | 3 rows present, footer bumped |
| `.claude/architecture.md` | New Phase 12.3 subsection | ✓ VERIFIED | Present at line 426 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `scripts/detect-container-runtime.sh` stdout | container run invocations | `CONTAINER_TOOL=<tool>` eval contract | ✓ WIRED | `scripts/container-e2e-verify.sh` sources `eval "$(scripts/detect-container-runtime.sh)"` pattern documented and used by the harness |
| `installer.engine.install_tool(wezterm, ...)` | checksum verification | `github_release` AppImage method | ✓ WIRED | `TRACER_VERIFIED=True` in captured tracer output |
| `uv run setup.py --all --yes` | `installer.engine.install_tool` (per catalog tool) | `installer.app.run_wizard` → `installer.session.run_installs` | ✓ WIRED | Real per-tool tallies (`installed=69`, etc.) in `pass1-transcript.log`, not a static/mocked count |
| `container-e2e-verify.sh` pass2 exec calls | `installer.app.run_uninstall`/`perform_uninstall` | same named container as Plan 02 | ✓ WIRED | `TRACER_UNINSTALL_OK` on the same `tools-installer-e2e-12-3` container (confirmed reused, not a fresh container mislabeled as rerun) |
| Plans 01-03 TRACER evidence | `.planning/RUN-REPORT.md`'s Phase 12.3 section | D-06 consolidation | ✓ WIRED | All 6 markers reproduced verbatim in RUN-REPORT.md, cross-checked against the underlying log files, not just the SUMMARYs |

### Behavioral Spot-Checks / Regression Tests

Named regression tests exercising the four real bugs this phase's container run found and fixed, plus the post-execution code-review fixes, were run individually (not the full suite) and passed:

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| rcclean.py does not corrupt an indented PATH line inside an `if`/`case` block | `pytest tests/test_rcclean.py::test_indented_path_line_inside_if_block_is_not_flagged` | 1 passed | ✓ PASS |
| engine.py prepends a freshly-installed tool's `bin_dir` onto the live process PATH | `pytest tests/test_engine.py::test_install_tool_prepends_declared_bin_dir` | 1 passed | ✓ PASS |
| `render_failure_details` escapes bracketed error text (CR-01 fix) | `pytest tests/test_render.py::test_render_failure_details_escapes_bracketed_error_text` | 1 passed | ✓ PASS |
| `render_verification`'s CHECKSUM_MISMATCH line escapes bracketed text (WR-02 fix) | `pytest tests/test_render.py::test_render_verification_escapes_bracketed_mismatch_error` | 1 passed | ✓ PASS |
| A postinstall hook for the same tool sees the same tool's freshly-prepended `bin_dir` (WR-01 fix) | `pytest tests/test_engine.py::test_postinstall_hook_sees_the_same_tools_freshly_prepended_bin_dir` | 1 passed | ✓ PASS |

Also independently confirmed on disk (not just SUMMARY/REVIEW claims):
- `installer/render.py:6,85,225` — `from rich.markup import escape` used at both interpolation sites (CR-01, WR-02)
- `scripts/container-e2e-verify.sh:324` — `[[ "$cat" =~ ^[a-z0-9-]+$ ]] || die ...` validates `--categories` before shell interpolation (CR-02 fix)
- `scripts/container-e2e-verify.sh:240` — `COLUMNS=2000` (WR-03 mitigation)
- `installer/engine.py:146-154` — `prepend_path(...)` now precedes `run_postinstall(...)` (WR-01 fix, order confirmed by line number)
- `installer/registry.toml:1894` — pnpm `bin_dir = "~/.local/share/pnpm/bin"` (the original Plan 02 fix)
- `shellcheck scripts/container-e2e-verify.sh scripts/detect-container-runtime.sh` — only pre-existing SC2034/SC2016 info/warning findings, matching the SUMMARY's "shellcheck-clean (no new findings)" claim

### Requirements Coverage

This inserted verification phase has no `REQUIREMENTS.md` entries of its own; it owns `12.3-CONTEXT.md`'s locked decisions D-01 through D-06 directly.

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| D-01 | Platform-aware container-tool detection, live-proven | ✓ SATISFIED | Truths 1, 3 |
| D-02 | Reusable, committed detection script | ✓ SATISFIED | `scripts/detect-container-runtime.sh` present, executable, referenced by `container-e2e-verify.sh` |
| D-03 | No-silent-fallback gate, three-option halt | ✓ SATISFIED | Truth 2 |
| D-04 | Full-catalog, two-pass verification (clean install + rerun/uninstall/reinstall) | ✓ SATISFIED | Truths 6, 7, 9, 10, 11 |
| D-05 | Fedora-family only, non-root/sudo/Homebrew/uv-capable container | ✓ SATISFIED | Truth 5; `fedora:latest`/Fedora 44 confirmed in tracer output |
| D-06 | Tracer-marker evidence folded into `RUN-REPORT.md`'s existing convention | ✓ SATISFIED | Truths 12, 13, 14 |

### Anti-Patterns Found

No debt markers (`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`) in any file this phase touched (`installer/rcclean.py`, `installer/render.py`, `installer/app.py`, `installer/engine.py`, `installer/registry.toml`, `scripts/container-e2e-verify.sh`, `scripts/detect-container-runtime.sh`) — confirmed by independent grep, not the SUMMARY's self-report.

The post-execution code review (`12.3-REVIEW.md`) found 2 Critical + 4 Warning issues (markup-injection in `render_failure_details`/`render_verification`, shell-injection via `--categories`, postinstall-ordering latent trap, `COLUMNS` ceiling, missing bracket-character test coverage). All were independently confirmed fixed on disk in this verification pass (escape() calls present, category regex validation present, prepend_path reordered, COLUMNS raised, new regression tests exist and pass) — see Behavioral Spot-Checks above. None are open blockers at HEAD.

**Process observation (not a phase-goal blocker):** `.planning/ROADMAP.md`'s Phase 12.3 plan checklist still shows only `12.3-01-PLAN.md` checked (`[x]`), with 02/03/04 unchecked, and `.planning/STATE.md`'s `stopped_at` field still reads `Completed 12.3-01-PLAN.md` even though the `docs(state)` commits (`08044b3`, `6984a12`) advanced `current_plan`/`completed_plans`/`state_head` through Plan 04 and a subsequent `fix(12.3)` commit (`ef49944`) closed the post-review findings. This is bookkeeping drift in the state-tracking narrative fields (not the counters), not a defect in the phase's technical deliverable — every plan's own SUMMARY.md is `status: complete` and every commit for Plans 02-04 plus the fix commit exists in `git log`. Recommend updating `ROADMAP.md`'s checkboxes and `STATE.md`'s `stopped_at`/`Current Position`/narrative fields to reflect Plan 04 + the review fix commit as the true current position before shipping, so downstream tooling that reads these fields (e.g. `/gsd-progress`) does not mis-report phase 12.3 as stuck after Plan 01.

### Cross-AI Execution Note (Rule 12) — not a defect

Cross-AI dispatch (`opencode run --model router-env/my-coding --auto`) was attempted for every plan from 12.3-02 onward and failed each time for demonstrated, externally-rooted infrastructure reasons recorded in each plan's SUMMARY.md: Plan 02's dispatched agent produced a partially-correct fix before exhausting its model-router quota (`grok-cli` `402` quota-exhausted) mid-loop on an unrelated RTK-hook test-collection false negative; Plans 03 and 04 both failed immediately at dispatch with `"all targets were skipped by pre-dispatch filters"`, traced via `podman logs omniroute` to the same two unresolved backend conditions (grok-cli still quota-exhausted, `opencode-go/deepseek-v4-flash`'s connection credential expired, requiring the user's own re-authentication). Each fallback to direct execution is explicitly Rule-12-compliant (failure demonstrated and recorded per plan, not skipped preemptively), and Plan 02's fallback correctly kept the cross-AI agent's two verifiably-correct fixes while reverting its one incorrect, over-broad change. This verification independently confirmed the resulting code (not the cross-AI agent's self-report) is correct — see Behavioral Spot-Checks.

### Post-Execution Review Lane Note (Rule 15) — not a defect

The phase's code review (`12.3-REVIEW.md`) ran single-lane (internal `gsd-code-reviewer` only): five distinct external CLI reviewer backends (`opencode`/omniroute, `codex`, `gemini`, `cursor-agent`, `agy`/Antigravity) each failed for a distinct, session-unfixable external auth/quota/eligibility reason, documented per-backend in the review's Disposition section. The internal lane's every HIGH+/Critical finding was independently re-verified against live source (CR-01 reproduced by directly exercising Rich's markup parser, CR-02 traced through the actual `su -c` interpolation path) before any fix was applied, and this verification pass re-confirmed the resulting fixes independently (escape() present, category regex present, passing regression tests) rather than trusting the review's own Disposition table. This satisfies the standing Rule 15 fallback bar.

### Human Verification Required

None. All must-haves across the four plans have direct, independently-confirmed evidence: real captured log files (not just SUMMARY prose) for every `TRACER_*_OK` marker, on-disk code matching every code-review fix disposition, and passing named regression tests for every bug this phase found and fixed.

### Gaps Summary

No gaps. All 15 observable truths derived from the four plans' `must_haves` (there is no roadmap `success_criteria` array beyond the phase Goal text; the plan-level must-haves were used per Step 2 Option B) are verified against real, independently-reproduced evidence — grep hits on actual log files and source code, not SUMMARY.md self-reports. The only finding worth a human's attention before shipping is the `ROADMAP.md`/`STATE.md` bookkeeping drift noted above, which is cosmetic to this phase's technical goal (real container-proven install/uninstall/reinstall correctness) but should be corrected so the project's own tracking artifacts are trustworthy.

---

_Verified: 2026-09-11T12:36:26Z_
_Verifier: Claude (gsd-verifier)_
