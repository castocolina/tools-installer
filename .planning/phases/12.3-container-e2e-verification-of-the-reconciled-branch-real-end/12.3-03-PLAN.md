---
phase: 12.3-container-e2e-verification-of-the-reconciled-branch-real-end
plan: 03
type: execute
wave: 3
depends_on: ["12.3-02"]
files_modified:
  - scripts/container-e2e-verify.sh
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/pass2-transcript.log
  - .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/host-attestation.log
autonomous: true
cross_ai: true
requirements: [D-04]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Installing the full catalog a second time over already-installed state, uninstalling it, then reinstalling a third time surfaces (and gets fixed for) any idempotency or uninstall bug, not just a green happy path."
    - "The disposable container from Plan 12.3-02 is fully torn down at the end of this plan — no leftover state on the host's container runtime."
    - "The host's real $HOME is provably unchanged across the ENTIRE two-pass, ~90-tool, twice-installed-once-uninstalled-once-reinstalled run, not just the one-tool tracer from Plan 12.3-01."
  artifacts:
    - scripts/container-e2e-verify.sh
  key_links:
    - "scripts/container-e2e-verify.sh's Pass 2 exec calls -> the SAME named container Plan 12.3-02 left running -> installer.app.run_uninstall/perform_uninstall (uninstall pass)"
---

<objective>
Complete D-04's two-pass verification: reusing the exact same, still-running
container Plan 12.3-02 built and populated, run the rerun pass — a second
full-catalog install over already-installed state, a full uninstall, and a
third full-catalog install (reinstall) — to catch the class of bug a single
clean-install pass structurally cannot surface. Tear the container down
afterward and produce the phase's final host-untouched proof across the
whole run, not just the Plan 12.3-01 tracer's one tool.

Purpose: idempotency and uninstall-sweep correctness at full-catalog scale
are exactly the properties a mocked unit-test suite cannot prove and a
single clean-install pass cannot surface (12.3-CONTEXT.md D-04).
Output: a real, evidenced rerun/uninstall/reinstall cycle over the full
catalog, the container fully disposed of, and a final, whole-run
host-untouched confirmation.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-CONTEXT.md
@scripts/container-e2e-verify.sh
@installer/session.py
@installer/render.py
@installer/app.py
@installer/uninstall.py
@CLAUDE.md
@.claude/testing.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pass 2 — install/uninstall/reinstall rerun over the already-installed container</name>
  <precondition>The named container `tools-installer-e2e-12-3` created by 12.3-02 is still present on this host (`$CONTAINER_TOOL ps -a` shows it) with the full catalog already installed from Pass 1.</precondition>
  <reversibility rating="reversible">D-04: the full two-pass scope is explicitly reversible — a future phase can narrow it if it proves too slow to repeat regularly.</reversibility>
  <files>scripts/container-e2e-verify.sh</files>
  <action>
Because each of these three steps reruns install/uninstall work across the
~90-tool catalog, apply the same background-execution discipline Plan
12.3-02 Task 2 uses to each step individually — this plan inherits the
identical execution-time-budget risk. Addresses review concern
(12.3-REVIEWS.md HIGH/MEDIUM, 12.3-03): redirect each step's stdout/stderr
verbatim into
`.planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/pass2-transcript.log`
(append per step, in order — never overwrite a prior step's output), launch
each step via the invoking agent's background-execution facility (the Bash
tool's `run_in_background: true`, or `nohup ... >> pass2-transcript.log 2>&1
& disown` if driving a raw shell) rather than a single blocking foreground
call, and poll the log for that step's own `TRACER_*_OK` marker or the
process's exit every few minutes rather than in a tight loop — treat 30-45
minutes per step on a cold cache as plausible, not a failure signal; state
the actual wall time observed in the task's own commit message/SUMMARY. If a
single `--all --yes` step repeatedly proves impractical to complete this
way, fall back to the same `--categories <name>[,<name>...] --yes` chunking
Plan 12.3-02 describes, applied per step.

Extend `scripts/container-e2e-verify.sh` with D-04's rerun sequence, run
entirely against the SAME `tools-installer-e2e-12-3` container Plan 12.3-02
left running (never a new container — that would not be a genuine rerun over
previously-installed state per 12.3-CONTEXT.md's Claude's Discretion note).
All three steps go through `$CONTAINER_TOOL exec tools-installer-e2e-12-3 su
- tester -c "cd /home/tester/workspace && ..."`, never with an allocated
pseudo-TTY (same non-interactive requirement as Plan 12.3-02):

1. `uv run setup.py --all --yes` AGAIN — a second full install over
   already-installed state. Catches a tool that installs clean but breaks
   on a second run (e.g. a script that assumes a fresh machine). Print
   `TRACER_RERUN_OK` once this completes with the same
   failed/dependency_failed/mismatched=0-or-explained bar Plan 12.3-02
   Task 2 established.
2. `uv run setup.py --uninstall --yes` — the full uninstall sweep. This
   project's `--uninstall` flag has NO `$(ARGS)` passthrough via `make
   uninstall` (check the Makefile's own `uninstall:` target), so call `uv
   run setup.py --uninstall --yes` directly, never `make uninstall`. With
   no allocated TTY and `--yes`, `setup.py::_run_uninstall` takes its
   non-interactive branch (`assume_yes=True`) and needs no confirmation.
   Print `TRACER_UNINSTALL_OK` once this completes.
3. `uv run setup.py --all --yes` a THIRD time — the reinstall. Catches an
   uninstall that left stray state a reinstall then trips over (e.g. a
   half-removed managed block, a shim the uninstall missed). Print
   `TRACER_REINSTALL_OK` once this completes with the same zero/explained
   failure bar.

Same rule as Plan 12.3-02 Task 2: any real `failed`/`dependency_failed`
outcome, or any leftover artifact after step 2's uninstall that step 3's
reinstall then trips over, that traces to an actual installer bug (not a
genuine platform/network limitation) gets fixed on the host in `installer/`
with a regression test FIRST (red), then the fix (green) — following this
project's TDD discipline. Addresses review concern (12.3-REVIEWS.md HIGH,
12.3-02/12.3-03): before re-running, re-sync the fix into the container's
writable workspace copy the same way Plan 12.3-02 Task 2 does —
`$CONTAINER_TOOL exec tools-installer-e2e-12-3 su - tester -c 'cp -r
/repo/. /home/tester/workspace/'` (the read-only `/repo` mount is always the
source of truth for this re-sync; never treat the container's own prior
in-place edits as authoritative) — then this task's whole three-step
sequence is re-run from the top (via the same background-execution
discipline above) until clean or explained. Confirm `make validate && make
test` still passes on the host after any such fix.

After all three steps genuinely succeed (or every deviation is explained),
tear the container down: `$CONTAINER_TOOL rm -f tools-installer-e2e-12-3`.
  </action>
  <verify>
    <automated>grep -E "^TRACER_(RERUN|UNINSTALL|REINSTALL)_OK" .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/pass2-transcript.log | wc -l | grep -q '^3$'</automated>
  </verify>
  <done>
A real install-over-installed, uninstall, and reinstall cycle ran against
the full catalog inside the one persistent disposable container; any real
bug found is fixed on the host with a regression test; the container is
fully disposed of afterward (`$CONTAINER_TOOL ps -a` no longer lists it).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Final whole-run host-untouched attestation</name>
  <files>scripts/container-e2e-verify.sh</files>
  <action>
Repeat Plan 12.3-01 Task 1's before/after host-comparison method, but now
across the ENTIRE Plan 12.3-02 + 12.3-03 run (clean install, rerun,
uninstall, reinstall of the full ~90-tool catalog) rather than the single
`wezterm` tracer. Compare `stat`/checksum output for the host's real
`~/.myshellrc`, the real zsh rc file, `~/.bashrc`, and `~/.local/bin`'s full
directory listing, captured once before Plan 12.3-02 Task 1 started (from
that plan's own evidence, if recorded, or re-derivable from this task by
checking git history / the earlier transcript) against a fresh capture now,
after the container has been torn down. They must be identical (or, for
`~/.local/bin`, contain none of the catalog's userspace binaries that were
installed inside the container). Also confirm `git status --short` on the
host repo shows nothing from this two-pass run beyond this plan's own
`files_modified` and any legitimate bug-fix files recorded in Task 1's
commit — no stray container-induced changes to the checked-out tree itself
(the repo mount was read-only throughout, so this should already be
structurally guaranteed; this step is the direct confirmation, not an
assumption). Record the full before/after comparison verbatim in this
task's evidence, and only once every comparison is confirmed identical
(or explained), print `HOST_UNTOUCHED_FULL_RUN_OK` into
`.planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/host-attestation.log`.
  </action>
  <verify>
    <automated>grep -q "^HOST_UNTOUCHED_FULL_RUN_OK$" .planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/host-attestation.log</automated>
  </verify>
  <done>
Direct, recorded evidence that the ENTIRE two-pass, full-catalog,
install/uninstall/reinstall run left the host's real shell configuration and
this project's own checked-out working tree exactly as it was before Plan
12.3-02 started.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Long-lived container <-> its own uninstall sweep | The uninstall pass runs `installer/uninstall.py`'s real sweep logic against the container's own filesystem, never the host's. |
| Container lifecycle <-> host's container runtime state | This plan is the one responsible for tearing the container down; leaving it running would be a real resource-leak / stale-state risk for any later run reusing the same container name. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.3-09 | Tampering | a second install pass over already-installed state, or the uninstall sweep, behaving unexpectedly | high | mitigate | This is precisely what D-04's rerun pass exists to catch; any real bug found is fixed test-first before this task is considered done, not shipped silently. |
| T-12.3-08 | Denial of Service | leftover named container consuming host resources indefinitely if teardown is skipped | low | mitigate | Task 1's action explicitly ends with `$CONTAINER_TOOL rm -f`, verified structurally by Task 1's `<verify>`. |
| T-12.3-05 | Repudiation | claiming the full-catalog run was safe without evidence | medium | mitigate | Task 2 records a direct, whole-run before/after comparison, not a bare assertion — extending Plan 12.3-01's single-tool proof to the full two-pass run. |
</threat_model>

<verification>
Confirm all three `TRACER_RERUN_OK`/`TRACER_UNINSTALL_OK`/`TRACER_REINSTALL_OK`
markers appear in order in the captured transcript. Confirm
`$CONTAINER_TOOL ps -a` no longer lists `tools-installer-e2e-12-3`. Confirm
the host's real shell-rc files and `~/.local/bin` are unchanged from before
Plan 12.3-02 started.
</verification>

<success_criteria>
- A full install/uninstall/reinstall cycle over the entire catalog ran inside one persistent disposable container.
- Any real idempotency/uninstall bug found is fixed with a regression test.
- The container is fully disposed of.
- The host is provably untouched across the whole two-pass run.
</success_criteria>

<output>
Create `.planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-03-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 78d2c84

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|----------------|
| No execution-time budget for this plan's own three sequential full-catalog passes (rerun + uninstall + reinstall) — same unaddressed timeout risk as 12.3-02 | HIGH (inherited from 12.3-02's shared finding) / MEDIUM (restated here) | Task 1: each of the three steps now launches via background-execution semantics, appends to `pass2-transcript.log`, polls for its own `TRACER_*_OK` marker rather than blocking, states an explicit wall-time expectation, and falls back to `--categories`-based chunking per step if needed. |
| The same missing repo-resync gap as 12.3-02 applies to this plan's own "fix on host, re-run the whole three-step sequence from the top" instruction | HIGH (inherited from 12.3-02) | Task 1: added the identical `cp -r /repo/. /home/tester/workspace/` re-sync step before any "re-run from the top" iteration. |
| `files_modified` frontmatter did not list the transcript/attestation files Task 1/Task 2's own `<verify>` blocks depend on | actionable (non-HIGH) | Added `.../pass2-transcript.log` and `.../host-attestation.log` to `files_modified`. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| N/A | No findings against this plan were deferred — the HIGH/MEDIUM findings and the related actionable frontmatter finding were addressed directly. |
