---
phase: 12.3-container-e2e-verification-of-the-reconciled-branch-real-end
plan: 04
type: execute
wave: 4
depends_on: ["12.3-03"]
files_modified:
  - .planning/RUN-REPORT.md
  - .planning/PROJECT.md
  - .claude/architecture.md
autonomous: true
cross_ai: true
requirements: [D-06]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "This phase's Tier-3 evidence (TRACER markers, transcripts, per-tool tallies, any bug found and its fix) is folded into .planning/RUN-REPORT.md in the same shape as every prior phase's Tier-3 write-up."
    - "This phase's real decisions (detection script, D-03 gate, Fedora-only scope, two-pass evidence format) are recorded in PROJECT.md's Key Decisions table."
    - "The reusable container-detection script is documented in architecture.md as the entry point future e2e phases should call."
    - "The final host tree is clean and make validate && make test pass."
  artifacts:
    - .planning/RUN-REPORT.md
    - .planning/PROJECT.md
    - .claude/architecture.md
  key_links:
    - "12.3-01-SUMMARY.md / 12.3-02-SUMMARY.md / 12.3-03-SUMMARY.md's TRACER_*_OK evidence -> RUN-REPORT.md's new Phase 12.3 section"
---

<objective>
Fold this phase's real Tier-3 container evidence into the milestone's
established evidence trail, per D-06 — reusing origin's own convention
(`.planning/ONESHOT-RULES.md` Rule 14, `.planning/RUN-REPORT.md`'s existing
shape) — and record this phase's real decisions in `PROJECT.md` and
`architecture.md`, so a future reader (human or agent) can find and trust
this evidence without re-deriving it.

Purpose: D-06 explicitly requires this phase's evidence to slot in alongside
origin's own Tier-3 record, not invent an incompatible shape — this is what
makes the whole milestone's evidence trail one coherent document instead of
two disconnected ones.
Output: a new Phase 12.3 section in `RUN-REPORT.md`, new rows in
`PROJECT.md`'s Key Decisions table, a new architecture.md subsection
documenting the reusable detection-script convention, and a final clean
`make validate && make test` on the host.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-CONTEXT.md
@.planning/RUN-REPORT.md
@.planning/PROJECT.md
@.claude/architecture.md
@.planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-01-SUMMARY.md
@.planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-02-SUMMARY.md
@.planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fold Phase 12.3's Tier-3 evidence into RUN-REPORT.md</name>
  <precondition>12.3-02-SUMMARY.md and 12.3-03-SUMMARY.md exist AND each contains its expected TRACER_*_OK markers verbatim, not merely as files present (12.3-02-SUMMARY.md must contain TRACER_HARNESS_READY_OK and TRACER_CLEAN_INSTALL_OK; 12.3-03-SUMMARY.md must contain TRACER_RERUN_OK, TRACER_UNINSTALL_OK, and TRACER_REINSTALL_OK).</precondition>
  <files>.planning/RUN-REPORT.md</files>
  <action>
Append a new `## Phase 12.3 — Container E2E Verification of the Reconciled
Branch` section to `.planning/RUN-REPORT.md`, positioned after the existing
Phase 12 section (the file's most recent per-phase write-up), matching that
section's own established shape (a "what it ships" summary line, followed
by any significant finding write-up in the same style as the Phase 11
LaunchAgent-bug narrative: what broke, why, how it was fixed, how it was
re-verified — only if Plans 12.3-02/03 actually found and fixed a real bug;
otherwise state plainly that both passes completed clean). Include,
verbatim: the `TRACER_WEZTERM_INSTALL_OK`, `TRACER_HARNESS_READY_OK`,
`TRACER_CLEAN_INSTALL_OK`, `TRACER_RERUN_OK`, `TRACER_UNINSTALL_OK`, and
`TRACER_REINSTALL_OK` markers from the three prior plans' SUMMARYs; the
per-tool outcome tallies (installed/no_method/failed/etc.) from both the
clean-install and rerun passes; and an explicit note that this run covered
Fedora-family only (D-05), reversible/scoped for this phase per
12.3-CONTEXT.md. Update the milestone-wide phase list table near the top of
the file to add a Phase 12.3 row if the table's own convention calls for it
(check the existing table's shape before editing — do not restructure it).

Addresses review concern (12.3-REVIEWS.md MEDIUM, 12.3-04): every one of the
six `TRACER_*_OK` markers this phase produced (`TRACER_WEZTERM_INSTALL_OK`,
`TRACER_HARNESS_READY_OK`, `TRACER_CLEAN_INSTALL_OK`, `TRACER_RERUN_OK`,
`TRACER_UNINSTALL_OK`, `TRACER_REINSTALL_OK`) must appear verbatim in the new
section — a heading string alone does not satisfy this task's own must-have,
and this is what the task's `<verify>` below now checks.
  </action>
  <verify>
    <automated>grep -c "Phase 12.3" .planning/RUN-REPORT.md >/dev/null && for m in TRACER_WEZTERM_INSTALL_OK TRACER_HARNESS_READY_OK TRACER_CLEAN_INSTALL_OK TRACER_RERUN_OK TRACER_UNINSTALL_OK TRACER_REINSTALL_OK; do grep -q "$m" .planning/RUN-REPORT.md || { echo "missing marker: $m" >&2; exit 1; }; done</automated>
  </verify>
  <done>
`.planning/RUN-REPORT.md` has a complete, evidence-backed Phase 12.3 section
containing every `TRACER_*_OK` marker this phase produced, in the same shape
as the milestone's other Tier-3 write-ups.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Consolidate decisions into PROJECT.md and document the reusable detection convention in architecture.md; final sanity gate</name>
  <files>.planning/PROJECT.md, .claude/architecture.md</files>
  <action>
Add rows to `.planning/PROJECT.md`'s Key Decisions table (Outcome column =
`Phase 12.3`), following the exact three-column shape (Decision | Rationale
| Outcome) every prior phase's rows use, for: (1) the OS/arch-aware
podman/colima+docker detection script and its D-03 no-silent-fallback gate;
(2) the Fedora-only scope for this phase's full-catalog run (D-05); (3) the
full-catalog two-pass (clean install + rerun/uninstall/reinstall) evidence
format folded into RUN-REPORT.md (D-06). Bump the file's `*Last updated:
...*` footer line to reference this phase.

Add a new subsection to `.claude/architecture.md`, positioned after the
existing `## Phase 12: version-aware status and the update action` section
(the file's most recent per-phase-numbered heading, matching its established
convention), documenting `scripts/detect-container-runtime.sh` as the
reusable Tier-3 container-tool detection entry point: what it detects, its
`CONTAINER_TOOL=<tool>` output contract, and its D-03 no-silent-fallback
refusal behavior — so a future phase reaches for it instead of re-deriving
OS/arch container-tool logic from scratch.

Finish by re-running `make validate && make test` on the final host tree
(confirming any bug fixes from Plans 12.3-02/03 are still green) and
confirming `git status --short` shows only this phase's own files (the four
PLAN.md files, the two new scripts, any real bug-fix files, and the three
doc files this task touches).
  </action>
  <verify>
    <automated>make validate && make test</automated>
  </verify>
  <done>
`PROJECT.md` and `architecture.md` reflect this phase's real decisions, and
the final host tree is clean and green (`make validate && make test` exit
0).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prior plans' evidence <-> this phase's permanent record | This plan's only job is transcription/consolidation of already-produced evidence into the project's standing documentation — it introduces no new runtime behavior or trust boundary of its own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.3-05 | Repudiation | this phase's evidence going stale or unfindable after the fact | medium | mitigate | D-06's whole purpose: fold verbatim TRACER markers and transcripts into the same standing document (`RUN-REPORT.md`) origin's own Tier-3 evidence already lives in, so a future reader finds one coherent trail, not two. |
</threat_model>

<verification>
`grep -c "Phase 12.3" .planning/RUN-REPORT.md` returns at least 1, AND all
six `TRACER_*_OK` markers (`TRACER_WEZTERM_INSTALL_OK`,
`TRACER_HARNESS_READY_OK`, `TRACER_CLEAN_INSTALL_OK`, `TRACER_RERUN_OK`,
`TRACER_UNINSTALL_OK`, `TRACER_REINSTALL_OK`) appear verbatim in
`.planning/RUN-REPORT.md` — a heading match alone is not sufficient. `git
grep -n "Phase 12.3" .planning/PROJECT.md` shows new rows.
`.claude/architecture.md` contains a new subsection naming
`scripts/detect-container-runtime.sh`. `make validate && make test` exits 0
on the final tree.
</verification>

<success_criteria>
- RUN-REPORT.md, PROJECT.md, and architecture.md all reflect this phase's real, evidenced work.
- The final host tree is clean and every quality gate passes.
</success_criteria>

<output>
Create `.planning/phases/12.3-container-e2e-verification-of-the-reconciled-branch-real-end/12.3-04-SUMMARY.md` when done
</output>

## Review Dispositions Ledger

### Round 1 — 78d2c84

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|----------------|
| Task 1's `<verify>` (`grep -c "Phase 12.3" ...`) only checks a heading string, not that any `TRACER_*_OK` marker was actually folded in | MEDIUM | Task 1: `<verify>` now also loop-checks all six `TRACER_*_OK` marker names appear verbatim in `RUN-REPORT.md`, failing loudly on any missing one. Phase-level `<verification>` updated to match. |
| Task 1's precondition only checks that the prior SUMMARY files exist, not that they actually contain their expected markers | LOW | Precondition now names each SUMMARY's specific expected markers and requires their verbatim presence, not just file existence. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| N/A | No findings against this plan were deferred — the MEDIUM and LOW findings were both addressed directly. |
