# Phase 12.3: Container E2E Verification of the Reconciled Branch - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-10
**Phase:** 12.3-container-e2e-verification-of-the-reconciled-branch-real-end
**Areas discussed:** Herramienta de contenedores, Alcance de la verificación, Matriz de imagenes base, Formato de evidencia

---

## Herramienta de contenedores

| Option | Description | Selected |
|--------|-------------|----------|
| Detección automática por plataforma | Linux: podman nativo. macOS: colima+docker (convención de origin, podman falla en Intel Mac por dependencia Apple-Silicon-only) | ✓ |
| Solo Linux/podman por ahora | Solo esta máquina, macOS queda para después | |
| Replicar colima+docker en Linux también | Igualar el proceso del otro equipo aunque podman ya sea nativo aquí | |

**User's choice:** Detección automática por plataforma (recommended option)
**Notes:** User added a cross-cutting constraint mid-selection: "This needs to be OS/arch aware, developers can use linux (AMD-64) or Mac (intel or arm) as dev env or target runtime env" — folded into D-01.

**Follow-up round 1:**
- User asked for two more details before moving on:
  1. The detection logic should live as reusable code (a script), not one-off phase logic.
  2. If neither tool is available, launch an interactive question offering: install inline, show instructions, or bypass the e2e check.
- Both captured as D-02 and D-03.

---

## Alcance de la verificación

| Option | Description | Selected |
|--------|-------------|----------|
| Solo lo que cambió la reconciliación | host_setup/skill_lifecycle, tiers remapeados, Fase 12.4's rtk/graphify | |
| Todo, desde cero | Re-verificar el catálogo completo sin confiar en la evidencia Tier-3 de origin | ✓ (refined) |

**User's choice:** Todo, desde cero — refined with: "Se debería ejecutar un run limpio y luego una rerun ya sea para install/uninstall/reinstall de algunas tools"
**Follow-up:** asked whether "algunas tools" (some tools) for the rerun pass meant the full catalog or a subset — user chose full catalog for both passes ("Catalogo completo, ambas pasadas ... si el tiempo no es problema").

---

## Matriz de imagenes base

| Option | Description | Selected |
|--------|-------------|----------|
| Las 3 distros Linux | debian, arch, fedora | |
| Solo una (debian) | Una imagen representativa | |
| Solo fedora family | Lo más cercano a Bazzite (this dev host's actual distro) | ✓ (user's own option, not originally offered) |

**User's choice:** "solo fedora family por ahora, lo mas cercano a bazzite" — neither original option, a third the user specified directly.

---

## Formato de evidencia

| Option | Description | Selected |
|--------|-------------|----------|
| Seguir la convención del otro equipo | Marcadores TRACER_OK, encaja con RUN-REPORT.md | ✓ |
| Formato simple propio | Solo pass/fail log, sin replicar el formato tracer | |

**User's choice:** Seguir la convención del otro equipo (recommended option)

---

## Claude's Discretion

- Exact script location/name for the platform-detection helper.
- Whether the two-pass run needs one container or two, as long as the rerun genuinely exercises previously-installed state.

## Deferred Ideas

- Debian and Arch base-image coverage.
- Re-verifying podman viability on Apple Silicon Macs specifically (origin's finding was Intel-Mac-specific).
- Narrowing full-catalog scope if the two-pass run proves too slow to repeat regularly.
