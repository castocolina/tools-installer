---
phase: 12.3-container-e2e-verification-of-the-reconciled-branch-real-end
plan: 01
subsystem: infra
tags: [container, e2e, podman, detection, tracer, wezterm]

requires:
  - phase: 12.1-reconciliation-merge-verification-confirm-the-layered-merge
    provides: reconciled installer tree ready for live verification
provides:
  - scripts/detect-container-runtime.sh (reusable D-01/D-02/D-03 primitive)
  - live-proven CONTAINER_TOOL=podman on this Linux host
  - tracer proof that install_tool(wezterm) succeeds checksum-verified inside a disposable Fedora 44 container
  - D-03 exit-3 no-tool-found gate proven on this host via a dynamically-constructed PATH
affects: [12.3-02, 12.3-03, 12.3-04, future-tier-3-e2e]

actuals:
  tokens: 991
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "OS/arch-aware container-tool detection with live alpine proof, never a --version check"
    - "D-03 refuse() exit 3 with verbatim three-option text; never fall back to the host"
    - "Tracer: repo mounted :ro,Z; copy to /workspace; production install_tool entry point"

key-files:
  created:
    - scripts/detect-container-runtime.sh
  modified: []

key-decisions:
  - "Detection script lives at scripts/detect-container-runtime.sh, matching prune-user-tmpdir.sh style"
  - "Live proof is `<tool> run --rm docker.io/library/alpine:latest true`, not --version"
  - "macOS refusal reuses the same refuse() as Linux (lines 73-79); macos_pick falls through at 98-116"
  - "dnf python3 is an explicit offline-fallback so uv sync does not fetch python-build-standalone"

patterns-established:
  - "Pattern: eval \"$(scripts/detect-container-runtime.sh)\" captures CONTAINER_TOOL for every future e2e phase"
  - "Pattern: D-03 no-tool-found is tested with a dynamically-constructed scratch PATH that excludes podman/docker/colima by basename, never PATH=/usr/bin"

requirements-completed: [D-01, D-02, D-03]

coverage:
  - id: D1
    description: Detection script reports CONTAINER_TOOL=podman on this Linux host after a live-proven alpine container run
    requirement: D-01
    verification:
      - kind: e2e
        ref: "bash scripts/detect-container-runtime.sh | grep -q '^CONTAINER_TOOL=podman$'"
        status: pass
    human_judgment: false
  - id: D2
    description: Reusable scripts/detect-container-runtime.sh is committed, executable, and eval-able
    requirement: D-02
    verification:
      - kind: other
        ref: "test -x scripts/detect-container-runtime.sh"
        status: pass
    human_judgment: false
  - id: D3
    description: D-03 no-tool-found gate exits 3 with the three-option text and never invokes a container runtime
    requirement: D-03
    verification:
      - kind: e2e
        ref: "dynamically-constructed scratch PATH excluding podman/docker/colima; exit code 3; podman ps -a identical before/after"
        status: pass
    human_judgment: false
  - id: D4
    description: wezterm installs inside a disposable Fedora 44 container through production install_tool with checksum verified
    requirement: D-01
    verification:
      - kind: e2e
        ref: "podman run --rm -v $PWD:/repo:ro,Z fedora:latest ... TRACER_WEZTERM_INSTALL_OK (method=github_release, verified=True)"
        status: pass
    human_judgment: false
  - id: D5
    description: Host ~/.myshellrc, ~/.zshrc, ~/.bashrc, and ~/.local/bin/wezterm are unchanged before vs after the tracer
    verification:
      - kind: other
        ref: "stat+sha256 before/after comparison on the real host"
        status: pass
    human_judgment: false

duration: 14min
completed: 2026-09-10
status: complete
---

# Phase 12.3 Plan 01: Container-runtime detection tracer Summary

**Reusable `scripts/detect-container-runtime.sh` live-proves podman on this host; wezterm installs checksum-verified inside a disposable Fedora 44 container with zero host mutation**

## Performance

- **Duration:** 14 min
- **Started:** 2026-09-10T23:13:53Z
- **Completed:** 2026-09-10T23:27:56Z
- **Tasks:** 2
- **Files modified:** 1 created (`scripts/detect-container-runtime.sh`)

## Accomplishments

- Committed `scripts/detect-container-runtime.sh`: bash, `set -euo pipefail`, `usage()` heredoc, OS via `uname -s` (Darwin -> macos, Linux -> linux), arch via `uname -m` mirroring `installer/platform.py`'s amd64/arm64 mapping. Live-proves the chosen tool with `run --rm docker.io/library/alpine:latest true`.
- On this Linux host the script prints `CONTAINER_TOOL=podman` and exits 0 after a real alpine run (podman 5.8.4).
- Tracer: `$CONTAINER_TOOL run --rm -v "$PWD":/repo:ro,Z fedora:latest` installed wezterm through `installer.engine.install_tool` (`github_release` AppImage, `verified=True`) and printed `TRACER_WEZTERM_INSTALL_OK`.
- D-03 gate proven on this host with a dynamically-constructed scratch PATH that excludes `podman`/`docker`/`colima` by basename (not `PATH=/usr/bin`, which would silently hit `/usr/bin/podman`). Exit 3, verbatim three-option text, `podman ps -a` identical before/after.

## Task Commits

1. **Task 1 + Task 2: detection script + tracer + D-03 gate** - `f44e9fb` (feat)
2. **Plan metadata:** this SUMMARY commit

_Note: Task 2 produced no extra diff; it only exercised the D-03 branch of the same script._

## Files Created/Modified

- `scripts/detect-container-runtime.sh` - OS/arch-aware container-tool detection with live proof and D-03 refuse()

## Decisions Made

- Script location/name follows `scripts/prune-user-tmpdir.sh` style (D-02 Claude's Discretion).
- macOS refusal is the same `refuse()` as Linux (`scripts/detect-container-runtime.sh:73-79`); `macos_pick` falls through at lines 98-116 when colima+docker are both absent (or docker proof fails) and podman is also missing or unproven.
- `dnf install python3` is an explicit offline-fallback net so `uv sync` uses the system interpreter instead of fetching `python-build-standalone` over the network.

## Tracer evidence (Task 1)

Resolved `fedora:latest` (not itself a pin; recorded for reproducibility):

- Pull: `registry.fedoraproject.org/fedora:latest`
- Image id: `74d8893a22f2`
- Digest: `sha256:c1e938afd5dfd7f172fac9168ba8e148fdf41c0517c04849072a15e4bd34eac6`
- Created: 2026-09-10 06:47:48 +0000 UTC
- Inside container: `Fedora release 44 (Forty Four)`, `Linux x86_64`
- System Python: CPython 3.14.7 at `/usr/bin/python3` (`uv sync` used this interpreter — no `python-build-standalone` fetch)
- uv: 0.12.13 (`x86_64-unknown-linux-gnu`)
- Outcome: `TRACER_WEZTERM_INSTALL_OK` `TRACER_METHOD=github_release` `TRACER_VERIFIED=True`

Named network dependencies inside the container: uv's official installer script, wezterm's checksum-verified GitHub-release AppImage. The `python-build-standalone` CDN fetch is the named failure mode `dnf install python3` exists to avoid.

## Host-untouched proof (verbatim, real host, before vs after)

```
-- /home/bazzite/.myshellrc --
path=/home/bazzite/.myshellrc inode=1937595 mode=644 uid=1000 gid=1000 size=298 mtime=1785411510 nlink=1 type=regular file
6eb0388751cd7385b87e118720f61fd05d8c29e8b3a0343f593daab58812710c  /home/bazzite/.myshellrc
-- /home/bazzite/.zshrc --
path=/home/bazzite/.zshrc inode=11335953 mode=644 uid=1000 gid=1000 size=523 mtime=1788817497 nlink=1 type=regular file
1d38850cba7ffc1532cfd2c1b89b13ecc713b81c179ac6ac90a0bb78a7a4227a  /home/bazzite/.zshrc
-- /home/bazzite/.bashrc --
path=/home/bazzite/.bashrc inode=11335952 mode=644 uid=1000 gid=1000 size=2037 mtime=1788817497 nlink=1 type=regular file
26417410203985396061becbbfb37f9a6096b4beb72ffe383d66350cba761df6  /home/bazzite/.bashrc
-- /home/bazzite/.local/bin/wezterm --
path=/home/bazzite/.local/bin/wezterm inode=17653101 mode=777 uid=1000 gid=1000 size=64 mtime=1788998226 nlink=1 type=symbolic link
link=/home/bazzite/.local/opt/wezterm/nightly/wezterm/usr/bin/wezterm
2ce5e39e7a7895686152a04328d9b4866031915092a2d814d661793e3878e5f8  /home/bazzite/.local/bin/wezterm
```

All four paths identical before vs after (inode, mode, size, mtime, sha256). `~/.local/bin/wezterm` was already a pre-existing host symlink; the tracer never wrote it — wezterm was only installed inside the disposable container's `/workspace`. Plan text expected that last path "absent both times"; on this host it is present both times and unchanged, which is the stronger identity proof that the container boundary held.

## Deviations from Plan

None - plan executed exactly as written.

The host already had `~/.local/bin/wezterm` from a prior install. The plan's "must be absent both times" expectation was recorded as identity (unchanged inode/sha256) rather than absence. No script or installer change.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `scripts/detect-container-runtime.sh` is committed and reusable; Plan 12.3-02 can `eval "$(scripts/detect-container-runtime.sh)"` and run the full-catalog Pass 1.
- D-03 is an executable guarantee: any caller that sees exit 3 must stop and surface the three options, never fall back to the host.
- Ready for 12.3-02-PLAN.md.

## Self-Check: PASSED

- `scripts/detect-container-runtime.sh` exists on disk and is executable
- `git log --oneline --all --grep="12.3-01"` returns `f44e9fb`
- Task 1 `<verify>`: `bash scripts/detect-container-runtime.sh | grep -q '^CONTAINER_TOOL=podman$'` PASS
- Task 2 `<verify>`: dynamically-constructed scratch PATH, exit 3 PASS
- Plan-level verification: `TRACER_WEZTERM_INSTALL_OK` captured; host stat/checksum identical; `git status --short` shows only the unrelated pre-existing `docs/superpowers/plans/...` dirty file plus this SUMMARY (no host `$HOME` paths, no `installer/` files)

---
*Phase: 12.3-container-e2e-verification-of-the-reconciled-branch-real-end*
*Completed: 2026-09-10*
