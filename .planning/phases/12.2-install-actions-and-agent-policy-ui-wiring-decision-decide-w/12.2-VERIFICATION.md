---
phase: 12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w
verified: 2026-09-10T23:15:00Z
status: passed
score: 5/5 must-haves verified
covered_files:
  - .planning/phases/12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w/12.2-01-PLAN.md
  - .planning/phases/12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w/12.2-01-SUMMARY.md
  - .planning/phases/12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w/12.2-CONTEXT.md
  - .planning/phases/12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w/12.2-REVIEW-codex.md
  - .planning/phases/12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w/12.2-REVIEW.md
  - installer/model.py
  - installer/shellrc.py
covered_digest: "v1:sha256:0dd457f6cc5e02d922d6a00c734cf626fa1c35f4be7bfbd95591b753f1d8ae3a"
behavior_unverified: 0
overrides_applied: 0
---

# Phase 12.2: Install-Actions and Agent-Policy UI Wiring Decision Verification Report

**Phase Goal:** Retire `installer/install_actions.py` entirely — origin's `installer/omz.py`/
`installer/daemon.py`/`installer/executors.py` already cover, with more review cycles behind
them, the "managed setup" cases it used to serve — and leave `installer/agent_policy.py` (plus
`agent_env.py`/`agent_guidance.py`) dormant with no code change, since origin has no equivalent
mechanism yet and wiring it now risks throwaway work ahead of the recorded `../ai-kit` migration
backlog item. `make validate && make test` confirm zero regression from the removal.

**Verified:** 2026-09-10T23:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

This is a pure-deletion / no-wiring decision phase (D-03 explicitly not applicable — no UI code
is added in this phase). Verification therefore focuses on confirming the deletion is complete,
the dormant modules are genuinely untouched, and the quality gate holds — not on UI behavior.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `installer/install_actions.py` and `tests/test_install_actions.py` no longer exist anywhere in the repository (D-01) | ✓ VERIFIED | `test -f` on both paths confirms absence; `git grep -n 'install_actions' -- '*.py'` on the current tracked tree returns zero hits (re-run independently, not taken from SUMMARY) |
| 2 | Every dangling comment reference to the deleted file's path is removed (`model.py:39`, `model.py:265`, `shellrc.py:49`) | ✓ VERIFIED | Read `installer/model.py` lines 30-50/255-275 and `installer/shellrc.py` lines 35-55 directly — no reference to `install_actions.py` remains in any comment/docstring; `git show a930b3d` diff confirms the edits are exactly these 3 lines reworded, nothing else touched |
| 3 | `installer/agent_policy.py`, `agent_env.py`, `agent_guidance.py`, `policy.py` are byte-for-byte unchanged from Phase 12.1's end state (D-02) | ✓ VERIFIED | `git diff --stat 8093e8f HEAD -- <4 files>` is empty (independently re-run against both the retirement commit and current HEAD, not just the commit reviewers cited); codex-lane review additionally cross-checked SHA-256 blob hashes, all identical |
| 4 | `make validate` (ruff/format/pyright/bandit/vulture/shellcheck) and `make test` (pytest --cov, 90% floor) both exit 0 on the tree with `install_actions.py` removed (D-04) | ✓ VERIFIED | Independently re-ran both commands on the current working tree (not reused from SUMMARY/REVIEW output): `make validate` — all checks passed, 0 pyright errors, 0 bandit/vulture/shellcheck findings; `make test` — 1809 passed, 1 skipped, 95.30% branch coverage (floor 90%) |
| 5 | No standing documentation (architecture.md, REQUIREMENTS.md, or similar) describes either module's disposition (D-01a, D-02a) | ✓ VERIFIED | `git grep -il "install_actions" -- '*.md' ':!.planning'` returns nothing; `grep -in "install_actions\|agent_policy" .claude/architecture.md` returns nothing; the only record of the retirement is commit `a930b3d`'s message, exactly as D-01a/D-02a require |

**Score:** 5/5 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `installer/install_actions.py` | MUST NOT exist | ✓ VERIFIED | Confirmed absent via `test -f` and `git grep` |
| `tests/test_install_actions.py` | MUST NOT exist | ✓ VERIFIED | Confirmed absent via `test -f` and `git grep` |
| `installer/model.py` | Comment-only edit; field/parsing logic unchanged | ✓ VERIFIED | Diff limited to 2 comment blocks (lines ~39, ~265); `Tool.post_install`, `POST_INSTALL_ACTIONS`, `SENSITIVE_POST_INSTALL_ACTIONS`, `default_enabled_sensitive_actions`, and `load_tools` (lines 612-701 region) all read byte-identical in the current file |
| `installer/shellrc.py` | Comment-only edit; `collect_bin_dirs` behavior unchanged | ✓ VERIFIED | Diff limited to 1 docstring line; function body (lines 52-69 region) untouched |
| `installer/agent_policy.py`, `agent_env.py`, `agent_guidance.py` | Present, untouched | ✓ VERIFIED | All three files exist; `git diff --stat` against pre-retirement commit and HEAD is empty for all |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `installer/model.py`'s `POST_INSTALL_ACTIONS`/`post_install` closed-list validation | zero `registry.toml` rows | `load_tools` parsing | ✓ VERIFIED | No registry row currently sets `post_install`; `uv run vulture installer --min-confidence 60` shows no hits tied to these fields — deletion of the sole former consumer orphans no live registry data (matches PLAN's must_haves key_link claim, independently re-checked) |
| `installer/policy.py`'s imports of `agent_policy.py`/`agent_env.py` | `setup.py`'s composition root | `TYPE_CHECKING`-guarded + lazy imports | ✓ VERIFIED | `git diff --stat` confirms `policy.py` byte-for-byte unchanged by this phase — Phase 12.1's reachability finding (dormant, zero production callers reaching the composition root for `agent_policy`/`agent_guidance`) is preserved unmodified, not re-derived |

### Data-Flow Trace

Not applicable — this phase is a pure deletion + no-op verification of dormant code; no new
rendered/dynamic data paths were introduced or claimed.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Deletion is complete, no dangling references | `git grep -n 'install_actions' -- '*.py'` | zero hits | ✓ PASS |
| Dormant modules unchanged | `git diff --stat 8093e8f HEAD -- installer/agent_policy.py installer/agent_env.py installer/agent_guidance.py installer/policy.py` | empty | ✓ PASS |
| Quality gate holds | `make validate` | ruff/format/pyright/bandit/vulture/shellcheck all clean | ✓ PASS |
| Test suite + coverage floor holds | `make test` | 1809 passed, 1 skipped, 95.30% coverage (floor 90%) | ✓ PASS |
| No new debt markers introduced in touched files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER" installer/model.py installer/shellrc.py` | no matches | ✓ PASS |

All spot-checks were re-run independently in this verification session against the live tree —
not copied from SUMMARY.md or the two review reports.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| D-01 | 12.2-01-PLAN.md | Retire `install_actions.py` and its test entirely | ✓ SATISFIED | Files absent, zero references remain |
| D-01a | 12.2-01-PLAN.md | No standing documentation entry about the retirement | ✓ SATISFIED | No `.md` file (outside `.planning/` narrative) mentions `install_actions`; commit message is sole record |
| D-02 | 12.2-01-PLAN.md | Leave `agent_policy.py` (+ deps) dormant, no code change | ✓ SATISFIED | `git diff --stat` empty across both the retirement commit and current HEAD |
| D-02a | 12.2-01-PLAN.md | Brief inline note acceptable, no architecture-doc section | ✓ SATISFIED | No architecture.md section added; dormant modules' own inline comments (pre-existing from Phase 12.1) untouched, no new section added anywhere |
| D-04 | 12.2-01-PLAN.md | `make validate && make test` both exit 0 after removal | ✓ SATISFIED | Independently re-run: both exit 0, coverage 95.30% vs 90% floor |
| D-03 | (explicitly N/A) | Not applicable — no UI wiring this phase | ✓ SATISFIED (N/A) | Commit `a930b3d` touches exactly 4 files, none under a UI surface; no view/screen file changed |

No orphaned requirements — REQUIREMENTS.md has no separate entries for this inserted phase (per
ROADMAP's own note); D-01/D-01a/D-02/D-02a/D-04 come from `12.2-CONTEXT.md`'s locked decisions
and are fully covered above.

### Anti-Patterns Found

None. Scanned both modified files (`installer/model.py`, `installer/shellrc.py`) for debt
markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) — zero hits. The diff itself is a pure subtraction
(646 deletions, 9 insertions, all insertions being reworded comments) with no stub patterns,
empty implementations, or hardcoded placeholder data introduced.

One pre-existing, non-blocking observation (also flagged independently by both cross-AI review
lanes as Info-level, not a finding against this phase): `Tool.post_install`/`POST_INSTALL_ACTIONS`
in `installer/model.py` now has zero consumers in the codebase (the field is still parsed/
validated by `load_tools` but nothing dispatches on it), and the surviving comment doesn't say so.
This condition pre-dates commit `a930b3d` (the field was already functionally unconsumed by any
registry row before the deletion) and D-01a explicitly forbids adding new retirement prose here,
so this is correctly out of scope for this phase — not a gap.

### Review Lane Note

`12.2-REVIEW-codex.md` documents that both true cross-AI lanes (codex CLI, opencode CLI) failed
for infrastructure reasons (codex: invalid flag then OAuth token expiry; opencode: known stub/
invocation issue #1936) and the reviewer transparently fell back to a rigorous self-review
against live source (including SHA-256 blob-hash verification of the four dormant files). This
verification did not rely on that self-report either — every claim in both `12.2-REVIEW.md` and
`12.2-REVIEW-codex.md` was independently re-derived above (fresh `git grep`, fresh `git diff
--stat`, fresh `make validate && make test` run). Both lanes converge with this verification's
independent findings: 0 Critical/High, clean deletion, dormant modules untouched.

### Human Verification Required

None. This phase is a pure deletion + dormant-code confirmation with no UI, no runtime behavior
change, and no external service integration — every must-have is mechanically verifiable and was
verified directly against the tree.

### Gaps Summary

No gaps. All 5 must-haves truths verified with independent evidence (not reused from SUMMARY.md
or the review reports): the deletion is complete and reference-free, the dormant modules are
byte-for-byte unchanged (re-confirmed against current HEAD, not just the retirement commit), the
quality gate (`make validate && make test`) was independently re-run and passes clean at 95.30%
coverage against the 90% floor, and no standing documentation was added anywhere in the tree.
D-03 (UI wiring) is correctly not applicable — no UI file appears in the phase's single commit.

---

_Verified: 2026-09-10T23:15:00Z_
_Verifier: Claude (gsd-verifier)_
