---
phase: 12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w
reviewed: 2026-09-10T22:21:47Z
depth: deep
files_reviewed: 4
files_reviewed_list:
  - installer/install_actions.py
  - tests/test_install_actions.py
  - installer/model.py
  - installer/shellrc.py
findings:
  critical: 0
  warning: 0
  info: 1
  total: 1
status: clean
---

# Phase 12.2: Code Review Report

**Reviewed:** 2026-09-10T22:21:47Z
**Depth:** deep
**Files Reviewed:** 4 (`git diff 8093e8f a930b3d`)
**Status:** clean

## Summary

Reviewed commit `a930b3d` ("refactor(install): retire unwired `installer/install_actions.py` (D-01)")
against `8093e8f` on branch `gsd/phase-12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w`.
The diff is a pure, tightly-scoped deletion: `installer/install_actions.py` (299 lines) and
`tests/test_install_actions.py` (338 lines) removed, plus 3 dangling prose-comment references to
the deleted module's path cleaned up in `installer/model.py` (2 comments) and `installer/shellrc.py`
(1 docstring line). No logic, field declaration, or validation code was touched.

Every plan requirement (`12.2-01-PLAN.md`, D-01/D-01a/D-02/D-02a/D-04) was independently re-verified
against the actual tree rather than taken on trust:

- `git grep -n 'install_actions' -- '*.py'` returns zero hits anywhere in the tracked tree — the
  module and its test are fully retired, with no orphaned import.
- `git diff 8093e8f a930b3d -- installer/model.py installer/shellrc.py`, filtered to added/removed
  lines, shows **only** the 3 named comment/docstring lines changed. `Tool.post_install`,
  `POST_INSTALL_ACTIONS`, `SENSITIVE_POST_INSTALL_ACTIONS`, `default_enabled_sensitive_actions`,
  and the `load_tools` parsing/validation logic (lines ~633-651) are byte-identical to before, and
  `collect_bin_dirs`'s actual logic (lines 52-69 of `installer/shellrc.py`) is untouched — only its
  docstring lost the `install_actions.py`-specific example.
- `git diff --stat 8093e8f a930b3d -- installer/agent_policy.py installer/agent_env.py
  installer/agent_guidance.py installer/policy.py` is empty — confirmed byte-for-byte unchanged,
  satisfying D-02's "no code change" exactly.
- A broader, non-`.py`-scoped search for `install_actions` across the tree turns up hits only in
  historical `.planning/` phase artifacts (12.1/12.2 summaries, ROADMAP.md, STATE.md — allowed,
  out of review scope as planning narrative) and gitignored `__pycache__`/`.pytest_cache` cruft.
  No living documentation (`.claude/*.md`, `docs/`, `README.md`) references the deleted module.
- `make validate` (ruff check, ruff format --check, pyright, bandit, vulture, shellcheck) and
  `make test` both exit 0 on the reviewed tree; `uv run pytest --cov` reports 95.30% branch
  coverage (1809 passed, 1 skipped), comfortably above the 90% `fail_under` floor — no orphaned
  import, no new dead-code hit, no coverage regression.
- No new standing documentation file describing the retirement was added — the commit message is
  the sole record, matching D-01a/D-02a and the user's explicit instruction. This is confirmed
  intentional, not a finding.

No bugs, security issues, or quality defects were found in the diff itself. The one Info item below
is a pre-existing condition this diff does not create or worsen, noted for completeness.

## Info

### IN-01: `Tool.post_install` now has literally zero consumers anywhere in the codebase, and the surviving comment does not say so

**File:** `installer/model.py:39-43` and `installer/model.py:264-266`
**Issue:** Before this commit, `installer/install_actions.py` was the only code that ever read
`Tool.post_install`/`POST_INSTALL_ACTIONS` values (confirmed: zero `registry.toml` rows currently
set `post_install`, per the plan's own live-verified claim). After this commit, `grep -rn
"post_install\b" --include="*.py" installer/ setup.py` (excluding `model.py` itself) returns no
hits — the field is now parsed and validated by `load_tools` but consumed by nothing. The
surviving comments ("Multi-hook post-install action names... `post_install` lists zero or more of
these reviewed shell/environment actions run once per tool" / "distinct from `postinstall` above")
read as if the mechanism is live and dispatched somewhere, with no indication that its sole
consumer was just deleted and no replacement wired it up (D-03: not applicable this phase). A
future reader skimming these comments alone (without the git history behind `a930b3d`) could
reasonably assume `post_install` values still drive some runtime behavior.
**Fix:** Not a defect in this diff — this condition pre-dates the commit (the field was already
functionally unconsumed by any registry row before the deletion) and D-01a explicitly forbids
adding new prose about the retirement here, so no action is required within this phase. If/when
`post_install` is either wired into origin's UI or formally retired (tracked separately per D-03),
consider adding a short "no current consumer" note analogous to the existing `requires`/`recommends`
"no-op dependency seam" comment style at `installer/model.py:250-252`, so the schema's dormant
status is self-evident without needing git archaeology.

---

_Reviewed: 2026-09-10T22:21:47Z_
_Reviewer: Claude (gsd-code-reviewer)_
_Depth: deep_
