---
phase: 12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w
plan: 01
type: execute
wave: 1
cross_ai: true
depends_on: []
files_modified:
  - installer/install_actions.py
  - tests/test_install_actions.py
  - installer/model.py
  - installer/shellrc.py
autonomous: true
requirements:
  - D-01
  - D-01a
  - D-02
  - D-02a
  - D-04

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`installer/install_actions.py` and `tests/test_install_actions.py` no longer exist anywhere in the repository — D-01 executed exactly as locked, with git history as the sole recovery path if the decision is ever reversed."
    - "Every dangling reference to the deleted file's path (previously 3: two comments in `installer/model.py` at lines 39/265, one in `installer/shellrc.py` at line 49) is removed — no comment in the tree points at a file that no longer exists."
    - "`installer/agent_policy.py`, `installer/agent_env.py`, `installer/agent_guidance.py`, and `installer/policy.py` are byte-for-byte unchanged from Phase 12.1's end state — dormant, tested, zero production callers reaching `setup.py`'s composition root for `agent_policy.py`/`agent_guidance.py`, `agent_env.py` imported-but-functionally-uninvoked — D-02 executed with genuinely no code change."
    - "`make validate` (ruff check/format/pyright/bandit/vulture/shellcheck) and `make test` (`uv run pytest --cov`, 90% branch-coverage floor per `pyproject.toml`) both exit 0 on the tree with `install_actions.py` removed — D-04's own stated acceptance bar, confirming no orphaned import, no new dead-code hit, and no coverage-floor regression."
    - "No standing documentation (an `.claude/architecture.md` section, a `.planning/REQUIREMENTS.md` entry, or similar) describes either module's disposition — D-01a and D-02a honored; this plan's own commit message is the sole record, per the user's explicit instruction against 'documenting something that no longer exists in the codebase'."
  artifacts:
    - "installer/install_actions.py — MUST NOT exist after this phase"
    - "tests/test_install_actions.py — MUST NOT exist after this phase"
    - installer/model.py (comment-only edit; `Tool.post_install`/`POST_INSTALL_ACTIONS`/`default_enabled_sensitive_actions` fields and their `load_tools` parsing/validation logic stay intact and unmodified)
    - installer/shellrc.py (comment-only edit; `collect_bin_dirs` behavior unchanged)
    - installer/agent_policy.py, installer/agent_env.py, installer/agent_guidance.py (present, untouched)
  key_links:
    - "installer/model.py's `POST_INSTALL_ACTIONS`/`post_install` closed-list validation (`load_tools`, ~lines 633-650) has zero registry.toml row currently setting `post_install` (confirmed live 2026-09-10) — removing the one file that used to CONSUME the parsed values orphans no live registry data, and `uv run vulture installer --min-confidence 60` already reports zero hits on these fields today, so the deletion introduces no new dead-code surface."
    - "installer/policy.py's `TYPE_CHECKING`-guarded import plus two function-local lazy imports of `agent_policy.py` (lines 43/143/202), and its unconditional module-level import of `agent_env.py` (line 19) -> setup.py's composition root (lines 39-46) — Phase 12.1's D-04 reachability finding, left byte-for-byte unchanged by this phase's D-02 (no code change)."
---

<objective>
Execute Phase 12.2's two locked decisions on the two ported-but-unwired local subsystems Phase
12.1 found with zero production callers: **retire** `installer/install_actions.py` entirely
(D-01), and **leave `installer/agent_policy.py` dormant** with no code change (D-02). This phase
has no `REQUIREMENTS.md` entries (ROADMAP lists this inserted phase's Requirements as `TBD`); its
locked decisions in `12.2-CONTEXT.md` — D-01 (delete install_actions.py), D-01a (no standing
documentation about the retirement), D-02 (agent_policy.py stays dormant, unmodified), D-02a (a
brief inline note is fine, no architecture-doc section), D-03 (not applicable — neither module is
wired into the UI this phase, so there is no "where" decision to make), and D-04 (the plan does
exactly two things: delete the file+test, then run `make validate && make test` to confirm no
regression) — are this plan's concrete, testable requirements and are cited as `D-01`..`D-04`
throughout.

D-04 is explicit that this phase's scope is exactly two steps, which this plan implements as its
two tasks: (1) the deletion itself, including the now-dangling comment references it creates
elsewhere in the tree (2 in `installer/model.py`, 1 in `installer/shellrc.py` — removing a stale
pointer to a deleted file is deletion hygiene, not the new documentation entry D-01a forbids); (2)
the quality-gate confirmation D-04 names by name. No UI code is added or changed (D-03: not
applicable), and no new test is written beyond the existing gate staying green (D-04).

`installer/agent_policy.py` needs no code change in this phase (D-02) — origin has no equivalent
mechanism for auditing/editing the `AGENTS-TOOLING.md` shared guidance block, and wiring it now
risks throwaway work ahead of the recorded `../ai-kit` migration backlog item. This plan verifies
that non-change holds rather than making one.

Purpose: close the two-competing-implementations-of-"managed setup" problem D-01 identifies —
origin's `installer/omz.py`/`installer/daemon.py`/`installer/executors.py` already cover, with 12
GSD review cycles behind them, the concrete cases `install_actions.py` used to serve in local's
older UI — without touching `agent_policy.py`, which has no origin equivalent and stays dormant
pending the ai-kit decision.

Output: `installer/install_actions.py` and its test deleted from the repository; the 3 comment
references to the deleted file's path removed from `installer/model.py`/`installer/shellrc.py`
with zero behavioral change; `installer/agent_policy.py`/`agent_env.py`/`agent_guidance.py`/
`policy.py` confirmed byte-for-byte unchanged; `make validate && make test` both green on the
resulting tree.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w/12.2-CONTEXT.md
@.planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/12.1-CONTEXT.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@installer/install_actions.py
@tests/test_install_actions.py
@installer/model.py
@installer/shellrc.py
@installer/policy.py
@installer/agent_env.py
@installer/agent_policy.py
@installer/agent_guidance.py
@Makefile
@pyproject.toml
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Delete `installer/install_actions.py` + its test; remove the 3 dangling comment references it leaves behind (D-01, D-01a, D-02, D-02a)</name>
  <files>installer/install_actions.py, tests/test_install_actions.py, installer/model.py, installer/shellrc.py</files>
  <read_first>
    - `installer/install_actions.py` (full file, 299 lines) — confirm the complete surface being
      removed: `ActionContext`/`ActionResult`/`ActionKey`/`ActionExecutionError`,
      `default_action_context`, and the 6 reviewed handlers (`configure_path`,
      `source_shell_init`, `pnpm_setup`, `enable_corepack`, `set_login_shell`,
      `write_agent_reference`) dispatched through `apply_actions`'s closed `_HANDLERS` table.
    - `tests/test_install_actions.py` (338 lines) — confirm its imports are exclusively
      `from installer.install_actions import ...` plus generic `installer.model`/
      `installer.platform`/`installer.run` fixtures shared by every test file in the suite, so
      deleting it drops no shared fixture another test file depends on.
    - Live confirmation (2026-09-10, re-run this before editing): `git grep -l 'install_actions'
      -- '*.py'` (tracked files only — deliberately excludes gitignored `.worktrees/`, which
      contains an unrelated, coincidentally-matching `render_post_install_actions` symbol on
      unrelated feature branches; `git grep` is also immune to whether a given shell/CI runner
      happens to wrap plain `grep` with gitignore-aware filtering) outside
      `installer/install_actions.py` and `tests/test_install_actions.py` themselves returns
      exactly `installer/model.py` and `installer/shellrc.py` — three prose-comment hits, zero
      import statements:
      `installer/model.py:39` ("dispatched by installer/install_actions.py's closed handler
      table"), `installer/model.py:265` ("dispatched by installer/install_actions.py"), and
      `installer/shellrc.py:49` ("installer/install_actions.py's ActionContext"). This is the
      complete, exhaustive reference set this task must clear.
    - `installer/model.py` lines 39-52 (`POST_INSTALL_ACTIONS`/`SENSITIVE_POST_INSTALL_ACTIONS`
      constants) and 255-274 (`Tool.postinstall`/`post_install`/
      `default_enabled_sensitive_actions` field declarations) and 612-701 (the tail of
      `load_tools`, the closed-list parsing/validation for both fields) — these stay exactly as
      they are; this task edits ONLY the prose comment text at lines 39 and 265, never the field
      declarations, the constants, or the parsing/validation logic that reads them.
    - `installer/shellrc.py` lines 38-51 (`collect_bin_dirs`'s docstring) — the one comment
      referencing `installer/install_actions.py`'s `ActionContext` as an example caller; the
      surrounding rationale ("letting a caller inject a non-default home for tests") stays true
      independent of that specific example, since `tests/test_shellrc.py` itself is exactly such
      a caller.
    - `.claude/architecture.md` rule 5 ("No orphan helpers... Only its own test keeping it
      covered is the tell") — the standard D-01's retirement satisfies; `12.2-CONTEXT.md` D-01a
      ("Do NOT write a standing documentation entry... a clear commit message explaining why is
      sufficient") — governs how this task's comment edits must be worded: remove the dangling
      path reference, do not add new prose explaining the retirement.
  </read_first>
  <action>
    Delete `installer/install_actions.py` and `tests/test_install_actions.py` via `git rm` (D-01).
    Confirmed live (2026-09-10, re-verify before deleting): neither file has any reachable
    production caller outside itself — the only 3 hits anywhere else in the tree are the prose
    comments named above, not imports; `tests/test_install_actions.py` shares no fixture another
    test file needs. Per D-01: this closes the two-competing-implementations-of-"managed setup"
    problem — origin's `installer/omz.py`/`installer/daemon.py`/`installer/executors.py` already
    cover, with more review cycles behind them, the concrete cases this module used to serve.

    Then remove the 3 now-dangling references to the deleted file's path so nothing in the tree
    points at a file that no longer exists — this is deletion hygiene, not the new standing
    documentation entry D-01a forbids (it explains nothing about WHY the module was retired, it
    only stops citing a path that is now false):
    - `installer/model.py:39` — reword the `POST_INSTALL_ACTIONS` comment to drop "dispatched by
      installer/install_actions.py's closed handler table"; keep the sentence describing what the
      constant IS (the closed vocabulary `Tool.post_install` values are validated against) without
      naming a dispatcher.
    - `installer/model.py:265` — same fix on the `post_install` field's own comment: drop
      "dispatched by installer/install_actions.py", keep the "distinct from `postinstall` above"
      clarification intact (that distinction is still true and still useful).
    - `installer/shellrc.py:49` — drop "installer/install_actions.py's ActionContext" from
      `collect_bin_dirs`'s docstring; keep "letting a caller inject a non-default home for tests,
      while every other path is unaffected" as the rationale, since that remains accurate on its
      own.

    Do NOT touch `Tool.post_install`, `POST_INSTALL_ACTIONS`, `SENSITIVE_POST_INSTALL_ACTIONS`, or
    `default_enabled_sensitive_actions` themselves — the dataclass field, the closed-list
    constant, or the `load_tools` parsing/validation logic that reads them. Zero registry.toml row
    currently sets `post_install`, and `uv run vulture installer --min-confidence 60` already
    reports zero hits on these fields today (confirmed live 2026-09-10) because they are read
    within `model.py`'s own validation — removing the file that used to consume the PARSED values
    does not orphan the schema fields themselves. Task 2's gate run re-confirms this holds, rather
    than assuming it.

    Do NOT touch `installer/agent_policy.py`, `installer/agent_env.py`,
    `installer/agent_guidance.py`, or `installer/policy.py` at all — D-02 makes no code change;
    they stay exactly as Phase 12.1 left them (dormant, tested, zero production callers reaching
    `setup.py`'s composition root for `agent_policy.py`/`agent_guidance.py`; `agent_env.py`
    imported-but-functionally-uninvoked, per Phase 12.1's corrected classification). Do NOT add a
    standing architecture-doc/REQUIREMENTS.md section documenting either module's disposition
    (D-01a, D-02a) — this task's own commit message is the sole record.
  </action>
  <verify>
    <automated>test ! -f installer/install_actions.py && test ! -f tests/test_install_actions.py && [ -z "$(git grep -n 'install_actions' -- '*.py' 2>/dev/null)" ]</automated>
    <fails_when>either file still exists on disk, OR any tracked `.py` file anywhere in the tree
    still contains the string "install_actions" (an import or a dangling comment). Uses `git grep`
    (tracked files only) rather than plain `grep -r` specifically so the check does not depend on
    a gitignore-aware grep wrapper and does not false-positive on the unrelated, coincidentally-
    matching `render_post_install_actions` symbol living in gitignored `.worktrees/`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- installer/agent_policy.py installer/agent_env.py
      installer/agent_guidance.py installer/policy.py` is empty — D-02's "no code change" holds
      exactly.
    - `git diff --stat -- installer/model.py installer/shellrc.py` shows only the 3 named comment
      lines changed — no field declaration, constant, or parsing/validation logic touched.
    - `git status --short` shows exactly two deletions (`installer/install_actions.py`,
      `tests/test_install_actions.py`) plus the two comment-only edits — no other file changed.
  </acceptance_criteria>
  <done>`installer/install_actions.py` and its test no longer exist anywhere in the repository,
  and every dangling reference to the deleted file's path is gone — D-01/D-01a closed; D-02/D-02a
  confirmed untouched.</done>
  <reversibility rating="reversible">Pure deletion plus 3 comment-line edits, fully recoverable
  via `git revert`/git history per D-01's own stated reversibility rationale; no schema, registry,
  or runtime behavior changes.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the full quality gate to confirm zero regression from the removal (D-04)</name>
  <files>None — verification-only; confirms Task 1's deletion introduced no orphaned import,
  dead-code hit, or coverage-floor regression. No production or test file changes in this task.</files>
  <read_first>
    - `Makefile` lines 34-48 (`validate`, `test` targets) — the exact gate commands: `uv run ruff
      check installer tests setup.py`, `uv run ruff format --check installer tests setup.py`,
      `uv run pyright`, `uv run bandit -q -r installer --skip B404,B603,B310`, `uv run vulture`,
      `uv run shellcheck install.sh`, then `uv run pytest --cov`.
    - `pyproject.toml` lines 46-61 (`[tool.pytest.ini_options]`, `[tool.coverage.report]` —
      `fail_under = 90`, `[tool.vulture]` — `min_confidence = 80`) — the exact floors this gate
      enforces.
    - `CLAUDE.md`'s "Validate before you commit" rule and "Never bypass a quality gate. Fix the
      root cause; never silence a check to make it pass" rule — the standard this task holds to
      if the gate surfaces anything unexpected from Task 1's deletion.
  </read_first>
  <action>
    Run `make validate` (ruff check, ruff format --check, pyright, bandit, vulture, shellcheck)
    then `make test` (`uv run pytest --cov`, 90% branch-coverage floor), in that order, on the
    exact tree Task 1 left — per D-04's own wording, "run make validate && make test afterward to
    confirm no orphaned import, dead-code (vulture), or coverage-floor regression from the
    removal." Both must exit 0.

    If either fails because of Task 1's removal (an orphaned import ruff/pyright catches, a new
    vulture dead-code hit, a coverage-floor drop below 90%, or a shellcheck/bandit regression),
    fix the root cause within this task — most likely a stray reference Task 1's grep sweep
    missed — per CLAUDE.md's "never silence a check to make it pass" rule; do not weaken a gate
    (no `# noqa`, no vulture allowlist addition, no coverage-floor change) to make it pass.

    Confirm the final working tree contains only Task 1's two deletions plus its two comment-only
    edits before this phase's commit — no other file changed across both tasks.
  </action>
  <verify>
    <automated>make validate && make test</automated>
    <fails_when>`make validate` exits non-zero on any of ruff-check/ruff-format/pyright/bandit/
    vulture/shellcheck, OR `make test` exits non-zero, OR reported branch coverage is below
    90%.</fails_when>
  </verify>
  <acceptance_criteria>
    - `make validate && make test` both exit 0 on the exact tree about to be committed.
    - `uv run pytest --cov` reports overall branch coverage >= 90% (the existing
      `fail_under = 90` gate itself enforces this — no coverage number needs to be read manually,
      only confirmed the command exits 0).
    - No new file under `.claude/` or `.planning/` documents the retirement of either module
      (D-01a) — the phase's own commit message is the only record.
  </acceptance_criteria>
  <done>`make validate && make test` both pass clean on the resulting tree — D-04's acceptance bar
  is met with no orphaned import, no new dead-code hit, and no coverage-floor regression, closing
  the phase with zero UI wiring (D-03: not applicable) and zero new documentation (D-01a,
  D-02a).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deletion diff -> committed tree | This phase's only "input" is the plan itself; the risk surface is accidental scope creep during the deletion/comment-cleanup, not any external or untrusted data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.2-01 | Tampering | installer/model.py, installer/shellrc.py (comment edits) | low | mitigate | Scope the diff to exactly the 3 named comment lines, verified via a `git diff --stat` acceptance criterion in Task 1, so no accidental field/logic change slips in alongside the doc cleanup. |
| T-12.2-02 | Denial of Service | installer/model.py's `load_tools` registry parsing | low | mitigate | Task 1 explicitly forbids touching `Tool.post_install`/`POST_INSTALL_ACTIONS`/`default_enabled_sensitive_actions`'s declarations or validation logic; Task 2's `make test` re-runs the full registry-loading test suite against all catalog rows, confirming parsing for every tool still succeeds unchanged. |
| T-12.2-03 | Repudiation | Retirement rationale (no standing doc) | low | accept | D-01a/D-02a intentionally forgo an architecture-doc entry; the commit message is the sole record of why `install_actions.py` was retired and `agent_policy.py` stays dormant — accepted per the user's explicit instruction, not an oversight. |

No package-manager install tasks exist in this phase (pure deletion + verification), so the
Package Legitimacy Gate does not apply.
</threat_model>

<verification>
Run `make validate && make test` on the final tree (Task 2's own verify). Confirm `git status
--short` shows exactly 4 changed paths: `installer/install_actions.py` (deleted),
`tests/test_install_actions.py` (deleted), `installer/model.py` (modified), `installer/shellrc.py`
(modified). Confirm `git grep -n "install_actions" -- "*.py"` returns nothing anywhere in the
tracked tree (`git grep` searches tracked files only, so it naturally excludes gitignored
`.worktrees/` — avoiding a false-positive match on the unrelated `render_post_install_actions`
symbol there — without depending on a gitignore-aware `grep` wrapper being present in the
execution environment). Confirm `git diff --stat -- installer/agent_policy.py
installer/agent_env.py installer/agent_guidance.py installer/policy.py` is empty.
</verification>

<success_criteria>
- `installer/install_actions.py` and `tests/test_install_actions.py` no longer exist (D-01).
- Zero dangling references to the deleted file's path remain anywhere in the tree.
- `installer/agent_policy.py`/`agent_env.py`/`agent_guidance.py`/`policy.py` are byte-for-byte
  unchanged (D-02).
- `make validate && make test` both exit 0, confirming no orphaned import, dead-code hit, or
  coverage-floor regression (D-04).
- No standing documentation about either module's disposition was added (D-01a, D-02a).
- No UI code was added or changed (D-03: not applicable).
</success_criteria>

<output>
Create `.planning/phases/12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w/12.2-01-SUMMARY.md` when done
</output>
