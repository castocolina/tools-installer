# Phase 12.2: Install-Actions and Agent-Policy UI Wiring Decision - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-10
**Phase:** 12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w
**Areas discussed:** Destino de install_actions.py, Destino de agent_policy.py, Dónde en la UI si se conecta, Alcance real de esta fase

---

## Destino de install_actions.py

| Option | Description | Selected |
|--------|-------------|----------|
| Retirar el módulo | Eliminar installer/install_actions.py y su test; origin ya cubre zsh/pnpm managed setup | ✓ |
| Wirear como acciones UI-triggered | Reutiliza trabajo ya hecho en local | |
| Dejarlo dormido, decidir después | No tocarlo, revisitar más adelante | |

**User's choice:** Retirar el módulo (recommended option)
**Notes:** Mid-discussion, the user raised a real concern about whether tools like rtk/codegraph/graphify need a post-install CLI setup step beyond binary install. Investigation confirmed: codegraph has a real postinstall hook (`codegraph-mcp-register`); graphify/rtk have no equivalent despite Phase 8's own research (`08-RESEARCH.md`) documenting `rtk init -g --claude/--opencode/--codex/--agent-cursor` and graphify's per-host setup — never wired into `installer/postinstall.py`. This confirmed gap, plus the user's request for a systematic "tool onboarding" research process, was split out into a new Phase 12.4 rather than folded into 12.2. install_actions.py itself was confirmed unrelated to this gap (skill_lifecycle/postinstall.py are the real mechanisms), so its retirement was reconfirmed after the tangent.

---

## Destino de agent_policy.py

| Option | Description | Selected |
|--------|-------------|----------|
| Dejarlo dormido | No conectar a la UI, queda inerte hasta definir el momento de ai-kit | ✓ |
| Conectar a la UI ahora | Completar el wiring de auditoría de permisos de agentes | |
| Retirarlo también | Eliminar agent_policy.py/agent_env.py/agent_guidance.py | |

**User's choice:** Dejarlo dormido (recommended option)
**Notes:** Re-presented as a plain list after the user reported the first table rendering broken in their terminal (columns cut off) — session was restarted between the two presentations.

---

## Dónde en la UI si se conecta

**Skipped** — not applicable, since neither module is wired into the UI in this phase. User confirmed skipping this area explicitly.

---

## Alcance real de esta fase

| Option | Description | Selected |
|--------|-------------|----------|
| Solo retiro + documentación | Delete install_actions.py + test; document decisions in architecture.md/REQUIREMENTS.md | |
| Retiro + verificación de que nada quede roto | Same deletion, plus make validate/test to confirm no orphaned imports/coverage regression | ✓ (modified) |

**User's choice:** Retiro + verificación, but explicitly WITHOUT writing standing documentation about the retired module — a commit message is sufficient. Quote: "documentar algo que ya no vale es como guardar el perfume de la ex."
**Notes:** User first asked for a reminder of what each module actually does before answering — provided inline, not via AskUserQuestion (plain text per the answer_validation "Other" empty-text-equivalent handling).

---

## Claude's Discretion

None outstanding — all four decisions locked without deferring to Claude's judgment.

## Deferred Ideas

- Tool onboarding research skill + rtk/graphify postinstall gap + full registry audit — Phase 12.4 (new phase, inserted mid-discussion).
- Wiring agent_policy.py into the UI — deferred indefinitely, pending the ai-kit migration.
