---
phase: 12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w
plan: 01
subsystem: installer
tags: [install-actions, retirement, agent-policy, dormant]

requires:
  - phase: 12.1-reconciliation-merge-verification-confirm-the-layered-merge
    provides: reachability finding that install_actions.py and agent_policy.py had zero production callers
provides:
  - installer/install_actions.py and tests/test_install_actions.py deleted
  - three dangling path comments removed from model.py and shellrc.py
  - agent_policy.py / agent_env.py / agent_guidance.py / policy.py left byte-for-byte unchanged
affects: [12.3, catalog, post_install schema]

actuals:
  tokens: 6024
  tasks: 2
  commits: 1

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - installer/model.py
    - installer/shellrc.py
  deleted:
    - installer/install_actions.py
    - tests/test_install_actions.py

key-decisions:
  - "D-01: retire install_actions.py entirely; origin omz/daemon/executors already cover the cases"
  - "D-01a: commit message is the sole record; no standing architecture/REQUIREMENTS entry"
  - "D-02: leave agent_policy.py dormant with no code change"
  - "D-02a: no architecture-doc section for the dormant module"
  - "D-03: not applicable — no UI wiring this phase"
  - "D-04: make validate && make test is the acceptance bar"

patterns-established: []

requirements-completed: [D-01, D-01a, D-02, D-02a, D-04]

coverage:
  - id: D1
    description: installer/install_actions.py and tests/test_install_actions.py no longer exist; git grep install_actions on tracked *.py is empty
    requirement: D-01
    verification:
      - kind: other
        ref: "test ! -f installer/install_actions.py && test ! -f tests/test_install_actions.py && [ -z \"$(git grep -n 'install_actions' -- '*.py')\" ]"
        status: pass
    human_judgment: false
  - id: D2
    description: agent_policy.py, agent_env.py, agent_guidance.py, and policy.py are byte-for-byte unchanged
    requirement: D-02
    verification:
      - kind: other
        ref: "git diff --stat -- installer/agent_policy.py installer/agent_env.py installer/agent_guidance.py installer/policy.py"
        status: pass
    human_judgment: false
  - id: D3
    description: make validate && make test both exit 0 after the deletion (90% coverage floor held)
    requirement: D-04
    verification:
      - kind: other
        ref: "make validate && make test"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-09-10
status: complete
---

# Phase 12.2 Plan 01 Summary

**Retired unwired `installer/install_actions.py` (D-01); left `agent_policy.py` dormant with no code change (D-02)**

## Performance

- **Duration:** 15 min
- **Completed:** 2026-09-10
- **Tasks:** 2
- **Files modified:** 4 (2 deleted, 2 comment-only)

## Accomplishments

- Deleted `installer/install_actions.py` (299 lines) and `tests/test_install_actions.py` (338 lines) — D-01. Origin's `omz.py` / `daemon.py` / `executors.py` already cover the concrete managed-setup cases this module used to serve, and Phase 12.1 found zero production callers.
- Removed the three dangling path comments in `installer/model.py` (`POST_INSTALL_ACTIONS`, `Tool.post_install`) and `installer/shellrc.py` (`collect_bin_dirs` docstring). `Tool.post_install`, `POST_INSTALL_ACTIONS`, `SENSITIVE_POST_INSTALL_ACTIONS`, and `load_tools` validation are untouched.
- Confirmed `installer/agent_policy.py`, `agent_env.py`, `agent_guidance.py`, and `policy.py` are byte-for-byte unchanged (D-02).
- `make validate` (ruff, pyright, bandit, vulture, shellcheck) and `make test` both exited 0: 1809 passed, 1 skipped, 95.30% branch coverage (floor 90%).

## Task Commits

1. **Task 1 + Task 2:** `a930b3d` (refactor) — retire `install_actions.py`, drop three dangling comments, quality gate green. Task 2 is verification-only and produced no extra diff.

## Files Created/Modified

- `installer/install_actions.py` — deleted
- `tests/test_install_actions.py` — deleted
- `installer/model.py` — comment-only: drop dispatcher path from `POST_INSTALL_ACTIONS` and `post_install` comments
- `installer/shellrc.py` — comment-only: drop `ActionContext` example from `collect_bin_dirs` docstring

## Decisions Made

Followed locked CONTEXT decisions as specified. No new decisions.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. The retirement commit `a930b3d` was already on the branch with the required message and trailers; Task 1's file deletions and comment edits matched that commit. Task 2 re-ran `make validate && make test` on that tree.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `install_actions.py` is gone; catalog `post_install` schema remains for a later consumer if needed.
- `agent_policy.py` stays dormant pending the recorded `../ai-kit` migration decision.
- Phase 12.2 has a single plan; ready for phase verification.

---
*Phase: 12.2-install-actions-and-agent-policy-ui-wiring-decision-decide-w*
*Completed: 2026-09-10*
