# Phase 12.2 — Second-Lane Review (cross-AI lane)

**Review method actually used:** self-review-fallback (direct diff inspection against live source).

- Lane 1 attempted: `codex exec` — failed. `--full-auto` is not a valid flag for this codex
  build's `exec` subcommand (`error: unexpected argument '--full-auto' found`). Retried with
  `-s workspace-write -c approval_policy=never` — blocked by the Claude Code auto-mode
  classifier (non-interactive approval bypass denied). Retried with `-s read-only` (default
  approval, interactive-safe) — codex CLI itself failed with OAuth token errors
  (`401 Unauthorized`, "Your refresh token has already been used... Please log out and sign in
  again"), consistent with the known OAuth/timeout issue flagged in the task.
- Lane 2 attempted: `node gsd-tools.cjs query review-lane invoke --slug opencode ...` — returned
  `"stubbed": true` and the run-dir artifact showed the known non-transient failure:
  `Error: You must provide a message or a command` (opencode CLI invocation issue, #1936 — agent
  ended its turn with no final message). Per task instructions this was not retried further.
- Falling back to direct self-review of `git diff 8093e8f a930b3d` against live source, as
  instructed.

## Scope

Commit `a930b3d` — "refactor(install): retire unwired installer/install_actions.py (D-01)".

```
installer/install_actions.py  | 299 -------------------------------------
installer/model.py            |  14 +-
installer/shellrc.py          |   4 +-
tests/test_install_actions.py | 338 ------------------------------------------
4 files changed, 9 insertions(+), 646 deletions(-)
```

## Checks performed

1. **Full deletion, zero remaining references in tracked `.py` files.**
   - `git grep -n "install_actions" -- '*.py'` → no matches (exit 1 / clean).
   - `installer/install_actions.py` and `tests/test_install_actions.py` confirmed absent from
     the working tree (`ls` → "No such file or directory" for both).
   - Broader repo-wide grep (`git grep -ln "install_actions" -- . ':!.planning'`) → zero hits
     outside `.planning/`. The only remaining occurrences of the string `install_actions` in the
     tracked tree are historical narrative text inside
     `.planning/phases/12.1-.../.review-diagnostics/gsd-review-codex.err`, which is a prior
     review transcript documenting the *decision* to retire the module — not a live reference
     from source, config, or docs. This is expected and appropriate; it is the audit trail this
     phase's own predecessor produced, not an orphaned reference.

2. **`installer/model.py` and `installer/shellrc.py` — comment-only, no behavior change.**
   - `model.py`: the diff touches two comment blocks only — the module-level note above
     `POST_INSTALL_ACTIONS` (dropping the `installer/install_actions.py's closed handler table`
     phrase, keeping the semantic distinction between `postinstall` and `post_install`) and the
     docstring-adjacent comment above the `post_install` field on `Tool` (dropping "dispatched by
     installer/install_actions.py --"). No code lines, no field/type/default changes. Confirmed
     visually against the diff hunk — every changed line starts with `#`.
   - `shellrc.py`: same pattern — the docstring paragraph in `collect_bin_dirs` drops the
     `(installer/install_actions.py's ActionContext)` parenthetical, rewording the sentence but
     preserving its meaning. No non-comment lines touched.
   - Verdict: both files' changes are genuinely comment-only; no logic, signature, or behavior
     changed.

3. **`installer/agent_policy.py`, `agent_env.py`, `agent_guidance.py`, `policy.py` —
   byte-for-byte unchanged.**
   - `git diff --stat 8093e8f a930b3d -- installer/agent_policy.py installer/agent_env.py
     installer/agent_guidance.py installer/policy.py` → empty output (no changes registered).
   - Cross-checked with SHA-256 of each file's blob at both commits (`git show <rev>:<path> |
     sha256sum`): all four files hash identically before and after.
     - `agent_policy.py`: `6572a621...` == `6572a621...`
     - `agent_env.py`: `b0f4201d...` == `b0f4201d...`
     - `agent_guidance.py`: `fce61523...` == `fce61523...`
     - `policy.py`: `fb13851b...` == `fb13851b...`
   - Verdict: confirmed byte-for-byte unchanged, as the commit message's D-02 note claims
     ("D-02 leaves agent_policy.py dormant with no code change; this commit is the sole record").

4. **No regressions, no orphaned imports, no dead code introduced.**
   - No orphaned imports: `install_actions.py`'s only import sites were itself and its own test
     file, both deleted together — nothing else in the tree imports it (confirmed by the
     zero-hit grep in check 1).
   - `POST_INSTALL_ACTIONS` catalog and `Tool.post_install` field are explicitly kept per the
     commit message (D-01 scope: delete the dead consumer, keep the registry-side data model
     intact for Phase 12.2's later wiring decision) — this is a deliberate, documented choice,
     not a regression or dead-code smell. `load_tools` validation over `post_install` values was
     not touched by this diff.
   - Ran the full test suite at current HEAD (`464729b`, several commits past `a930b3d`, so this
     also exercises later phase-12.2 work layered on top): `make test` → **1809 passed, 1
     skipped**, coverage 95.30% (required 90.0%). No failures attributable to this diff.
   - No dead code introduced by this diff — it is a pure subtraction (646 deletions, 9
     insertions, all insertions being reworded comments).

## Findings by severity

- **Critical:** None.
- **High:** None.
- **Medium:** None.
- **Low:**
  - None found in the diff itself. Noting for completeness (not a defect): the historical
    references to `install_actions.py` inside
    `.planning/phases/12.1-.../.review-diagnostics/gsd-review-codex.err` are stale in the sense
    that the file they describe no longer exists, but they are a frozen record of a prior
    review's reasoning (not living documentation), so no action is warranted.

## Overall verdict

The diff is exactly what the commit message claims: a clean, fully-contained deletion of the
unwired `install_actions.py` module and its test, with only comment-text cleanup in the two
sibling files that referenced it by path, and zero changes — verified byte-for-byte — to the
`agent_policy.py`/`agent_env.py`/`agent_guidance.py`/`policy.py` group. No regressions, no
orphaned imports, no dead code. **Approved, no changes requested.**
