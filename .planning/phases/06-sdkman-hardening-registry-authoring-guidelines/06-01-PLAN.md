---
phase: 06-sdkman-hardening-registry-authoring-guidelines
plan: 01
type: execute
wave: 1
cross_ai: true
depends_on: []
files_modified:
  - installer/registry.toml
  - tests/test_registry.py
  - .claude/architecture.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - REQ-sdkman-exclusivity
  - REQ-registry-authoring-verification-checklist
  - REQ-brew-preference-guideline

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`java`/`gradle`/`maven`/`groovy`/`springbootcli` are confirmed installing exclusively through SDKMAN with REAL, non-mocked verification — a fresh Tier-3 container run (colima+docker, per ONESHOT-RULES Rule 14) of `sdk install java` on an unconfigured `ubuntu:24.04` machine completes non-interactively, exit 0, reproducing 06-RESEARCH.md's finding rather than only citing it (ROADMAP SC#1)."
    - "Whether `java`'s SDKMAN candidate needs a pinned `version` to avoid an interactive prompt is resolved (no) and recorded, not left open — SDKMAN's only prompt in `sk_install` requires an existing `$CURRENT` version, which a first install (this project's actual scenario) never has, and the project's `?ci=true` bootstrap additionally disables the prompt permanently (ROADMAP SC#2)."
    - "The SC#2 resolution is recorded as a `# Verified {date}: ...` comment directly above `installer/registry.toml`'s `java` entry (and a lighter one above `sdkman`'s), mirroring the existing `codegraph`/`mmdc`/`puppeteer` comment precedent — the D-01 recording mechanism — with a `tests/test_registry.py` guard test pinning the comment's key claims so they cannot be silently deleted."
    - "A documented, mandatory per-tool per-OS verification checklist for future registry additions exists in `.claude/architecture.md`, naming the `# Verified {date}: ...` comment-citation mechanism (D-01) and citing this phase's own `java`/`sdkman` entries as a fresh worked example alongside the pre-existing `codegraph`/`mmdc`/`puppeteer` precedent (ROADMAP SC#3, REQ-registry-authoring-verification-checklist)."
    - "\"Prefer brew over other userspace package managers, except SDKMAN for the Java toolchain\" is written down as a registry-authoring guideline in `.claude/architecture.md`, documented-only with no lint/test enforcement (D-02, ROADMAP SC#4, REQ-brew-preference-guideline)."
    - "The pre-existing unit-test mirror coverage for the `sdkman` kind (`tests/test_model.py`, `tests/test_executors.py`, `tests/test_resolve.py`, `tests/test_registry.py`, `tests/test_status.py` — ONESHOT-RULES Rule 7) still passes unchanged; this phase adds one new registry-content guard test rather than replacing or weakening any of it."
    - "No `version` param is added to `java`'s `sdkman` method — the resolution is recorded as a comment, not as a code/schema change."
  artifacts:
    - installer/registry.toml
    - tests/test_registry.py
    - .claude/architecture.md
    - .planning/PROJECT.md
  key_links:
    - "`installer/executors.py::_sdkman`'s existing code comment (lines 456-461), which already asserts `?ci=true` prevents an interactive prompt, is now independently verified against SDKMAN's own installed source and a live Tier-3 container run — not merely an assumption carried since commit `0e05f50`."
    - "`tests/test_registry.py`'s new guard test reads `REGISTRY.read_text()` and asserts substrings of the new `java`/`sdkman` comments are present, mirroring `test_mmdc_entry_records_the_brew_rejection_finding` / `test_puppeteer_entry_records_the_postinstall_and_arm64_caveats` — the same comment-pinning pattern, applied to this phase's finding."
    - "`.claude/architecture.md`'s new \"Registry-authoring guidelines\" section cites the concrete `# Verified` comment precedent (`codegraph` at registry.toml:1225-1241, `mmdc`/`puppeteer` at registry.toml:1829-1928, and this phase's own `java`/`sdkman` entries) as the worked examples of D-01's comment-citation mechanism, so the checklist is not abstract policy with no example to point at."
    - "`.planning/PROJECT.md`'s Key Decisions table gains rows for the SC#2 resolution and both D-01/D-02 guideline decisions, following the same three-column pattern Phase 5's `mmdc` decision row already established."
---

<objective>
Give the already-shipped SDKMAN-exclusivity work (commit `0e05f50`) the real,
non-mocked verification pass it skipped, resolve whether `java` needs a pinned
SDKMAN candidate version (it does not), and write down the two
registry-authoring guidelines this whole PRD batch has been leaning on without
ever stating explicitly: a mandatory per-tool per-OS verification checklist
with a defined recording mechanism (D-01), and a brew-preference convention
with SDKMAN's Java carve-out (D-02).

Purpose: REQ-sdkman-exclusivity, REQ-registry-authoring-verification-checklist,
REQ-brew-preference-guideline — ROADMAP Phase 6, all four numbered success
criteria (SC#1-4). 06-RESEARCH.md already did the hard research work: it
proved live, via Tier-3 container verification and direct inspection of
SDKMAN's own installed shell-script source, that `sdk install java` needs no
pinned version, and it identified the exact comment-citation pattern
(`installer/registry.toml:1829-1834`, the `mmdc`/`puppeteer` entries) that
already demonstrates D-01's recording mechanism. This plan's job is to
reproduce that verification fresh (evidence gathered in one session should not
be the only evidence a later reader can check), record the finding durably in
the registry with a test guarding it, and then write the two guidelines down
in the one place this project already documents authoring conventions.

Decisions implemented:
- D-01 (06-CONTEXT.md): a `# Verified {date}: ...` comment citing what was
  checked is the recording mechanism for both this phase's own SC#2 finding
  and the general per-tool per-OS verification checklist — never a
  stronger checked-in excerpt/snapshot of the verified external source.
- D-02 (06-CONTEXT.md): "prefer brew over other userspace package managers,
  except SDKMAN for the Java toolchain" is documented-only, pure convention —
  no lint/test enforces it, matching how the SDKMAN carve-out itself is
  already handled (prose, not a lint rule).

Scope boundary: this phase verifies and documents already-shipped behavior. No
new registry `kind`, executor, or resolver logic is added — `installer/model.py`,
`installer/executors.py`, `installer/resolve.py`, and `installer/status.py` are
read for grounding only and are not modified by this plan.

Output: two `# Verified {date}: ...` comments in `installer/registry.toml`
(above `sdkman` and above `java`), one new guard test in
`tests/test_registry.py`, a new "Registry-authoring guidelines" section in
`.claude/architecture.md`, and three new rows in `.planning/PROJECT.md`'s Key
Decisions table.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@.planning/phases/06-sdkman-hardening-registry-authoring-guidelines/06-CONTEXT.md
@.planning/phases/06-sdkman-hardening-registry-authoring-guidelines/06-RESEARCH.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@.claude/python-tooling.md
@.claude/git-workflow.md
@installer/registry.toml
@installer/executors.py
@tests/test_registry.py

`.codegraph/` exists at the repo root. Use `codegraph_explore` (MCP tool) or
`codegraph explore "<symbols>"` (shell) BEFORE Grep/Read when locating code.
Run `codegraph index || codegraph init -i` once before starting.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reproduce the Tier-3 SDKMAN verification fresh, resolve SC#2, record it as a guarded registry comment</name>
  <precondition>colima is running and `docker info` succeeds (Tier-3 container runtime per ONESHOT-RULES Rule 14) — run `colima start` first if not; already verified working on this machine, not a Rule 5 violation.</precondition>
  <files>installer/registry.toml, tests/test_registry.py</files>
  <read_first>
    - 06-RESEARCH.md "SC#2 Resolution: `sdk install java` Non-Interactivity (definitive)" in full — the exact container commands, the SDKMAN source excerpts (`sdkman-install.sh:37-41`, `sdkman-env-helpers.sh:41-64`) and the "Code Examples" section's reproducible script. This task reproduces this section's Container A run fresh; it does not re-derive the mechanism, which rests on SDKMAN's own source and does not change between runs.
    - installer/registry.toml lines 1297-1327 (the `sdkman` and `java` `[[tool]]` blocks, read directly this session — `sdkman` ends at line 1311, `java` starts at line 1313 and ends at line 1326)
    - installer/registry.toml lines 1225-1241 (the `codegraph` `# Verified 2026-09-05 ...` comment — the exact style to mirror: dated, cites its verification method, states what it does and does not establish, names a re-verify condition)
    - installer/registry.toml lines 1829-1928 (the `mmdc`/`puppeteer` comment block — a second style reference, and the block `test_mmdc_entry_records_the_brew_rejection_finding` / `test_puppeteer_entry_records_the_postinstall_and_arm64_caveats` in tests/test_registry.py pin)
    - installer/executors.py lines 456-469 (`_sdkman` — its own code comment already claims the `?ci=true` bootstrap prevents an interactive prompt; this task independently verifies that claim rather than assuming it)
    - tests/test_registry.py lines 103-140 (`test_java_tools_depend_on_sdkman`, `test_sdkman_uses_init_script_and_declares_bin_dir`, `test_java_tools_install_exclusively_through_sdkman` — the existing Rule 7 mirror coverage this task must not weaken) and lines 283-300 (`test_volta_entry_records_the_npm_postinstall_finding`, `test_mmdc_entry_records_the_brew_rejection_finding` — the exact "read committed text, assert substrings" pattern this task's new test follows)
  </read_first>
  <action>
    This task has two parts: reproduce the real end-to-end evidence (Tier-3
    container, not a pytest mock — `.claude/testing.md` requires tests stay
    deterministic and offline, so this check can never become a `pytest`
    file), then record the resolved finding durably in the registry with a
    guard test.

    PART A — reproduce the Tier-3 verification fresh. Run, on this machine,
    exactly the container commands in 06-RESEARCH.md's "Code Examples ->
    Reproducing the Tier-3 SDKMAN non-interactivity check" section (Container
    A only — the `?ci=true` bootstrap, which is this project's actual
    registered URL; the Container B contrast case is already fully explained
    by the SDKMAN source excerpts in the same research section and does not
    need re-running):

    Bootstrap SDKMAN inside a disposable `ubuntu:24.04` container via
    `curl -s "https://get.sdkman.io?ci=true" | bash`, confirm
    `~/.sdkman/etc/config` shows `sdkman_auto_answer=true`, then run
    `sdk install java` (no version) with stdin closed
    (`< /dev/null`) under a `timeout 150` guard, and capture the exit code.
    Remove the container afterward (`docker rm -f`).

    Record in the SUMMARY, verbatim, this run's: the `sdkman_auto_answer`
    value read from the container's own `~/.sdkman/etc/config`, the full
    `sdk install java` output, and the final `EXIT_CODE=` line. Confirm the
    run matches 06-RESEARCH.md's finding (non-interactive, exit 0, no
    "(Y/n)" prompt observed) — if a prompt DOES appear or the run hangs, this
    is a destructive anomaly per ONESHOT-RULES Rule 9: stop, do not touch
    the registry, and report exactly what happened instead of writing a
    comment that contradicts the evidence.

    Do not hardcode the specific Java version SDKMAN resolves (e.g.
    `25.0.4-tem`) into the registry comment written in Part B — 06-RESEARCH.md
    explicitly notes this value is dynamic (tracks SDKMAN's own
    `candidates/default/java` API) and is not the thing being verified; the
    MECHANISM (no prompt, non-interactive, exit 0) is what the comment
    records.

    PART B — record the finding as a `# Verified {date}: ...` comment, using
    the actual date this task runs (`date +%Y-%m-%d`), mirroring the style at
    `installer/registry.toml:1225-1241` and `:1829-1834`: dated, states the
    verification method, states the finding, and states what it resolves.

    Add directly above the `sdkman` `[[tool]]` block (before line 1297):

    a comment stating that the `?ci=true` query param on this bootstrap URL
    persists `sdkman_auto_answer=true` to `~/.sdkman/etc/config`, which is
    what permanently disables SDKMAN's one interactive prompt
    (`sk_install`'s "Do you want X set as default? (Y/n)") for every
    candidate this project installs through it, independent of whether a
    version is pinned, and that the `java` entry below carries the full
    SC#2 finding this bootstrap choice resolves. Cite this session's
    Tier-3 container reproduction (colima+docker, ubuntu:24.04) and point to
    this plan's own SUMMARY for the exact output.

    Add directly above the `java` `[[tool]]` block (before line 1313):

    a comment stating: `sdk install java` (no version given) resolves
    SDKMAN's server-side "default" candidate version and completes
    non-interactively, exit 0, with no vendor prompt of any kind. SDKMAN's
    only interactive prompt in `sk_install` ("Do you want X set as default?
    (Y/n)") requires an existing `$CURRENT` version for that candidate (the
    upgrade-only path) AND `sdkman_auto_answer != true`; on a first install —
    this project's actual scenario, since `sdkman` and a JVM tool install
    together — `$CURRENT` is empty, so the prompt is never reached at all,
    regardless of `sdkman_auto_answer`. The `?ci=true` bootstrap above
    additionally sets `sdkman_auto_answer=true` permanently, so even a
    future re-install/upgrade cannot hit the prompt. State explicitly that
    this resolves ROADMAP Phase 6 SC#2: no pinned `version` param is needed,
    and none is added. Cite this session's Tier-3 container reproduction the
    same way as the `sdkman` comment.

    Both comments MUST literally contain the substrings `sdkman_auto_answer`,
    `Tier-3 container`; the `java` comment MUST additionally contain the
    literal substrings `$CURRENT`, `no vendor prompt`, and `SC#2` — these
    exact substrings are what the new test in `tests/test_registry.py`
    below asserts, so write the comment text to genuinely contain them
    rather than paraphrasing around them.

    Do NOT add a `version` key to `java`'s `sdkman` method params. The
    resolution is a recorded comment, not a code or schema change — adding
    an unneeded pin would contradict the very finding being recorded.

    PART C — add the guard test. Add a new test function to
    `tests/test_registry.py`, placed near the existing
    `test_java_tools_install_exclusively_through_sdkman` for locality,
    named `test_java_and_sdkman_entries_record_the_sc2_no_pin_verification`,
    that reads `REGISTRY.read_text(encoding="utf-8")` and asserts each of
    the five substrings named above (`sdkman_auto_answer`, `Tier-3
    container`, `$CURRENT`, `no vendor prompt`, `SC#2`) is present in the
    committed file — the same "read committed text, assert substrings"
    pattern `test_volta_entry_records_the_npm_postinstall_finding` and
    `test_mmdc_entry_records_the_brew_rejection_finding` already use. Write
    this test BEFORE writing the two comments and confirm it fails first
    (the substrings do not exist yet), then add the comments and confirm it
    passes — this project's `workflow.tdd_mode` convention, applied to
    registry-content the same way Phase 5's comment-recording tasks already
    did.

    Do not modify `installer/model.py`, `installer/executors.py`,
    `installer/resolve.py`, or `installer/status.py` — this task changes
    only comments and one test; the existing Rule 7 mirror coverage for the
    `sdkman` kind across all five test files is already complete
    (06-RESEARCH.md's "Test coverage — Rule 7 mirror check" section) and is
    not touched.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py tests/test_model.py tests/test_executors.py tests/test_resolve.py tests/test_status.py -q</automated>
    <fails_when>pytest exits non-zero; or the new `test_java_and_sdkman_entries_record_the_sc2_no_pin_verification` fails because one of the five required substrings is missing from `installer/registry.toml`; or any pre-existing sdkman-mirror test (`test_java_tools_depend_on_sdkman`, `test_sdkman_uses_init_script_and_declares_bin_dir`, `test_java_tools_install_exclusively_through_sdkman`, or the sdkman-shaped cases in test_model.py/test_executors.py/test_resolve.py/test_status.py) regresses.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry.py tests/test_model.py tests/test_executors.py tests/test_resolve.py tests/test_status.py -q` exits 0.
    - `uv run python3 -c "from pathlib import Path; from installer.model import load_tools; ts={t.id:t for t in load_tools(Path('installer/registry.toml'))}; j=ts['java']; assert [m.kind for m in j.methods]==['sdkman']; assert 'version' not in j.methods[0].params"` exits 0 — the registry still parses cleanly and `java` still has exactly one `sdkman` method with no `version` param added.
    - `grep -c 'sdkman_auto_answer' installer/registry.toml` is at least 2 (the existing `_sdkman` executor's own comment lives in executors.py, not registry.toml — this counts only the two new registry comments).
    - `grep -c '\$CURRENT' installer/registry.toml` is at least 1.
    - `grep -c 'no vendor prompt' installer/registry.toml` is at least 1.
    - `grep -c 'SC#2' installer/registry.toml` is at least 1.
    - `grep -c 'Tier-3 container' installer/registry.toml` is at least 2.
    - The SUMMARY records this run's fresh container output verbatim: the `sdkman_auto_answer` value read inside the container, the full `sdk install java` transcript, and the final `EXIT_CODE=` line — and states plainly that it matches 06-RESEARCH.md's finding (non-interactive, exit 0, no prompt observed).
    - `make validate` exits 0.
  </acceptance_criteria>
  <done>A fresh Tier-3 container run of `sdk install java` on an unconfigured machine is reproduced and recorded in the SUMMARY; the `java`/`sdkman` entries in `installer/registry.toml` carry `# Verified {date}: ...` comments recording that no pinned `version` is needed (ROADMAP SC#1/SC#2), mirroring the existing `codegraph`/`mmdc`/`puppeteer` comment precedent; a guard test pins the comment's key claims; and the existing five-file Rule 7 mirror coverage for the `sdkman` kind is unchanged and still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write the two registry-authoring guidelines into `.claude/architecture.md`, record the decisions in `.planning/PROJECT.md`</name>
  <files>.claude/architecture.md, .planning/PROJECT.md</files>
  <read_first>
    - .claude/architecture.md in full (157 lines) — the existing "phase adds a section" convention: "## The five rules" (top), "## Tier is a browsing label" (Phase 1), "## Phase 3: install, uninstall, and tweak lifecycle" (Phase 3). This task appends a fourth section in the same style, after line 157.
    - 06-RESEARCH.md "Registry-Authoring Precedent" section in full — the exact placement reasoning (no `CONTRIBUTING.md` exists; `registry.toml`'s own header is terse and mechanical; `.claude/architecture.md` is the established home) and the exact existing comment-citation examples to cite (`codegraph` at registry.toml:1225-1241, `mmdc`/`puppeteer` at registry.toml:1829-1834 quoted verbatim, plus the commit `0e05f50` message quote for the brew-preference precedent)
    - .planning/PROJECT.md lines 88-98 (the Key Decisions table — three columns: Decision, Rationale, Outcome — and its most recent Phase 5 rows, the pattern to extend)
    - .planning/phases/06-sdkman-hardening-registry-authoring-guidelines/06-CONTEXT.md `<decisions>` block (D-01, D-02, both quoted in full) — this task must cite both decision IDs explicitly in the text it writes, not merely implement their intent
    - This plan's own Task 1 (already executed by the time this task runs, same plan, sequential within the plan) — the two new `# Verified {date}: ...` comments this task cites as the fresh worked example
  </read_first>
  <action>
    Append a new `## Registry-authoring guidelines` section to `.claude/architecture.md`,
    after the existing `## Phase 3: install, uninstall, and tweak lifecycle` section
    (currently the file's last section, ending at line 157), in the same prose
    style as the file's other sections — explain the WHY, cite real code
    locations, do not just restate the requirement text. The section has two
    named subsections:

    "### Per-tool, per-OS verification checklist" — states that before any
    new `registry.toml` entry ships, its actual install script or package
    metadata is read directly (never assumed from a marketing page or a
    plausible-sounding guess) and confirmed independently for EACH `os`/
    `arch` the entry declares, since a prerequisite that is a no-op on one
    platform is not assumed to behave the same way on another. States that
    the recording mechanism is a `# Verified {date}: ...` prose comment
    directly above the `[[tool]]` block it documents (D-01) — never a
    checked-in excerpt or snapshot of the verified external source, since a
    comment is lower-friction and carries no staleness risk from content
    that changes upstream. Cites the precedent by name and line range:
    `codegraph` (`installer/registry.toml:1225-1241`), `mmdc`/`puppeteer`
    (`installer/registry.toml:1829-1928`), and this phase's own `java`/
    `sdkman` entries (Task 1 of this plan, the SC#2 non-interactivity
    finding) as three worked examples. States that where a finding is
    load-bearing enough that silently deleting the comment would be a
    regression, a test in `tests/test_registry.py` asserts specific
    substrings of the comment are present in the committed file, naming
    `test_mmdc_entry_records_the_brew_rejection_finding` and this phase's
    own `test_java_and_sdkman_entries_record_the_sc2_no_pin_verification`
    as examples — the comment is the mechanism, the test is what keeps it
    from rotting away unnoticed. States explicitly that the checklist has no
    automated enforcement of its OWN existence — whether a given entry's
    author actually did the verification is not machine-checkable — so it
    is a process discipline for code review to hold new entries against,
    not a lint rule.

    "### Prefer brew" — states that for a new user-tier tool with no other
    constraint, Homebrew is preferred over pnpm/npm, uv tool, or a bespoke
    script, and names the Java toolchain (`java`/`gradle`/`maven`/`groovy`/
    `springbootcli`) as the one standing exception: those install
    exclusively through SDKMAN (REQ-sdkman-exclusivity), never a native or
    brew package. Cites the existing precedent for this exact reasoning: the
    `java` entry's own `desc` field in `installer/registry.toml`, and commit
    `0e05f50`'s message ("brew is preferred generally, but Java-toolchain
    tools must go through SDKMAN specifically, never a native/brew
    package"). States explicitly that this guideline is documented-only —
    no lint or test enforces it (D-02) — because brew availability differs
    too much per OS/tool for an automated "why isn't this brew" check to
    avoid noisy false positives, the same reasoning that already governs
    how the SDKMAN carve-out itself is enforced (prose, not a lint rule).

    Both subsections must literally contain the strings `D-01` and `D-02`
    respectively, and the section heading must be exactly
    `## Registry-authoring guidelines`.

    Then add three new rows to `.planning/PROJECT.md`'s Key Decisions table
    (after the existing Phase 5 rows, before the closing `---`), following
    the exact three-column `| Decision | Rationale | Outcome |` shape the
    table already uses:

    1. Decision: `java`'s SDKMAN candidate needs no pinned `version`.
       Rationale: verified live in a Tier-3 container (colima+docker,
       ubuntu:24.04) — `sdk install java` completed non-interactively, exit
       0, because SDKMAN's one interactive prompt requires an existing
       `$CURRENT` version that a first install never has, and this
       project's `?ci=true` bootstrap additionally disables the prompt
       permanently. Outcome: `Phase 6 (06-01)`.
    2. Decision: registry-authoring verification is recorded as a
       `# Verified {date}: ...` prose comment above the entry, never a
       checked-in snapshot of the verified source (D-01). Rationale: lower
       friction, matches the project's existing ad hoc convention
       (`codegraph`/`mmdc`/`puppeteer`), avoids staleness risk from
       snapshotting upstream content that can change. Outcome:
       `Phase 6 (06-01)`.
    3. Decision: "prefer brew over other userspace package managers, except
       SDKMAN for the Java toolchain" is a documented-only registry-
       authoring guideline, not lint/test enforced (D-02). Rationale:
       matches how the SDKMAN carve-out is already handled — prose, not a
       lint rule; brew availability differs too much per OS/tool for a
       reliable automated check. Outcome: `Phase 6 (06-01)`.

    Do not alter any existing row or the table's column order.
  </action>
  <verify>
    <automated>grep -q "## Registry-authoring guidelines" .claude/architecture.md && grep -q "### Per-tool, per-OS verification checklist" .claude/architecture.md && grep -q "### Prefer brew" .claude/architecture.md && grep -q "D-01" .claude/architecture.md && grep -q "D-02" .claude/architecture.md && grep -q "REQ-sdkman-exclusivity" .claude/architecture.md && [ "$(grep -c 'Phase 6 (06-01)' .planning/PROJECT.md)" -eq 3 ]</automated>
    <fails_when>Any `grep -q` in the chain matches zero times (a required heading, decision citation, or requirement id is missing from `.claude/architecture.md`), or `.planning/PROJECT.md` does not contain exactly three occurrences of the literal outcome marker `Phase 6 (06-01)` — meaning a decision row is missing, duplicated, or was written with a different outcome string.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `<automated>` command above exits 0.
    - `.claude/architecture.md` contains the section heading `## Registry-authoring guidelines` with two subsections, `### Per-tool, per-OS verification checklist` and `### Prefer brew`, appended after the existing `## Phase 3: install, uninstall, and tweak lifecycle` section.
    - The verification-checklist subsection cites `codegraph` at `installer/registry.toml:1225-1241` and `mmdc`/`puppeteer` at `installer/registry.toml:1829-1928` by name and line range, plus this phase's own `java`/`sdkman` entries from Task 1, and contains the literal substring `D-01`.
    - The brew-preference subsection names all five Java-toolchain tools (`java`, `gradle`, `maven`, `groovy`, `springbootcli`) as the SDKMAN exception, cites commit `0e05f50`'s message, and contains the literal substring `D-02`.
    - `.planning/PROJECT.md`'s Key Decisions table gains exactly three new rows (each with `Outcome` = `Phase 6 (06-01)`), and `git diff -- .planning/PROJECT.md` shows only additions — no existing row is altered, reordered, or removed.
    - `make validate` exits 0.
  </acceptance_criteria>
  <done>`.claude/architecture.md` documents both registry-authoring guidelines — D-01's comment-citation verification checklist and D-02's documented-only brew-preference convention — citing real code precedent including this phase's own Task 1 comments; `.planning/PROJECT.md`'s Key Decisions table records all three decisions this phase makes (the SC#2 resolution, D-01, D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `installer/registry.toml` (project-authored data) -> `installer/executors.py::_sdkman`'s `bash -c` pipeline | `candidate`/`version` params are interpolated into a shell pipeline via `shlex.join`; values originate only from this project's own committed registry data, never runtime/user input |
| Disposable Tier-3 container (colima+docker) -> public network (`get.sdkman.io`, SDKMAN's CDN/API) | Live verification fetches and executes SDKMAN's real install script and candidate archives inside an isolated container, removed after use; never touches the host or the real developer machine (ONESHOT-RULES Rule 5) |
| `.claude/architecture.md` guidelines (advisory prose) -> future registry-authoring contributors/agents | The verification checklist and brew-preference guideline are read but not machine-enforced; a future entry can still ship without following them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `installer/executors.py::_sdkman` candidate/version interpolation into `bash -c` pipeline | low | mitigate | Already mitigated pre-existing: `shlex.join(install)` (executors.py:467) plus non-empty-string, config-time validation of `candidate` (model.py:258-263); values are sourced only from this project's committed `registry.toml`, never runtime input. No new mitigation needed this phase — existing coverage confirmed by `tests/test_executors.py:917-932`. |
| T-06-02 | Denial of Service (self-inflicted terminal hang under Textual's raw-mode stdin, no way to answer a stray prompt) | `sk_install`'s one interactive prompt ("Do you want X set as default? (Y/n)") in the `sdk install <candidate>` code path | low | accept | This phase's Tier-3 verification confirms the prompt's guard (`-n "$CURRENT"`) never fires on a first install — this project's actual scenario — regardless of `sdkman_auto_answer`; the `?ci=true` bootstrap additionally disables it permanently for any future re-install. Residual: a future SDKMAN release could add a new prompt elsewhere in the install path (Assumption A2, 06-RESEARCH.md); flagged for review at the next SDKMAN version bump, no code guard added this phase (documented-finding scope, matching D-02's style). |
| T-06-03 | Tampering (a future registry entry ships with an unverified or fabricated "Verified" comment, misleading code review into trusting an unconfirmed install method) | Any future `[[tool]]` entry added without following the new checklist | medium | mitigate | REQ-registry-authoring-verification-checklist codifies the mandatory per-tool per-OS check plus the `# Verified {date}: ...` comment-citation mechanism in `.claude/architecture.md` (Task 2), giving code review an explicit, citable standard to hold new entries against. Not machine-enforced by design (D-01) — residual trust remains on human/code-review diligence, the same residual this project already accepts for every other documented-only convention (e.g. the SDKMAN carve-out itself). |

</threat_model>

<verification>
Phase-level checks, run after both tasks:

- `uv run pytest tests/test_registry.py tests/test_model.py tests/test_executors.py tests/test_resolve.py tests/test_status.py -q` exits 0 (Rule 7 mirror coverage intact, new guard test passes).
- `make validate && make test` exits 0 on the committed tree (ONESHOT-RULES Rule 6, CLAUDE.md's definition of done).
- `grep -c "Verified" installer/registry.toml` is at least 4 (the two pre-existing `codegraph`/`mmdc` comment blocks plus this plan's two new `sdkman`/`java` comments).
- `.claude/architecture.md` and `.planning/PROJECT.md` both show only additions in `git diff` — no existing content is altered or removed.
- The phase's SUMMARY records the fresh Tier-3 container transcript in full, per `human_verify_mode: end-of-phase`'s consolidation design (ONESHOT-RULES Rule 14).
</verification>

<success_criteria>
Maps 1:1 to ROADMAP Phase 6's four numbered success criteria:

1. (SC#1) `java`/`gradle`/`maven`/`groovy`/`springbootcli` are confirmed installing exclusively through SDKMAN with a fresh, real Tier-3 container verification of `sdk install java` — not only the pre-existing unit-test mirror coverage.
2. (SC#2) Whether `java` needs a pinned SDKMAN candidate version is resolved (no) and recorded as a guarded registry comment.
3. (SC#3) `.claude/architecture.md` documents a mandatory per-tool per-OS verification checklist with the `# Verified {date}: ...` comment-citation recording mechanism (D-01).
4. (SC#4) `.claude/architecture.md` documents "prefer brew over other userspace package managers, except SDKMAN for the Java toolchain" as a registry-authoring guideline (D-02).
</success_criteria>

<output>
Create `.planning/phases/06-sdkman-hardening-registry-authoring-guidelines/06-01-SUMMARY.md` when done.
</output>