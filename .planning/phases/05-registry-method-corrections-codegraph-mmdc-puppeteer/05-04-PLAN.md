---
phase: 05-registry-method-corrections-codegraph-mmdc-puppeteer
plan: 04
type: execute
wave: 4
cross_ai: true
depends_on:
  - 05-01
  - 05-03
files_modified:
  - installer/pnpm_globals.py
  - setup.py
  - tests/test_pnpm_globals.py
  - tests/test_setup.py
autonomous: true
requirements:
  - REQ-puppeteer-catalog-entries

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Doctor's pnpm-globals reinstall replays `@mermaid-js/mermaid-cli` and `puppeteer` as ONE comma-joined install group, so running it does not undo the co-install the registry declares and leave `mmdc` unable to resolve `puppeteer`."
    - "That same replay is the recorded remedy for the brownfield case — a machine that already had a standalone `mmdc` and therefore never reached the grouped invocation — and a regression test named for that failure pins it."
    - "The replay carries `--allow-build=<pkg>` for every package the registry declares a build allowance for, de-duplicated, so a replay onto a cold `~/.cache/puppeteer` still downloads the browser instead of silently skipping it."
    - "The replay reproduces the registry's declared version pins for packages the registry knows, so a repair does not silently move a pinned peer onto a new major line."
    - "A global package the registry knows nothing about is still replayed exactly as before — as its own space-separated argument, ungrouped, unpinned and with no build allowance — and the module states plainly that its version and any hand-made group cannot be reconstructed."
    - "`reinstall_node_globals` returns the package specs it invoked, not the flags, and it refuses to run the grouped/build-allowed form on a pnpm too old to support it."
    - "`reinstall_preview` shows the user the same grouped, flag-carrying command line the reinstall will actually run; preview and effect cannot diverge."
    - "`installer/pnpm_globals.py`'s module docstring no longer claims that one `pnpm add -g` invocation supersedes the whole global set, which pnpm's documented v11 isolation model contradicts."
  artifacts:
    - installer/pnpm_globals.py
    - setup.py
    - tests/test_pnpm_globals.py
    - tests/test_setup.py
  key_links:
    - "`setup.py::_build_app` already holds `tools`, so the registry-derived install policy is constructed at the same wiring point that already builds `_globals_preview` and `_reinstall_globals` — no new dependency reaches into the module."
    - "`installer/wizard_app.py` consumes the preview and the reinstall as injected callables and needs no change: its own `_default_preview` fallback keeps today's ungrouped behaviour, and `setup.py` always injects the registry-aware one."
    - "`installer/pnpm_globals.py::reinstall_argv` remains the single argv builder that both `reinstall_preview` and `reinstall_node_globals` call, so the preview stays the effect."
    - "`installer/versions.py` (plan 05-01) owns `probe_version` and both pnpm feature floors, so the replay gates on the same numbers `installer/executors.py::_node` does, from one definition."
---

<objective>
Teach the pnpm-globals snapshot/replay mechanism about install groups, version pins and
build allowances, so the Doctor's reinstall action cannot silently undo the `mmdc` +
`puppeteer` co-install that plan 05-03 ships — and so it becomes the working remedy for the
brownfield machine that never got the co-install in the first place.

Purpose: Phase 4 shipped `installer/pnpm_globals.py` to replay pnpm's live global set in one
invocation after a pnpm self-update loses it (plan 04-05). It builds
`[pnpm, "add", "-g", *packages]` — packages separated by SPACES. pnpm's own Global Packages
documentation states that space-separated packages in a single command each get their own
isolated `node_modules` tree and lockfile, and that only a comma-separated list bundles them
into one group that resolves peer dependencies against each other. Before this phase that
distinction was invisible, because the residual `kind="node"` set was `mmdc` alone. Plan
05-03 makes `mmdc` + `puppeteer` the first real install group in this catalog, and from that
moment the existing replay actively breaks it: `pnpm list -g --json` reports both packages,
the replay reinstalls them space-separated as two isolated groups, and `mmdc` stops being
able to load `puppeteer`. That is exactly the runtime failure 05-RESEARCH.md Pitfall 1
describes, reintroduced by a remediation action the user is invited to run.

This plan closes it rather than leaving it as a known-broken gap. It is additive: every
existing caller that passes no policy keeps today's exact argv and spawns no extra process.

Second purpose, added in cross-AI review cycle 1: this replay is also the documented remedy
for the phase's brownfield gap. A machine that already had a standalone `mmdc` never runs
the grouped invocation, because `installer/engine.py::install_tool` returns
`ALREADY_INSTALLED` before `resolve_methods` is consulted and `installer/deps.py` drops
already-installed tools out of the order. Plan 05-01's Tier-3 container measured that
failure and then measured this exact replay argv repairing it. Plan 05-03 writes the gap and
the remedy onto the registry entry. This plan makes the remedy real and pins it with a
regression test named for the failure rather than for the mechanism.

Decisions implemented:
- D-01: `mmdc` stays on pnpm, so it remains a pnpm-managed global and stays inside this
  module's scope — the Volta redirect never covers it. That is the whole reason this
  interaction exists and has to be handled here.
- D-03: `puppeteer` is a real catalog entry with its own build allowance and version pin, so
  the replay must carry both or a replay onto a cold browser cache reproduces Pitfall 2 and a
  repair silently moves the pinned peer.

Recorded deviation from 05-CONTEXT.md: the `NodeInstallPolicy` layer this plan adds is a new
mechanism, which `<specifics>` said this phase would not add. The deviation is recorded here,
in plan 05-01's objective, and as a Key Decisions row in `.planning/PROJECT.md` (plan 05-03
Task 3). The reason is the same in every case: without it, a remediation action this project
ships would actively re-break the thing ROADMAP SC#3 asks for.

Output: a registry-derived install policy carrying groups, allowances and pins; a
group-aware, pin-aware, allowance-aware argv builder with a truthful return contract and a
version floor; the two `setup.py` wiring lines that feed it; and a corrected module docstring
that states what the replay cannot reconstruct.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@.planning/phases/05-registry-method-corrections-codegraph-mmdc-puppeteer/05-CONTEXT.md
@.planning/phases/05-registry-method-corrections-codegraph-mmdc-puppeteer/05-RESEARCH.md
@.planning/phases/05-registry-method-corrections-codegraph-mmdc-puppeteer/05-REVIEWS.md
@.planning/phases/05-registry-method-corrections-codegraph-mmdc-puppeteer/05-01-SUMMARY.md
@.planning/phases/05-registry-method-corrections-codegraph-mmdc-puppeteer/05-03-SUMMARY.md
@.planning/phases/04-package-manager-redirect-policy/04-05-SUMMARY.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@installer/pnpm_globals.py
@installer/versions.py
@setup.py
@tests/test_pnpm_globals.py

`.codegraph/` exists at the repo root. Use `codegraph_explore` (MCP tool) or
`codegraph explore "<symbols>"` (shell) BEFORE Grep/Read when locating code.
Run `codegraph index || codegraph init -i` once before starting.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A registry-derived install policy and a group-aware, pin-aware `reinstall_argv`</name>
  <files>installer/pnpm_globals.py, tests/test_pnpm_globals.py</files>
  <read_first>
    - installer/pnpm_globals.py lines 1-31 (the module docstring — its "one invocation supersedes whatever the global set currently holds" claim is the sentence this task corrects)
    - installer/pnpm_globals.py lines 92-107 (`node_globals` — the existing registry walk this task's new function mirrors: same "a declaration, not an installation" framing)
    - installer/pnpm_globals.py lines 110-132 (`parse_global_packages` — it flattens pnpm's JSON to bare package names across every project object, which is why the replay cannot see group membership or versions; this task documents that limit, it does not change the parser)
    - installer/pnpm_globals.py lines 191-227 (`reinstall_argv` and `reinstall_node_globals` — the argv builder and its one runtime caller; note `reinstall_node_globals` returns `tuple(argv[3:])`, which stops being "the package specs" the moment a flag is prepended)
    - installer/pnpm_globals.py lines 230-249 (`reinstall_preview` — the third caller, whose output must stay identical to what runs)
    - installer/versions.py as amended by plan 05-01 Task 4 (`probe_version`, `meets_minimum`, `PNPM_CO_INSTALL_MIN`, `PNPM_ALLOW_BUILD_MIN` — imported here, never redefined)
    - installer/registry.toml as amended by plan 05-03 (the `mmdc` `co_install`/`allow_build`/`versions` params and the `puppeteer` `allow_build`/`versions` params this policy is derived from)
    - installer/executors.py `_node` as amended by plan 05-01 (the grouped argv shape this replay must reproduce for the same packages)
    - tests/test_pnpm_globals.py in full (the existing `reinstall_argv` / `reinstall_preview` cases whose current expectations must keep passing when no policy is supplied)
    - .claude/architecture.md rule 5 (no orphan helpers: every new function here must have a production caller by the end of Task 2)
  </read_first>
  <behavior>
    - `node_install_policy(tools)` over the shipped registry returns a policy whose `groups` contains exactly one group, and that group's members are `@mermaid-js/mermaid-cli` and `puppeteer`.
    - The same policy's `allow_build` equals `("puppeteer",)` — exactly one element, even though both the `mmdc` entry and the `puppeteer` entry declare it.
    - The same policy's `versions` equals `(("puppeteer", "^25"),)` on the branch plan 05-03 pinned the group; if plan 05-03 recorded that a version specifier cannot ride inside a comma group, it equals `(("puppeteer", "^25"),)` still, sourced from the `puppeteer` entry alone.
    - `node_install_policy([])` returns an empty policy, and `NodeInstallPolicy()` with no arguments is empty.
    - The shipped registry declares no two different ranges for the same npm package — asserted directly, so a future registry edit that introduced a conflicting pin fails a test rather than silently taking the first one.
    - `reinstall_argv(["a", "b"], pnpm="/x/pnpm")` with no policy still returns `["/x/pnpm", "add", "-g", "a", "b"]` — byte-identical to today.
    - `reinstall_argv(["@mermaid-js/mermaid-cli", "puppeteer"], pnpm="/x/pnpm", policy=<shipped policy>)` returns `["/x/pnpm", "add", "-g", "--allow-build=puppeteer", "@mermaid-js/mermaid-cli,puppeteer@^25"]`.
    - With the same policy and packages `["typescript", "@mermaid-js/mermaid-cli", "puppeteer"]`, the result carries the group as one comma-joined element and `typescript` as its own separate, unpinned element, and the flags precede both.
    - A package set that contains only PART of a declared group (for example `puppeteer` but not `@mermaid-js/mermaid-cli`) replays only the packages actually present — the replay never installs something pnpm was not already managing — and the present member still carries its pin.
    - The `packages` order is otherwise preserved, and duplicates are still collapsed.
    - `reinstall_argv([], pnpm=...)` still raises `ValueError`.
    - `reinstall_preview(packages, known=True, policy=<shipped policy>)` returns the shell-joined form of exactly the argv `reinstall_argv` returns for the same inputs, and its `known=False` / empty / unresolvable-pnpm branches are unchanged.
    - `reinstall_node_globals(packages, policy=...)` passes the policy through to `reinstall_argv` and returns exactly the package-spec elements it invoked — never the `--allow-build=` flags.
    - `reinstall_node_globals` with an EMPTY policy performs no version probe at all and behaves exactly as today.
    - `reinstall_node_globals` with a policy that actually contributes a group or an allowance to THIS package set probes the resolved pnpm once; on a version below the applicable floor it raises `PnpmUnavailable` naming the observed and required versions and invokes the runner zero times.
  </behavior>
  <action>
    Before the first edit of this plan, capture its base commit — `git rev-parse HEAD` — and
    record that hash in the SUMMARY as `PLAN_BASE`, on its own line, as a bare
    40-character hash. Task 2's "wizard_app.py was not touched" criterion is evaluated as a
    diff against that ref with a path filter, so it measures what THIS plan's commits changed
    rather than whatever unrelated modifications the working tree happened to be carrying
    when the plan started. An unscoped working-tree `git diff` would both fail on
    pre-existing dirt from an earlier phase and, once this plan's own commits land, prove
    nothing at all.

    Add to `installer/pnpm_globals.py`, beside `NodeGlobal`:

    - A frozen dataclass `NodeInstallPolicy` with three fields, all defaulting to empty
      tuples: `groups: tuple[tuple[str, ...], ...]` (each inner tuple is one set of npm
      package names that must be installed together), `allow_build: tuple[str, ...]` (npm
      package names whose postinstall scripts the registry explicitly permits), and
      `versions: tuple[tuple[str, str], ...]` (package name paired with the semver range the
      registry pins it to). `versions` is a tuple of pairs rather than a dict so the whole
      value stays frozen, hashable and order-deterministic, which is what makes the
      preview-equals-effect assertion a stable equality rather than a dict-ordering accident.
    - `node_install_policy(tools: Iterable[Tool]) -> NodeInstallPolicy`, which walks the
      catalog exactly the way `node_globals` does — first `kind="node"` method per tool —
      and builds the policy from the method's `co_install`, `allow_build` and `versions`
      params. A group is the method's own `npm_pkg` followed by its `co_install` names,
      de-duplicated with `dict.fromkeys`, and is emitted only when `co_install` is
      non-empty. `allow_build` is the concatenation of every method's allowances,
      de-duplicated with `dict.fromkeys` — both the `mmdc` entry and the `puppeteer` entry
      declare `puppeteer`, and a literal concatenation would put two identical flags in the
      replay argv. `versions` is the concatenation of every method's pins, de-duplicated by
      package name with `dict.fromkeys`, first declaration in catalog order winning; state
      that first-wins rule in the docstring, and note that the accompanying registry
      integrity test is what keeps a conflicting second declaration from ever reaching it,
      so this function never silently arbitrates a real disagreement. Give the whole
      function the same "a declaration, not an installation" framing `node_globals` already
      carries: this describes what the registry SAYS about how packages must be installed
      together, and it is intersected with pnpm's live set before it affects any argv.

    Extract the argv tail into a private helper
    `_reinstall_parts(packages, policy) -> tuple[list[str], list[str]]` returning
    `(flags, specs)`, and build it in three steps:

    1. De-duplicate `packages` with `dict.fromkeys`, as today.
    2. Walk that order once. When a package is the first member (in `packages` order) of a
      declared group, emit ONE spec element joining every group member that is actually
      present in `packages`, comma separated, each member rendered as its bare name or as
      name, `@` and its pinned range when `policy.versions` names it; mark the other members
      consumed so they are not emitted again. Every package in no group, or already
      consumed, is emitted as its own element, likewise pinned when the policy names it.
      Never add a package to the specs that is not in `packages` — the replay's contract is
      that it replays what pnpm already manages, and widening it would install something the
      user never chose.
    3. Build the flags: one `--allow-build=<name>` element per `policy.allow_build` entry
      that appears in `packages`, de-duplicated with `dict.fromkeys`, in policy order.

    Add a comment on the join explaining, with the pnpm Global Packages documentation as the
    reason, that comma versus space is not formatting: space-separated packages in one
    invocation each get their own isolated install, which for a peer-dependency pair means
    the replay silently breaks the dependent.

    `reinstall_argv` takes a keyword-only `policy: NodeInstallPolicy = NodeInstallPolicy()`,
    calls `_reinstall_parts`, and returns `[pnpm, "add", "-g", *flags, *specs]`. Its empty
    `packages` `ValueError` branch is unchanged.

    Thread the same keyword-only `policy` parameter through `reinstall_node_globals` and
    `reinstall_preview`, each defaulting to an empty policy so present-day callers are
    unaffected. `reinstall_node_globals` must call `_reinstall_parts` itself (or receive its
    result) and return `tuple(specs)` — NOT `tuple(argv[3:])`. That slice is correct only
    while the argv carries no flags; once `--allow-build=` elements are prepended it returns
    flags as if they were package specs, contradicting the function's own documented
    contract. Fix the slice rather than the docstring: the contract is the useful one.

    Add the pnpm floor check to `reinstall_node_globals`, after the pnpm path resolves and
    before the runner is invoked. It runs ONLY when `_reinstall_parts` actually produced a
    comma-joined group element or at least one flag for THIS package set; an empty policy,
    or a policy whose groups and allowances do not intersect the live set, probes nothing
    and behaves exactly as today. Import `probe_version`, `meets_minimum`,
    `PNPM_CO_INSTALL_MIN` and `PNPM_ALLOW_BUILD_MIN` from `installer.versions` with a plain
    `from ... import ...` so `installer.pnpm_globals.probe_version` is a rebindable module
    attribute a test can `monkeypatch.setattr`; do not redefine either floor here. Raise the
    existing `PnpmUnavailable` on an unmet floor, naming the observed and required versions
    and the remedy — the Doctor screen already renders that exception, so this needs no
    change in `installer/wizard_app.py`. Do NOT add the probe to `reinstall_preview`: the
    preview is rendered on the UI path and must stay free of subprocesses, and it is still
    truthful because it shows the argv the reinstall builds — the floor is a precondition of
    running it, not a different command. Say exactly that in `reinstall_preview`'s docstring.

    Correct the module docstring's final paragraph and extend it. It currently states that
    one `pnpm add -g` invocation supersedes whatever the global set currently holds, and
    that an argv built from the registry would discard hand-added globals. The second half
    remains true and is why the replay is still driven by pnpm's own live list. The first
    half is not true under pnpm v11's isolation model — each package or comma-joined group
    is its own hash-keyed install and supersedes nothing else — so restate it as: the replay
    reinstalls pnpm's own live set, and the registry contributes three things, the command
    name a package installs, how packages must be grouped and build-allowed when they are
    put back, and what version range they are pinned to.

    Then add a short, explicit LIMITATIONS paragraph to the same docstring, because these
    are the replay's real edges and they were previously unwritten:
    - `parse_global_packages` flattens pnpm's JSON to bare package NAMES, so the replay
      knows neither the version a package was at nor which live group it belonged to. A
      package the registry does not know is therefore reinstalled at whatever the registry's
      dist-tag resolves to now, ungrouped — its previous version and any hand-created group
      are not reconstructible from the snapshot.
    - Consequently a hand-made group of packages this catalog does not declare is replayed
      as separate isolated installs, which can break a peer relationship the user set up
      themselves. This is a known limitation of replaying a name-only snapshot, not an
      oversight; reconstructing it would require reading pnpm's per-group project structure,
      which is out of this phase's scope.
    - pnpm removes a whole comma group when either member is removed with `pnpm remove -g`,
      so a group this replay creates is also a coupling the user inherits.

    Write the `<behavior>` cases in `tests/test_pnpm_globals.py` first and watch them fail
    before implementing — `workflow.tdd_mode` is enabled. Build the fixture policies
    explicitly in the tests as well as deriving one from the shipped registry, so a future
    registry edit cannot quietly turn a behavioural test into a tautology. Stub
    `installer.pnpm_globals.probe_version` with `monkeypatch.setattr` in every test that
    reaches `reinstall_node_globals` with a contributing policy; no test may reach a real
    `pnpm --version` subprocess.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pnpm_globals.py -x -q</automated>
    <fails_when>pytest exits non-zero, or its output reports the grouped packages emitted as separate space-separated argv elements, or reports a package appearing in the argv that was not in the input `packages` list, or reports `reinstall_preview` returning a command line that differs from `reinstall_argv`'s output, or reports `reinstall_node_globals` returning an element beginning with `--allow-build`, or reports a real `pnpm --version` subprocess being spawned.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pnpm_globals.py -x -q` exits 0.
    - `uv run python3 -c "from installer.model import load_tools; from installer.pnpm_globals import node_install_policy, reinstall_argv; p=node_install_policy(load_tools('installer/registry.toml')); assert p.groups==(('@mermaid-js/mermaid-cli','puppeteer'),), p.groups; assert p.allow_build==('puppeteer',), p.allow_build; assert p.versions==(('puppeteer','^25'),), p.versions; assert reinstall_argv(['@mermaid-js/mermaid-cli','puppeteer'], pnpm='/x/pnpm', policy=p)==['/x/pnpm','add','-g','--allow-build=puppeteer','@mermaid-js/mermaid-cli,puppeteer@^25']"` exits 0 — the allowance is de-duplicated to one element even though two registry entries declare it.
    - `uv run python3 -c "from installer.pnpm_globals import reinstall_argv; assert reinstall_argv(['a','b'], pnpm='/x/pnpm')==['/x/pnpm','add','-g','a','b']"` exits 0 — the no-policy argv is byte-identical to today's.
    - `uv run python3 -c "from installer.model import load_tools; from installer.pnpm_globals import node_install_policy, reinstall_argv; p=node_install_policy(load_tools('installer/registry.toml')); got=reinstall_argv(['puppeteer','typescript'], pnpm='/x/pnpm', policy=p); assert 'mermaid' not in ' '.join(got), got; assert 'typescript' in got, got"` exits 0 — a partial group replays only what pnpm already manages.
    - `uv run python3 -c "import shlex; from installer.model import load_tools; from installer.pnpm_globals import node_install_policy, reinstall_argv, reinstall_preview; p=node_install_policy(load_tools('installer/registry.toml')); pkgs=['@mermaid-js/mermaid-cli','puppeteer']; assert reinstall_preview(pkgs, policy=p, resolve_pnpm=lambda: '/x/pnpm')==shlex.join(reinstall_argv(pkgs, pnpm='/x/pnpm', policy=p))"` exits 0 — preview equals effect.
    - `uv run python3 -c "import installer.pnpm_globals as pg; from installer.model import load_tools; p=pg.node_install_policy(load_tools('installer/registry.toml')); pg.probe_version=lambda argv: '11.9.0'; calls=[]; got=pg.reinstall_node_globals(['@mermaid-js/mermaid-cli','puppeteer'], runner=calls.append, resolve_pnpm=lambda: '/x/pnpm', policy=p); assert got==('@mermaid-js/mermaid-cli,puppeteer@^25',), got; assert not any(s.startswith('--allow-build') for s in got), got"` exits 0 — the return value is the specs, never the flags.
    - `uv run python3 -c "import unittest, installer.pnpm_globals as pg; from installer.model import load_tools; p=pg.node_install_policy(load_tools('installer/registry.toml')); pg.probe_version=lambda argv: '10.9.0'; calls=[]; unittest.TestCase().assertRaisesRegex(pg.PnpmUnavailable, '(?s)(?=.*10[.]9[.]0)(?=.*11[.]0[.]0)', pg.reinstall_node_globals, ['@mermaid-js/mermaid-cli','puppeteer'], runner=calls.append, resolve_pnpm=lambda: '/x/pnpm', policy=p); assert calls == [], calls"` exits 0 — a too-old pnpm refuses the grouped replay, names both versions, and runs nothing.
    - `uv run python3 -c "import installer.pnpm_globals as pg; seen=[]; pg.probe_version=lambda argv: seen.append(argv) or '1.0.0'; calls=[]; got=pg.reinstall_node_globals(['a','b'], runner=calls.append, resolve_pnpm=lambda: '/x/pnpm'); assert seen==[], seen; assert calls==[['/x/pnpm','add','-g','a','b']], calls; assert got==('a','b'), got"` exits 0 — an empty policy probes nothing and behaves exactly as today.
    - `grep -c 'NodeInstallPolicy' installer/pnpm_globals.py` is at least 4.
    - `grep -c 'NodeInstallPolicy' tests/test_pnpm_globals.py` is at least 3.
    - `grep -c 'PNPM_CO_INSTALL_MIN' installer/pnpm_globals.py` is at least 1 — the floor is imported from `installer/versions.py`, not redefined.
    - The SUMMARY records `PLAN_BASE`, the `git rev-parse HEAD` value captured before this plan's first edit, on its own line as a bare 40-character hash.
    - `make validate` exits 0.
  </acceptance_criteria>
  <done>`reinstall_argv` reproduces the registry's declared install groups, version pins and build allowances when given a policy, keeps its exact present-day output when given none, never widens the replay set, returns the specs it invoked rather than the flags, refuses the grouped form on a pnpm too old to support it, and the module docstring states both the corrected isolation model and the replay's real limitations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the registry policy into the Doctor's preview and reinstall, and pin the brownfield remedy</name>
  <files>setup.py, tests/test_setup.py, tests/test_pnpm_globals.py</files>
  <read_first>
    - setup.py lines 246-288 (`_build_app`'s node-globals closures: `_node_globals_report`, `_globals_preview`, `_reinstall_globals`, and the `UnifiedApp(...)` construction that injects all three — `tools` is already in scope here)
    - installer/wizard_app.py the `node_globals` / `globals_preview` / `reinstall_globals` parameters and the `_default_preview` fallback used only when nothing is injected (confirm no change is needed there)
    - installer/wizard_app.py the Doctor screen's reinstall action and the comment about not calling `reinstall_argv` on that path, plus how it renders `PnpmUnavailable`
    - .claude/architecture.md rule 4 (`setup.py` is wiring only: it may construct, but no decision function may live there)
    - tests/test_setup.py (the existing conventions for asserting `_build_app`'s wiring without launching a Textual app)
    - .planning/phases/05-registry-method-corrections-codegraph-mmdc-puppeteer/05-01-SUMMARY.md (the container evidence that this exact replay argv repairs a brownfield split — the fact the regression test is named after)
  </read_first>
  <behavior>
    - `setup.py`'s preview closure passes a policy derived from the same `tools` list `_build_app` already holds, so the previewed command line for a global set containing both `@mermaid-js/mermaid-cli` and `puppeteer` shows them comma-joined, pinned, with `--allow-build=puppeteer`.
    - `setup.py`'s reinstall closure passes the same policy, so the previewed line and the executed argv are identical.
    - The preview's `known=False`, empty-set and unresolvable-pnpm branches are unchanged.
    - `installer/wizard_app.py` is not modified: its injected callables carry the policy, its own `_default_preview` fallback (used only when nothing is injected) keeps the empty-policy behaviour, and it already renders `PnpmUnavailable` for the new floor failure.
    - `make test` passes with no change to the existing `tests/test_wizard_app.py` node-globals cases.
    - A regression case named for the brownfield failure asserts that for the shipped registry's policy and a live set of exactly `["@mermaid-js/mermaid-cli", "puppeteer"]` — the state a brownfield machine is left in after the catalog installs puppeteer alone — the replay produces exactly one spec element naming both packages.
  </behavior>
  <action>
    In `setup.py::_build_app`, construct the policy once from the `tools` list already in
    scope — `policy = pnpm_globals.node_install_policy(tools)` — placed beside the existing
    node-globals closures, and pass it as the `policy=` keyword in both
    `pnpm_globals.reinstall_preview(...)` inside `_globals_preview` and
    `pnpm_globals.reinstall_node_globals(...)` inside `_reinstall_globals`.

    This is construction, not decision-making, so it satisfies `.claude/architecture.md`
    rule 4: the policy is derived by a function in `installer/`, and `setup.py` only hands
    it to the two closures it already builds. Add a short comment on the construction saying
    that the preview and the reinstall must receive the SAME policy object, because the
    module's whole preview-equals-effect guarantee rests on both paths reaching one argv
    builder with one set of inputs.

    Do not modify `installer/wizard_app.py`. Its `_default_preview` fallback runs only when
    `globals_preview` is not injected, which `setup.py` always does; leaving that fallback
    on the empty policy keeps the module's default behaviour honest for any caller that
    supplies no registry. Confirm by reading — not by assuming — that the Doctor's reinstall
    path already surfaces `PnpmUnavailable` to the user, since Task 1 now raises it for an
    unmet pnpm floor as well as for a missing pnpm; record in the SUMMARY where that
    handling lives. If it turns out not to be handled, STOP and report it rather than
    editing `wizard_app.py`, because that would change this plan's file set and its stated
    success criterion.

    Add wiring assertions to `tests/test_setup.py` following its existing conventions:
    exercise `_build_app`'s constructed preview closure against a report whose `managed` set
    contains both `@mermaid-js/mermaid-cli` and `puppeteer`, with a stubbed pnpm resolver,
    and assert the returned string contains the comma-joined group, the version pin and the
    build-allowance flag. If `_build_app`'s current shape does not permit reaching that
    closure without a Textual run, assert the wiring at the closest seam it does expose
    rather than launching the app, and record in the SUMMARY which seam was used and why.

    Add the brownfield regression case to `tests/test_pnpm_globals.py`, named for the
    failure rather than for the mechanism — for example
    `test_replay_regroups_a_brownfield_split_mmdc_and_puppeteer`. Give it a docstring
    stating the exact scenario it pins: a machine that already had a standalone `mmdc` never
    runs the catalog's grouped invocation, because `installer/engine.py::install_tool`
    returns `ALREADY_INSTALLED` and `installer/deps.py` drops installed tools from the
    order, so it ends up with the two packages in two isolated groups; this replay is the
    documented remedy, and plan 05-01's Tier-3 container measured it rendering successfully
    afterwards. Assert that for the shipped registry's policy and a managed set of
    `["@mermaid-js/mermaid-cli", "puppeteer"]` the resulting argv contains exactly one spec
    element naming both packages, so the replay cannot re-split the pair into two isolated
    installs.

    Tests first, then the wiring.
  </action>
  <verify>
    <automated>uv run pytest tests/test_setup.py tests/test_pnpm_globals.py tests/test_wizard_app.py -x -q</automated>
    <fails_when>pytest exits non-zero, or its output reports the Doctor preview rendering the two packages as separate space-separated arguments, or reports the preview and the reinstall argv disagreeing, or reports the brownfield regression case producing two spec elements, or reports a regression in the existing `tests/test_wizard_app.py` node-globals cases.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_setup.py tests/test_pnpm_globals.py tests/test_wizard_app.py -x -q` exits 0.
    - `grep -c 'node_install_policy' setup.py` is at least 1.
    - `uv run python3 -c "import inspect, setup; src=inspect.getsource(setup._build_app); assert src.count('policy=') >= 2, src.count('policy=')"` exits 0 — both closures receive the policy.
    - `grep -ci 'brownfield' tests/test_pnpm_globals.py` is at least 1 — the regression case is named and documented for the failure it prevents.
    - `PLAN_BASE=$(grep -oE '[0-9a-f]{40}' .planning/phases/05-registry-method-corrections-codegraph-mmdc-puppeteer/05-04-SUMMARY.md | head -1); test -n "$PLAN_BASE"; git diff --name-only "$PLAN_BASE"..HEAD -- installer/wizard_app.py` produces no output. The path is the full repo-relative path to the SUMMARY Task 1 wrote, not a bare filename — an earlier draft of this criterion grepped `05-04-SUMMARY.md` from the repo root, where no such file exists, so `PLAN_BASE` came back empty and the deliberate `test -n` guard failed for the wrong reason. The guard itself stays: an unset `PLAN_BASE` would silently degrade the range to `HEAD..HEAD`, always empty and always "passing". This is deliberately a commit range plus a path filter, never a working-tree check: `installer/wizard_app.py` is already modified in this branch's working tree by unrelated in-progress work, so any criterion reading the working tree would fail on dirt this plan did not create. That is why the previously-paired `git status --porcelain -- installer/wizard_app.py` criterion is removed rather than kept alongside this one — it contradicted this plan's own stated reasoning.
    - `make validate` exits 0.
    - `make test` exits 0.
  </acceptance_criteria>
  <done>The Doctor's reinstall preview and its executed command both carry the registry's install groups, version pins and build allowances, they remain identical to each other, the brownfield remedy is pinned by a test named for the failure it prevents, and `installer/wizard_app.py` needed no change to get there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pnpm's live global set -> replay argv | Package names read from `pnpm list -g --json` become argv elements of a global install. |
| registry -> build allowances in a replay | Registry-declared names decide which postinstall scripts run unrestricted during a replay, and pnpm records that permission persistently. |
| local toolchain -> replay | `pnpm --version` output gates whether the grouped, build-allowed form may be used at all. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Elevation of Privilege | `--allow-build` flags added to the replay argv | high | mitigate | **Scope correction (cross-AI review cycle 1):** pnpm's `add` documentation records `--allow-build` as writing the package into pnpm's build-allowance configuration, so the grant is PERSISTENT and package-level rather than scoped to this replay — a later version of that package runs its postinstall unprompted. Controls in force: the flags come only from `node_install_policy`, which reads the registry's `allow_build` params, themselves constrained at load time by plan 05-01's rule that an allowance may only name a package the same invocation installs; the replay emits an allowance only for a package present in pnpm's live set, so it cannot un-gate a build for something it is not reinstalling; the same policy carries the registry's `^25` pin, so the replay does not widen the pre-authorised major line; and the replay creates no allowance the install path did not already create, since both read the same registry field. Residual risk is the same T-05-19 accepted in plan 05-03, not a second one. |
| T-05-15 | Tampering | the replay widening the installed set | high | mitigate | Task 1's group logic emits only group members that already appear in `packages`. A declared group whose other half pnpm does not manage replays only the half it does, so the remediation action can never install a tool the user never chose. Covered by an explicit acceptance criterion and a `<behavior>` case. |
| T-05-16 | Repudiation | preview diverging from effect | medium | mitigate | `reinstall_preview` and `reinstall_node_globals` both reach the one `_reinstall_parts` builder, and Task 2 passes the same policy object to both from a single construction site. An acceptance criterion asserts the preview string equals `shlex.join` of the argv for the same inputs. The pnpm floor check is deliberately on the reinstall only and is a precondition rather than a different argv, which `reinstall_preview`'s docstring states so the asymmetry is disclosed rather than discovered. |
| T-05-17 | Denial of Service | a replay onto a cold `~/.cache/puppeteer` | medium | mitigate | The build allowance travels with the replay, so the browser download runs rather than being silently skipped — the same Pitfall 2 signal plan 05-01's tracer asserts against. |
| T-05-23 | Tampering | replaying a name-only snapshot over a richer live state | medium | accept | `parse_global_packages` flattens pnpm's JSON to bare names, so the replay cannot restore the version a package was at, nor a group the user created by hand. A hand-made group is replayed as separate isolated installs, which can break a peer relationship the user set up. Accepted and now written into the module docstring rather than left unstated: reconstructing it needs pnpm's per-group project structure, which is out of this phase's scope. Registry-known packages are unaffected because the policy carries their pins and groups. |
| T-05-24 | Denial of Service | the grouped replay running on a pnpm that cannot parse it | medium | mitigate | Task 1's floor check, gating on the same `PNPM_CO_INSTALL_MIN` / `PNPM_ALLOW_BUILD_MIN` constants `installer/executors.py` uses, from one definition in `installer/versions.py`. On an unmet floor the replay raises `PnpmUnavailable` with the observed and required versions and runs nothing, instead of asking pnpm to install a package literally named with a comma in it. An empty policy probes nothing, so today's behaviour is untouched. |
| T-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan adds no dependency to the project and introduces no new package into the catalog. The npm packages it can replay were legitimacy-checked in plan 05-03 Task 1 before their registry entries landed. |
</threat_model>

<verification>
- `uv run pytest tests/test_pnpm_globals.py tests/test_setup.py tests/test_wizard_app.py -x -q`.
- `make validate && make test` on the exact tree before commit (ONESHOT-RULES Rule 6).
- No real pnpm invocation and no real `pnpm --version` probe runs against this machine: every test resolves pnpm through a stub or a planted fake under an isolated `HOME`, and `probe_version` is monkeypatched wherever it would be reached (ONESHOT-RULES Rule 5).
</verification>

<success_criteria>
- The remediation action Phase 4 shipped for the residual pnpm-managed set no longer breaks the co-install this phase introduces — the known-broken gap is closed inside this phase rather than deferred.
- The same action is a working, test-pinned remedy for the phase's brownfield gap, proven end-to-end by plan 05-01's container run rather than asserted.
- ROADMAP SC#3 holds after a Doctor reinstall, not only after a first install: `mmdc` can still resolve `puppeteer` at runtime, at the pinned major line.
- What the replay cannot reconstruct is written down in the module it belongs to, so the limitation is a recorded decision rather than a latent surprise.
- `installer/wizard_app.py` and `installer/deps.py` are both untouched, so neither the navigation surface nor the ordering mechanism grows a special case for one tool pair.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Purpose |
|----------|------|---------|
| `installer/pnpm_globals.py::NodeInstallPolicy` | code | The registry's install-grouping, build-allowance and version-pin facts, as one frozen value |
| `installer/pnpm_globals.py::node_install_policy` | code | Derives that policy from the catalog, de-duplicating allowances and pins, mirroring `node_globals`' declaration-not-installation framing |
| `installer/pnpm_globals.py::_reinstall_parts` | code | One builder for `(flags, specs)`, so the return contract can be the specs rather than an argv slice |
| `installer/pnpm_globals.py::reinstall_argv` policy support | code | Group-aware, pin-aware, allowance-aware replay argv; unchanged when no policy is given |
| `installer/pnpm_globals.py` pnpm floor check | code | The replay refuses the grouped/build-allowed form on a pnpm that cannot parse it (T-05-24) |
| `installer/pnpm_globals.py` docstring correction and limitations | doc | Removes the superseding claim pnpm's v11 isolation model contradicts; states what a name-only snapshot cannot restore (T-05-23) |
| `setup.py` policy wiring | code | Feeds one policy object to both the preview and the reinstall closures |
| `tests/test_pnpm_globals.py` group/pin/allowance cases | test | Grouping, partial groups, order/dedup, preview-equals-effect, the return contract, and the floor |
| `tests/test_pnpm_globals.py` brownfield regression case | test | The documented brownfield remedy cannot regress silently |
| `tests/test_setup.py` wiring case | test | The Doctor preview really carries the group, the pin and the flag |
</artifacts>

<output>
Create `.planning/phases/05-registry-method-corrections-codegraph-mmdc-puppeteer/05-04-SUMMARY.md` when done.
Record in it: `PLAN_BASE` on its own line as a bare 40-character hash; the exact replay argv
for the shipped registry's policy; the partial-group behaviour and why the replay never
widens the installed set; the corrected return contract and why the old `argv[3:]` slice
became wrong; the pnpm floor check, what it gates and what it deliberately does not gate
(the preview); the corrected docstring text and the limitations paragraph verbatim; where
the Doctor path surfaces `PnpmUnavailable`; which seam `tests/test_setup.py` used to assert
the wiring and why; the brownfield regression case and the container evidence it rests on;
and confirmation that `installer/wizard_app.py` was not modified by this plan's commits.
</output>
