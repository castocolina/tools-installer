---
phase: 12.1-reconciliation-merge-verification-confirm-the-layered-merge
plan: 01
type: execute
wave: 1
cross_ai: true
depends_on: []
files_modified:
  - tests/test_version_status.py
  - tests/test_registry.py
  - tests/test_engine.py
  - installer/session.py
  - installer/render.py
  - tests/test_session.py
  - tests/test_render.py
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04

estimate:
  tokens: 118000
  raw_tokens: 118000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "The full test suite (`uv run pytest -q`) is 100% green — zero known-failing tests — proving the reconciled branch's baseline is trustworthy going into Phase 12.3's container e2e run (D-03)."
    - "Every one of the 11 registry rows re-added by the reconciliation (`glab`, `drawio-desktop`, `drawio-cli`, `pi`, `ponytail`, `openspec`, `superpowers`, `softaworks-agent-toolkit`, `matt-pocock-skills`, `opengsd`, `spec-kit`) carries a `tier` that matches origin's real tier semantics (system = machine prerequisite, user = personal-pick tool, ai = agent-facing tooling), individually audited against this registry's own established precedents rather than trusted from the porting agent's skill-pack/agent→ai, everything-else→user heuristic, and that finding is pinned as a permanent automated regression test (D-02)."
    - "`installer/install_actions.py`, `installer/install_ownership.py`, `installer/agent_policy.py`, and `installer/agent_guidance.py` are confirmed to have zero production callers reachable from `setup.py`'s composition root, audited across the FULL `installer/` package (not a hand-picked file subset) — structurally inert, per `.claude/architecture.md` rule 5's own 'only its own test keeping it covered' definition of an orphan helper. `installer/agent_env.py` is the one corrected exception (per cross-AI review, `12.1-REVIEWS.md@90b7d57` HIGH finding): it IS imported unconditionally at module level by `installer/policy.py` (itself imported by `setup.py`), so its top-level code executes on every run, but its only production-facing symbol (`adapters()`) is never CALLED anywhere outside its own tests — imported yet functionally uninvoked, not 'unimported'. All five modules stay unwired into the UI and unflagged with a TODO, and `make validate`'s vulture/coverage gates stay green (locked D-04; Phase 12.2 owns the wiring decision)."
    - "A real terminal session (tmux) driving the actual `uv run setup.py --link-mode centralized` process, launched with an isolated temporary `HOME` so every file write the app performs (including the version-refresh cache at `~/.local/state/tools-installer/versions.json`, which every catalog-view mount triggers via `catalog_tui.py::on_mount`/`enter_view`) lands under that temp directory instead of the real machine, navigates all six views (System/User/AI/Doctor/Uninstall/Policies); each renders without a crash, the 11 re-added rows are visible in their expected tier views, the isolated temp HOME's own version-cache file is positively confirmed written (proof the write was contained, not merely unobserved), and the REAL machine's `~/.myshellrc`, `~/.zshrc`, and `~/.local/state/tools-installer/versions.json` are all provably unchanged — D-01's static-plus-manual-smoke verification depth, never Phase 12.3's real install/uninstall scope."
    - "The reconciliation's two highest-risk merge points — `installer/model.py`'s merged `Tool` dataclass/`load_tools` parsing (origin's typed fields composed with local's ported `post_install`/`owner_probes`/`skill_lifecycle` fields) and `installer/engine.py`'s merged 5-way `_perform` dispatch (origin's download/app/generic-executor kinds composed with local's `skill_pack`/`host_setup` kinds) — are confirmed to compose correctly via a real `git diff b9a8876..a570bf3` read, not inferred from a green suite alone; the concrete coverage gap that read surfaced (the `host_setup` dispatch branch had zero direct engine-level test) is closed with a new regression test — D-01's 'architecturally sound, not just green' standard (per cross-AI review, `12.1-REVIEWS.md@90b7d57` HIGH finding)."
    - "`installer/session.py::summarize` no longer raises `KeyError` on a `MANUAL_REQUIRED` outcome — the `InstallStatus.MANUAL_REQUIRED` bucket this reconciliation introduced in `installer/enums.py`/`installer/engine.py` but never wired into `session.py`'s closed `buckets` dict is added to both `Summary` and `summarize`, rendered by `render_summary` on the same footing as every other bucket, and pinned by a regression test in `tests/test_session.py` — closing the real, live `KeyError` crash a real CLI install of `pi` or any of the 7 `skill_pack` catalog rows previously hit at `installer/app.py:158` (per cross-AI review, `12.1-REVIEWS.md@58e9435` round-2 HIGH finding; D-01's 'architecturally sound, not just green' standard)."
  artifacts:
    - tests/test_version_status.py
    - tests/test_registry.py
    - tests/test_engine.py
    - installer/session.py
    - installer/render.py
    - tests/test_session.py
    - tests/test_render.py
    - .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/12.1-01-SUMMARY.md
  key_links:
    - "installer/ownership.py::owner_dirs (reads `os.environ.get(\"PNPM_HOME\")` unconditionally) → tests/test_version_status.py::_which_for_fixture (hardcodes a macOS-shaped path) — the fragile link D-03's fix isolates via `monkeypatch.delenv`."
    - "installer/policy.py's module-level `from installer.agent_env import AgentAdapter` (line 19) → setup.py's `from installer.policy import (...)` (lines 39-46) — the real, confirmed transitive import edge the corrected Task 3 audit traces; separately, `installer/policy.py::agent_environment_policy`/`compose_agent_environment_policy` (which lazily import `installer/agent_policy.py` only inside their own bodies, lines 143/202) → setup.py's `policy_inputs` composition (lines 290-314) — confirmed NOT called, so `agent_policy.py`'s code never executes at runtime even though its name is lexically present in `policy.py`."
    - "installer/registry.toml's 11 re-added rows → installer/model.py::Tool.tier → installer/ui_common.py::VIEWS-driven CatalogScreen rendering — the link both D-02's audit test and Task 3's live tmux walkthrough independently verify."
    - "installer/engine.py::_perform's `host_setup` branch (lines 82-83, routes to `executors.host_setup_handoff`) → zero pre-existing test calling `install_tool()` with a `host_setup`-kind `Method` (unlike the sibling `skill_pack` branch, already exercised end-to-end by `tests/test_skill_lifecycle.py::test_every_shipped_skill_pack_install_is_an_honest_manual_handoff`) — the concrete gap Task 4's new `tests/test_engine.py` regression test closes."
    - "installer/engine.py::install_tool (lines 138-144, returns `InstallOutcome(..., InstallStatus.MANUAL_REQUIRED, ...)` on any `host_setup`/`skill_pack` handoff) → installer/session.py::summarize's `buckets` dict (lines 129-136, a closed literal previously enumerating only 6 of the 7 `InstallStatus` members) → installer/app.py:158 (the real CLI's `summarize(outcomes)` call, reached from `run_wizard`) — the confirmed live `KeyError` crash chain Task 5 closes by adding the missing `InstallStatus.MANUAL_REQUIRED` bucket to both `Summary` and `summarize`."
---

<objective>
Verify — not merely re-run `make validate`/`make test` on — the layered merge of origin's
12-phase GSD milestone (`b9a8876`, adopted as base) with local's independent line's unique
subsystems (`985f602`, ported on top via commits `8416d5c`..`a570bf3` on
`recon/tui-interaction-consistency-2026-09-07`) is architecturally sound. This phase has no
`REQUIREMENTS.md` entries (ROADMAP lists this inserted phase's Requirements as `TBD`); its four
locked decisions in `12.1-CONTEXT.md` (D-01 verification depth, D-02 tier remapping audit, D-03
pre-existing failing test, D-04 ported-but-unwired code hygiene) are this plan's concrete,
testable requirements and are cited as `D-01`..`D-04` throughout.

This plan was revised after cross-AI review (`12.1-REVIEWS.md`, round 1, `opencode-sol`, commit
`90b7d57`) found three HIGH-severity gaps — an incomplete reconciliation-diff audit (model.py/
engine.py's merged dispatch logic went unread), a false "zero real-machine mutation" claim (every
catalog-view mount writes a real version-cache file), and a factually wrong `agent_env.py`
classification (it IS imported transitively, just never called) — plus one MEDIUM (smoke-script
cleanup/polling) and one LOW (temp-script disposal) finding. All five are incorporated into the
tasks below; see the `## Review Dispositions Ledger` at the end of this file for the full
per-finding disposition table.

It was revised again after cross-AI review round 2 (`12.1-REVIEWS.md@58e9435`, `opencode-sol`)
confirmed all five round-1 findings resolved but found one new HIGH-severity production defect —
`installer/session.py::summarize` raises an unhandled `KeyError` on any real `MANUAL_REQUIRED`
outcome, a crash directly introduced by this reconciliation and reachable through the real CLI
install path (`installer/app.py:158`) for `pi` or any of the 7 `skill_pack` catalog rows — plus
one LOW open question (whether the tmux walkthrough reaches any macOS `launchctl`/daemon-apply
code path). Both are incorporated below: a new Task 5 fixes the `KeyError` with a regression
test, and Task 3's SUMMARY requirement is extended to record, with source-level evidence, that no
daemon/launchctl code path is reachable during the walkthrough. See the Review Dispositions
Ledger's new `### Round 2` section for the full disposition.

Five closed findings drive this plan's five tasks, each already confirmed live on this
machine on 2026-09-10 before writing this plan (not assumed):

1. **D-03**: `tests/test_version_status.py::test_fresh_manager_snapshot_issues_zero_queries` is
   the ONLY failing test in the entire suite (confirmed via a full `uv run pytest -q` run with
   and without `--deselect` on that one test). Root cause: this machine's real `PNPM_HOME` env
   var leaks into `installer/ownership.py::owner_dirs`'s unconditional `os.environ.get` read,
   which the test's `_which_for_fixture`/`_node_tool` fixtures never isolate — unlike
   `tests/test_ownership.py`, which already does via `monkeypatch.setenv`/`delenv`. This
   reproduces identically on unmodified origin `b9a8876` per D-03, confirming it predates the
   reconciliation.
2. **D-02**: all 11 registry rows commit `9f6274b` re-added were individually audited against
   origin's real tier semantics and this registry's own established precedents (`gh`↔`glab`,
   `mmdc`↔`drawio-cli`/`drawio-desktop`, `claude`/`codex`/`opencode`/`cursor-agent`/`antigravity`
   ↔`pi`, `codegraph`/`graphify`/`rtk`↔the seven `skill_pack` rows). No misclassification found.
3. **D-04** (corrected per cross-AI review, `12.1-REVIEWS.md@90b7d57` HIGH finding): the
   original 6-file grep (`setup.py installer/wizard_app.py installer/app.py
   installer/catalog_tui.py installer/engine.py installer/ui_common.py`) never scanned
   `installer/policy.py` — and `installer/policy.py` imports `installer/agent_env.py`
   unconditionally at module level (line 19), while `setup.py` imports `installer/policy.py`
   (lines 39-46). So `agent_env.py`'s top-level code DOES execute on every run. A corrected
   full-package audit (every `installer/*.py` file plus `setup.py`, excluding `tests/`) confirms:
   `installer/install_actions.py`, `installer/install_ownership.py`, `installer/agent_policy.py`,
   and `installer/agent_guidance.py` remain genuinely unreachable from any production call path
   (zero callers outside their own tests, or — for `agent_policy.py` — referenced only inside a
   `TYPE_CHECKING` guard plus two function-local lazy imports whose wrapping functions,
   `installer/policy.py::agent_environment_policy`/`compose_agent_environment_policy`, are
   themselves never called by `setup.py`); `installer/agent_env.py` alone IS imported (its
   module-level code runs) but its one production-facing symbol, `adapters()`, is never CALLED
   anywhere outside its own tests — imported yet functionally uninvoked, not unimported. A live
   tmux Policies-view capture (only 9 policies render; no agent-environment/permission-audit row)
   independently confirms none of this reaches the UI. This matches `.claude/architecture.md`
   rule 5's "only its own test keeping it covered" definition of an orphan helper for all five
   modules' actual BEHAVIOR, with the import-graph classification now factually precise. Locked
   D-04 explicitly declines wiring or even a TODO note here (Phase 12.2 owns that decision) —
   this plan verifies without blocking, per that decision.
4. **D-01, architectural diff audit** (added per cross-AI review, `12.1-REVIEWS.md@90b7d57` HIGH
   finding): `git diff b9a8876..a570bf3 -- installer/model.py installer/engine.py
   installer/resolve.py installer/host_setup.py installer/skill_lifecycle.py` (557 insertions
   across the 5 files, confirmed live 2026-09-10) shows origin's and local's logic compose
   correctly at both of the reconciliation's highest-risk merge points — but reading it also
   surfaced a real, closeable test-coverage gap: `engine.py::_perform`'s `host_setup` dispatch
   branch (lines 82-83, added by the merge, routing to `executors.host_setup_handoff`) has zero
   direct engine-level test, unlike its sibling `skill_pack` branch, which
   `tests/test_skill_lifecycle.py::test_every_shipped_skill_pack_install_is_an_honest_manual_handoff`
   already exercises end-to-end through `install_tool()`. Task 4 closes that gap with a new
   regression test, so this plan's "architecturally sound, not just green" verification (this
   phase's own stated ROADMAP Goal) rests on read evidence plus a closed gap, not a green suite
   alone.
5. **D-01, session.py::summarize KeyError** (added per cross-AI review round 2,
   `12.1-REVIEWS.md@58e9435` HIGH finding): `installer/enums.py::InstallStatus.MANUAL_REQUIRED`
   and its whole handoff mechanism (`installer/engine.py:138-144`, returned whenever a
   `host_setup`/`skill_pack` method's `_perform` produces a non-empty `handoff`) were added BY
   this reconciliation, but `installer/session.py::summarize`'s `buckets` dict (lines 129-136)
   was never updated to include it — confirmed live 2026-09-10: `git diff b9a8876..a570bf3 --
   installer/session.py` is empty, so `session.py` was never touched by the reconciliation at
   all. `buckets[outcome.status]` raises `KeyError` the first time `outcome.status ==
   InstallStatus.MANUAL_REQUIRED`, which `summarize()` — called from the real CLI install path at
   `installer/app.py:158` via `run_wizard` — hits on any real install of `pi` or any of the 7
   `skill_pack` rows already shipped in the catalog (`ponytail`, `openspec`, `superpowers`,
   `softaworks-agent-toolkit`, `matt-pocock-skills`, `opengsd`, `spec-kit`). Zero existing test
   covers this: `tests/test_session.py::test_summarize_buckets_by_status` and its siblings
   exercise every OTHER status but never `MANUAL_REQUIRED`. This is a real bug in the reconciled
   `engine.py`/`session.py` seam Task 4 already audits, not a plan-documentation gap — Task 5
   closes it with an actual code fix (the missing bucket, threaded through `Summary` and
   `render_summary`) plus a regression test, not merely an audit note.

Purpose: close the phase's verification scope (D-01: static/automated checks PLUS a manual
tmux smoke walkthrough, no real install/uninstall — that stays Phase 12.3's job) so the branch
is provably, not just apparently, ready for Phase 12.3's container e2e run.

Output: a fully green test suite (zero known failures); a permanent, individually-reasoned
regression test pinning D-02's tier audit; a corrected full-package static import-graph audit
confirming D-04's orphan finding (with `agent_env.py`'s import status factually precise) without
new wiring; a recorded architectural diff audit of the reconciliation's two highest-risk merge
points plus a new regression test closing the `host_setup` dispatch coverage gap it found; a
recorded, verbatim real-terminal tmux transcript — run under an isolated temporary `HOME` — proving
all six views render correctly with zero real-machine mutation, verified rather than merely
asserted, with its SUMMARY explicitly recording that no daemon/launchctl code path was reached;
and a real code fix (not merely an audit note) closing `installer/session.py::summarize`'s
`MANUAL_REQUIRED` `KeyError` crash, pinned by a regression test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/12.1-CONTEXT.md
@.planning/ONESHOT-RULES.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@installer/ownership.py
@installer/version_status.py
@tests/test_version_status.py
@installer/registry.toml
@tests/test_registry.py
@installer/policy.py
@installer/model.py
@installer/ui_common.py
@installer/cli.py
@setup.py
@installer/engine.py
@installer/resolve.py
@installer/host_setup.py
@installer/skill_lifecycle.py
@installer/executors.py
@tests/test_engine.py
@tests/test_host_setup.py
@tests/test_skill_lifecycle.py
@installer/version_cache.py
@installer/catalog_tui.py
@installer/agent_env.py
@installer/session.py
@installer/render.py
@installer/app.py
@tests/test_session.py
@tests/test_render.py
@.planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/12.1-REVIEWS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): Fix the confirmed pre-existing failing test — pin PNPM_HOME/UV_TOOL_BIN_DIR so ownership attribution is host-independent (D-03)</name>
  <files>tests/test_version_status.py</files>
  <read_first>
    - `installer/ownership.py` lines 231-270 (`owner_dirs`) and 303-382 (`resolve_ownership`) —
      the real production logic this test exercises; `PNPM_HOME`/`UV_TOOL_BIN_DIR` are read
      directly from `os.environ` (lines 256, 262), unconditionally, never gated by the fixture's
      simulated `platform.os="macos"`.
    - `tests/test_ownership.py` lines 24, 205-285, 500-515 — the ALREADY-established
      `monkeypatch.setenv("PNPM_HOME", ...)` / `monkeypatch.delenv("UV_TOOL_BIN_DIR",
      raising=False)` pattern this task reuses; `test_version_status.py`'s `_which_for_fixture`/
      `_node_tool` fixtures (lines 313-323, 397-402) never adopted it, which is the confirmed
      root cause.
  </read_first>
  <action>
    Confirmed live on this machine (2026-09-10): `uv run pytest
    tests/test_version_status.py -k test_fresh_manager_snapshot_issues_zero_queries -q` fails
    with `assert 'unknown' == 'pnpm'` on `statuses["mmdc"]`, and D-03 (12.1-CONTEXT.md) records
    the same failure reproducing on an unmodified origin `b9a8876` checkout — pre-existing, not
    introduced by the reconciliation. Root cause: this machine's real `PNPM_HOME`
    (`/home/bazzite/.local/share/pnpm`) leaks into `installer/ownership.py::owner_dirs`, which
    reads `os.environ.get("PNPM_HOME")` unconditionally, while the test's `_which_for_fixture`
    hardcodes a macOS-shaped `~/Library/pnpm/mmdc` active path — a path that only matches
    `owner_dirs`'s OWN platform-derived default (`_PNPM_DEFAULTS["macos"]`) when the real env
    var happens to be unset. `tests/test_ownership.py` already isolates this exact env var via
    `monkeypatch`; `test_version_status.py` never adopted that pattern for its
    `_node_tool()`/`_which_for_fixture` fixtures.

    Add `import pytest` to `tests/test_version_status.py`'s import block (it currently imports
    no pytest symbol at all). Add a `monkeypatch: pytest.MonkeyPatch` parameter to
    `test_fresh_manager_snapshot_issues_zero_queries`, and as its first two statements call
    `monkeypatch.delenv("PNPM_HOME", raising=False)` and `monkeypatch.delenv("UV_TOOL_BIN_DIR",
    raising=False)` — DELETE rather than set a specific value: `owner_dirs`'s own
    `_PNPM_DEFAULTS["macos"]` fallback (`~/Library/pnpm`) and its default `uv_dir` (`~/.local/bin`)
    already produce exactly the paths `_which_for_fixture` hardcodes once the real env vars
    stop shadowing them. Do not touch `installer/ownership.py` or `installer/version_status.py`
    production logic, and do not touch any other test function in this file — a full-suite run
    with this one test deselected (`uv run pytest -q --deselect
    tests/test_version_status.py::test_fresh_manager_snapshot_issues_zero_queries`, confirmed
    2026-09-10) already passes every other test, so this is the SOLE fix this task makes.
  </action>
  <verify>
    <automated>uv run pytest tests/test_version_status.py -q</automated>
    <fails_when>any test in tests/test_version_status.py fails or errors, or
    test_fresh_manager_snapshot_issues_zero_queries still asserts statuses["mmdc"].source is not
    "pnpm".</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` (full suite, no deselect) exits 0 — the branch is fully green with zero
      known-failing tests before Phase 12.3's container e2e run (D-03's stated purpose).
    - `git diff -- tests/test_version_status.py` touches only the one test function's signature
      plus its two new `monkeypatch.delenv` lines and the added `import pytest` — no other test
      in the file is modified.
    - `git diff --stat -- installer/ownership.py installer/version_status.py` is empty.
  </acceptance_criteria>
  <done>The full test suite (`uv run pytest -q`) passes with zero failures — the reconciled
  branch is unambiguously green, not merely green-except-for-one-known-pre-existing-failure,
  closing D-03.</done>
  <reversibility rating="reversible">A two-line test-fixture isolation change with a direct git
  revert path; no production code, schema, or registry data touched.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Codify the tier remapping audit — one regression test asserting all 11 re-added registry rows carry the correct tier (D-02)</name>
  <files>tests/test_registry.py</files>
  <read_first>
    - `installer/registry.toml`'s 11 rows re-added by commit `9f6274b` (`glab`,
      `drawio-desktop`, `drawio-cli`, `pi`, `ponytail`, `openspec`, `superpowers`,
      `softaworks-agent-toolkit`, `matt-pocock-skills`, `opengsd`, `spec-kit`) — read each row's
      `tier`/`category`/`audience`/`desc` field as committed.
    - `tests/test_registry.py`'s existing per-tool precedent tests: `gh` (lines 403-411, git
      category, tier="user", priority="P0", audience="both"), `test_puppeteer_is_a_user_tier_node_tool_requiring_pnpm`
      (line 571), the `mmdc` registry entry (diagram category, tier="user"), the `ai`-tier tests
      for `graphify`/`cursor-agent`/`antigravity`/`rtk` (lines 652-749), and
      `test_bootstrap_package_managers_are_system_tier` (lines 926-929, using the `_tools_by_id()`
      helper this task reuses) plus `test_registry_tier_distribution_is_pinned` (lines 915-923,
      the tripwire this task's new assertions must not perturb).
    - `.claude/architecture.md`'s "Tier is a browsing label" section — origin's real tier
      semantics this audit checks each row against (system = machine prerequisite, user =
      personal-pick tool, ai = agent-facing tooling), not the porting agent's
      skill-pack/agent→ai, everything-else→user heuristic (12.1-CONTEXT.md D-02).
  </read_first>
  <action>
    Full individual audit performed (2026-09-10) against origin's real tier semantics and this
    registry's own established precedents, not the porting heuristic:
    - `glab` (git CLI, audience=both, priority=P0) → user: matches the existing `gh` (GitHub
      CLI) precedent exactly — same category, same audience, same priority; `gh` is already
      tier="user" despite agents using it too, per its own desc ("agents and users").
    - `drawio-desktop`/`drawio-cli` (diagram render tools, audience=human/both) → user: matches
      the existing `mmdc` precedent (diagram-category, tier="user", audience="both") — a
      general-purpose rendering tool any developer or agent can invoke, not an agent-specific
      harness.
    - `pi` (own coding-agent harness, `kind="host_setup"`) → ai: matches the existing
      `claude`/`codex`/`opencode`/`cursor-agent`/`antigravity` precedent — a standalone
      runnable agent CLI, the same shape as those five ai-tier agent hosts.
    - `ponytail`/`openspec`/`superpowers`/`softaworks-agent-toolkit`/`matt-pocock-skills`/
      `opengsd`/`spec-kit` (all `kind="skill_pack"`, each installing a plugin/workflow into an
      agent harness's own skill directory) → ai: matches the existing `codegraph`/`graphify`/
      `rtk` precedent (ai-tier tools that exist specifically to serve agent hosts) — these are
      agent-facing tooling by definition (their entire function is extending an AI coding
      agent), not a personal-pick tool a human would use standalone the way `kitty`/`wezterm`/
      `glab` are.
    No misclassification was found in any of the 11 rows. Add ONE new test function,
    `test_reconciliation_tier_audit_confirms_all_11_re_added_rows`, placed immediately after
    `test_bootstrap_package_managers_are_system_tier` (line 929) and before
    `test_gitui_is_linux_download_and_brew_only_on_macos` (line 932), using the existing
    `_tools_by_id()` helper, asserting `.tier` for all 11 ids with the rationale above captured
    as inline comments grouped by precedent (mirroring this file's existing per-tool assertion
    style) — so the audit conclusion is pinned as an automated, individually-reasoned
    regression, not a one-time claim. Do not alter `installer/registry.toml` — this task
    confirms the existing tier values are correct, it does not change any of them.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py -q</automated>
    <fails_when>the full test_registry.py suite fails, OR
    test_reconciliation_tier_audit_confirms_all_11_re_added_rows is absent, OR any of its 11
    assertions does not match the tier value actually committed in installer/registry.toml.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry.py::test_registry_tier_distribution_is_pinned -q` still
      passes unchanged (26 system / 22 ai / 41 user) — this task adds assertions, it never
      touches the registry's tier VALUES.
    - `git diff --stat -- installer/registry.toml` is empty.
    - The new test asserts all 11 ids by name: glab, drawio-desktop, drawio-cli, pi, ponytail,
      openspec, superpowers, softaworks-agent-toolkit, matt-pocock-skills, opengsd, spec-kit.
  </acceptance_criteria>
  <done>Every one of the 11 rows re-added by the reconciliation is individually confirmed
  against origin's real tier semantics (not the porting heuristic) and that finding is pinned
  as a permanent, automated regression test — closing D-02.</done>
  <reversibility rating="reversible">Adds one test function asserting already-committed data; a
  direct git revert path, no registry or schema change.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Corrected static import-graph audit of the two ported-but-unwired subsystems, plus an isolated-HOME real-terminal tmux structural smoke walkthrough of all six views (D-01, D-04)</name>
  <files>None — verification-only; no production or test files change. A temporary script is
  written to .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/verify-static-and-smoke.sh
  for this task's evidence and MUST be deleted (`rm`) after its output is recorded in the
  SUMMARY — mandatory, not optional, so this task leaves no undeclared working-tree change
  (corrects the original "or leave it uncommitted" wording per cross-AI review,
  `12.1-REVIEWS.md@90b7d57` LOW finding; mirrors 09-02-PLAN.md Task 1's tier3-verify.sh
  precedent, which was itself deleted after recording its output).</files>
  <precondition>tmux is installed and on PATH (`command -v tmux`); `mktemp` is available for the
  isolated temporary HOME; this machine's ~/.myshellrc and ~/.zshrc already exist so their
  mtimes can be recorded before and after the tmux session as proof of zero mutation.</precondition>
  <read_first>
    - `installer/install_actions.py`, `installer/install_ownership.py` (subsystem A, ported by
      commit `2af3a47`), `installer/agent_policy.py`, `installer/agent_env.py`,
      `installer/agent_guidance.py` (subsystem B, ported by commit `bdcdcb4`) — the two local
      subsystems D-04 names.
    - `installer/policy.py` line 19 (`from installer.agent_env import AgentAdapter`, module
      level, unconditional) and lines 132-216 (`agent_environment_policy`/
      `compose_agent_environment_policy`, whose lines 143/202 lazily import
      `installer/agent_policy.py` only inside their own function bodies, plus line 43's
      `TYPE_CHECKING`-guarded import) — confirms subsystem B splits into two different
      reachability stories: `agent_env.py` is imported unconditionally (its top-level code runs
      whenever `policy.py` is imported); `agent_policy.py`/`agent_guidance.py` are reachable only
      through a `TYPE_CHECKING` guard (never executes) or lazy imports inside functions that are
      themselves never called.
    - `setup.py` lines 39-46 (`from installer.policy import (...)`) — the edge that makes
      `installer/policy.py`, and therefore `installer/agent_env.py`, load on every `setup.py`
      run — and lines 290-314 (`policy_inputs` composition) — confirms `agent_environment_policy`/
      `compose_agent_environment_policy` are never called there, so `agent_policy.py`'s code
      still never executes even though `policy.py` (which lexically names it) does.
    - `.claude/architecture.md` rule 5 ("No orphan helpers... Only its own test keeping it
      covered is the tell") and `tests/test_ownership.py::test_abandoned_drift_helpers_are_absent`
      (line 567) — this project's own prior precedent for this exact class of finding.
      12.1-CONTEXT.md D-04 (locked) explicitly declines wiring or a TODO/architecture note here,
      deferring the resolution to Phase 12.2 — this task performs ONLY the verify-without-blocking
      check D-04 requires, never a fix, a TODO, or an integration test.
    - `.planning/ONESHOT-RULES.md` Rule 5 (never mutate this machine's real shell rc files or run
      a real install/uninstall during this check) and Rule 14's Tier 2 definition (structural
      text-match via `capture-pane`, navigation-only, no gate needed) — the exact governing
      rules this task's tmux script follows; Checklist item 7 — the precedent this walkthrough
      instantiates.
    - `installer/ui_common.py` lines 103-166 (`VIEWS`, including each `View.label`/`View.palette`
      field used below as a per-view readiness marker) and 188 (`GLOBAL_NAV`, `"1-6 views | ^p
      nav | esc back | q quit"`, the first-paint readiness marker) — the 1-6 nav keys and
      `q`-quit binding the tmux script drives.
    - `installer/cli.py` line 55 (`--link-mode`) — skips the first-run "How should PATH be wired"
      questionary prompt, confirmed live 2026-09-10 (unchanged by this revision).
    - `installer/catalog_tui.py` lines 258-263 (`on_mount`/`enter_view`, both call
      `_start_version_refresh`) and `installer/version_status.py` lines 495-556
      (`VersionRefreshService.refresh`, unconditionally calls `save_version_cache` at line 556
      inside its lock, regardless of per-tool network success/failure) and
      `installer/version_cache.py` line 197 (`cache_path`, returns `Path.home() / ".local" /
      "state" / "tools-installer" / "versions.json"`) — the real, confirmed write path (per
      cross-AI review, `12.1-REVIEWS.md@90b7d57` HIGH finding) that makes the original
      "zero real-machine mutation" claim false: EVERY catalog-view mount (including view 1 at
      launch) starts this write. `Path.home()` resolves via the `HOME` env var on POSIX, which
      is how this task isolates the write.
    - `.venv/bin/python` (a symlink whose target is an ABSOLUTE path under this machine's real
      `~/.local/share/uv/...`, confirmed live 2026-09-10) — resolving that symlink needs no
      `HOME` lookup, so launching the smoke-tested process via this interpreter directly (instead
      of `uv run`, whose own cache/Python-install resolution IS `HOME`-relative) keeps the
      isolated-HOME launch fast and network-independent while still isolating the INSTALLER's
      own `Path.home()`-based writes.
    - `setup.py:166-197` (`_build_daemon_policy`, returns `None` immediately on any non-macOS
      `platform.os`) and `setup.py:374-386` (`daemon_default`/`daemon_default_policy_id` stay
      `None` whenever `daemon is None`) plus `installer/wizard_app.py:1543-1544`
      (`if self._daemon_default is not None: self._apply_daemon_default_worker()`, the ONLY
      on-mount-without-a-keypress code path that can reach a real `launchctl bootstrap` call) and
      `installer/daemon.py:258-274` (the only two `launchctl bootstrap`/`bootout` call sites,
      otherwise reached only through `daemon_policy.apply()` on an explicit policy-toggle
      keypress) — this machine's `Platform.os` is `fedora` (confirmed live via
      `installer/platform.py::detect()`), so `_build_daemon_policy` returns `None` here,
      `daemon_default` is structurally `None`, and `_apply_daemon_default_worker` never fires on
      mount; separately, the tmux script's own key sequence (`1`-`6` then `q`, never `space`/
      `enter`) never triggers a policy-toggle apply either — together, source-level proof that
      zero daemon/launchctl code path is reachable during this walkthrough (per cross-AI review,
      `12.1-REVIEWS.md@58e9435` round-2 LOW open question).
  </read_first>
  <action>
    Part A — corrected static import-graph check (D-04): the original audit grepped only 6
    hand-picked files and missed `installer/policy.py`, which is how it wrongly classified
    `installer/agent_env.py` as unimported (per cross-AI review, `12.1-REVIEWS.md@90b7d57` HIGH
    finding). Redo it as a full-package audit: `grep -rn` for each of `install_actions`,
    `install_ownership`, `agent_policy`, `agent_guidance`, and `agent_env` across EVERY file in
    `installer/*.py` plus `setup.py` (excluding `tests/`), then classify every hit found outside
    each module's own file:
    - `installer/install_actions.py`, `installer/install_ownership.py`: zero hits outside their
      own file (only comment mentions in `installer/model.py`/`installer/shellrc.py`, which are
      prose, not imports) — genuinely orphaned, matches the original finding.
    - `installer/agent_guidance.py`: one hit, `installer/agent_policy.py` line 9 — reachable only
      if `agent_policy.py` itself executes.
    - `installer/agent_policy.py`: three hits, all in `installer/policy.py` — line 43 (inside a
      `TYPE_CHECKING` block, never executes at runtime) and lines 143/202 (function-local lazy
      imports inside `agent_environment_policy`/`compose_agent_environment_policy`). Confirm
      neither of those two functions is called anywhere in `installer/*.py` or `setup.py` outside
      `policy.py`'s own body (the only two call sites are the mutual self-reference at line 177
      and 209) — so `agent_policy.py`'s (and therefore `agent_guidance.py`'s) code never executes
      at runtime, even though its name is lexically present in `policy.py`'s source. This
      confirms the original "unimported" conclusion for THESE two modules, but on the correct
      evidence (never-called wrapping functions, not merely "not in this file").
    - `installer/agent_env.py`: one hit, `installer/policy.py` line 19 — an UNCONDITIONAL
      module-level `from installer.agent_env import AgentAdapter`, not inside `TYPE_CHECKING` and
      not function-local. Since `setup.py` imports `installer.policy` unconditionally (lines
      39-46), `agent_env.py`'s top-level code (the `AgentAdapter` dataclass definition and the
      `adapters()` function definition) executes on every `setup.py` run. Separately confirm
      `adapters()` itself is never CALLED anywhere in `installer/*.py` or `setup.py` (grep for
      `adapters(` finds only its own definition and test call sites) — so `agent_env.py` is
      imported (loaded, executes at module scope) but its only production-facing behavior is
      never invoked. State this precisely as "imported yet functionally uninvoked," never as
      "unimported" — that is the factual correction this task makes; it changes no code and no
      severity/disposition in the threat model (agent_env.py is pure dataclasses, no
      subprocess/file-write capability).
    Per D-04 (locked): do NOT wire any of the five modules into `setup.py`, do NOT add an
    integration test exercising them end-to-end, and do NOT add a TODO/architecture-note flagging
    the gap (12.1-CONTEXT.md Deferred Ideas explicitly considered and declined that option) —
    Phase 12.2 owns this decision. Confirm `make validate` (ruff/pyright/bandit/vulture) and
    `uv run pytest --cov` (90% floor) both stay green with all five modules present and
    unmodified.

    Part B — isolated-HOME real-terminal tmux structural smoke walkthrough (D-01): write
    `.planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/verify-static-and-smoke.sh`
    containing BOTH parts as one failure-propagating gate (`set -euo pipefail`), shaped as
    follows:
    - At the top: resolve `REPO_ROOT` via `git rev-parse --show-toplevel` and `cd` into it;
      capture `REAL_HOME="$HOME"`; create `FAKE_HOME="$(mktemp -d)"`; pick a unique tmux session
      name. Immediately register `trap 'tmux kill-session -t "$SESSION" 2>/dev/null || true; rm
      -rf "$FAKE_HOME"' EXIT` (per cross-AI review, `12.1-REVIEWS.md@90b7d57` MEDIUM finding) so
      ANY failure — including a `set -e`-triggered early exit from a later assertion — still
      kills the tmux session and removes the temp HOME.
    - Record `$REAL_HOME/.myshellrc`, `$REAL_HOME/.zshrc` mtimes (`stat -c '%Y'` on Linux,
      `stat -f '%m'` on macOS), and — if it exists — `$REAL_HOME/.local/state/tools-installer/versions.json`'s
      mtime and sha256, BEFORE launching tmux.
    - Part A as a script step: the corrected full-package grep-and-classify check above (fail the
      script if the classification above no longer holds — i.e., if `install_actions.py`,
      `install_ownership.py`, `agent_policy.py`, or `agent_guidance.py` gains ANY reachable
      caller, or if `agent_env.py`'s `adapters()` gains a production call site), then
      `make validate`.
    - Part B: launch with `HOME="$FAKE_HOME" "$REPO_ROOT/.venv/bin/python" setup.py --link-mode
      centralized` inside `tmux new-session -d -s "$SESSION" -x 220 -y 55 "..."` (direct venv
      interpreter, not `uv run` — keeps `uv`'s own HOME-relative cache/Python-install resolution
      out of the isolated run entirely, per this task's `read_first` grounding). POLL, do not
      sleep blindly (per cross-AI review, `12.1-REVIEWS.md@90b7d57` MEDIUM finding): after
      launch, loop `tmux capture-pane -p -t "$SESSION"` every 0.5s, up to 10s total, until the
      pane contains the literal `GLOBAL_NAV` substring `"1-6 views"`; fail with a clear timeout
      message if it never appears. For each key in `1 2 3 4 5 6` in order: `tmux send-keys -t
      "$SESSION" "<key>"`, then poll `tmux capture-pane -p -t "$SESSION"` every 0.3s up to 8s
      until that view's own distinguishing marker appears (view 1 System: `View.palette`'s
      "prerequisites a fresh machine needs first"; view 2 User: `glab`; view 3 AI: `ponytail`;
      view 4 Doctor: `View.palette`'s "audit PATH and apply the safe fix"; view 5 Uninstall:
      `View.palette`'s "remove installed tools"; view 6 Policies: `View.label` "Policies") or the
      per-view timeout elapses (fail with a clear message naming the view). At every capture,
      fail immediately if the pane contains `Traceback` or `Error:`. Assert view 2 (User)'s final
      capture contains `glab` AND `drawio-desktop` AND `drawio-cli`; assert view 3 (AI)'s final
      capture contains `ponytail` AND `openspec` AND `superpowers` AND
      `softaworks-agent-toolkit` AND `matt-pocock-skills` AND `opengsd` AND `spec-kit` AND the
      literal description text `Minimal coding-agent harness` (`pi`'s distinguishing text — the
      bare id `pi` is too short a substring to grep safely against a full-screen capture). Assert
      view 6 (Policies)'s capture does NOT contain any of `agent environment`/`permission
      audit`/`agent-policy` (confirms subsystem B's Policy factory is still not surfaced in the
      live UI, consistent with Part A's static finding). Then poll (up to 15s, since the version
      refresh runs on a background worker thread) for
      `test -f "$FAKE_HOME/.local/state/tools-installer/versions.json"` to become true — positive,
      verified proof the version-cache write happened and was contained inside the isolated HOME,
      not merely unobserved (per cross-AI review, `12.1-REVIEWS.md@90b7d57` HIGH finding: "must
      be explicit and verified, not asserted"); fail with a clear message if it never appears.
      Send `q`; poll (up to 5s) for `tmux has-session -t "$SESSION"` to fail (clean exit, matching
      this project's own quit binding) rather than assuming immediate death. Finally re-`stat`
      `$REAL_HOME/.myshellrc`/`$REAL_HOME/.zshrc` and re-check
      `$REAL_HOME/.local/state/tools-installer/versions.json`'s mtime/sha256 (or its continued
      absence, if it did not exist before) and fail if ANY of the three changed from the value
      recorded before the session started — the concrete, checkable proof this walkthrough never
      mutated the real machine even though the app's own version-refresh write path ran for real
      inside the isolated HOME (ONESHOT-RULES.md Rule 5; corrects the original mtime-only,
      two-file check per `12.1-REVIEWS.md@90b7d57` HIGH finding).

    Part C — close the round-2 LOW open question: the SUMMARY MUST explicitly record, as its own
    labeled subsection, that no daemon/launchctl code path was reached during the walkthrough,
    citing the source-level evidence gathered above (per cross-AI review,
    `12.1-REVIEWS.md@58e9435` round-2 LOW finding: "have Task 3's SUMMARY explicitly record that
    the tmux walkthrough's navigation-only key sequence never reaches any launchctl/daemon-apply
    code path"). State plainly: (1) this machine's `platform.os` is `fedora`, so
    `setup.py::_build_daemon_policy` returns `None` and `daemon_default`/
    `_apply_daemon_default_worker`'s auto-mount trigger never fires; (2) the script's key sequence
    never sends `space`/`enter`, so the OTHER path to `installer/daemon.py`'s `launchctl
    bootstrap`/`bootout` calls (via an explicit policy-toggle) is also never reached. This is a
    recorded, evidenced closing of the question, not an implicit or omitted one.

    Run `bash verify-static-and-smoke.sh`, record its FULL output — every captured pane, per
    ONESHOT-RULES.md Rule 14's evidence-recording convention — verbatim in the SUMMARY, then
    delete the script (`rm .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/verify-static-and-smoke.sh`):
    it is a verification tool, not production or test code, mirroring 09-02-PLAN.md Task 1's
    `tier3-verify.sh` precedent, and MUST be deleted (not merely "left uncommitted") so this
    task's own commit — if any — leaves no undeclared working-tree change, matching the phase's
    own `<verification>` requirement below (per cross-AI review, `12.1-REVIEWS.md@90b7d57` LOW
    finding).
  </action>
  <verify>
    <automated>bash .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/verify-static-and-smoke.sh</automated>
    <fails_when>the corrected full-package grep-and-classify finds ANY reachable production
    caller for install_actions.py, install_ownership.py, agent_policy.py, or agent_guidance.py
    outside their own tests, OR finds a production call site for agent_env.py's adapters()
    outside its own tests; make validate exits non-zero; the first-paint poll times out without
    "1-6 views" appearing; any per-view poll times out without that view's marker appearing; any
    captured tmux pane contains "Traceback" or "Error:"; the User view's capture is missing
    "glab", "drawio-desktop", or "drawio-cli"; the AI view's capture is missing any of
    "ponytail", "openspec", "superpowers", "softaworks-agent-toolkit", "matt-pocock-skills",
    "opengsd", "spec-kit", or "Minimal coding-agent harness"; the Policies view's capture
    contains "agent environment", "permission audit", or "agent-policy"; the fake-HOME version
    cache file never appears within its poll timeout; the tmux session is still alive after
    sending "q" and its poll timeout elapses; or the real ~/.myshellrc mtime, ~/.zshrc mtime, or
    ~/.local/state/tools-installer/versions.json mtime/sha256/presence changed from the value
    recorded before the session started.</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the full verbatim tmux transcript for all six views plus Part A's
      grep/classification/make-validate output.
    - `stat`/`sha256sum` before/after comparison for `~/.myshellrc`, `~/.zshrc`, and (if present)
      `~/.local/state/tools-installer/versions.json` on the REAL machine shows identical values —
      confirmed zero real-machine mutation, verified rather than asserted.
    - `$FAKE_HOME/.local/state/tools-installer/versions.json` is confirmed created — positive,
      verified proof the version-refresh write occurred and was safely contained.
    - The temporary script no longer exists on disk after this task completes
      (`test ! -f .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/verify-static-and-smoke.sh`),
      and no production or test file changed in this task's commit (or no commit at all if the
      evidence is folded into the phase SUMMARY) — this task is pure verification.
    - The SUMMARY has a labeled subsection explicitly stating no daemon/launchctl code path was
      reached during the walkthrough, with both source-level facts cited (this machine's
      `platform.os == "fedora"` making `_build_daemon_policy`/`daemon_default` structurally
      `None`, and the script never sending `space`/`enter`) — closing the round-2 LOW open
      question with recorded evidence, not silence.
  </acceptance_criteria>
  <done>Both ported-but-unwired local subsystems are confirmed structurally inert with a
  factually corrected classification (agent_env.py is imported-yet-uninvoked, not unimported;
  the other four modules are neither imported outside dead-code paths nor invoked) — zero
  production callers reach setup.py's composition root, make validate/coverage stay green,
  without being wired into the UI or flagged with a TODO, per D-04's locked scope; a real tmux
  terminal session, launched under an isolated temporary HOME, proves all six views
  (System/User/AI/Doctor/Uninstall/Policies) render correctly with the 11 re-added rows visible
  in their expected tier views, no crash, the version-cache write is positively confirmed
  contained inside the temp HOME, and the real machine (~/.myshellrc, ~/.zshrc,
  ~/.local/state/tools-installer/versions.json) is provably unmutated — closing D-01 and
  D-04; the SUMMARY additionally records, with source-level evidence, that no daemon/launchctl
  code path was reached during the walkthrough, closing the round-2 LOW open question.</done>
  <reversibility rating="reversible">Verification-only; no production or test files are
  modified by this task, and its temporary script is deleted after its output is recorded, not
  committed.</reversibility>
</task>

<task type="auto">
  <name>Task 4: Architectural diff audit of the reconciliation's two highest-risk merge points (model.py's merged Tool dataclass/parsing, engine.py's merged 5-way dispatch), closing the host_setup dispatch test-coverage gap it finds (D-01)</name>
  <files>tests/test_engine.py</files>
  <read_first>
    - `installer/model.py` lines 239-322 (`Tool` dataclass fields + `__init__`) — origin's typed
      fields (`id`/`name`/`category`/`cmd`/`methods`/`priority`/`audience`/`tier`/`desc`/
      `requires`/`recommends`/`postinstall`) composed with local's ported fields (`post_install`,
      `default_enabled_sensitive_actions`, `owner_probes`, `source`, `owner`, `uninstall`,
      `skill_lifecycle`) in one frozen dataclass with a hand-written `__init__` (required because
      `frozen=True, init=False`) that `object.__setattr__`s every field — confirm no field name
      collides and every field from both lineages is actually assigned.
    - `installer/model.py` lines 612-701 (the tail of `load_tools`, from `if kind ==
      "host_setup"` through the `Tool(...)` construction) — confirm the closed-set validation for
      BOTH lineages survives the merge: origin's `postinstall` membership check against
      `POSTINSTALL_HOOK_NAMES`, and local's `post_install`/`default_enabled_sensitive_actions`/
      `owner_probes` closed-list parsing plus the `skill_lifecycle` mutual-exclusivity constraint
      (`skill_pack cannot declare fallback install methods` when `len(methods) != 1`).
    - `installer/engine.py` lines 64-85 (`_perform`) and 88-158 (`install_tool`) — the merged
      5-way dispatch: origin's `download.DOWNLOAD_KINDS`/`apps.APP_KINDS`/generic
      `executors.execute` branches composed with local's `skill_pack` (routes to
      `skill_lifecycle.perform_action`) and `host_setup` (routes to
      `executors.host_setup_handoff`) branches — confirm `install_tool`'s outcome contract
      (`verified`, `handoff` → `InstallStatus.MANUAL_REQUIRED`, `postinstall_warning`) threads
      correctly through all 5 branches, not just origin's 3.
    - `installer/resolve.py` — `git diff b9a8876..a570bf3 -- installer/resolve.py` (13
      insertions, confirmed live 2026-09-10): the `_RANK` ladder gained `"host_setup": 15` and
      `"skill_pack": 15`, and `_applies`'s platform-gate kind tuple was extended to include
      `"host_setup"`/`"skill_pack"` alongside origin's existing kinds — confirm neither new kind
      is silently filtered out by a platform check it shouldn't be subject to.
    - `installer/host_setup.py` (44 new lines) and `installer/skill_lifecycle.py` (73 new lines)
      — the two local-only modules `engine.py`'s new branches dispatch into; read
      `HOST_SETUPS["pi"]` and confirm its `handoff`/`probes`/`owner` shape matches what
      `executors.host_setup_handoff` (line 496) returns.
    - `tests/test_engine.py` lines 1-45 (`_tool`/`_platform` fixtures, `test_already_installed_short_circuits`)
      and 307-326 (`test_app_kind_routes_to_app_executor`) — the exact fixture/assertion shape
      this task's new test mirrors.
    - `tests/test_skill_lifecycle.py` lines 242-264
      (`test_every_shipped_skill_pack_install_is_an_honest_manual_handoff`) — confirms the
      `skill_pack` branch of `_perform` IS already exercised end-to-end through `install_tool()`
      for every shipped skill_pack tool.
    - `tests/test_host_setup.py` (the file's only test, asserting `HOST_SETUPS` registry
      membership) and `grep -rn "host_setup_handoff"` across `tests/` (confirmed live
      2026-09-10: zero matches) — the concrete gap this task closes: unlike `skill_pack`, no test
      anywhere calls `install_tool()` with a `host_setup`-kind `Method`, so `engine.py`'s
      `host_setup` branch (added by the merge) has never been exercised at the engine-dispatch
      layer, only implied by the registry-level `pi` entry.
  </read_first>
  <action>
    Read `git diff b9a8876..a570bf3 -- installer/model.py installer/engine.py
    installer/resolve.py installer/host_setup.py installer/skill_lifecycle.py` in full (557
    insertions across the 5 files) and confirm, against the read_first pointers above: (1)
    `model.py`'s `Tool` dataclass and `load_tools` correctly compose origin's and local's fields
    with no collision and both lineages' closed-set/mutual-exclusivity validation intact; (2)
    `engine.py`'s `_perform` 5-way dispatch is exhaustive across both lineages' method kinds and
    `install_tool`'s outcome contract threads correctly through all 5; (3) `resolve.py`'s ladder
    and platform-gate extension correctly admit the two new kinds without misapplying an
    origin-only platform filter to them. Record this confirmation, with specific line references,
    in the SUMMARY as the architectural-diff audit D-01's "not just green" standard requires (per
    cross-AI review, `12.1-REVIEWS.md@90b7d57` HIGH finding) — this is read evidence, not a
    narrative claim.

    That read surfaces one concrete, closeable gap: `engine.py::_perform`'s `host_setup` branch
    (lines 82-83) has zero direct engine-level test, unlike its sibling `skill_pack` branch. Close
    it: add ONE new test function to `tests/test_engine.py`, named
    `test_host_setup_kind_routes_to_host_setup_handoff`, placed immediately after
    `test_app_kind_routes_to_app_executor` (mirroring its exact shape — monkeypatch
    `engine.is_installed` to return `False`, build a `Tool` via the file's existing `_tool()`
    helper with a single `Method(kind="host_setup", params={"setup_id": "pi"})`, call
    `install_tool()` with `_platform()` and a no-op `runner`). Assert the returned
    `InstallOutcome.status == InstallStatus.MANUAL_REQUIRED` (import `InstallStatus` from
    `installer.enums` if not already imported in the file), `.method_kind == "host_setup"`, and
    `.handoff == executors.host_setup_handoff(Method(kind="host_setup", params={"setup_id":
    "pi"}))` (import `executors` from `installer` alongside this file's existing `engine` import,
    or reference the known literal `HOST_SETUPS["pi"].handoff` tuple from `installer.host_setup`
    directly — either is acceptable as long as the assertion does not hardcode the handoff text a
    second time by hand, so a future edit to `HOST_SETUPS["pi"]` cannot silently desync the
    test). Do not modify `installer/engine.py`, `installer/model.py`, `installer/resolve.py`,
    `installer/host_setup.py`, or `installer/skill_lifecycle.py` production logic — this task
    confirms the merge is architecturally correct and closes a test-coverage gap, it does not
    change dispatch behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/test_engine.py -q</automated>
    <fails_when>tests/test_engine.py fails or errors, OR
    test_host_setup_kind_routes_to_host_setup_handoff is absent, OR it asserts a status other
    than InstallStatus.MANUAL_REQUIRED, OR its handoff assertion hardcodes literal instruction
    text instead of referencing executors.host_setup_handoff/HOST_SETUPS["pi"].handoff.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_engine.py -q` passes, including the new test.
    - `git diff --stat -- installer/model.py installer/engine.py installer/resolve.py
      installer/host_setup.py installer/skill_lifecycle.py` is empty — this task reads the
      historical diff, it does not re-touch these files.
    - The SUMMARY records the architectural-diff confirmation (fields/branches composed
      correctly, with specific line references) for both model.py and engine.py's merge points,
      not merely "tests pass."
  </acceptance_criteria>
  <done>The reconciliation's two highest-risk merge points (model.py's merged Tool
  dataclass/parsing, engine.py's merged 5-way dispatch) are confirmed architecturally sound via a
  real diff read, and the one concrete gap that read found — zero engine-level test coverage for
  the host_setup dispatch branch — is closed with a new regression test, closing D-01's
  "architecturally sound, not just green" standard.</done>
  <reversibility rating="reversible">Adds one test function against unmodified production code; a
  direct git revert path, no production behavior change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Fix the confirmed `KeyError` crash in `installer/session.py::summarize` — add the missing `MANUAL_REQUIRED` bucket, thread it through `Summary`/`render_summary`, and pin it with a regression test (D-01, round-2 HIGH finding)</name>
  <files>installer/session.py, installer/render.py, tests/test_session.py, tests/test_render.py</files>
  <read_first>
    - `installer/session.py` lines 48-55 (`Summary` dataclass, 6 fields, none named
      `manual_required`) and lines 122-146 (`summarize`, whose `buckets` dict enumerates exactly
      the same 6 `InstallStatus` members — `INSTALLED`, `ALREADY_INSTALLED`, `FAILED`,
      `NO_METHOD`, `CHECKSUM_MISMATCH`, `DEPENDENCY_FAILED` — and whose own docstring at lines
      125-127 already predicts this exact failure mode: "an out-of-range status is impossible
      under pyright and would surface as a KeyError if one ever slipped through").
    - `installer/enums.py` lines 54-68 (`InstallStatus`, a 7-member `StrEnum`) — `MANUAL_REQUIRED`
      (line 65) is the 7th member `summarize`'s `buckets` dict never gained.
    - `installer/engine.py` lines 138-144 (`install_tool`, the `if handoff:` branch) — the real,
      confirmed production producer of `InstallOutcome(tool.id, InstallStatus.MANUAL_REQUIRED,
      method_kind=method.kind, handoff=handoff)`, returned whenever a `host_setup`/`skill_pack`
      method's `_perform` yields a non-empty `handoff` — reached by any real install of `pi`
      (host_setup) or the 7 `skill_pack` rows (`ponytail`, `openspec`, `superpowers`,
      `softaworks-agent-toolkit`, `matt-pocock-skills`, `opengsd`, `spec-kit`).
    - `installer/app.py` lines 148-159 (`run_wizard`) — the real CLI call chain:
      `run_installs(...)` → `summarize(outcomes)` (line 158) → `render_summary(summary, console)`
      (line 159). `git diff b9a8876..a570bf3 -- installer/session.py` is empty (confirmed live
      2026-09-10) — `session.py` was never touched by the reconciliation, so it was never updated
      for the `MANUAL_REQUIRED` status the SAME reconciliation added to `enums.py`/`engine.py`.
    - `installer/render.py` lines 48-67 (`render_summary`) — the existing per-bucket
      count-then-id-list pattern (`Installed:`/`Already:`/`Failed:`/`Dependency failed:`/
      `Checksum mismatch:`/`No method:`, each paired with a conditional `  {label}: {ids}` line)
      this task's new `manual_required` bucket follows exactly, with no new rendering mechanism
      invented; `installer/render.py` already imports `Summary` from `installer.session` (line
      17), so no new import is needed there.
    - `tests/test_session.py` lines 74-91 (`test_summarize_buckets_by_status`,
      `test_summarize_empty_is_all_empty`) and lines 187-194
      (`test_summarize_buckets_checksum_mismatch`) — the exact fixture/assertion shape this
      task's new regression test mirrors; none of the existing tests construct a
      `MANUAL_REQUIRED` outcome, confirming the gap is real and untested (per cross-AI review,
      `12.1-REVIEWS.md@58e9435` round-2 HIGH finding).
    - `tests/test_render.py` lines 52-89 (`test_render_summary_reports_counts_and_ids`,
      `test_render_summary_counts_and_lists_dependency_failed`) — the exact shape this task's new
      `render_summary` regression test mirrors.
  </read_first>
  <behavior>
    - `summarize([InstallOutcome("pi", "manual-required", method_kind="host_setup",
      handoff=("do the thing",))])` currently raises `KeyError: <InstallStatus.MANUAL_REQUIRED:
      'manual-required'>`; after this task it returns `Summary(installed=(), already=(),
      failed=(), no_method=(), manual_required=("pi",))` with every other bucket empty.
    - `render_summary` on a `Summary` carrying `manual_required=("pi",)` prints a
      `Manual setup required: 1` count line and a `  manual setup required: pi` id line, on the
      same footing as every other non-empty bucket; on an empty `manual_required` it prints
      `Manual setup required: 0` and no id line, matching every other bucket's empty behavior.
  </behavior>
  <action>
    Add `manual_required: tuple[str, ...] = ()` as a new field on `installer/session.py`'s
    `Summary` dataclass (after the existing `dependency_failed` field, keeping every field
    keyword-defaulted so no existing positional-style construction anywhere in the codebase
    breaks). In `summarize`, add `InstallStatus.MANUAL_REQUIRED: []` to the `buckets` dict literal
    (alongside the existing 6 entries) and add `manual_required=tuple(buckets[
    InstallStatus.MANUAL_REQUIRED])` to the `Summary(...)` return construction. Do not change
    `_UNRESOLVED`, `run_installs`, or any other function in the file — `MANUAL_REQUIRED` is a
    resolved terminal state (the tool WAS handled, just via a manual handoff rather than an
    automated install), so it must never join `_UNRESOLVED` and must never block a dependent the
    way `FAILED`/`NO_METHOD`/`CHECKSUM_MISMATCH`/`DEPENDENCY_FAILED` do.

    In `installer/render.py`'s `render_summary`, append the new bucket as the LAST entry in both
    places (simplest, lowest-risk insertion point — the existing count line's final segment,
    `f"No method: {len(summary.no_method)}"`, currently has no trailing two-space separator
    because it is last; moving that special-casing anywhere but the end risks a formatting typo
    the executor need not introduce). Change that final segment to
    `f"No method: {len(summary.no_method)}  "` (now carrying the trailing separator like every
    other segment) and add a new final segment, `f"Manual setup required:
    {len(summary.manual_required)}"` (this one now carries no trailing separator, being last).
    Add `("manual setup required", summary.manual_required)` as the LAST entry in the `for label,
    ids in (...)` tuple, after `("no method", summary.no_method)` — the existing generic loop
    body (`if ids: console.print(f"  {label}: {', '.join(ids)}")`) needs no change.

    Add ONE new test to `tests/test_session.py`, `test_summarize_buckets_manual_required`,
    constructing `InstallOutcome("pi", "manual-required", method_kind="host_setup",
    handoff=("run these commands",))` alongside an `InstallOutcome("rg", "installed")`, calling
    `summarize`, and asserting `summary.manual_required == ("pi",)` and `summary.installed ==
    ("rg",)` — this is the direct regression test: without this task's fix, constructing this
    input and calling `summarize` raises `KeyError` before any assertion runs. Add ONE new test
    to `tests/test_render.py`, `test_render_summary_counts_and_lists_manual_required`, mirroring
    `test_render_summary_counts_and_lists_dependency_failed`'s shape: build a `Summary` with
    `manual_required=("pi",)`, call `render_summary`, and assert `"Manual setup required: 1" in
    out` and `"pi" in out`. Do not modify `installer/engine.py`, `installer/enums.py`, or any
    other production file — `InstallStatus.MANUAL_REQUIRED` and its producer in `engine.py`
    already exist and are correct; this task only closes the consumer-side gap in `session.py`/
    `render.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_session.py tests/test_render.py -q</automated>
    <fails_when>tests/test_session.py or tests/test_render.py fails or errors, OR
    test_summarize_buckets_manual_required is absent, OR
    test_render_summary_counts_and_lists_manual_required is absent, OR calling `summarize` with a
    `MANUAL_REQUIRED`-status `InstallOutcome` raises `KeyError`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` (full suite) still exits 0 with the new tests included.
    - `uv run python3 -c "from installer.session import summarize; from installer.engine import
      InstallOutcome; print(summarize([InstallOutcome('pi', 'manual-required',
      method_kind='host_setup')]))"` run from the repo root exits 0 and prints a `Summary` with
      `manual_required=('pi',)` — the exact live crash the reviewer identified, now fixed,
      verified by direct execution rather than test-suite inference alone.
    - `git diff --stat -- installer/engine.py installer/enums.py` is empty — this task fixes the
      consumer (`session.py`/`render.py`), it does not touch the already-correct producer.
  </acceptance_criteria>
  <done>`installer/session.py::summarize` no longer raises `KeyError` on a `MANUAL_REQUIRED`
  outcome — the missing bucket is added to `Summary` and `summarize`, rendered by `render_summary`
  on the same footing as every other bucket, and pinned by regression tests in both
  `tests/test_session.py` and `tests/test_render.py` — closing the real, live crash path a real
  CLI install of `pi` or any of the 7 `skill_pack` catalog rows previously hit at
  `installer/app.py:158`, per cross-AI review round 2's HIGH finding.</done>
  <reversibility rating="reversible">Adds one dataclass field (keyword-defaulted, so no existing
  construction site breaks) and one dict entry plus one render line, all additive; a direct git
  revert path, no removal or behavior change to any existing bucket/status.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/dev-machine environment variables (`PNPM_HOME`, `UV_TOOL_BIN_DIR`) → unit test assertions | A test previously let real host environment state leak into a deterministic assertion (`installer/ownership.py::owner_dirs` reads `os.environ` unconditionally), producing a host-dependent pass/fail rather than a reproducible one |
| Task 3's temporary verification script → the real `.venv/bin/python setup.py` process in a real terminal | Drives the actual production entry point through `tmux send-keys` under an isolated temporary `HOME`; must never reach a key that mutates the REAL machine's shell rc files, real version cache, or performs a real install/uninstall |
| Ported-but-unwired local subsystems (`install_actions.py`'s multi-hook `post_install`, `agent_policy.py`'s permission auditing, `agent_env.py`'s adapter descriptions) → production composition root (`setup.py`) | Real subprocess/file-write-capable code currently has zero live callers into the app a user actually runs; `agent_env.py` alone is imported (pure dataclasses, no subprocess/file-write capability) but its behavior is never invoked — confirmed inert, not reachable from any real user action, pending Phase 12.2's wiring decision |
| Merged install dispatch (`installer/engine.py::_perform`, origin's download/app/executor kinds composed with local's `skill_pack`/`host_setup` kinds) → every catalog install action a user can trigger | The reconciliation's highest-risk runtime merge point; an incorrect composition here would misroute or silently skip an install method for any tool |
| `installer/engine.py::install_tool`'s `MANUAL_REQUIRED` outcome (added by this reconciliation) → `installer/session.py::summarize`'s closed `buckets` dict (never updated by this reconciliation) → the real CLI install path (`installer/app.py:158`) | A tool's producer-side status and its consumer-side bucket dict drifted out of sync across the merge, crashing the whole CLI with an unhandled `KeyError` on any real install reaching the new status |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.1-01 | Tampering | `tests/test_version_status.py::test_fresh_manager_snapshot_issues_zero_queries` | low | mitigate | Task 1 pins `PNPM_HOME`/`UV_TOOL_BIN_DIR` via `monkeypatch.delenv` so the test's ownership-attribution assertion is deterministic regardless of the host machine's real pnpm/uv install location (D-03). |
| T-12.1-02 | Elevation of Privilege | `installer/install_actions.py` / `installer/install_ownership.py` / `installer/agent_policy.py` / `installer/agent_guidance.py` — ported, unwired subsystems with real subprocess/file-write capability; `installer/agent_env.py` — imported but functionally uninvoked (pure dataclasses, no subprocess/file-write capability) | medium | accept | Task 3 confirms zero reachable production callers via a full-package (not 6-file) static import-graph audit plus a live Policies-view smoke capture; `agent_env.py`'s corrected classification (imported, uninvoked) does not change this disposition since it has no mutating capability. Intentionally left unwired per locked CONTEXT.md D-04 — Phase 12.2 owns the wiring decision, this phase only verifies the gap is real and bounded. |
| T-12.1-03 | Tampering | Task 3's `verify-static-and-smoke.sh` tmux session driving the real `.venv/bin/python setup.py` process | low | mitigate | The script is navigation-only (keys `1`-`6` then `q`, never `space`/`enter`); `--link-mode centralized` skips the one interactive prompt that could reach a write path; the process is launched with an isolated temporary `HOME` so its own version-refresh cache write (`~/.local/state/tools-installer/versions.json`, triggered by every catalog-view mount) lands outside the real machine; a before/after `stat`/`sha256sum` check on the REAL `~/.myshellrc`, `~/.zshrc`, and `~/.local/state/tools-installer/versions.json` is the script's own automated, verified (not merely asserted) proof of zero real-machine mutation, matching ONESHOT-RULES.md Rule 5; an `EXIT` trap guarantees session/tempdir cleanup on any failure. |
| T-12.1-04 | Repudiation | `installer/engine.py::_perform`'s `host_setup` dispatch branch (lines 82-83) | low | mitigate | Task 4 closes the confirmed zero-engine-level-test-coverage gap for this merge-added branch with a new regression test asserting the correct `InstallOutcome` (status, method_kind, handoff) for a `host_setup`-kind install, so a future regression in this dispatch path is caught rather than silently passing an otherwise-green suite. |
| T-12.1-05 | Denial of Service | `installer/session.py::summarize`'s `buckets` dict (lines 129-136) | high | mitigate | Task 5 adds the missing `InstallStatus.MANUAL_REQUIRED` bucket to `Summary`/`summarize`, threads it through `render_summary`, and pins it with a regression test in `tests/test_session.py` asserting `summarize` no longer raises `KeyError` for a `MANUAL_REQUIRED` outcome — closes a confirmed, live crash path reachable by installing `pi` or any of the 7 `skill_pack` catalog rows through the real CLI (per cross-AI review round 2's HIGH finding). |

</threat_model>

<verification>
Phase-level checks, run after all five tasks:

- `uv run pytest -q` (full suite) exits 0 — zero known-failing tests (Task 1).
- `uv run pytest tests/test_registry.py -q` exits 0, including the new
  `test_reconciliation_tier_audit_confirms_all_11_re_added_rows` and the unchanged
  `test_registry_tier_distribution_is_pinned` (26/22/41) (Task 2).
- `bash .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/verify-static-and-smoke.sh`
  exits 0, then is DELETED — its recorded output lives verbatim in the SUMMARY, not on disk —
  covering the corrected full-package orphan-import audit, `make validate`, and the isolated-HOME
  six-view tmux smoke walkthrough, all passing with real-machine mutation verified (not merely
  asserted) as zero, and the SUMMARY explicitly recording no daemon/launchctl code path was
  reached (Task 3).
- `uv run pytest tests/test_engine.py -q` exits 0, including the new
  `test_host_setup_kind_routes_to_host_setup_handoff`, and the SUMMARY records the architectural
  diff-read confirmation for `model.py`'s and `engine.py`'s merge points (Task 4).
- `uv run pytest tests/test_session.py tests/test_render.py -q` exits 0, including the new
  `test_summarize_buckets_manual_required` and
  `test_render_summary_counts_and_lists_manual_required` — `installer/session.py::summarize` no
  longer raises `KeyError` on a `MANUAL_REQUIRED` outcome (Task 5).
- `make validate && make test` exits 0 on the committed tree (CLAUDE.md's definition of done).
- `git status --short` shows no modification to any file outside this plan's declared
  `files_modified` (`tests/test_version_status.py`, `tests/test_registry.py`,
  `tests/test_engine.py`, `installer/session.py`, `installer/render.py`, `tests/test_session.py`,
  `tests/test_render.py`) plus the phase's own SUMMARY, and no leftover
  `verify-static-and-smoke.sh`.
- The SUMMARY confirms D-01, D-02, D-03, and D-04 are each satisfied by a specific task, per
  `human_verify_mode: end-of-phase`'s consolidation design (ONESHOT-RULES.md), and additionally
  confirms Task 5's `session.py::summarize` fix closes cross-AI review round 2's HIGH finding.
</verification>

<success_criteria>
The reconciled merge on `recon/tui-interaction-consistency-2026-09-07` is confirmed
architecturally sound, not just green on `make validate`/`make test`:

1. The test suite is unambiguously green — the one confirmed pre-existing failure is fixed and
   root-caused, not silently tolerated (D-03).
2. Every re-added registry row's tier is individually confirmed correct against origin's real
   tier semantics, with the audit pinned as a permanent automated test rather than a one-time
   claim (D-02).
3. The ported-but-unwired local subsystems are confirmed structurally inert with a factually
   precise classification (agent_env.py is imported-yet-uninvoked; the other four modules are
   neither imported outside dead-code paths nor invoked) — zero reachable production callers,
   `make validate`'s vulture/coverage gates stay green — without being wired into the UI or
   flagged, honoring the locked scope boundary with Phase 12.2 (D-04).
4. A real terminal session, launched under an isolated temporary HOME, proves the merged TUI's
   six views all render correctly with zero crash, the version-refresh cache write is positively
   confirmed contained inside that temp HOME, and the real machine's shell rc files and version
   cache are provably, verifiably unmutated, with its SUMMARY recording that no daemon/launchctl
   code path was reached (D-01).
5. The reconciliation's two highest-risk merge points (model.py's merged Tool dataclass/parsing,
   engine.py's merged 5-way dispatch) are confirmed architecturally sound via a real diff read
   against `b9a8876..a570bf3`, not inferred from a green suite alone, and the one concrete gap
   that read found (zero engine-level coverage for the host_setup dispatch branch) is closed
   with a new regression test (D-01).
6. `installer/session.py::summarize`'s confirmed `KeyError` crash on any `MANUAL_REQUIRED`
   outcome — a real defect introduced by this reconciliation and reachable through the real CLI
   install path for `pi` or any of the 7 `skill_pack` catalog rows — is fixed with a real code
   change (the missing bucket, threaded through `Summary` and `render_summary`) and pinned by a
   regression test, not merely documented (D-01).
</success_criteria>

<output>
Create `.planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/12.1-01-SUMMARY.md` when done.
</output>

## Review Dispositions Ledger

### Round 1 — 90b7d57

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| The plan does not audit the full reconciliation it claims to verify — `installer/model.py:239-322`, `installer/model.py:612-701`, and `installer/engine.py:64-85` receive no explicit architectural/diff audit beyond "tests are green." | HIGH | New Task 4: reads `git diff b9a8876..a570bf3` for `model.py`/`engine.py`/`resolve.py`/`host_setup.py`/`skill_lifecycle.py`, records a field-by-field/branch-by-branch composition confirmation in the SUMMARY, and closes the concrete gap that read surfaced (zero engine-level test coverage for the `host_setup` dispatch branch) with a new `tests/test_engine.py` regression test. |
| The "zero real-machine mutation" claim is false — every catalog view mount triggers a version-cache refresh that writes `~/.local/state/tools-installer/versions.json`; the plan only checked two shell-file mtimes. | HIGH | Task 3 rewritten: the smoke-tested process now launches under an isolated temporary `HOME` (via `.venv/bin/python setup.py`, not `uv run`, to keep `uv`'s own cache resolution HOME-independent), so the version-cache write is verified to land inside the temp HOME (positive `test -f` check) while the REAL `~/.myshellrc`, `~/.zshrc`, AND `~/.local/state/tools-installer/versions.json` are all stat/hash-compared before and after and asserted unchanged. |
| The static import audit is incomplete and factually wrong on `agent_env.py` — it IS loaded transitively at module-import time via `setup.py` -> `installer/policy.py`, contradicting Task 3's "structurally inert, unimported" classification. | HIGH | Task 3's Part A rewritten as a corrected full-package audit (all of `installer/*.py` + `setup.py`, not a 6-file subset). `agent_env.py`'s classification is corrected to "imported unconditionally via `policy.py` line 19, but its only symbol (`adapters()`) is never called" — imported yet functionally uninvoked, not unimported. The other four modules (`install_actions.py`, `install_ownership.py`, `agent_policy.py`, `agent_guidance.py`) are re-confirmed genuinely unreachable on corrected evidence (dead `TYPE_CHECKING`/lazy-import paths whose wrapping functions are never called, not merely "absent from 6 files"). Threat model T-12.1-02 updated accordingly; disposition/severity unchanged since `agent_env.py` has no subprocess/file-write capability. |
| The tmux smoke script lacks failure cleanup (no `EXIT` trap) and relies on fixed sleeps instead of polling for readiness. | MEDIUM | Task 3's script now registers an `EXIT` trap (kills the tmux session, removes the temp HOME) immediately after creating them, and replaces every fixed `sleep` with a bounded poll loop against `tmux capture-pane` for a known per-view readiness marker (first-paint: `GLOBAL_NAV`'s `"1-6 views"`; per-view: that view's `label`/`palette` text or its already-required content assertion), failing with a clear timeout message rather than a blind wait. |
| Temporary-script handling is internally inconsistent — Task 3 permitted leaving the script uncommitted while phase verification elsewhere requires no undeclared working-tree changes. | LOW | Task 3's `<files>`, action, acceptance_criteria, and the phase-level `<verification>` all now require the script be deleted (`rm`) after its output is recorded in the SUMMARY — mandatory, not "or leave it uncommitted." |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| Cycle 1 Check: reviewer could not map this round's findings to a prior "Cycle 1 Review" section (not present in this checkout, commit `19131c2` absent). | Not actionable — informational note only, not a finding about this plan's content; no cycle-1 disposition exists to reconcile against. |

### Round 2 — 58e9435

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|----------------|
| `installer/session.py::summarize`'s `buckets` dict never gained a bucket for `InstallStatus.MANUAL_REQUIRED` — a status this reconciliation introduced in `enums.py`/`engine.py` but never wired into `session.py`, crashing the real CLI with `KeyError` via `installer/app.py:158` on any real install of `pi` or the 7 `skill_pack` catalog rows; zero test coverage existed for this path. | HIGH | New Task 5: adds `InstallStatus.MANUAL_REQUIRED` to `summarize`'s `buckets` dict and a matching `manual_required` field on `Summary`, threads it through `render_summary` on the same footing as every other bucket, and pins the fix with regression tests in both `tests/test_session.py` and `tests/test_render.py` — a real code fix, not an audit note. |
| Task 3's SUMMARY should explicitly record that no daemon/launchctl code path was reached during the tmux walkthrough, closing the reviewer's open (unsubstantiated) `launchctl`-isolation question with recorded evidence. | LOW | Task 3's `read_first`, `action` (new Part C), `acceptance_criteria`, and `<done>` now require the SUMMARY to explicitly state, with source-level citations (`setup.py::_build_daemon_policy` returns `None` on this machine's non-macOS `platform.os`; the script's key sequence never sends `space`/`enter`), that zero daemon/launchctl code path is reachable during the walkthrough. |

### Review Feedback Deferred (Round 2)

| Concern | Reason |
|---------|--------|
| N/A | Both round-2 findings (one HIGH, one LOW) are incorporated above; nothing deferred this round. |


