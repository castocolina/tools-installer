# Phase 12.1: Reconciliation Merge Verification - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-07
**Phase:** 12.1-reconciliation-merge-verification-confirm-the-layered-merge
**Areas discussed:** Verification depth, Tier remapping audit, Pre-existing failing test, Ported-but-unwired code hygiene

---

## Verification depth

| Option | Description | Selected |
|--------|-------------|----------|
| Static + manual smoke | Import/orphan/vulture checks plus a manual tmux TUI walkthrough, no real install actions yet | ✓ |
| Static/automated only | make validate + import/coverage/vulture analysis, no manual TUI touch | |
| Full manual now | Real install/uninstall actions in this phase, pulling forward Phase 12.3's scope | |

**User's choice:** Static + manual smoke walkthrough (recommended option)
**Notes:** Keeps this phase scoped to structural soundness; real actions stay in Phase 12.3's container e2e.

---

## Tier remapping audit

| Option | Description | Selected |
|--------|-------------|----------|
| Full individual audit | Review all 11 re-added rows against origin's real tier semantics, correct misclassifications | ✓ |
| Quick sampling | Only review ambiguous cases (e.g. drawio-desktop/drawio-cli) | |
| Trust the agent's mapping | No audit, fix later if the smoke test surfaces something odd | |

**User's choice:** Full individual audit (recommended option)
**Notes:** The porting agent's skill-pack/agent→ai, everything-else→user heuristic was noted as a simplification, not a verified mapping.

---

## Pre-existing failing test

| Option | Description | Selected |
|--------|-------------|----------|
| Fix it here | Resolve test_fresh_manager_snapshot_issues_zero_queries in this phase | ✓ |
| Document and defer | Note it as known pre-existing, non-blocking, leave for later | |

**User's choice:** Fix it here (recommended option)
**Notes:** Confirmed pre-existing on unmodified origin b9a8876 via isolated worktree during the earlier reconciliation execution; fixing it now avoids ambiguity before Phase 12.3.

---

## Ported-but-unwired code hygiene

| Option | Description | Selected |
|--------|-------------|----------|
| Verify without blocking | Confirm install_actions.py/agent_policy.py don't break make validate; leave inert until Phase 12.2 | ✓ |
| Mark explicitly as pending | Same verification, plus a TODO/architecture.md note flagging the unwired state | |

**User's choice:** Verify without blocking (recommended option)
**Notes:** Phase 12.2 already exists as the tracked follow-up for wiring these modules, so an extra flag was judged redundant.

---

## Claude's Discretion

- Exact static-analysis tooling/commands beyond `make validate`'s existing gates.
- Whether the D-03 test fix touches `installer/version_status.py` logic or only test fixtures — left for research/implementation time.

## Deferred Ideas

- Wiring install_actions.py/agent_policy.py into the UI — Phase 12.2.
- Real install/uninstall actions and container e2e — Phase 12.3.
