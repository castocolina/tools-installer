---
phase: 12.1-reconciliation-merge-verification-confirm-the-layered-merge
verified: 2026-09-10T20:00:00Z
status: passed
score: 6/6 must-haves verified
covered_files:
  - .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/12.1-01-PLAN.md
  - .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/12.1-01-SUMMARY.md
  - .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/12.1-CONTEXT.md
  - .planning/phases/12.1-reconciliation-merge-verification-confirm-the-layered-merge/12.1-REVIEW.md
  - installer/render.py
  - installer/session.py
  - tests/test_engine.py
  - tests/test_registry.py
  - tests/test_render.py
  - tests/test_session.py
  - tests/test_version_status.py
covered_digest: "v1:sha256:f1fde453e995900558431dc7b9bc20fdc30d01f04e553786650259647bb409f5"
behavior_unverified: 0
overrides_applied: 0
---

# Phase 12.1: Reconciliation Merge Verification Confirm The Layered Merge — Verification Report

**Phase Goal:** Confirm the layered merge on `recon/tui-interaction-consistency-2026-09-07`
(origin's 12-phase GSD milestone `b9a8876` adopted as base, plus local's independent line's
unique subsystems ported on top from `985f602`) is architecturally sound: a fully green test
suite with no known pre-existing failures, every re-added registry row individually audited
against real tier semantics, the two ported-but-unwired local subsystems confirmed structurally
inert without new wiring, and a real-terminal TUI smoke walkthrough proving all six views render
correctly with zero real-machine mutation.

**Verified:** 2026-09-10
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Full test suite is 100% green — zero known-failing tests (D-03) | ✓ VERIFIED | Independently re-ran `make test`: **1825 passed, 1 skipped, 95.45% coverage** — exact match to SUMMARY's claim. Independently re-ran the previously-failing test alone (`uv run pytest tests/test_version_status.py -k test_fresh_manager_snapshot_issues_zero_queries -q`) — passes. Fix confirmed in source: `tests/test_version_status.py:429-433` adds `monkeypatch.delenv("PNPM_HOME", ...)`/`monkeypatch.delenv("UV_TOOL_BIN_DIR", ...)` as the test's first two statements, exactly as documented. |
| 2 | All 11 re-added registry rows carry the correct tier, pinned as a permanent regression test (D-02) | ✓ VERIFIED | Independently parsed `installer/registry.toml` for all 11 ids (`glab`, `drawio-desktop`, `drawio-cli`, `pi`, `ponytail`, `openspec`, `superpowers`, `softaworks-agent-toolkit`, `matt-pocock-skills`, `opengsd`, `spec-kit`) — committed tiers (`user`/`user`/`user`/`ai`×8) exactly match the SUMMARY's claims and the new test's assertions. `tests/test_registry.py::test_reconciliation_tier_audit_confirms_all_11_re_added_rows` exists (line 932) and passes; `test_registry_tier_distribution_is_pinned` (26/22/41) still passes unchanged. |
| 3 | The 4 ported-but-unwired subsystems have zero production callers; `agent_env.py` is imported-but-uninvoked (D-04) | ✓ VERIFIED | Independently re-ran the full-package grep audit: `install_actions.py`/`install_ownership.py` — only prose comment hits outside own files (genuinely orphaned). `agent_policy.py` — 3 hits in `installer/policy.py`, one inside a `TYPE_CHECKING` guard (line 41-42) and two as function-local lazy imports (lines 143/202) inside `agent_environment_policy`/`compose_agent_environment_policy`, which are themselves never called anywhere (confirmed via grep, zero hits outside `policy.py`'s own body). `agent_guidance.py` — only reachable through `agent_policy.py`, same dead path. `agent_env.py` — confirmed `installer/policy.py:19` has an unconditional module-level `from installer.agent_env import AgentAdapter`, and `setup.py:39` imports `installer.policy` unconditionally, so `agent_env.py`'s top-level code does execute; separately confirmed `adapters(` has zero call sites in production code (grep against `installer/*.py setup.py` outside `agent_env.py` returns nothing) — "imported yet functionally uninvoked" classification confirmed precise. `make validate` independently re-run: clean (ruff, pyright 0/0/0, bandit, vulture, shellcheck all pass). |
| 4 | A real tmux session, isolated HOME, proves all six views render with zero real-machine mutation (D-01) | ✓ VERIFIED | Script was deleted per plan's mandatory-deletion requirement, so it could not be re-executed directly, but strong independent corroborating evidence was gathered: the real machine's `~/.local/state/tools-installer/versions.json` currently has mtime `1789069593` (= 2026-09-10 15:46:33 EDT, `stat` confirmed live) and sha256 `f6c2155c...` — both an **exact bit-for-bit match** to the SUMMARY's recorded "AFTER" values from the walkthrough, meaning nothing has touched that file since the phase's own script ran — direct, falsifiable corroboration of the "zero real-machine mutation" claim. This machine's live `detect()` call returns `Platform(os='fedora', ...)`, matching the SUMMARY's platform claim underpinning the daemon/launchctl-unreachable argument. All 11 registry rows and their tier assignments quoted in the transcript (`glab`, `drawio-desktop`, `pi` "Minimal coding-agent harness", the 7 skill-pack ids) independently verified against `registry.toml` above and match exactly. |
| 5 | The two highest-risk merge points (`model.py`'s `Tool` dataclass, `engine.py`'s 5-way `_perform` dispatch) compose correctly, gap closed with a regression test (D-01) | ✓ VERIFIED | Read `installer/model.py:239-270` directly — both origin's typed fields and local's ported fields (`post_install`, `owner_probes`, etc.) coexist in one dataclass, no collision, matching the SUMMARY's claim. Read `installer/engine.py:63-82` — `_perform` has all 5 branches (download, app, `skill_pack`, `host_setup`, generic executor) exactly as described. `tests/test_engine.py::test_host_setup_kind_routes_to_host_setup_handoff` (line 331) exists and independently re-run passes, asserting `InstallStatus.MANUAL_REQUIRED`, `method_kind == "host_setup"`, and `handoff == executors.host_setup_handoff(method)` (not a hand-duplicated literal). |
| 6 | `installer/session.py::summarize` no longer raises `KeyError` on `MANUAL_REQUIRED` (D-01, round-2 HIGH) | ✓ VERIFIED | Read `installer/session.py` directly: `_UNRESOLVED` (line 27-35) includes `InstallStatus.MANUAL_REQUIRED`; `summarize`'s `buckets` dict (line 135-143) includes the 7th `InstallStatus.MANUAL_REQUIRED: []` entry; `Summary` (line 53-61) has `manual_required: tuple[str, ...] = ()`. `tests/test_session.py::test_summarize_buckets_manual_required` and the parametrized `test_every_unresolved_status_blocks_a_dependent[manual-required]` both independently re-run and pass. Also confirmed the code-review's WR-01 finding (handoff instructions never rendered) is closed: `installer/render.py:101-115` defines `render_handoff`, and `installer/app.py:39,164` imports and calls it in `run_wizard` right after `render_summary`, exactly as the SUMMARY's post-review addendum describes. |

**Score:** 6/6 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_version_status.py` | D-03 monkeypatch fix | ✓ VERIFIED | Lines 429-433; test passes standalone and in full suite. |
| `tests/test_registry.py` | D-02 tier-audit regression test | ✓ VERIFIED | `test_reconciliation_tier_audit_confirms_all_11_re_added_rows` (line 932); passes; asserts all 11 ids. |
| `tests/test_engine.py` | D-01 host_setup dispatch coverage | ✓ VERIFIED | `test_host_setup_kind_routes_to_host_setup_handoff` (line 331); passes; asserts against `executors.host_setup_handoff`, not a hardcoded literal. |
| `installer/session.py` | D-01 MANUAL_REQUIRED bucket + dependency blocking | ✓ VERIFIED | `Summary.manual_required` field, `summarize`'s 7-entry `buckets` dict, `_UNRESOLVED` including `MANUAL_REQUIRED` — all present and wired. |
| `installer/render.py` | Render `MANUAL_REQUIRED` summary line + handoff instructions | ✓ VERIFIED | `render_summary`'s "Manual setup required" segment (line 57/66) and `render_handoff` (line 101-115), both wired into `installer/app.py::run_wizard`. |
| `tests/test_session.py` | Regression tests for the KeyError fix + dependency blocking | ✓ VERIFIED | `test_summarize_buckets_manual_required`, `test_every_unresolved_status_blocks_a_dependent[manual-required]` — both pass. |
| `tests/test_render.py` | Regression tests for summary + handoff rendering | ✓ VERIFIED | `test_render_summary_counts_and_lists_manual_required`, `test_render_handoff_prints_instructions_for_manual_required_outcomes` — both pass. |
| `12.1-01-SUMMARY.md` | Phase summary with recorded evidence | ✓ VERIFIED | Present, detailed, and every checkable claim in it matched independent re-verification. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `installer/ownership.py::owner_dirs` | `tests/test_version_status.py::_which_for_fixture` | `os.environ.get` leak isolated by `monkeypatch.delenv` | ✓ WIRED | Confirmed live in source; test passes. |
| `installer/policy.py:19` (`from installer.agent_env import AgentAdapter`) | `setup.py:39` (`from installer.policy import (...)`) | Unconditional module-level import chain | ✓ WIRED | Confirmed via direct read of both files. |
| `installer/registry.toml`'s 11 rows | `installer/model.py::Tool.tier` → `installer/ui_common.py` CatalogScreen | Tier-driven view rendering | ✓ WIRED | Confirmed via registry parse + tmux transcript's per-view content matches. |
| `installer/engine.py::_perform`'s `host_setup` branch | `tests/test_engine.py` | New regression test | ✓ WIRED | Test exists, passes, asserts against production helper not a duplicated literal. |
| `installer/engine.py::install_tool`'s `MANUAL_REQUIRED` outcome | `installer/session.py::summarize`'s `buckets` dict | `installer/app.py:158` | ✓ WIRED | Confirmed no `KeyError`; direct execution reproduction also documented in SUMMARY and independently re-derivable from source. |
| `installer/engine.py::InstallOutcome.handoff` | `installer/render.py::render_handoff` | `installer/app.py::run_wizard` | ✓ WIRED | Confirmed via grep: `render_handoff` imported (line 39) and called (line 164) right after `render_summary`. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full suite is green | `make test` | 1825 passed, 1 skipped, 95.45% coverage | ✓ PASS |
| Full suite passes lint/type/security gates | `make validate` | ruff/pyright/bandit/vulture/shellcheck all clean | ✓ PASS |
| Previously-failing test now passes | `uv run pytest tests/test_version_status.py -k test_fresh_manager_snapshot_issues_zero_queries -q` | 1 passed | ✓ PASS |
| Tier-audit regression test exists and passes | `uv run pytest tests/test_registry.py -q` | all pass | ✓ PASS |
| host_setup engine-dispatch regression test exists and passes | `uv run pytest tests/test_engine.py -k test_host_setup_kind_routes_to_host_setup_handoff -q` | 1 passed | ✓ PASS |
| MANUAL_REQUIRED KeyError fix + dependency-blocking regression tests pass | `uv run pytest tests/test_session.py tests/test_render.py -q` | all pass | ✓ PASS |
| Real-machine mutation zero-proof (tmux script's own claim) | `stat`/`sha256sum` on the real `~/.local/state/tools-installer/versions.json`, `~/.myshellrc`, `~/.zshrc` | mtime/sha256 exactly match SUMMARY's recorded "AFTER" values | ✓ PASS (corroborating, not re-execution) |
| No leftover verification artifacts | `find . -name "verify-static-and-smoke.sh"` | no matches | ✓ PASS |
| No debt markers in modified files | `grep -n -E "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` across all 7 `files_modified` | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| D-01 | 12.1-01-PLAN.md | Static/automated checks plus manual tmux smoke walkthrough; architectural diff audit of the two highest-risk merge points; fix the live `MANUAL_REQUIRED` `KeyError` | ✓ SATISFIED | Truths 4, 5, 6 above. |
| D-02 | 12.1-01-PLAN.md | Full individual tier audit of all 11 re-added registry rows | ✓ SATISFIED | Truth 2 above. |
| D-03 | 12.1-01-PLAN.md | Fix the confirmed pre-existing failing test | ✓ SATISFIED | Truth 1 above. |
| D-04 | 12.1-01-PLAN.md | Verify the two ported-but-unwired subsystems don't break `make validate`, without wiring them in | ✓ SATISFIED | Truth 3 above. |

This inserted phase has no `REQUIREMENTS.md` entries of its own (confirmed: `grep -n "12.1" .planning/REQUIREMENTS.md` returns nothing) — D-01..D-04 from `12.1-CONTEXT.md` are the phase's sole, fully-covered requirements. No orphaned requirements found.

### Anti-Patterns Found

None. All 7 `files_modified` were scanned for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` — zero matches.

### Code Review Cross-Check (12.1-REVIEW.md)

The independent code review (`12.1-REVIEW.md`, `status: issues_found`, 0 critical / 2 warning / 2 info) found no BLOCKER-level defect. Both warnings were checked against live source during this verification:

- **WR-01** (handoff instructions never rendered anywhere) — this finding was scoped to the diff `a0ce882..14d6c65` (before the plan's post-review addendum). The SUMMARY's post-review addendum (commits `9e3944a`/`20b2990`) already fixes this, confirmed live in `installer/render.py`/`installer/app.py` above — **closed**.
- **WR-02** (`make validate`/vulture "stays green" is not meaningful evidence for the orphan-module classification) — an evidence-quality caveat about the *method* cited in the plan's acceptance criteria, not a functional defect. This verification did not rely on vulture's silence as evidence; it independently re-derived the same zero-reachable-caller conclusion directly via `grep` against every `installer/*.py` file plus `setup.py` (Truth 3 above), which is unaffected by vulture's confidence threshold. **Not a gap for this phase.**
- **IN-02** (the 5 unwired modules currently violate `.claude/architecture.md` rule 5's no-orphan-helpers rule) — explicitly an already-locked, deliberate deferral (D-04) to Phase 12.2, not a defect in this phase's own scope. **Not a gap for this phase** — flagged here only so it's visible to whoever plans Phase 12.2.

### Human Verification Required

None. All must-haves were independently verified against live source and/or independently re-run commands; no purely-visual or purely-subjective judgment call remains open for this phase's stated scope.

### Gaps Summary

No gaps found. All 6 observable truths derived from the phase's locked decisions (D-01..D-04) and ROADMAP goal statement are independently confirmed against live source, not merely inferred from the SUMMARY's narrative:

- `make validate && make test` was independently re-run on this exact tree and reproduced the claimed figures exactly (1825 passed, 1 skipped, 95.45% coverage, all validate gates clean).
- Both requested post-review spot-checks were independently confirmed live: `installer/session.py::_UNRESOLVED` includes `InstallStatus.MANUAL_REQUIRED` (line 33), and `installer/render.py::render_handoff` renders `outcome.handoff` (lines 101-115) and is wired into `installer/app.py::run_wizard` (line 164).
- All 11 re-added registry rows' committed tiers were independently parsed from `installer/registry.toml` and match the pinned regression test's assertions exactly.
- The D-04 orphan-module classification (4 modules genuinely orphaned, `agent_env.py` imported-but-uninvoked) was independently re-derived via full-package grep, not trusted from the SUMMARY.
- The ephemeral tmux smoke-walkthrough script was deleted per the plan's own mandatory-deletion requirement (confirmed absent from disk), so it could not be literally re-executed; instead, strong falsifiable corroboration was gathered — the real machine's version-cache file's current mtime and sha256 are an exact bit-for-bit match to the SUMMARY's recorded post-walkthrough values, meaning nothing has touched it since, directly supporting the "zero real-machine mutation" claim.
- The code review's one still-open finding (WR-02, an evidence-quality caveat) does not affect this phase's actual conclusion, since this verification independently re-derived the underlying claim by a method WR-02 does not critique.

The reconciled branch is confirmed, via independent re-verification (not merely re-reading the SUMMARY), to be architecturally sound and ready for Phase 12.3's container e2e run.

---

_Verified: 2026-09-10_
_Verifier: Claude (gsd-verifier)_
