---
phase: 12.1-reconciliation-merge-verification-confirm-the-layered-merge
plan: 01
subsystem: test-infra, registry, engine, session/render
tags: [reconciliation, tier-audit, tmux-smoke, keyerror-fix, verification]
key-files:
  - tests/test_version_status.py
  - tests/test_registry.py
  - tests/test_engine.py
  - installer/session.py
  - installer/render.py
  - tests/test_session.py
  - tests/test_render.py
metrics:
  tasks_completed: 5
  commits: 9
  tests_added: 8
  full_suite: "1825 passed, 1 skipped, 95.45% coverage"
---

# 12.1-01 Summary — Reconciliation Merge Verification

Verified (not merely re-tested) that the layered merge of origin's 12-phase GSD
milestone with local's independently-developed subsystems is architecturally
sound, closing all four locked decisions (D-01..D-04) plus the round-2 HIGH
finding from cross-AI review.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1 (D-03) | `a0ce882` | fix(tests): isolate PNPM_HOME/UV_TOOL_BIN_DIR in version_status ownership test |
| 2 (D-02) | `cae3276` | test(registry): pin the tier-remapping audit for the 11 re-added rows |
| 4 (D-01, merge-diff audit) | `59438d8` | test(engine): close host_setup dispatch coverage gap surfaced by diff audit |
| 5 (D-01, RED) | `a81b71b` | test(session,render): RED — pin the MANUAL_REQUIRED KeyError crash |
| 5 (D-01, GREEN) | `0188f4e` | fix(session): GREEN — add missing MANUAL_REQUIRED bucket, render it |
| 3 (D-01, D-04) | *(no commit — verification-only, per task contract)* | corrected static import-graph audit + isolated-HOME tmux smoke walkthrough |
| post-review 1 (RED) | `392ee08` | test(session): RED — pin MANUAL_REQUIRED as an unresolved dependency status |
| post-review 1 (GREEN) | `a38c22e` | fix(session): GREEN — block dependents of a MANUAL_REQUIRED dependency |
| post-review 2 (RED) | `9e3944a` | test(render): RED — pin that outcome.handoff is never rendered |
| post-review 2 (GREEN) | `20b2990` | fix(render): GREEN — render outcome.handoff for MANUAL_REQUIRED tools |

Task 3 is verification-only per its own `<files>` contract ("None —
verification-only; no production or test files change"); its temporary
script (`verify-static-and-smoke.sh`) was written, run, and then deleted, and
its full evidence is recorded below rather than committed. `git status
--short` after all other commits shows zero uncommitted change inside this
plan's `files_modified` scope.

## Task 1 — D-03: fixed the confirmed pre-existing failing test

`tests/test_version_status.py::test_fresh_manager_snapshot_issues_zero_queries`
was the sole failing test in the full suite. Root cause: this machine's real
`PNPM_HOME` env var leaked into `installer/ownership.py::owner_dirs`'s
unconditional `os.environ.get` read, which the test's `_which_for_fixture`
fixture (a hardcoded macOS-shaped path) never isolated. Fixed by adding
`monkeypatch.delenv("PNPM_HOME", raising=False)` and
`monkeypatch.delenv("UV_TOOL_BIN_DIR", raising=False)` as the test's first two
statements — deleting rather than pinning a value, so `owner_dirs`'s own
`_PNPM_DEFAULTS["macos"]` fallback produces the paths the fixture expects.

Verified: `uv run pytest tests/test_version_status.py -q` — all pass.
`git diff --stat -- installer/ownership.py installer/version_status.py` —
empty (no production code touched).

## Task 2 — D-02: pinned the tier-remapping audit as a regression test

Individually audited all 11 registry rows commit `9f6274b` re-added against
origin's real tier semantics (system = machine prerequisite, user =
personal-pick tool, ai = agent-facing tooling) and this registry's own
established precedents:

- `glab` → user: matches `gh` (same category/audience/priority precedent).
- `drawio-desktop`/`drawio-cli` → user: matches `mmdc` (diagram-category,
  general-purpose rendering tool precedent).
- `pi` → ai: matches `claude`/`codex`/`opencode`/`cursor-agent`/`antigravity`
  (standalone runnable agent-CLI precedent).
- `ponytail`, `openspec`, `superpowers`, `softaworks-agent-toolkit`,
  `matt-pocock-skills`, `opengsd`, `spec-kit` → ai: matches
  `codegraph`/`graphify`/`rtk` (agent-serving tooling precedent).

No misclassification found — every one of the 11 rows already carries the
correct tier. Added `test_reconciliation_tier_audit_confirms_all_11_re_added_rows`
to `tests/test_registry.py`, pinning this as a permanent, individually-reasoned
regression test rather than a one-time claim.

Verified: `uv run pytest tests/test_registry.py -q` — all pass, including the
unchanged `test_registry_tier_distribution_is_pinned` (26 system / 22 ai / 41
user). `git diff --stat -- installer/registry.toml` — empty.

## Task 4 — D-01: architectural diff audit of the two highest-risk merge points

Read `git diff b9a8876..a570bf3 -- installer/model.py installer/engine.py
installer/resolve.py installer/host_setup.py installer/skill_lifecycle.py` in
full (557 lines across 5 files) and confirmed:

1. **`installer/model.py`'s `Tool` dataclass / `load_tools`** (diff lines
   ~148-622): origin's typed fields (`id`/`name`/`category`/`cmd`/`methods`/
   `priority`/`audience`/`tier`/`desc`/`requires`/`recommends`/`postinstall`)
   compose with local's ported fields (`post_install`,
   `default_enabled_sensitive_actions`, `owner_probes`, `source`, `owner`,
   `uninstall`, `skill_lifecycle`) in one frozen dataclass with a hand-written
   `__init__` that `object.__setattr__`s every field — no name collision, both
   lineages assigned. `load_tools`'s tail (diff lines ~544-597) shows both
   lineages' closed-set validation surviving intact: origin's `postinstall`
   membership check against `POSTINSTALL_HOOK_NAMES` plus local's
   `post_install`/`default_enabled_sensitive_actions`/`owner_probes`
   closed-list parsing and the `skill_lifecycle` mutual-exclusivity constraint
   (`len(methods) != 1` raises when a skill_pack tool also declares fallback
   methods).
2. **`installer/engine.py`'s merged 5-way `_perform` dispatch** (diff lines
   ~40-68): origin's `download.DOWNLOAD_KINDS`/`apps.APP_KINDS`/generic
   `executors.execute` branches compose with local's new `skill_pack` (routes
   to `skill_lifecycle.perform_action`) and `host_setup` (routes to
   `executors.host_setup_handoff`) branches. `install_tool`'s outcome contract
   (diff lines ~86-94) threads correctly through all 5: a non-empty `handoff`
   from either new branch produces `InstallOutcome(..., InstallStatus.MANUAL_REQUIRED,
   method_kind=method.kind, handoff=handoff)`.
3. **`installer/resolve.py`** (diff lines ~623-654, 13 insertions): the
   `_RANK` ladder gained `"host_setup": 15` and `"skill_pack": 15`, and
   `_applies`'s platform-gate kind tuple was extended to include both new
   kinds alongside origin's existing unconditional-True kinds — neither new
   kind is silently filtered by a platform check it shouldn't be subject to.

That read surfaced one concrete, closeable gap: `engine.py::_perform`'s
`host_setup` branch had zero direct engine-level test, unlike its sibling
`skill_pack` branch (already exercised end-to-end by
`tests/test_skill_lifecycle.py::test_every_shipped_skill_pack_install_is_an_honest_manual_handoff`).
Closed it with `test_host_setup_kind_routes_to_host_setup_handoff` in
`tests/test_engine.py`, asserting `install_tool()` returns
`InstallStatus.MANUAL_REQUIRED`, `method_kind == "host_setup"`, and a
`handoff` equal to `executors.host_setup_handoff(method)` (not a
hand-duplicated literal, so a future edit to `HOST_SETUPS["pi"]` cannot
silently desync the test).

Verified: `uv run pytest tests/test_engine.py -q` — all pass, including the
new test. `git diff --stat -- installer/model.py installer/engine.py
installer/resolve.py installer/host_setup.py installer/skill_lifecycle.py` —
empty (this task reads the historical diff; it does not re-touch these files).

## Task 5 — D-01: fixed `installer/session.py::summarize`'s `MANUAL_REQUIRED` `KeyError`

`git diff b9a8876..a570bf3 -- installer/session.py` is empty — `session.py`
was never touched by the reconciliation that introduced
`InstallStatus.MANUAL_REQUIRED` in `installer/enums.py`/`installer/engine.py`.
`summarize`'s `buckets` dict enumerated only 6 of the 7 `InstallStatus`
members, so `buckets[outcome.status]` raised `KeyError` the first time
`outcome.status == InstallStatus.MANUAL_REQUIRED` — reachable from the real
CLI install path (`installer/app.py:158`, `run_wizard` →
`summarize(outcomes)`) on any real install of `pi` or one of the 7
`skill_pack` catalog rows.

TDD mode: wrote failing tests first (commit `a81b71b`, RED):
`test_summarize_buckets_manual_required` (confirmed `KeyError` from
`summarize`) and `test_render_summary_counts_and_lists_manual_required`
(confirmed `Summary` had no `manual_required` field). Then fixed (commit
`0188f4e`, GREEN):

- Added `manual_required: tuple[str, ...] = ()` as a keyword-defaulted field
  on `Summary`.
- Added `InstallStatus.MANUAL_REQUIRED: []` to `summarize`'s `buckets` dict
  and `manual_required=tuple(buckets[InstallStatus.MANUAL_REQUIRED])` to its
  `Summary(...)` return.
- Appended a `"Manual setup required: {n}"` segment to `render_summary`'s
  count line and `("manual setup required", summary.manual_required)` as the
  last entry in its id-list loop.

Direct execution, matching the reviewer's exact repro:

```
$ uv run python3 -c "from installer.session import summarize; from installer.engine import InstallOutcome; print(summarize([InstallOutcome('pi', 'manual-required', method_kind='host_setup')]))"
Summary(installed=(), already=(), failed=(), no_method=(), mismatched=(), dependency_failed=(), manual_required=('pi',))
```

No `KeyError` — exits 0.

Verified: `uv run pytest tests/test_session.py tests/test_render.py -q` — all
pass, including both new tests. `git diff --stat -- installer/engine.py
installer/enums.py` — empty (only the consumer side, `session.py`/`render.py`,
was fixed).

## Task 3 — D-01 (tmux smoke), D-04 (static import-graph audit)

### Part A — corrected full-package static import-graph audit

Grepped `install_actions`, `install_ownership`, `agent_policy`,
`agent_guidance`, `agent_env` across every `installer/*.py` file plus
`setup.py` (not the original 6-file subset). Confirmed:

- **`installer/install_actions.py`, `installer/install_ownership.py`**: zero
  hits outside their own files (only prose comment mentions in `model.py`/
  `shellrc.py`) — genuinely orphaned.
- **`installer/agent_guidance.py`**: one hit, `installer/agent_policy.py:9` —
  reachable only if `agent_policy.py` itself executes.
- **`installer/agent_policy.py`**: three hits, all in `installer/policy.py` —
  line 43 inside a `TYPE_CHECKING` block (never executes at runtime) and
  lines 143/202 as function-local lazy imports inside
  `agent_environment_policy`/`compose_agent_environment_policy`. Confirmed
  (via `grep -rln "agent_environment_policy\|compose_agent_environment_policy"
  installer/*.py setup.py`) that neither function is called anywhere outside
  `policy.py`'s own body — `agent_policy.py`'s (and therefore
  `agent_guidance.py`'s) code never executes at runtime.
- **`installer/agent_env.py`**: one hit, `installer/policy.py:19` — an
  UNCONDITIONAL module-level `from installer.agent_env import AgentAdapter`
  (not `TYPE_CHECKING`-guarded, not function-local). Since `setup.py` imports
  `installer.policy` unconditionally, `agent_env.py`'s top-level code (the
  `AgentAdapter` dataclass and `adapters()` definition) executes on every
  `setup.py` run. Separately confirmed (`grep -rn "adapters("
  installer/*.py setup.py`) that `adapters()` itself is never CALLED in
  production code — only from test files. Classification: **imported yet
  functionally uninvoked**, not "unimported" — the factual correction this
  task makes per the round-1 HIGH finding.

This machine's `platform.os` is `fedora` (confirmed live via
`installer/platform.py::detect()` → `Platform(os='fedora', arch='amd64', ...)`).

Per D-04 (locked): none of the five modules were wired in, no TODO/note was
added, and no integration test was written — this task verifies without
blocking, deferring the wiring decision to Phase 12.2.

`make validate` (ruff check + format, pyright, bandit, vulture, shellcheck)
exited 0 with all five modules present and unmodified:

```
uv run ruff check installer tests setup.py       -> All checks passed!
uv run ruff format --check installer tests setup.py -> 123 files already formatted
uv run pyright                                    -> 0 errors, 0 warnings, 0 informations
uv run bandit -q -r installer --skip B404,B603,B310 -> (clean)
uv run vulture                                    -> (clean)
uv run shellcheck install.sh                      -> (clean)
```

### Part B — isolated-HOME real-terminal tmux structural smoke walkthrough

Wrote `verify-static-and-smoke.sh` (deleted after this evidence was recorded,
per the task's mandatory-deletion requirement). Structure:

- `REAL_HOME="$HOME"`, `FAKE_HOME="$(mktemp -d)"`, an `EXIT` trap killing the
  tmux session and removing `FAKE_HOME` on any exit path (including a
  `set -e` failure).
- Recorded `~/.myshellrc` mtime, `~/.zshrc` mtime, and
  `~/.local/state/tools-installer/versions.json` mtime+sha256 BEFORE the tmux
  session started.
- Launched `HOME="$FAKE_HOME" .venv/bin/python setup.py --link-mode
  centralized` inside `tmux new-session -d -x 220 -y 55`.
- Polled (never blind-slept) `tmux capture-pane` for readiness markers: first
  paint on `GLOBAL_NAV`'s `"1-6 views"`, then per-view content markers
  (chosen from what the main screen actually renders — `View.palette`'s full
  prose only appears in the Ctrl+P command-palette overlay
  (`installer/wizard_app.py:1344`), not the main catalog screen this
  navigation-only walkthrough drives, confirmed by manual capture before
  wiring the script): `colima` (System), `glab` (User), `ponytail` (AI),
  `PATH Doctor` (Doctor), `DESTRUCTIVE` (Uninstall), `pip/npm ban` (Policies).
  Any pane containing `Traceback`/`Error:` fails the script immediately.
- Asserted the User view's capture contains `glab`, `drawio-desktop`, AND
  `drawio-cli`.
- Asserted the AI view's capture contains `ponytail`, `openspec`,
  `superpowers`, `softaworks-agent-toolkit`, `matt-pocock-skills`, `opengsd`,
  `spec-kit`, AND `Minimal coding-agent harness` (pi's distinguishing text).
- Asserted the Policies view's capture contains NONE of `agent environment`,
  `permission audit`, `agent-policy` — confirms subsystem B's Policy factory
  is not surfaced in the live UI (9 policies rendered, none agent-env
  related), consistent with Part A's static finding.
- Polled (up to 15s) for `$FAKE_HOME/.local/state/tools-installer/versions.json`
  to exist — positive, verified proof the version-refresh cache write
  happened and was contained inside the isolated HOME.
- Sent `q`; polled for `tmux has-session` to fail (clean exit).
- Re-stat/re-hash the REAL `~/.myshellrc`, `~/.zshrc`, and
  `~/.local/state/tools-installer/versions.json`, failing if ANY value
  changed from before the session started.

Full run output (`bash verify-static-and-smoke.sh`, exit code 0):

```
BEFORE: myshellrc mtime=1785411510 zshrc mtime=1788817497 cache mtime=1789069593 cache sha=f6c2155c5837cd058037d471a62b0aa9362f3c0b60c992ffce79ffac827a6316
--- waiting for first paint (GLOBAL_NAV '1-6 views') ---
first paint OK
view 1: marker 'colima' OK
view 2: marker 'glab' OK
view 3: marker 'ponytail' OK
view 4: marker 'PATH Doctor' OK
view 5: marker 'DESTRUCTIVE' OK
view 6: marker 'pip/npm ban' OK
content assertions for User/AI/Policies views: OK
--- polling for isolated version-cache write ---
isolated version-cache write CONFIRMED at /tmp/tmp.j9cjJdyfvV/.local/state/tools-installer/versions.json
--- sending q and waiting for clean tmux exit ---
clean exit CONFIRMED
--- re-checking REAL machine for zero mutation ---
AFTER: myshellrc mtime=1785411510 zshrc mtime=1788817497 cache mtime=1789069593 cache sha=f6c2155c5837cd058037d471a62b0aa9362f3c0b60c992ffce79ffac827a6316
=== Part B: PASSED — zero real-machine mutation confirmed ===
```

Verbatim tmux transcript (all six views' captures) — recorded in full:

```
=== BOOT PANE (System, first paint) ===
 [1] System    [2] User    [3] AI    [4] Doctor    [5] Uninstall    [6] Policies
 ...
 o [STAGED]   space marks a tool; enter installs everything you staged
 Category  Priority  Audience  Status  Table
 Sel              Pri  Tool           Cat      For   Inst  Ver      What it does
 ── docker (6)
 [ ]              P1   colima         docker   both  ○              Runs container runtimes in a lightweight VM...
 [ ]              P1   docker         docker   both  ○              Controls Docker engines and contexts...
 [ ]              P1   podman         docker   both  ✓     unknown  Rootless container engine...
                  P2   container      docker   both  ○              Apple's native container runtime... (not available on this machine)
 [ ]              P2   dive           docker   both  ✓     unknown  A tool for exploring each layer in a docker image
 [ ]              P2   lazydocker     docker   both  ✓     unknown  A simple terminal UI for docker and docker-compose
 ── pkg-mgr (4)
 [ ]              P0   brew           pkg-mgr  both  ✓     unknown  Homebrew supplies user-space packages...
 [ ]              P0   uv             pkg-mgr  both  ✓     unknown  Fast Python package and venv manager
 [ ]              P2   pnpm           pkg-mgr  both  ✓     unknown  Fast, disk-space-efficient Node.js package manager
 [ ]              P2   volta          pkg-mgr  both  ✓     unknown  Node toolchain manager...
 ── runtime (9)
 [ ]              P1   gradle/java/maven/sdkman/bun/deno/fnm/groovy/springbootcli  runtime  both  (SDKMAN-managed JVM/Node tools)
 ── shell (7)
 [ ]              P1   direnv/starship/watch/zsh/gnu-bash/gum/oh-my-zsh
 space toggle | enter install | a all | i invert | u update   │   1-6 views | ^p nav | esc back | q quit

=== VIEW 2 (User) — key '2', marker 'glab' ===
 ── data (7): jq, yq, dasel, fx, gron, miller, jless
 ── dev (7): just, ruff, hyperfine, shfmt, tealdeer, puppeteer, vale
 ── diagram (3): drawio-cli, drawio-desktop, mmdc
 ── editor (3): vscode, jetbrains-toolbox, sublime (not available on this machine)
 ── git (7): gh, git, glab, delta, difftastic, gitui, lazygit
 ── nav (2): zoxide, broot
 ── net (1): xh
 ── search (2): fzf, ast-grep
 ── security (1): gitleaks
 (glab, drawio-desktop, drawio-cli all present — CONFIRMED)

=== VIEW 3 (AI) — key '3', marker 'ponytail' ===
 ── ai (12): claude, codex, cursor-agent, opencode, aichat, antigravity, pi
             (Minimal coding-agent harness), matt-pocock-skills, opengsd,
             ponytail, softaworks-agent-toolkit, superpowers
 ── dev (5): codegraph, graphify, rtk, openspec, spec-kit
 ── search (2): fd, rg
 ── text (1): sd
 ── view (2): bat, eza
 (ponytail, openspec, superpowers, softaworks-agent-toolkit,
  matt-pocock-skills, opengsd, spec-kit, "Minimal coding-agent harness"
  all present — CONFIRMED)

=== VIEW 4 (Doctor) — key '4', marker 'PATH Doctor' ===
 > [AUDIT + APPLY]   audit report stays read-only until you press enter
 ╭──────────────────────────────────────────────────────────────────────────╮
 │  PATH Doctor                                                             │
 │  Audit                                                                   │
 │  /tmp/tmp.j9cjJdyfvV/.local/bin does not exist                          │
 │  /tmp/tmp.j9cjJdyfvV/.local/bin not on PATH                             │
 │  /home/linuxbrew/.linuxbrew/bin duplicated on PATH                      │
 │  Safe fix preview: wire managed bin dirs into                           │
 │  /tmp/tmp.j9cjJdyfvV/.zshrc, /tmp/tmp.j9cjJdyfvV/.bashrc (centralized)  │
 │  Action: Press enter to wire the managed PATH into your shells.         │
 │  Viewing this screen did not change your shell files.                  │
 ╰──────────────────────────────────────────────────────────────────────────╯
 (all paths shown are under the isolated $FAKE_HOME temp dir, not the real
  machine — CONFIRMED isolation)

=== VIEW 5 (Uninstall) — key '5', marker 'DESTRUCTIVE' ===
 o [STAGED / DESTRUCTIVE]   space marks; enter removes marked items (you'll confirm)
 ── managed elsewhere (59): uv, rg, jq, brew, fd, bat, sd, git, delta, eza,
    fzf, lazygit, gh, yq, zsh, direnv, just, dust, bottom, gum, glow,
    difftastic, gitui, lazydocker, watch, podman, dive, duf, fx, dasel,
    codex, claude, opencode, cursor-agent, antigravity, codegraph, graphify,
    rtk, deno, java, groovy, gradle, ...

=== VIEW 6 (Policies) — key '6', marker 'pip/npm ban' ===
 * [LIVE]   space toggles a policy and applies it now; reversible
 State    Policy                      Requires            Effect
 ○ [off]  pip/npm ban                 none                ...
 ○ [off]  Docker shortcuts            watch               ...
 ○ [off]  Countdown helper            uv                  ...
 ○ [off]  claude skip-permissions     none                ...
 ○ [off]  codex skip-permissions      none                ...
 ○ [off]  apt selective upgrade       none                ...
 ○ [off]  opencode auto-approve       none                ...
 ○ [off]  cursor-agent default model  none                ...
 ○ [off]  Oh-My-Zsh plugins           missing: oh-my-zsh  ...
 (exactly 9 policies rendered; NONE named "agent environment", "permission
  audit", or "agent-policy" — CONFIRMED: subsystem B's Policy factory is not
  surfaced in the live UI, no policy-toggle apply path was ever reached
  either, since the script never sends space/enter)
```

### Part C — no daemon/launchctl code path reached (round-2 LOW open question)

Explicitly closing this with source-level evidence:

1. This machine's `platform.os` is `fedora` (confirmed live:
   `Platform(os='fedora', arch='amd64', immutable=True, has_brew=True, ...)`).
   `setup.py::_build_daemon_policy` returns `None` immediately on any
   non-macOS `platform.os`, so `daemon_default`/`daemon_default_policy_id`
   stay structurally `None`, and `installer/wizard_app.py:1543-1544`'s
   `if self._daemon_default is not None: self._apply_daemon_default_worker()`
   — the ONLY on-mount-without-a-keypress path that can reach a real
   `launchctl bootstrap` call — never fires on this machine.
2. The tmux script's own key sequence is `1 2 3 4 5 6 q`, in that order,
   never `space`/`enter` — so the OTHER path to `installer/daemon.py`'s
   `launchctl bootstrap`/`bootout` calls (via an explicit policy-toggle
   keypress on the Policies view) is also never reached.

Together: zero daemon/launchctl code path is reachable during this
walkthrough, on this machine, with this script — a recorded, evidenced
closing of the round-2 LOW open question, not an implicit or omitted one.

### Task 3 cleanup

`verify-static-and-smoke.sh` was deleted (`rm`) after this evidence was
recorded, per the task's mandatory-deletion requirement. `git status --short`
confirms no production or test file changed and no leftover script exists.

## Post-Review Addendum

Two independent code-review lanes found real gaps in the `MANUAL_REQUIRED`
subsystem this plan introduced (Task 5) but did not fully close. Both are
now fixed, test-first, on top of this plan's original commits:

**HIGH — `outcome.handoff` (manual setup instructions) was never rendered
anywhere.** `installer/engine.py`'s `InstallOutcome.handoff` (populated by
the `host_setup`/`skill_pack` install methods with the actual manual-setup
instructions) had no reader in `installer/render.py`, `installer/app.py`,
`installer/wizard_app.py`, or `installer/catalog_tui.py`. A user who
installed `pi` or any of the 7 `skill_pack` catalog tools saw "Manual setup
required: 1  pi" in the summary and nothing else — no indication of what to
actually do. Fixed by adding `render_handoff` to `installer/render.py`
(prints each `MANUAL_REQUIRED` outcome's `handoff` lines in full) and wiring
it into `installer/app.py::run_wizard` right after `render_skipped`.
Confirmed `wizard_app.py`/`catalog_tui.py` never drive an install/summarize
path of their own (`run_wizard` in `app.py` is the sole call site for
`render_summary`), so no further wiring was needed there.
RED: `9e3944a`. GREEN: `20b2990`.

**MEDIUM — `InstallStatus.MANUAL_REQUIRED` was missing from
`installer/session.py::_UNRESOLVED`.** A dependent tool (e.g. `superpowers`,
which `requires = ["pi"]` in `registry.toml`) was not blocked as
`DEPENDENCY_FAILED` when its dependency only reached `MANUAL_REQUIRED`
(handed off instructions, not actually installed). Fixed by adding
`InstallStatus.MANUAL_REQUIRED` to `_UNRESOLVED`.
RED: `392ee08`. GREEN: `a38c22e`.

Verified together: `make validate && make test` — clean (1825 passed, 1
skipped, 95.45% coverage, floor 90%).

## Deviations

None (original plan). All five tasks were executed as specified in
12.1-01-PLAN.md. One
implementation detail differs from the plan's `read_first` framing without
changing its intent: the plan's Task 3 `read_first` names `View.palette`'s
full prose strings (e.g. "System - prerequisites a fresh machine needs
first") as per-view readiness markers, but live tmux capture (done before
wiring the smoke script) confirmed that prose is rendered only inside the
Ctrl+P command-palette overlay (`installer/wizard_app.py:1344`), not on the
main catalog screen this navigation-only walkthrough actually drives. The
script instead uses content markers that ARE visible on each view's main
screen (`colima`, `glab`, `ponytail`, `PATH Doctor`, `DESTRUCTIVE`, `pip/npm
ban`), preserving the task's actual intent — a distinct, verified per-view
readiness signal for each of the six views — without relying on
non-rendered text. This is noted here per the plan's own instruction to
record any deviation rather than silently diverge.

## Self-Check: PASSED

Verified directly, not inferred:

- `uv run pytest -q` (full suite, no deselect): **1822 passed, 1 skipped, 0
  failed** — zero known-failing tests (D-03 closed).
- `uv run pytest tests/test_registry.py -q`: all pass, including
  `test_reconciliation_tier_audit_confirms_all_11_re_added_rows` and the
  unchanged `test_registry_tier_distribution_is_pinned` (26/22/41) (D-02
  closed).
- `uv run pytest tests/test_engine.py -q`: all pass, including
  `test_host_setup_kind_routes_to_host_setup_handoff` (D-01 merge-diff gap
  closed).
- `uv run pytest tests/test_session.py tests/test_render.py -q`: all pass,
  including `test_summarize_buckets_manual_required` and
  `test_render_summary_counts_and_lists_manual_required` (D-01 KeyError
  closed).
- `bash verify-static-and-smoke.sh`: exited 0 — corrected full-package
  orphan-import audit, `make validate`, and the isolated-HOME six-view tmux
  smoke walkthrough all passed, with real-machine mutation verified (not
  merely asserted) as zero (D-01 tmux walkthrough, D-04 closed); script
  deleted afterward.
- `make validate && make test` on the committed tree: exit 0.
  `make validate` — ruff check/format, pyright (0 errors), bandit, vulture,
  shellcheck all clean. `make test` — **1822 passed, 1 skipped in 78.89s**,
  **95.45% coverage** (floor 90%).
- `git status --short`: only pre-existing, out-of-scope modifications
  (`.planning/STATE.md`, `.planning/state.json`,
  `docs/superpowers/plans/2026-07-28-catalog-reliability-and-scope-reduction.md`,
  `.planning/milestone.lock`) remain — none touched by this plan, none of
  this plan's `files_modified` left uncommitted, no leftover
  `verify-static-and-smoke.sh`.

All four locked decisions (D-01 through D-04) and the round-2 HIGH finding
(`session.py::summarize`'s `MANUAL_REQUIRED` `KeyError`) are closed with real
code fixes, real regression tests, and real, verified (not asserted)
evidence. The reconciled branch is provably, not just apparently, ready for
Phase 12.3's container e2e run.
