---
phase: 12-version-aware-status-update-action
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - installer/update.py
  - installer/download.py
  - installer/apps.py
  - installer/catalog_tui.py
  - installer/wizard_app.py
  - setup.py
  - tests/test_update.py
  - tests/test_download.py
  - tests/test_apps.py
  - tests/test_catalog_tui.py
  - tests/test_wizard_app.py
  - tests/test_setup.py
autonomous: true
cross_ai: true
requirements:
  - REQ-update-action-manager-delegation
  - REQ-pnpm-global-reinstall-mitigation

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The update action resolves OWNERSHIP FIRST via `installer/ownership.py::ManagerOwnership` (Plan 12-02) and dispatches on `ownership.owner`; it never treats `resolve_methods(tool, platform)[0]` as the method that installed the tool. Pressing update on a brew-installed `rg` runs `brew upgrade ripgrep` and never installs a second copy into `~/.local/bin`."
    - "A tool whose `ownership.owner` is `unknown` is refused with a visible reason and ZERO mutation — no subprocess, no filesystem write."
    - "Updating a download/archive/app-kind tool stages to a temporary location, validates, then atomically replaces via `os.replace`, retaining the prior installation until the new one is confirmed good — never a raw overwrite of a live executable (`installer/download.py:123`), never an extraction into the live opt directory (`installer/download.py:128`), never a move into `~/Applications` over an existing bundle (`installer/apps.py:44`)."
    - "`installer/update.py::perform_update` returns a typed `UpdateOutcome` on EVERY path and never raises: `CommandError`, `OSError`, `ExecutorError`, `VersionError`, `ChecksumMismatch`, `UpdateError`, and `PnpmUnavailable` are all caught, because `installer/ui_common.py::run_live` catches only `OSError`/`CommandError` (ui_common.py:41-50)."
    - "The update worker declares `exit_on_error=False` and posts its completion message from a `finally` block, mirroring `installer/wizard_app.py:1545-1571`, so no exception from this path can terminate the TUI or wedge the in-flight flag."
    - "An explicit in-flight guard on the shared `UpdateService` blocks a second update trigger while the first is still running, because a cancelled Textual Worker cannot stop a thread already inside `subprocess.run` (wizard_app.py:266-272) — `exclusive=True` alone is not a concurrency guard."
    - "`installer.pnpm_globals.reinstall_node_globals` fires automatically ONLY when the tool that was just updated is `pnpm` itself — REQ-pnpm-global-reinstall-mitigation's literal text is 'reinstall it together in one invocation after pnpm itself updates'."
    - "A tool declaring a `postinstall` hook has that hook re-dispatched after a successful update, through `installer/postinstall.py::run_postinstall` with the owning `Method`, isolated in its own try/except and carried on `UpdateOutcome.postinstall_warning` — mirroring `installer/engine.py:122-134` — so an updated `codegraph` does not lose its MCP registration."
  artifacts:
    - installer/update.py
  key_links:
    - "CatalogScreen 'u' binding -> UpdateService.begin(tool_id) [in-flight guard, UI thread] -> @work(thread=True, exclusive=True, group='tool-update', exit_on_error=False) -> update.perform_update(UpdateTarget(tool, ownership), runner=run_captured) -> [owner dispatch: brew/cask/pnpm/uv argv, or download.update_download / apps.update_app / executors.execute for installer-owned] -> run_postinstall -> re-probe version -> finally post_message(ToolUpdated) -> UpdateService.end() -> row Ver refresh -> (tool_id == 'pnpm' only) reinstall_node_globals"
---

<objective>
Add the manual update action: press `u` on an outdated Catalog row and this installer delegates to the manager that ACTUALLY owns that tool — brew, cask, pnpm, uv, or its own download/app/script path — resolved from Plan 12-02's ownership model, executed off the event loop, failure-safe for the paths this installer owns, and impossible to fire twice concurrently.

Also lands REQ-pnpm-global-reinstall-mitigation's automatic post-pnpm-update trigger (ROADMAP.md Phase 12 note), reusing the already-shipped `installer/pnpm_globals.py::reinstall_node_globals` mechanism from Phases 4 and 5 unchanged.

Purpose: SC#4 of ROADMAP.md Phase 12 — the "act on it" half of the phase goal.
Output: `installer/update.py` (new), update-safe executor variants in `installer/download.py`/`installer/apps.py`, an `UpdateService` wired from `setup.py` through `UnifiedApp` into all three tier `CatalogScreen`s, and a `u` keybinding.

## Cycle-1 cross-AI review disposition (12-REVIEWS.md)

| Finding | Resolution in this plan |
|---|---|
| HIGH — the "actual manager" guarantee is false (12-REVIEWS.md:127) | `perform_update` takes an `UpdateTarget` carrying a resolved `ManagerOwnership` and dispatches on `ownership.owner`. `resolve_methods` is never indexed for provenance. The `rg` case has a dedicated regression test. |
| HIGH — production mutation dependencies not wired (12-REVIEWS.md:129) | `setup.py` and `tests/test_setup.py` are in `files_modified`; Task 3 threads `Platform`, `Runner`, tag resolver, tools map, and the pnpm-replay closure through `setup.py -> UnifiedApp -> CatalogScreen`. |
| HIGH — existing install executors are not update-atomic (12-REVIEWS.md:131) | Task 2 adds `download.update_download` and `apps.update_app`: stage, validate, `os.replace`, retain the prior installation until confirmed, restore on failure. |
| HIGH — exception isolation incomplete (12-REVIEWS.md:133) | Task 1's `perform_update` catches the full domain-exception set and returns a typed outcome; Task 3's worker uses `exit_on_error=False` and a `finally` post. |
| HIGH — `exclusive=True` does not prevent concurrent updates (12-REVIEWS.md:135) | Task 3 adds an explicit `UpdateService` in-flight guard with a lock, plus a test proving a second keypress is a no-op while the first update holds a latch. |
| MEDIUM — pnpm recovery trigger too broad (12-REVIEWS.md:137) | Narrowed to `tool.id == "pnpm"` only, which is REQUIREMENTS.md's literal wording for REQ-pnpm-global-reinstall-mitigation. Recorded as a decision in the module docstring. |
| MEDIUM — direct executor dispatch skips postinstall hooks (12-REVIEWS.md:139) | Wired: `run_postinstall` is dispatched after a successful update for any tool declaring a hook, isolated exactly as `engine.py` does it. |
| MEDIUM — TUI subprocess output unresolved (12-REVIEWS.md:141) | Decided: production runner is `installer/run.py::run_captured`, never `run_command`. Progress is a status-line in-flight message plus a completion line carrying the re-probed version; on failure the `CommandError` detail (the child's stderr) is shown. No live streaming this phase, stated explicitly. |
| Suggested: re-probe inside the worker, never from the UI handler | Task 3 re-probes inside the worker and carries the fresh `VersionStatus` on `ToolUpdated`. |
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-version-aware-status-update-action/12-CONTEXT.md
@.planning/phases/12-version-aware-status-update-action/12-RESEARCH.md
@.planning/phases/12-version-aware-status-update-action/12-REVIEWS.md

@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md

@installer/engine.py
@installer/executors.py
@installer/download.py
@installer/apps.py
@installer/postinstall.py
@installer/pnpm_globals.py
@installer/uninstall.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `installer/update.py` — ownership-first dispatch, total exception containment, postinstall re-dispatch</name>
  <files>installer/update.py, tests/test_update.py</files>
  <read_first>
    - `installer/ownership.py` as left by Plan 12-02: `Owner`, `ManagerOwnership(tool_id, owner, method, package, current_version, confidence, shadowed)`, and the module docstring's statement that `resolve_methods` ordering is install preference, not provenance.
    - `12-REVIEWS.md:127` in full — the `rg` example: `installer/registry.toml`'s `rg` entry declares `github_release`, native, and `brew` methods, and `installer/resolve.py:12-26` ranks `github_release` at 20 against `brew` at 40, so the top-ranked method is the wrong manager for a brew-installed copy.
    - `installer/engine.py:74-135` (`install_tool`): the `is_installed(tool)` short-circuit at 101-102 returns `ALREADY_INSTALLED` and performs NO action, which is why an update cannot simply call it again; `_perform`'s routing at 58-71; and the postinstall dispatch at 122-134 (`run_postinstall(tool.postinstall, method, runner, tools or {})` wrapped in its own `except Exception`, warning carried on the outcome, never turning success into failure) — the exact shape this task mirrors.
    - `installer/executors.py:26-27` (`ExecutorError`), `:375-376` (`_brew`), `:456-458` (`_uv_tool`), `:482-487` (`_cask`, including the `--appdir` argument and why it exists), `:503-508` (`execute`).
    - `installer/download.py:96-115` (`install_download`, `ExecContext`) and `installer/apps.py:44-76` (`install_app`).
    - `installer/checksums.py` (`ChecksumMismatch`), `installer/versions.py:19-20` (`VersionError`), `installer/pnpm_globals.py`'s `PnpmUnavailable`.
    - `installer/guards.py:315-332` (`real_pnpm`).
    - `installer/locations.py:40-42` (`applications_dir`).
    - `installer/uninstall.py:130-145` (`_manager_name`) — the formula/cask-name resolver to reuse rather than re-derive.
    - `.planning/REQUIREMENTS.md`'s `REQ-pnpm-global-reinstall-mitigation` entry — read its literal wording ("after `pnpm` itself updates") before implementing the trigger, and its 2026-09-05 R-03 note assigning the automatic trigger to this phase.
    - `.planning/phases/12-version-aware-status-update-action/12-CONTEXT.md` D-01 (immediate, no confirmation prompt).
  </read_first>
  <behavior>
    - `installer.update.UpdateError(RuntimeError)`.
    - `installer.update.UpdateTarget(tool: Tool, ownership: ManagerOwnership)` (frozen dataclass). Construction is the caller's job; `perform_update` never resolves ownership itself, so the mutating path can only act on ownership that was already resolved from real evidence.
    - `installer.update.UpdateOutcome(tool_id: str, status: Literal["updated", "failed", "unknown-owner", "unsupported"], owner: str, detail: str = "", errors: tuple[Exception, ...] = (), postinstall_warning: str | None = None, replayed_globals: tuple[str, ...] = ())` (frozen dataclass).
    - `installer.update.resolve_update_argv(target: UpdateTarget) -> list[str] | None`, dispatching on `target.ownership.owner`:
      * `"brew"` -> `["brew", "upgrade", package]` where `package` is `ownership.package` (already resolved through `_manager_name`).
      * `"cask"` -> `["brew", "upgrade", "--cask", f"--appdir={applications_dir()}", package]` — the `--appdir` argument matches `_cask`'s install-side reason (keep the bundle in userspace, never `/Applications`).
      * `"pnpm"` -> `[real_pnpm(), "update", "-g", package]`, raising `UpdateError` when `real_pnpm()` returns `None`.
      * `"uv"` -> `["uv", "tool", "upgrade", package]`.
      * `"installer"` -> `None` (sentinel: this installer owns the artifacts, so the update runs through Task 2's update-safe executors rather than a manager verb).
      * `"unknown"` -> `None` is NOT used here; `perform_update` short-circuits on `unknown` before calling this function, and this function raises `UpdateError` if it is ever reached with `unknown`.
    - `installer.update.perform_update(target, *, platform, runner, resolve_tag, tools: Mapping[str, Tool], postinstall=run_postinstall) -> UpdateOutcome`:
      * `ownership.owner == "unknown"` -> `UpdateOutcome(status="unknown-owner", detail=<why: which inventories were unreadable, or that no manager claimed it>)`, with no subprocess and no filesystem write.
      * `ownership.method` is a `sdkman`-kind method -> `UpdateOutcome(status="unsupported", detail=...)`. SDKMAN's own model is `sdk upgrade <candidate>`, a materially different shape neither CONTEXT.md nor REQUIREMENTS.md asked this phase to build; the detail string says so.
      * `owner` in brew/cask/pnpm/uv -> `runner(resolve_update_argv(target))`.
      * `owner == "installer"` -> route on `ownership.method.kind`: `download.DOWNLOAD_KINDS` -> `download.update_download(method, ExecContext(runner=runner, platform=platform, resolve_tag=resolve_tag))`; `apps.APP_KINDS` -> `apps.update_app(method, runner)`; `"script"` -> `executors.execute(method, runner)` (a vendor install script is also the vendor's own documented update path, and it is the only update mechanism such a tool has; the docstring records this).
      * On success and when `target.tool.postinstall` is set, dispatch `postinstall(tool.postinstall, ownership.method, runner, tools)` inside its OWN `try/except Exception`, storing the result or the crash text on `postinstall_warning`. A postinstall problem never turns `updated` into `failed`, exactly as `engine.py:122-134` decides for installs.
      * The whole body is wrapped so that `CommandError`, `OSError`, `ExecutorError`, `VersionError`, `ChecksumMismatch`, `UpdateError`, and `PnpmUnavailable` all produce `UpdateOutcome(status="failed", errors=(exc,), detail=str(exc))`. `perform_update` has no path that raises.
    - `installer.update.should_replay_node_globals(target: UpdateTarget) -> bool`: `True` only when `target.tool.id == "pnpm"`. The module docstring records the decision and its source: REQUIREMENTS.md's REQ-pnpm-global-reinstall-mitigation says "reinstall it together in one invocation after `pnpm` itself updates", and 12-REVIEWS.md:137 flagged replaying after every node-package update as a significant unrequested secondary mutation, since `reinstall_node_globals` deliberately includes globals unknown to this registry (`pnpm_globals.py:665`).
  </behavior>
  <action>
    Create `installer/update.py` per `<behavior>`. Every subprocess argv is a Python list handed to `runner`; no `shell=True` and no f-string command anywhere. Package, formula, cask, npm, and pypi identifiers come only from `ownership.package` (itself derived from `method.params` and `tool.cmd`, both trusted registry data) — never from a cached version string, a manager's reported "latest", or a GitHub tag.

    Write `tests/test_update.py`:
    - The `rg` regression: a `Tool` declaring `github_release` and `brew` methods, an `UpdateTarget` whose ownership is `owner="brew"`, `package="ripgrep"`, asserts the runner received exactly `["brew", "upgrade", "ripgrep"]` and that no download executor was invoked. The same test asserts `resolve_methods(tool, platform)[0].kind == "github_release"`, so the test documents the trap.
    - `owner="unknown"` produces `status="unknown-owner"` and a runner spy with zero invocations.
    - A `sdkman`-owned method produces `status="unsupported"` and zero runner invocations.
    - `owner="pnpm"` with a patched `real_pnpm` returning `/opt/homebrew/bin/pnpm` puts that absolute path at argv index 0; a `real_pnpm` returning `None` produces `status="failed"` carrying an `UpdateError`, not a raised exception.
    - `owner="cask"` includes the `--appdir` argument.
    - Exception containment: one parametrised test raising each of `CommandError`, `OSError`, `ExecutorError`, `VersionError`, `ChecksumMismatch`, `UpdateError`, and `PnpmUnavailable` from the runner or executor seam, asserting every case returns `status="failed"` and that `perform_update` did not raise.
    - Postinstall: a tool with `postinstall` set gets the hook dispatched with the OWNING method after a successful update; a hook raising an exception yields `status="updated"` with a populated `postinstall_warning`; a tool with `postinstall=None` never calls the hook seam.
    - `should_replay_node_globals` is `True` for a tool whose id is `pnpm` and `False` for another `node`-owned tool, with the test naming REQ-pnpm-global-reinstall-mitigation in its docstring.
  </action>
  <verify>
    <automated>uv run pytest tests/test_update.py -q</automated>
    <fails_when>non-zero exit; or the `rg` case builds a download/GitHub argv instead of `brew upgrade ripgrep`; or any of the seven domain exceptions escapes `perform_update` instead of becoming `status="failed"`; or `should_replay_node_globals` returns True for a non-pnpm node-owned tool; or a `postinstall`-declaring tool's hook is not dispatched after a successful update</fails_when>
    <automated>uv run pyright installer</automated>
    <fails_when>non-zero exit under strict pyright</fails_when>
  </verify>
  <acceptance_criteria>
    - `perform_update` accepts an `UpdateTarget` carrying a `ManagerOwnership` and never calls `resolve_ownership` or indexes a `resolve_methods` result itself.
    - `owner="unknown"` performs zero mutations, proven by a zero-invocation runner spy.
    - A parametrised test covers all seven named domain exceptions and asserts `perform_update` never raises.
    - `should_replay_node_globals` is true only for `tool.id == "pnpm"`, and the module docstring cites REQUIREMENTS.md's wording as the reason.
    - A `postinstall`-declaring tool has its hook re-dispatched with the owning `Method` after a successful update, with failures carried as a warning that leaves `status="updated"`.
  </acceptance_criteria>
  <done>`installer/update.py` dispatches updates on resolved ownership, refuses to act on an unknown owner, contains every domain exception behind a typed outcome, re-dispatches declared postinstall hooks, and narrows the pnpm-globals replay to a pnpm self-update.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update-safe executors — stage, validate, atomically replace, keep the old copy until the new one is confirmed</name>
  <files>installer/download.py, installer/apps.py, tests/test_download.py, tests/test_apps.py</files>
  <read_first>
    - `installer/download.py` in full (209 lines): `install_download` (96-115), `_install_unverified` (118-150 — the raw branch at 123-127 writes `curl -o <link>` DIRECTLY over the live executable, and the archive branch at 128-150 extracts into the live `opt_dir`), `_install_verified` (153-187 — the temp-dir fetch-and-verify flow to reuse), `_place_verified` (190-209 — placement from a temp path, the closest existing analogue of what this task needs), `_resolve_target`, `DownloadTarget`, `ExecContext`.
    - `installer/apps.py` in full (76 lines): `install_app` (44-76) — its `mv "$tmp/x/"<app> ~/Applications/` moves directly into place with no replace-safety, so a second install with the bundle already present behaves differently from the first; `cli_spec` (23-41).
    - `installer/locations.py:35-42` (`opt_dir`, `applications_dir`, `ensure_dir`).
    - `installer/omz.py:190-222` and `installer/atomic.py` as left by Plan 12-01 — the sibling-temp + `os.replace` reasoning to carry into directory and bundle replacement.
    - `12-REVIEWS.md:131` — the HIGH finding naming download.py:123, download.py:128, and apps.py:44 as the three unsafe update paths.
    - `tests/test_download.py` and `tests/test_apps.py` — the existing fake-runner conventions these tests extend.
  </read_first>
  <behavior>
    - `installer.download.update_download(method: Method, ctx: ExecContext) -> bool`: resolves the target exactly as `install_download` does, then:
      * Always fetches into a fresh `tempfile.mkdtemp(prefix="tools-installer-update-")` staging directory through `ctx.runner` (the same DI seam the install path uses), never straight to the destination. When the method declares a checksum, verification happens in staging and a mismatch raises `ChecksumMismatch` BEFORE anything live is touched. Returns the same "was verified" boolean `install_download` returns.
      * Raw single-file methods: write to `<link>.tools-installer.new` in the SAME directory as `link`, `chmod +x`, then `os.replace(new, link)` — atomic on POSIX, and the live binary is never partially written.
      * Archive methods: extract into `<opt>.tools-installer.new`, then move the existing `opt` aside with `os.replace(opt, <opt>.tools-installer.old)` when it exists, `os.replace(<opt>.new, opt)`, re-create the `ln -sf` symlink, and only then `shutil.rmtree` the `.old` directory. If the second `os.replace` fails, restore with `os.replace(<opt>.old, opt)` and raise `ExecutorError` naming the restored state.
      * Every staging directory is removed in a `finally`, mirroring `_install_verified`'s own cleanup.
    - `installer.apps.update_app(method: Method, runner: Runner) -> None`: validates `app`/`cli` params before any side effect exactly as `install_app` does, extracts the zip into a temp directory with `ditto -x -k` through `runner`, then moves the existing `~/Applications/<app>` aside to `<app>.tools-installer.old` when present, moves the staged bundle into place, re-creates the CLI symlink when declared, and removes the `.old` bundle only after the move succeeded. A failure after the aside-move restores the original bundle and raises `ExecutorError`.
    - Neither function calls `installer/status.py::is_installed` and neither goes through `installer/engine.py::install_tool` — the `ALREADY_INSTALLED` short-circuit at engine.py:101-102 is precisely what makes those unusable for an update.
  </behavior>
  <action>
    Add `update_download` to `installer/download.py` and `update_app` to `installer/apps.py` per `<behavior>`, next to their install counterparts so the two flows are read side by side. Fetch and extraction keep using `ctx.runner`/`runner` so the existing shell-quoting discipline and test seams are unchanged; staging-to-live placement is Python-side (`os.replace`, `shutil.rmtree`, `Path.chmod`) so failure handling is real and testable without a live subprocess.

    Tests in `tests/test_download.py`, all on `tmp_path` with a fake runner that materialises files where a real `curl`/`tar`/`unzip` would:
    - Raw update: a pre-existing executable at the link path with known content is replaced with the staged content, and at no point is the link path opened for writing directly by the fetch step (assert the fetch argv's output path is inside the staging directory, not the link path).
    - Raw update where the staged fetch fails: the pre-existing executable is byte-identical to its original content afterwards.
    - Archive update: a pre-existing `opt` directory containing an extra marker file is fully replaced by the staged tree, the symlink still resolves, and no `.tools-installer.old` or `.tools-installer.new` path survives.
    - Archive update where placement fails after the aside-move: the original `opt` directory is restored with its marker file intact and `ExecutorError` is raised.
    - Checksum mismatch during an update raises `ChecksumMismatch` and leaves the live artifacts untouched.

    Tests in `tests/test_apps.py`:
    - A pre-existing bundle directory is replaced and the CLI symlink is re-created.
    - A failure after the aside-move restores the original bundle.
    - Param validation (`app` containing a path separator, a traversing `cli`) raises before any filesystem mutation.
  </action>
  <verify>
    <automated>uv run pytest tests/test_download.py tests/test_apps.py -q</automated>
    <fails_when>non-zero exit; or the raw-update test finds the fetch argv writing directly to the live link path; or a failed update leaves the pre-existing executable or bundle altered; or a `.tools-installer.new`/`.tools-installer.old` path survives a successful update</fails_when>
  </verify>
  <acceptance_criteria>
    - `installer/download.py::update_download` and `installer/apps.py::update_app` exist and are the only paths the update action uses for installer-owned download/archive/app tools.
    - Every update fetch lands in a staging directory; the live path is written only by `os.replace` or a `mv` of an already-complete staged tree.
    - A failure at any point leaves the prior installation intact, proven by three dedicated failure tests (raw fetch failure, archive placement failure, app move failure).
    - Checksum verification happens in staging, before the live artifact is touched.
    - No leftover `.tools-installer.new` or `.tools-installer.old` path remains after a successful update.
  </acceptance_criteria>
  <done>Updating a download, archive, or app tool stages the new copy, validates it, replaces the live one atomically, and keeps the previous installation recoverable until the replacement is confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the update action into the Catalog TUI from setup.py, with an explicit in-flight guard and a crash-proof worker</name>
  <files>installer/catalog_tui.py, installer/wizard_app.py, setup.py, tests/test_catalog_tui.py, tests/test_wizard_app.py, tests/test_setup.py</files>
  <read_first>
    - `installer/update.py` and the update-safe executors as left by Tasks 1 and 2 (read the committed state).
    - `installer/catalog_tui.py` as left by Plan 12-02: `BINDINGS` (155-158, where `r`/`d` already live), `__init__` and the `version_refresh` parameter added by Plan 12-01, `_row_cells`, `_detail_text`, and `_version_statuses`.
    - `installer/ui_common.py:41-50` (`run_live` — catches ONLY `OSError`/`CommandError`, which is why `perform_update` must contain everything else itself) and `:53-58` (`highlighted_key` — how the row under the cursor is read).
    - `installer/wizard_app.py:262-275` (`_start_globals_audit` and its comment explaining that `exclusive=True` cancels the Worker but cannot stop a thread already inside `subprocess.run`), `:429-441` (`_reinstall_globals_worker`), `:1545-1571` (`_apply_daemon_default_worker` — the `exit_on_error=False` + `try/finally`-post pattern to mirror verbatim), `:1430-1512` (`UnifiedApp.__init__` and the documented default-`None` convention for optional closures), `:1452-1463` (the three `CatalogScreen` constructions).
    - `installer/pnpm_globals.py:690-724` (`reinstall_node_globals(packages, *, runner=run_captured, resolve_pnpm=real_pnpm, policy=...)`) and `:396-414` (`pnpm_global_packages`) — the mechanism the automatic trigger reuses unchanged.
    - `installer/run.py:64-71` (`run_captured`) and `:23-30` (`run_command`, which INHERITS the terminal and must not be used while Textual owns it).
    - `setup.py:326-356` — the existing `policy`/`_reinstall_globals` closures already built in `_build_app`, which the update service reuses rather than rebuilding.
    - `12-CONTEXT.md` D-01 (update runs immediately, no extra confirmation prompt) and `12-REVIEWS.md:135`/`:141` (the concurrency-guard and output findings).
    - `tests/test_wizard_app.py:144-196` (the `_app()` DI factory and `_settle`) and `:1984-2019` (the `threading.Event()` latch technique).
  </read_first>
  <behavior>
    - `installer.update.UpdateService(*, platform, runner=run_captured, resolve_tag=resolve_github_tag, tools: Mapping[str, Tool], managed_packages: Callable[[], tuple[str, ...] | None], replay_globals: Callable[[Sequence[str]], tuple[str, ...]])`:
      * `begin(tool_id) -> bool`: under a `threading.Lock`, returns `False` when any update is already in flight (and records nothing), otherwise records `tool_id` and returns `True`. `end()` clears it. `in_flight -> str | None` is a read-only property the screen renders from.
      * `run(target) -> UpdateOutcome`: calls `update.perform_update(...)`, and when `update.should_replay_node_globals(target)` and the outcome is `updated`, calls `managed_packages()` and, on a non-`None` result, `replay_globals(packages)`, recording the replayed specs on `UpdateOutcome.replayed_globals`. A failure inside the replay is caught and appended to `detail` as a secondary warning; it never downgrades the primary `updated` status.
      * The service is constructed once and shared by all three tier screens, which is what makes the in-flight guard global rather than per-screen.
    - `installer.catalog_tui.ToolUpdated(Message)`: `tool_id: str`, `outcome: UpdateOutcome | None`, `status: VersionStatus | None`, `error: str | None`.
    - `CatalogScreen.BINDINGS` gains `Binding("u", "update_tool", "update", show=True)`.
    - `CatalogScreen.action_update_tool()` (UI thread, no subprocess, no blocking call): reads the highlighted row id; no-ops when there is no update service, no ownership from `version_refresh.ownership_of(tool_id)`, or the row's `VersionStatus.outdated` is not `True`. When `ownership.owner == "unknown"` it sets a status-line message naming the tool and does not start a worker. When `updates.begin(tool_id)` returns `False` it sets a status-line message naming the in-flight tool and returns — this is the explicit concurrency guard. Otherwise it sets an in-flight status line and starts the worker. Per D-01 there is no confirmation modal.
    - `@work(thread=True, exclusive=True, group="tool-update", exit_on_error=False) def _update_tool_worker(self, tool_id)`: builds the `UpdateTarget`, calls `run_live(lambda: self._updates.run(target))`, re-probes the tool's version INSIDE the worker to build the post-update `VersionStatus`, and posts `ToolUpdated` from a `finally` so the message lands on every path, with `self._updates.end()` in that same `finally`.
    - `on_tool_updated` updates `_version_statuses[tool_id]`, calls `self._browser.reload(self._adapter())`, and writes one status line: the new version on success, the outcome `detail` on failure, plus a secondary segment naming the replayed pnpm globals when `replayed_globals` is non-empty.
    - Output policy, recorded in `CatalogScreen`'s docstring: the update runs through `run_captured`, so the child's stdout never reaches the rendered frame; the user sees an in-flight line, then the re-probed version or the child's stderr from `CommandError.detail`. Live streaming of manager output is explicitly not built this phase.
  </behavior>
  <action>
    Add `UpdateService` to `installer/update.py` per `<behavior>`. Add the binding, action, worker, message, and handler to `installer/catalog_tui.py`. Add a keyword-only `updates: UpdateService | None = None` to `UnifiedApp.__init__` and pass it into all three `CatalogScreen` constructions, following the same documented default-`None` convention as the existing optional closures.

    In `setup.py::_build_app`, construct one `UpdateService` with the detected `platform`, `run_captured`, `resolve_github_tag`, `{tool.id: tool for tool in tools}`, `pnpm_globals.pnpm_global_packages`, and the EXISTING `_reinstall_globals` closure already defined at setup.py:352-355 (so preview and effect keep sharing one `NodeInstallPolicy` object, as that closure's comment requires). Pass it as `updates=` to `UnifiedApp(...)`. Construction only, per `.claude/architecture.md` rule 4.

    Tests in `tests/test_catalog_tui.py`: the `u` binding exists and is shown; `action_update_tool` on a row whose status is not outdated invokes nothing.

    Tests in `tests/test_wizard_app.py`:
    - Concurrency guard: an injected update runner that blocks on a `threading.Event()`; press `u`, assert the screen still handles input, press `u` again on the same and on a different row, then release and settle — assert the runner was invoked exactly once and that the second press produced a status-line message naming the in-flight tool.
    - Ownership refusal: a row whose resolved ownership is `owner="unknown"` yields zero runner invocations and a visible message.
    - The `rg` end-to-end delegation: a fixture `rg` declaring `github_release` and `brew`, ownership resolved to brew, pressing `u` invokes exactly `["brew", "upgrade", "ripgrep"]` and no download executor.
    - Worker crash containment: an update runner raising a domain exception outside `run_live`'s tuple leaves the app running, clears the in-flight flag, and still posts `ToolUpdated`.
    - The automatic replay: updating a fixture tool whose id is `pnpm` calls a spied replay closure exactly once with the package set from `managed_packages`; updating a DIFFERENT `node`-owned tool calls it zero times.
    - Off-the-loop end-to-end: with the latch held, assert a keypress still has an observable effect; release, settle, then assert the row's Ver cell shows the re-probed post-update version and that no confirmation modal was ever pushed (assert the screen stack depth is unchanged), per D-01.

    Tests in `tests/test_setup.py`: `_build_app` wires an `UpdateService` whose `platform` is the one it received and whose replay closure is the same object `_reinstall_globals` binds, and all three tier screens share that one service instance.
  </action>
  <verify>
    <automated>uv run pytest tests/test_catalog_tui.py tests/test_wizard_app.py tests/test_setup.py -k "update" -q</automated>
    <fails_when>non-zero exit; or the second `u` press while the latch is held reaches the runner; or an unknown-owner row triggers any runner invocation; or the `rg` fixture produces a download argv; or updating a non-pnpm node-owned tool invokes the replay spy; or the off-the-loop test omits an observable keypress effect while the latch is held; or a confirmation modal is pushed</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit anywhere in the suite</fails_when>
    <automated>make validate</automated>
    <fails_when>non-zero exit from ruff, pyright, bandit, or vulture on the exact tree about to be committed</fails_when>
  </verify>
  <acceptance_criteria>
    - Pressing `u` on an outdated row runs the owning manager's command with no confirmation prompt, off the event loop.
    - A second `u` press while an update is in flight is refused by `UpdateService.begin`, proven by a single-invocation runner spy under a held latch.
    - A row whose ownership is `unknown` produces zero mutations and a visible message.
    - The update worker declares `exit_on_error=False` and posts `ToolUpdated` plus calls `UpdateService.end()` from a `finally`.
    - The pnpm-globals replay fires only for `tool.id == "pnpm"`, proven by both a positive and a negative test.
    - `setup.py::_build_app` wires one `UpdateService` shared by all three tier screens, reusing the existing `_reinstall_globals` closure rather than building a second `NodeInstallPolicy`.
    - The production update runner is `run_captured`; `grep -n "run_command" installer/update.py installer/catalog_tui.py` returns nothing.
  </acceptance_criteria>
  <done>The update action is live from `setup.py`'s composition root: ownership-first, immediate per D-01, off the event loop, guarded against concurrent triggers, crash-contained, and followed by an automatic pnpm-globals replay only when pnpm itself was the tool updated.</done>
  <reversibility rating="costly">Per D-01 no confirmation prompt gates the update, matching this project's existing install-action convention. Rated `costly` rather than `one-way`: Task 2's update-safe executors keep the prior installation until the replacement is confirmed, and a manager-delegated upgrade can be reversed by the user through the real manager (`brew install <formula>@<version>`, `pnpm add -g <pkg>@<version>`, `uv tool install <pkg>==<version>`). This installer ships no downgrade action of its own, so undoing an unwanted update means leaving its UI. No `checkpoint:decision` is inserted, consistent with D-01's locked decision.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keypress -> mutating subprocess | The `u` binding crosses from user input directly into a real `brew upgrade`/`pnpm update -g`/`uv tool upgrade` or a filesystem replacement, with no confirmation gate (D-01) |
| Resolved ownership -> which manager is mutated | An ownership misattribution here does not merely misreport; it runs the wrong manager against a live installation |
| Staging directory -> live installation | Task 2's update-safe executors move content from a temp path over a running tool's own files |
| Update outcome -> automatic secondary mutation | A successful pnpm self-update triggers a SECOND mutating action (`reinstall_node_globals`) with no additional user gate |
| Background worker thread -> main render thread | The update outcome and re-probed status cross back into Textual UI state via `post_message` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Tampering / Injection | Update argv construction in `resolve_update_argv` and the installer-owned executor path | high | mitigate | Every argv element is a list item handed to `runner`, never a shell string. Formula, cask, npm, and pypi identifiers come only from `ownership.package`, derived from `method.params`/`tool.cmd` (trusted registry data fixed at build time) — never from a cached version, a manager-reported "latest", or a GitHub tag. A spoofed version string can change displayed text only. |
| T-12-13 | Spoofing | Ownership-directed dispatch of a mutating action | high | mitigate | `perform_update` acts only on a `ManagerOwnership` resolved from installer-artifact presence or real manager inventory membership (Plan 12-02), refuses `owner="unknown"` outright with zero side effects, and has a regression test for the `rg` case where install-preference ranking and real ownership disagree. |
| T-12-14 | Denial of Service / data loss | Replacing a live executable, opt tree, or app bundle | high | mitigate | Task 2 stages every update, verifies declared checksums before touching anything live, replaces with `os.replace`, retains the prior installation until the replacement succeeds, and restores it on failure — with three dedicated failure tests. |
| T-12-15 | Denial of Service | Concurrent update triggers | medium | mitigate | `UpdateService.begin` is a lock-guarded explicit in-flight guard, because `exclusive=True` cancels a Worker but cannot stop a thread already inside `subprocess.run` (wizard_app.py:266-272). A test proves the second keypress is refused. |
| T-12-16 | Denial of Service | An uncaught domain exception in the update worker | high | mitigate | `perform_update` contains all seven relevant domain exception types behind a typed outcome, and the worker declares `exit_on_error=False` with a `finally` that both posts `ToolUpdated` and clears the in-flight flag — mirroring `wizard_app.py:1545-1571`. Textual's default worker behavior would otherwise crash the whole app. |
| T-12-08 | Elevation of Privilege | The automatic post-pnpm-update `reinstall_node_globals` trigger | medium | mitigate | Narrowed to a pnpm self-update only, per REQUIREMENTS.md's own wording. It calls the already-shipped, already-tested Phase 4/5 mechanism Doctor's manual `r` key already invokes, replaying only the ALREADY-INSTALLED pnpm-managed set through one absolute-path pnpm invocation; it never installs anything new. |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 on the exact tree being committed.
- `make validate` passes (ruff, pyright strict, bandit, vulture).
- `grep -n "install_tool" installer/update.py` returns nothing — the update path deliberately bypasses the engine's `ALREADY_INSTALLED` short-circuit.
</verification>

<success_criteria>
- An update action exists on the Catalog screen, delegates to the manager that actually owns the tool, refuses to act when ownership is unknown, runs immediately with no confirmation (D-01), and never blocks the event loop.
- Installer-owned update paths are failure-safe: stage, validate, atomically replace, keep the prior copy until confirmed.
- A second update cannot start while the first is running; no exception from the update path can terminate the TUI.
- REQ-pnpm-global-reinstall-mitigation's automatic trigger fires with no separate user action, scoped to a pnpm self-update, reusing `reinstall_node_globals` unchanged.
</success_criteria>

<output>
Create `.planning/phases/12-version-aware-status-update-action/12-03-SUMMARY.md` when done
</output>
