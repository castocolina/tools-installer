---
phase: 12-version-aware-status-update-action
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - installer/update.py
  - installer/wizard_app.py
  - installer/catalog_tui.py
  - tests/test_update.py
  - tests/test_wizard_app.py
autonomous: true
cross_ai: true
requirements:
  - REQ-update-action-manager-delegation

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Triggering update on an outdated brew-installed tool runs `brew upgrade <formula>` (or `brew upgrade --cask <cask>`), never this installer's own install path — per D-01, immediately, with no confirmation prompt."
    - "Triggering update on an outdated node (pnpm)-managed tool resolves pnpm via `real_pnpm()`'s absolute path (installer/guards.py:315-332), never this installer's own argv-conditional pnpm wrapper."
    - "Triggering update on an outdated uv-tool-managed tool runs `uv tool upgrade <pypi_pkg>`."
    - "Triggering update on an outdated script/github_release/tarball/app-kind tool re-invokes that tool's OWN install method directly (bypassing installer.engine.install_tool's is_installed short-circuit, which would otherwise report ALREADY_INSTALLED and do nothing) — these kinds are re-installed to update, unlike brew/pnpm/uv-tool which have a distinct upgrade verb."
    - "After any update whose resolved method was node (pnpm)-kind, or when the updated tool_id is pnpm itself, installer.pnpm_globals.reinstall_node_globals is invoked automatically with no separate user action — this is REQ-pnpm-global-reinstall-mitigation's automatic post-pnpm-update trigger, noted in ROADMAP.md's Phase 12 section as landing in this phase alongside REQ-update-action-manager-delegation."
    - "The update action runs on a Textual @work thread worker (mirroring DoctorScreen's _reinstall_globals_worker), never blocking the event loop during a real brew/pnpm/uv-tool subprocess call."
  artifacts:
    - installer/update.py
  key_links:
    - "CatalogScreen's new 'u' keybinding -> @work(thread=True, exclusive=True, group=\"tool-update\") worker -> run_live(lambda: update.perform_update(...)) -> post_message(ToolUpdated) -> row Ver-column refresh -> (node-kind only) installer.pnpm_globals.reinstall_node_globals(...) auto-invoked"
---

<objective>
Add the manual "update" action: delegate to the tool's actual owning manager (brew/pnpm/uv-tool, or this installer's own re-install path for script/github_release/tarball/app kinds) rather than assuming a single executor owns every tool — reusing `UninstallState`'s existing "managed elsewhere" classification concept (per CONTEXT.md), not a parallel one. Also lands REQ-pnpm-global-reinstall-mitigation's automatic post-pnpm-update trigger (ROADMAP.md Phase 12 note), reusing the already-built, already-tested `installer.pnpm_globals.reinstall_node_globals` mechanism from Phase 4/5.

Purpose: SC#4 of ROADMAP.md's Phase 12 — the "act on it" half of the phase goal, and the requirement `REQ-pnpm-global-reinstall-mitigation` was waiting on.
Output: `installer/update.py` (new), a new "update" keybinding + worker on the Catalog screen, wired end to end.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-version-aware-status-update-action/12-CONTEXT.md
@.planning/phases/12-version-aware-status-update-action/12-RESEARCH.md

@installer/uninstall.py
@installer/executors.py
@installer/engine.py
@installer/download.py
@installer/apps.py
@installer/pnpm_globals.py
@installer/guards.py
@installer/resolve.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: `installer/update.py` — resolve the real update command per method kind, including the re-install-to-update nuance for script/github_release/tarball/app</name>
  <files>installer/update.py, tests/test_update.py</files>
  <read_first>
    - `.planning/phases/12-version-aware-status-update-action/12-RESEARCH.md` section 5 in full (the executor-to-manager mapping table, the two-architecture finding, and the `real_pnpm()` absolute-path requirement)
    - `installer/uninstall.py` lines 130-154 (`_manager_hint`/`_managed_hint` — the "managed elsewhere" classification concept this task extends, per CONTEXT.md's explicit reuse instruction) and lines 169-223 (`classify_tools`/`UninstallState.MANAGED` — confirms there is no literal "managed elsewhere" enum value, only this hint-building pattern)
    - `installer/executors.py` lines 375-458 (`_brew`, `_cask`, `_node`, `_uv_tool` — the INSTALL argv each builds; this task builds the corresponding UPDATE argv, a different verb, not a call into these functions)
    - `installer/engine.py` lines 74-135 (`install_tool` — read the `is_installed(tool)` short-circuit at the top carefully: calling `install_tool` again on an already-installed script/github_release/tarball/app tool returns `ALREADY_INSTALLED` and performs NO action — this is why "update" for these kinds cannot simply call `install_tool` again)
    - `installer/download.py` (`install_download`, `ExecContext`) and `installer/apps.py` (`install_app`) — the direct executor entry points `install_tool._perform` calls internally, which THIS task's update path must call directly to bypass the short-circuit
    - `installer/guards.py` lines 315-332 (`real_pnpm()`)
    - `installer/resolve.py` (`resolve_methods` — returns platform-applicable methods in ladder order; the FIRST one is the method that installed the tool)
  </read_first>
  <behavior>
    - `resolve_update_argv(tool: Tool, method: Method) -> list[str] | None`: `brew` -> `["brew", "upgrade", method.params["formula"] or tool.cmd]`; `cask` -> `["brew", "upgrade", "--cask", method.params["cask"] or tool.cmd, f"--appdir={applications_dir()}"]`; `node` -> `[real_pnpm(), "update", "-g", method.params["npm_pkg"]]` (raises `UpdateError` when `real_pnpm()` is `None`); `uv-tool` -> `["uv", "tool", "upgrade", method.params["pypi_pkg"]]`; any other kind (`script`/`github_release`/`tarball`/`app`/`sdkman`) -> `None` (sentinel: "no manager argv — re-run this tool's own install method instead").
    - `perform_update(tool: Tool, platform: Platform, runner: Runner, resolve_tag: TagResolver) -> UpdateOutcome` (new frozen dataclass: `tool_id: str`, `status: Literal["updated","failed","no-method"]`, `method_kind: str | None`, `errors: tuple[Exception, ...] = ()`): resolves `methods = resolve_methods(tool, platform)`; empty -> `no-method`; takes `methods[0]` as the active method; if `resolve_update_argv(tool, methods[0])` is not `None`, runs it via `runner` directly; else (script/github_release/tarball/app) calls the SAME per-kind executor `install_tool` would have called — `download.install_download`/`apps.install_app`/`executors.execute` — directly against `methods[0]`, bypassing `is_installed`'s short-circuit entirely (the whole point: these kinds update BY re-running their own install action). `sdkman` is explicitly out of scope this phase (returns `no-method` with a clear reason) — its own `sdk upgrade <candidate>` verb is a materially different shape CONTEXT.md's Claude's-Discretion did not ask this phase to build.
  </behavior>
  <action>
    Create `installer/update.py` with `UpdateError(RuntimeError)`, `UpdateOutcome`, `resolve_update_argv`, and `perform_update` per `<behavior>` above. Every subprocess argv is a Python list passed to `runner`, never a shell string (this project's existing no-`shell=True` discipline, `installer/run.py`) — package/formula/cask/npm/pypi names come ONLY from `method.params`/`tool.cmd` (trusted registry data), never from a cached or remote version string.
  </action>
  <verify>
    <automated>uv run pytest tests/test_update.py -q</automated>
    <fails_when>non-zero exit, or `resolve_update_argv` for a `node`-kind method does not resolve pnpm via a patched `real_pnpm` (never a bare `"pnpm"` string in the returned argv), or `perform_update` on a `github_release`-kind tool does not call the download executor directly (i.e. it incorrectly short-circuits on `is_installed`)</fails_when>
  </verify>
  <done>`installer/update.py` exists with a tested per-kind update-argv resolver and a `perform_update` orchestrator that correctly bypasses `install_tool`'s `is_installed` short-circuit for script/github_release/tarball/app kinds while delegating brew/node/uv-tool kinds to their real manager's upgrade verb.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the update action into the Catalog TUI, with D-01's immediate-no-confirmation UX and the automatic post-pnpm-update reinstall trigger</name>
  <files>installer/wizard_app.py, installer/catalog_tui.py, tests/test_wizard_app.py</files>
  <read_first>
    - `installer/wizard_app.py` lines 429-441 (`_reinstall_globals_worker` — the exact `@work(thread=True, exclusive=True, group=...)` + `run_live` + `post_message` shape to mirror for the new update worker) and lines 155-158 (`CatalogScreen.BINDINGS` — where the new `u` binding is added, parallel to the existing `r`/`d` recommends bindings)
    - `installer/pnpm_globals.py` lines 659-725 (`reinstall_argv`/`reinstall_node_globals`/`reinstall_preview` — the existing, already-tested Phase 4/5 mechanism this task's automatic trigger reuses verbatim, never reimplements)
    - `installer/update.py` as left by Task 1 (read the actual committed state)
    - `.planning/phases/12-version-aware-status-update-action/12-CONTEXT.md` D-01 (no confirmation prompt, same UX as install)
  </read_first>
  <action>
    Add `Binding("u", "update_tool", "update", show=True)` to `CatalogScreen.BINDINGS`. Add `action_update_tool(self) -> None` that reads the highlighted row's tool id, looks up its `VersionStatus` from `self._version_statuses`; if `outdated is not True`, no-op (nothing to update); else — per D-01, immediately, no confirmation modal — spawns `@work(thread=True, exclusive=True, group="tool-update") def _update_tool_worker(self, tool_id: str) -> None:` that calls `run_live(lambda: update.perform_update(tool, platform, runner, resolve_tag))` and `self.post_message(ToolUpdated(tool_id, outcome, error))` (new `Message` subclass). In `on_tool_updated`, refresh that tool's row (re-probe `is_installed`, clear/update its `VersionStatus` to reflect the new state) and call the browser's re-render. THEN — the automatic trigger — when `outcome.method_kind == "node"` (the tool just updated was pnpm-managed) OR `tool_id == "pnpm"`, immediately (still inside the SAME worker, before posting `ToolUpdated`, or in a chained second worker call — either is acceptable as long as no user keypress is required) call `installer.pnpm_globals.reinstall_node_globals(packages, policy=..., resolve_pnpm=real_pnpm, ...)` for pnpm's current live-managed set (obtained via `installer.pnpm_globals.pnpm_global_packages()`), exactly mirroring what Doctor's manual `r`-key press already does — this is D-01's no-confirmation rule applied to the trigger too. A failure in this auto-trigger step must not overwrite/hide the original update's own success — surface it as a secondary warning on the status line, never as the primary outcome.
  </action>
  <verify>
    <automated>uv run pytest tests/test_wizard_app.py -k "update_tool or tool_update or pnpm_reinstall_trigger" -q</automated>
    <fails_when>non-zero exit, or pressing `u` on a fixture row whose `VersionStatus.outdated` is `True` does not invoke a fake `runner` with the expected update argv, or updating a fixture `node`-kind tool does not also invoke a spied `reinstall_node_globals`</fails_when>
  </verify>
  <done>Pressing `u` on an outdated row immediately runs the real manager-delegated update command with no confirmation prompt, off the event loop; updating a pnpm-managed tool automatically triggers `reinstall_node_globals` with no separate user action.</done>
  <reversibility rating="costly">Per D-01, no confirmation prompt gates the update (matches this project's existing install-action convention — an upgrade is not treated as more dangerous than an install). Rated `costly`, not `one-way`: an upgraded package is not destroyed data — the prior version can be reinstalled by the user directly via the real manager (`brew install <formula>@<old-version>`, `pnpm add -g <pkg>@<old-version>`, `uv tool install <pkg>==<old-version>`) — but this installer itself has no built-in downgrade/rollback action, so undoing an unwanted update requires leaving this project's own UI. No `checkpoint:decision` inserted, consistent with D-01's explicit locked decision.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End-to-end proof — off-thread update, row refresh, and the automatic reinstall trigger together</name>
  <files>tests/test_wizard_app.py</files>
  <read_first>
    - `tests/test_wizard_app.py` lines 1984-2019 (`test_doctor_audit_runs_off_the_event_loop`, the `threading.Event()` latch pattern) and lines 182-196 (`_settle`)
    - `installer/wizard_app.py`'s `CatalogScreen` update wiring as left by Task 2 (read the actual committed state)
  </read_first>
  <action>
    Add one Pilot-driven end-to-end test: construct a Catalog screen with a fixture `node`-kind tool marked outdated (via an injected `VersionStatus`), an injected `runner` that blocks on a `threading.Event()` (proving the update runs off-thread, same technique as `test_doctor_audit_runs_off_the_event_loop`), and a spied `reinstall_node_globals`. Press `u`, assert the screen still handles a keypress while the update is blocked, release the latch, `await` a bounded settle loop (mirroring `_settle`), then assert: (1) the row's Ver column now reads "up to date" (or the fixture's post-update value), (2) `reinstall_node_globals` was called exactly once with the correct package set, (3) no confirmation modal was ever pushed (per D-01).
  </action>
  <verify>
    <automated>uv run pytest tests/test_wizard_app.py -k "update_end_to_end or update_pilot" -q</automated>
    <fails_when>non-zero exit, or the test passes without ever asserting the screen handled a keypress while the update's `threading.Event()` latch was still held (i.e. the off-thread proof is missing, not just the end state)</fails_when>
  </verify>
  <done>A single Pilot test proves the whole update path end to end: off-thread execution, row refresh, the automatic pnpm-reinstall trigger, and D-01's no-confirmation UX, in one committed, real, runnable test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keypress -> mutating subprocess | The `u` keybinding crosses from user input directly into a real `brew upgrade`/`pnpm update -g`/`uv tool upgrade`/re-install subprocess call, with no confirmation gate (D-01) |
| Update outcome -> automatic secondary mutation | A successful pnpm-managed update automatically triggers a SECOND mutating action (`reinstall_node_globals`) with no additional user gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Tampering / Injection | Update-argv construction (`resolve_update_argv`, the re-install fallback path) | high | mitigate | Every argv element is a Python list item passed straight to `runner` (never a shell string); formula/cask/npm/pypi identifiers come only from `method.params`/`tool.cmd` (trusted registry data set at build time), never from a cached or remote version string — a spoofed `tag_name`/manager-reported "latest" value can influence DISPLAYED text only, never which command or package name is executed. |
| T-12-08 | Elevation of Privilege | The automatic post-pnpm-update `reinstall_node_globals` trigger runs without a user confirmation gate | medium | mitigate | The trigger calls the SAME, already-shipped, already-tested Phase 4/5 mechanism (`installer/pnpm_globals.py::reinstall_node_globals`) Doctor's manual `r` keybinding already invokes with no additional privilege — it replays only the ALREADY-INSTALLED pnpm-managed set (never installs anything new), scoped to packages this project's own registry declares, per the mechanism's own existing threat register from Phase 4/5. |

</threat_model>

<verification>
- `uv run pytest tests/test_update.py tests/test_wizard_app.py -q` exits 0.
- `make validate` passes on the exact tree before commit.
</verification>

<success_criteria>
- An "update" action exists on the Catalog screen, delegates to the tool's real owning manager (brew/pnpm/uv-tool) or this installer's own re-install path (script/github_release/tarball/app), runs immediately with no confirmation (D-01), and never blocks the event loop.
- REQ-pnpm-global-reinstall-mitigation's automatic post-pnpm-update trigger fires with no separate user action, reusing the existing `reinstall_node_globals` mechanism unchanged.
</success_criteria>

<output>
Create `.planning/phases/12-version-aware-status-update-action/12-03-SUMMARY.md` when done
</output>
