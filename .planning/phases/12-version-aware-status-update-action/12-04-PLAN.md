---
phase: 12-version-aware-status-update-action
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .claude/architecture.md
  - tests/test_ownership.py
  - tests/test_docs.py
autonomous: true
cross_ai: true
requirements:
  - REQ-manager-drift-alerting

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "REQ-manager-drift-alerting is NOT implemented in Phase 12. This plan records its deferral with the structural reason, in `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`'s Phase 12 SC#5 wording, and `.planning/PROJECT.md` — so no downstream verifier reads SC#5 in isolation and registers a silent miss."
    - "The deferral rationale names three independently-checkable facts: `brew outdated` lists only ALREADY-INSTALLED formulae and casks (confirmed 2026-09-07 from `brew help outdated`: 'List installed casks and formulae that have an updated version available'), so it structurally cannot detect an uninstalled brew alternative to a pnpm-managed tool; ZERO tools in the current `installer/registry.toml` declare BOTH a `node`/`uv-tool` method AND a `brew`/`cask` method (12-RESEARCH.md section 6); and shipping the helper unwired would violate `.claude/architecture.md` rule 5, which forbids a production helper with zero callers."
    - "No unwired production helper is created by this phase. `installer/manager_versions.py` contains no `has_declared_manager_drift` or equivalent drift function."
    - "`.claude/architecture.md` gains a Phase 12 section stating the load-bearing distinction the cross-AI review found: `installer/resolve.py::resolve_methods` answers install PREFERENCE and its ordering is never provenance; `installer/ownership.py` answers which manager actually OWNS an installed tool, from installer-artifact presence and real manager inventory membership; and the mutating update action refuses to act when ownership is unknown."
  artifacts: []
  key_links:
    - "REQUIREMENTS.md status table row for REQ-manager-drift-alerting -> ROADMAP.md Phase 12 SC#5 amended wording -> .claude/architecture.md's Phase 12 section -> the guard test in tests/test_docs.py that keeps the recorded findings from silently rotting away"
---

<objective>
Close Phase 12 honestly. Two things are recorded rather than built.

First, `REQ-manager-drift-alerting` (ROADMAP.md Phase 12 SC#5, CONTEXT.md D-02's stretch item) is DEFERRED and not implemented in this phase. The cycle-1 cross-AI review independently verified that the data source the original 12-04 plan proposed cannot observe the target condition, that no real catalog row exercises it, and that shipping the helper unwired would violate this repository's own architecture rules. Rather than build a function that cannot fire, this plan records the deferral where every downstream reader looks.

Second, Phase 12 introduced genuinely new mechanisms — ownership resolution as a concept distinct from install-preference ranking, a persisted version cache, update-safe executors, and an ownership-first mutating action. Every prior phase in this project recorded its mechanism in `.claude/architecture.md`; this plan does the same, so the preference-versus-provenance distinction that drove this phase's entire replan is written down rather than living only in a review file.

Purpose: an accurate, checkable disposition for REQ-manager-drift-alerting, plus the Phase 12 decision consolidation this project performs at the end of every phase (see the Phase 7, 8, and 9 precedents in ROADMAP.md).
Output: amended `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`, and `.claude/architecture.md`, plus guard tests.

## Why deferral, not implementation — the cycle-1 review finding (12-REVIEWS.md:163-197)

The reviewer ran `brew help outdated` directly and reported that it "defines the command as listing **installed** formulae/casks with updates", so a node-managed tool's declared-but-uninstalled brew alternative can never appear in its output. Re-confirmed here on 2026-09-07: `brew help outdated`'s own usage line reads "List installed casks and formulae that have an updated version available." The original plan's synthetic fake returning that formula therefore constructed a state the real command cannot produce.

The review raised two further blocking points: the helper had no active-versus-alternative comparison (its inputs carried no active pnpm version, so it could not prove brew was newer), and the plan explicitly stated the helper would not be wired into the status loop — which `.claude/architecture.md` rule 5 forbids ("A shared helper with zero production callers is deleted or adopted at its duplicate sites — never kept 'for later'. Only its own test keeping it covered is the tell.").

The reviewer's own recommendation was "Prefer deferring 12-04 entirely. It is non-MVP, has no real registry case, and the current proposal violates the no-orphan-helper rule." That is the option taken. Implementing it properly would require a structured `brew info <formula> --json` availability query for an UNINSTALLED formula, a normalized `active_current < brew_available` comparison built on Plan 12-02's ownership data, and a drift state threaded through `VersionStatus`, the Ver cell, the detail text, and a Pilot test — a materially larger feature than D-02's "minimal version if scope allows", with no registry row to prove it against. It is recorded as a future candidate, gated on a real catalog case appearing.

## What is NOT being claimed

This plan does not implement REQ-manager-drift-alerting. It is listed in this plan's `requirements` field because this plan owns that requirement's recorded DISPOSITION for the milestone, not its delivery. Any end-of-phase verification should read REQ-manager-drift-alerting as deferred, with the rationale in `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-version-aware-status-update-action/12-CONTEXT.md
@.planning/phases/12-version-aware-status-update-action/12-RESEARCH.md
@.planning/phases/12-version-aware-status-update-action/12-REVIEWS.md

@CLAUDE.md
@.claude/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the REQ-manager-drift-alerting deferral in every place a reader checks</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, tests/test_ownership.py</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` line 90 (`REQ-manager-drift-alerting`'s full text) and line 158 (its row in the status table, currently `Deferred (stretch, not MVP)`).
    - `.planning/ROADMAP.md` Phase 12's Success Criteria block, specifically SC#5's current wording ("Stretch, deferred/non-MVP; attempt a minimal version if scope allows per 2026-09-04 discuss-phase"), and the Phase 12 `Plans:` list, whose 12-04 line still describes a declared-methods-only drift alert.
    - `.planning/ROADMAP.md`'s Phase 5 planning-note and replan-note blocks — the established house style for recording a criterion whose wording was amended after research or review overturned its premise.
    - `.planning/PROJECT.md`'s Key Decisions table — the format for a one-line phase decision entry.
    - `.planning/phases/12-version-aware-status-update-action/12-CONTEXT.md` D-02, whose "attempt a minimal version if scope allows" and "planner's call" clause is the authority this deferral exercises.
    - `12-RESEARCH.md` section 6 in full — the zero-qualifying-rows count and the reasoning about guessing an undeclared formula name.
    - `12-REVIEWS.md:163-197` — the four findings and the reviewer's own deferral recommendation.
    - `installer/manager_versions.py` as left by Plan 12-02 — confirm no drift helper was added there.
  </read_first>
  <behavior>
    - `.planning/REQUIREMENTS.md`'s REQ-manager-drift-alerting entry gains an in-place amendment note dated 2026-09-07 recording: the deferral decision, the three structural reasons (uninstalled brew alternatives are invisible to `brew outdated`; zero registry rows declare both a node/uv-tool and a brew/cask method; an unwired helper violates architecture rule 5), the authority for the call (12-CONTEXT.md D-02's explicit "planner's call" clause), and what a future implementation would need (a structured `brew info <formula> --json` availability query, a normalized active-versus-available comparison built on `installer/ownership.py`, and a drift state threaded through `VersionStatus`, the Ver cell, and a Pilot test).
    - Its status-table row changes from `Deferred (stretch, not MVP)` to `Deferred (not implemented in Phase 12 — see entry)`.
    - `.planning/ROADMAP.md`'s Phase 12 SC#5 is amended in place, in the same bracketed-amendment style Phases 2, 5, and 8 already use, to state that the criterion was not delivered and why, so an end-of-phase verifier reading the numbered criteria in isolation does not register a silent miss.
    - `.planning/ROADMAP.md`'s Phase 12 `Plans:` list is updated so each of the four lines describes what its plan actually does after this replan, including 12-04 as the deferral-and-consolidation plan.
    - `.planning/PROJECT.md`'s Key Decisions table gains one row for the deferral.
    - `tests/test_ownership.py` gains a guard test asserting `installer/manager_versions.py` and `installer/ownership.py` expose no drift-alert function, so a later well-meaning edit cannot reintroduce a zero-caller helper without the suite noticing.
  </behavior>
  <action>
    Amend the four planning documents per `<behavior>`, using `Edit` with scoped replacements — never a whole-file rewrite of `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md`, both of which carry twelve phases of history outside the edit window.

    Add the guard test to `tests/test_ownership.py`: import `installer.manager_versions` and `installer.ownership`, and assert that neither module's public namespace contains a callable whose name contains the substring `drift`. The test's docstring names 12-REVIEWS.md's architecture-rule-5 finding as the reason it exists.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ownership.py -k "drift" -q</automated>
    <fails_when>non-zero exit, or the guard test passes vacuously because it imported nothing — the test must import both modules and assert on their real namespaces</fails_when>
    <automated>uv run python -c "import pathlib,sys; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(); sys.exit(0 if 'REQ-manager-drift-alerting' in t and 'Deferred (not implemented in Phase 12' in t else 1)"</automated>
    <fails_when>non-zero exit, meaning the amended status-table wording is absent from REQUIREMENTS.md</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md`'s REQ-manager-drift-alerting entry carries a dated 2026-09-07 amendment naming all three structural reasons and what a future implementation would require.
    - Its status-table row reads `Deferred (not implemented in Phase 12 — see entry)`.
    - `.planning/ROADMAP.md`'s Phase 12 SC#5 carries an in-place amendment note in the existing bracketed style, and the Phase 12 `Plans:` list describes all four plans as they now stand.
    - `.planning/PROJECT.md`'s Key Decisions table has one row recording the deferral.
    - A guard test asserts no drift-alert callable exists in `installer/manager_versions.py` or `installer/ownership.py`.
    - No whole-file rewrite of `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` appears in the diff; both are scoped edits.
  </acceptance_criteria>
  <done>Every document a reader consults — the requirement, the roadmap criterion, the plan list, and the project decision log — states that REQ-manager-drift-alerting was deferred in Phase 12 and exactly why, and a test prevents a zero-caller drift helper from reappearing.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate Phase 12's mechanisms into .claude/architecture.md</name>
  <files>.claude/architecture.md, tests/test_docs.py</files>
  <read_first>
    - `.claude/architecture.md` in full, especially "The five rules" (lines 7-25, in particular rule 4 on `setup.py` being wiring only and rule 5 on orphan helpers), the "Phase 3", "Phase 9", and "Platform-unavailable catalog rows" sections — the house style for a phase-mechanism section: prose, load-bearing reasons, and an explicit statement of what must NOT be conflated.
    - `installer/ownership.py`, `installer/version_cache.py`, `installer/version_status.py`, `installer/update.py`, `installer/download.py::update_download`, and `installer/apps.py::update_app` as left by Plans 12-01 through 12-03 — write the section from the committed code, not from the plans.
    - `installer/resolve.py:9-26` and `:68-71` — the `_RANK` ladder and `resolve_methods`, so the section states the distinction precisely.
    - `12-REVIEWS.md:15` — the review's own one-sentence framing of the shared false premise across all four original plans, which is the sentence this section exists to make permanent.
  </read_first>
  <behavior>
    - `.claude/architecture.md` gains a `## Phase 12: version-aware status and the update action` section stating, in this project's existing prose style:
      * `installer/resolve.py::resolve_methods` returns platform-applicable methods ordered by the `_RANK` install-preference ladder. That ordering answers "which method should we try first when installing", and it is never provenance. `rg` declares `github_release` (rank 20) and `brew` (rank 40), so a brew-installed `rg` would be misread as a GitHub download by anything that treats index 0 as the owning manager.
      * `installer/ownership.py` answers the different question. Ownership comes from installer-artifact presence (`installer/uninstall.py::plan_uninstall`) and real manager inventory membership (`brew list --versions`, `pnpm list -g --json` via the existing `pnpm_global_packages`, `uv tool list`). An inventory that could not be read is `None`, never an empty set, and an unresolvable owner is `unknown` — fail closed. The two concepts must not be merged.
      * The mutating update action acts only on a resolved `ManagerOwnership` and refuses `unknown` outright. This is why ownership is a prerequisite of `installer/update.py` rather than a convenience.
      * Update is not install re-run: `installer/engine.py::install_tool`'s `is_installed` short-circuit returns `ALREADY_INSTALLED` before resolving a method, and the install-side download/archive/app executors write over live artifacts. `installer/download.py::update_download` and `installer/apps.py::update_app` stage, validate, replace with `os.replace`, and retain the prior installation until the replacement is confirmed.
      * `installer/version_cache.py` is this project's first persisted JSON state file (`~/.local/state/tools-installer/versions.json`, the `~/.local/...` userspace convention from `installer/locations.py`). It is written through `installer/atomic.py::atomic_write_text`, the one sibling-temp-plus-`os.replace` implementation this repository has, shared with `installer/omz.py`. A malformed entry degrades to "never checked"; it is never trusted and never reaches an argv.
      * The refresh fires on catalog view entry for stale entries only, mirroring `DoctorScreen`'s own screen-entry audit, and is bounded by a per-pass fetch budget plus a failed-attempt backoff so an offline or rate-limited machine cannot retry on every navigation.
      * A one-line pointer that REQ-manager-drift-alerting was deferred in this phase, with the reason and a pointer to `.planning/REQUIREMENTS.md` for the full record.
    - `tests/test_docs.py` (new) asserts that `.claude/architecture.md` contains the load-bearing phrases this section exists to preserve, following the same "the comment is the mechanism; the test is what keeps it from rotting away unnoticed" pattern `tests/test_registry.py` already uses for registry comments. Required substrings: `installer/ownership.py`, `install-preference ladder`, `never provenance`, `plan_uninstall`, `update_download`, and `atomic_write_text`.
  </behavior>
  <action>
    Append the Phase 12 section to `.claude/architecture.md` per `<behavior>`, using `Edit` with a scoped replacement anchored on the end of the existing "Platform-unavailable catalog rows" section. Write it from the committed code produced by Plans 12-01 through 12-03; if any detail in `<behavior>` disagrees with what actually shipped, the shipped code wins and the section records what is true.

    Create `tests/test_docs.py` with the substring guard described in `<behavior>`. Keep it to one test function reading the file once and asserting membership of each required substring, with a failure message naming which one is missing.
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs.py -q</automated>
    <fails_when>non-zero exit, or the guard test does not name the specific missing substring in its assertion message</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit anywhere in the suite</fails_when>
    <automated>make validate</automated>
    <fails_when>non-zero exit from ruff, pyright, bandit, or vulture on the exact tree about to be committed</fails_when>
  </verify>
  <acceptance_criteria>
    - `.claude/architecture.md` has a Phase 12 section covering: the preference-versus-provenance distinction with the `rg` example; ownership's evidence sources and its fail-closed `unknown`; why update is not install re-run; the update-safe executors; the version cache and its shared atomic writer; and the refresh/backoff policy.
    - The section includes a one-line pointer to the REQ-manager-drift-alerting deferral recorded in `.planning/REQUIREMENTS.md`.
    - `tests/test_docs.py` asserts all six required substrings are present and names the missing one on failure.
    - The architecture.md edit is a scoped append, not a whole-file rewrite.
    - `uv run pytest -q` and `make validate` both pass on the tree being committed.
  </acceptance_criteria>
  <done>Phase 12's mechanisms and the one distinction that drove its entire replan are written into `.claude/architecture.md` and guarded by a test, so the next contributor reads them in the repository rather than in a review file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(none new)* | This plan writes only documentation and a guard test. It adds no runtime code, no subprocess call, no filesystem mutation outside the repository's own tracked files, and no new input crosses into the installer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Repudiation | An undocumented requirement deferral | low | mitigate | The deferral is recorded in `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/PROJECT.md` with its date, its authority (12-CONTEXT.md D-02), and its three verifiable structural reasons, so an end-of-milestone audit cannot mistake a deliberate, reasoned deferral for an unnoticed gap. |
| T-12-17 | Tampering | Silent loss of the recorded findings | low | mitigate | `tests/test_docs.py` and the drift-helper guard test in `tests/test_ownership.py` make deletion of either record a failing test rather than an invisible edit — the same mechanism `tests/test_registry.py` already applies to load-bearing registry comments. |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 on the exact tree being committed.
- `make validate` passes (ruff, pyright strict, bandit, vulture).
- `git diff --stat` shows no `installer/` source file modified by this plan.
</verification>

<success_criteria>
- REQ-manager-drift-alerting's deferral is recorded, dated, reasoned, and discoverable from the requirement, the roadmap criterion, and the project decision log.
- No zero-caller drift helper exists anywhere in `installer/`, and a test enforces that.
- Phase 12's mechanisms — ownership versus install preference, the version cache and its shared atomic writer, and the update-safe executors — are documented in `.claude/architecture.md` and guarded against silent removal.
</success_criteria>

<output>
Create `.planning/phases/12-version-aware-status-update-action/12-04-SUMMARY.md` when done
</output>
