---
phase: 12-version-aware-status-update-action
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - installer/manager_versions.py
  - tests/test_manager_versions.py
autonomous: true
cross_ai: true
requirements:
  - REQ-manager-drift-alerting

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A synthetic tool declaring BOTH a node (pnpm) method and a brew/cask method, currently active via the node method, with a newer version available via the declared brew formula, is reported as a manager-drift alert — the alert half only, never auto-remediation (per REQ-manager-drift-alerting's own explicit deferral of the GitHub-issue/registry-edit half)."
    - "The check reads only DECLARED registry data (a tool's own tool.methods) — it never guesses an undeclared brew formula name for a tool that only declares a node/uv-tool method, per 12-RESEARCH.md section 6's live-verified finding that zero real tools in the current registry declare both kinds, so a guessing strategy would have no correctness signal to validate against."
  artifacts: []
  key_links:
    - "has_declared_manager_drift() is a pure function, callable from the same per-tool loop installer/version_status.py already runs, but not wired into that loop by this plan (no real registry row exercises it yet — wiring it live is a follow-up once/if a tool ever declares both a node/uv-tool AND a brew/cask method)"
---

<objective>
Attempt the minimal version of manager-drift alerting per CONTEXT.md D-02: if a tool is installed via pnpm/npm and a newer/safer version is available via brew, surface a distinct alert. This is explicitly the LAST, optional/stretch task of the phase — sequenced after Plan 12-02 (REQ-manager-version-resolution, its own stated dependency) and Plan 12-03 (the MVP update action), attempted only because CONTEXT.md's D-02 asked for a minimal attempt if scope allows, not because it has a real proving case in the current catalog today.

Purpose: SC#5 of ROADMAP.md's Phase 12 (stretch, deferred/non-MVP, explicitly not blocking).
Output: `has_declared_manager_drift()` in `installer/manager_versions.py`, tested against a synthetic fixture.

IMPORTANT (read before starting): 12-RESEARCH.md section 6 found that ZERO tools in the current `installer/registry.toml` declare both a `node`/`uv-tool` method AND a `brew`/`cask` method — Phase 5's own research explicitly rejected brew as a fallback for `mmdc` (the one tool this scenario would most plausibly apply to). This function is therefore correct-but-currently-unexercised-by-any-real-catalog-row. That is an accepted, documented consequence of D-02's own framing ("attempt a minimal version if scope allows... not guaranteed to ship"), not a defect in this plan — do not attempt to manufacture a real registry entry to "prove" this feature; that would be scope creep beyond what D-02 asked for.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-version-aware-status-update-action/12-CONTEXT.md
@.planning/phases/12-version-aware-status-update-action/12-RESEARCH.md

@installer/manager_versions.py
@installer/model.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `has_declared_manager_drift()` — declared-methods-only drift check, per D-02's minimal-attempt framing</name>
  <files>installer/manager_versions.py, tests/test_manager_versions.py</files>
  <read_first>
    - `.planning/phases/12-version-aware-status-update-action/12-CONTEXT.md` D-02 (the exact scope/framing this task must not exceed)
    - `.planning/phases/12-version-aware-status-update-action/12-RESEARCH.md` section 6 in full (the zero-real-proving-case finding and its rationale)
    - `installer/manager_versions.py` as left by Plan 12-02 (read the actual committed state — `brew_outdated`'s shape is reused here)
    - `installer/model.py` lines 121-153 (`Tool.methods: tuple[Method, ...]`, `Method.kind`, `Method.params`)
  </read_first>
  <behavior>
    - `DriftAlert(tool_id: str, active_kind: str, alt_kind: str, alt_current: str | None, alt_latest: str)` (frozen dataclass).
    - `has_declared_manager_drift(tool: Tool, active_kind: str, *, brew_outdated: Callable[..., dict[str, str] | None] = brew_outdated) -> DriftAlert | None`: returns `None` immediately when `active_kind not in {"node", "uv-tool"}` (no drift concept applies otherwise) OR when `tool.methods` contains no `brew`/`cask`-kind method (the "zero real rows today" case — this is the common path for every current catalog tool and must return `None` cheaply, not attempt any lookup). When a `brew`/`cask` method IS declared, resolve its `formula`/`cask` param name, look it up in `brew_outdated()`'s result; a hit (the brew-side package IS in brew's own outdated set, meaning a newer version than what the user has via brew is available — read `brew_outdated`'s `{name: current_version}` shape from Plan 12-02 exactly, this is comparing the DECLARED alternative manager's availability, not this tool's OWN currently-active-manager version) returns a `DriftAlert`; a miss returns `None`.
  </behavior>
  <action>
    Add `DriftAlert` and `has_declared_manager_drift` to `installer/manager_versions.py` per `<behavior>` above. Write `tests/test_manager_versions.py` cases using a SYNTHETIC `Tool` fixture (per the file's own docstring note explaining why: no real registry row qualifies today, per 12-RESEARCH.md section 6) with both a `node` method (`npm_pkg="example-pkg"`) and a `brew` method (`formula="example-pkg"`) declared, an injected `brew_outdated` fake returning `{"example-pkg": "2.0.0"}`, asserting a `DriftAlert` is returned; a second case with the SAME fixture but `active_kind="brew"` (not node/uv-tool) asserts `None`; a third case using a REAL catalog tool (e.g. `mmdc`, which per Phase 5's research declares only `node`, no `brew`/`cask`) asserts `None` — this is the explicit "real catalog tools correctly return None today" regression guard naming why, so a future registry change re-adding a brew fallback is what would ever flip this on for a real row, not a bug in this function.
  </action>
  <verify>
    <automated>uv run pytest tests/test_manager_versions.py -k "drift" -q</automated>
    <fails_when>non-zero exit, or the real `mmdc` catalog-tool case returns anything other than `None`, or the synthetic dual-method fixture with a brew-side hit fails to return a `DriftAlert`</fails_when>
  </verify>
  <done>`has_declared_manager_drift()` exists, is tested against both a synthetic dual-method fixture (the positive case) and a real catalog tool with no brew fallback (the documented negative case), and reads only registry-declared data — never an undeclared/guessed formula name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brew_outdated() output -> drift comparison | The same brew JSON output from Plan 12-02 crosses into this stretch feature's comparison logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Spoofing | `brew_outdated()`'s returned version string, as read by `has_declared_manager_drift` | low | mitigate | Reuses Plan 12-02's already-mitigated `brew_outdated`/`parse_brew_outdated_json` (T-12-05) unchanged — no new parsing surface is introduced by this task; a malformed brew response already degrades to `None` upstream, which this function already treats as "no drift" (fail-closed on the alert, never a false alarm). |

</threat_model>

<verification>
- `uv run pytest tests/test_manager_versions.py -q` exits 0.
- `make validate` passes on the exact tree before commit.
</verification>

<success_criteria>
- `has_declared_manager_drift()` exists, is correctly scoped to declared registry data only, and is tested against both the synthetic positive case and a real negative case — with the zero-real-proving-case finding explicitly documented rather than papered over.
</success_criteria>

<output>
Create `.planning/phases/12-version-aware-status-update-action/12-04-SUMMARY.md` when done
</output>
