---
phase: 12-version-aware-status-update-action
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - installer/manager_versions.py
  - installer/version_status.py
  - installer/wizard_app.py
  - installer/catalog_tui.py
  - tests/test_manager_versions.py
  - tests/test_version_status.py
  - tests/test_wizard_app.py
  - tests/test_catalog_tui.py
autonomous: true
cross_ai: true
requirements:
  - REQ-manager-version-resolution
  - REQ-background-version-refresh-worker

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A `brew`/`cask`-installed catalog tool with a newer formula/cask version renders `current → latest` in the Ver column the same way a `github_release` tool does, via `brew outdated --json=v2` with `HOMEBREW_NO_AUTO_UPDATE=1` set (live-verified: without it, `brew outdated` triggers a slow `brew auto-update` first)."
    - "A `node` (pnpm-managed)-installed tool's outdated state is read via `pnpm outdated -g --json` (NOT `--format json`, which silently produces non-JSON on this pnpm version) and is correctly parsed even though pnpm exits 1 (not 0) when outdated packages exist — the existing `installer/run.py::run_output` cannot be reused as-is for this call because it discards stdout on any non-zero exit."
    - "A `uv-tool`-installed tool's outdated state is read from `uv tool list --outdated`'s plain-text output (there is no JSON flag for this subcommand, live-verified against `--help`), keyed by the PyPI distribution name matching the registry's `pypi_pkg` param."
    - "The same 7-day staleness cache from Plan 12-01 (`installer/version_cache.py`) governs manager-backed tools identically to github_release tools — one cache, one staleness rule, keyed by `tool_id` regardless of source."
  artifacts:
    - installer/manager_versions.py
  key_links:
    - "version_status.py's per-tool resolution branches on resolve_methods(tool, platform)[0].kind: github_release -> resolve_github_release_status (12-01); brew/cask/node/uv-tool -> a new resolve_manager_status() built on installer/manager_versions.py, sharing the same VersionCacheEntry/is_stale/VersionStatus shapes"
---

<objective>
Extend the version-aware status mechanism from Plan 12-01 (github_release tools only) to cover the three other manager-backed method kinds this catalog actually uses: `brew`/`cask`, `node` (pnpm), and `uv-tool`. Every exact command and its real, live-verified quirks (pnpm's exit-1-on-outdated behavior, `uv tool list --outdated`'s lack of a JSON flag, brew's auto-update side effect) come from 12-RESEARCH.md section 4 — nothing here is assumed from documentation.

Purpose: completes REQ-manager-version-resolution and extends REQ-background-version-refresh-worker's coverage beyond github_release, so SC#2/SC#3 of ROADMAP.md's Phase 12 hold for the WHOLE catalog, not one method kind.
Output: `installer/manager_versions.py` (new), extended `installer/version_status.py`, the Ver column and refresh worker now cover brew/cask/node/uv-tool tools.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-version-aware-status-update-action/12-CONTEXT.md
@.planning/phases/12-version-aware-status-update-action/12-RESEARCH.md

@installer/pnpm_globals.py
@installer/guards.py
@installer/resolve.py
@installer/run.py
@installer/version_cache.py
@installer/version_status.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: `installer/manager_versions.py` — brew/pnpm/uv-tool outdated parsers, each command live-verified in 12-RESEARCH.md section 4</name>
  <files>installer/manager_versions.py, tests/test_manager_versions.py</files>
  <read_first>
    - `.planning/phases/12-version-aware-status-update-action/12-RESEARCH.md` section 4 in full — the exact commands, exit codes, env vars, and output shapes for brew/pnpm/uv-tool, all with real captured output to use as test fixtures
    - `installer/pnpm_globals.py` lines 1-80 (module docstring + imports), 264-297 (`_load_projects`/`parse_global_packages` — the pure-parser pattern), 383-416 (`_run_list`/`pnpm_global_packages` — the IO-wrapper-around-a-pure-parser pattern to mirror exactly), and 535-560 (`audit_node_globals` — "None means unknown, not empty" convention)
    - `installer/guards.py` lines 315-332 (`real_pnpm()` — MUST be used to resolve pnpm by absolute path, never a bare `"pnpm"` argv element, or the call could hit this installer's own argv-conditional pnpm wrapper)
    - `installer/run.py` lines 36-61 (`run_output`/`CommandError` — read carefully: `CommandError` from `run_output` never exposes `exc.stdout`, only `exc.stderr` via `detail`, which is exactly why pnpm's exit-1-on-outdated case needs a dedicated wrapper, not `run_output` reused as-is)
    - `installer/model.py` lines 121-153 (`Method.params` — `formula`/`cask` params for brew/cask methods, `npm_pkg` for node, `pypi_pkg` for uv-tool)
  </read_first>
  <behavior>
    - `brew_outdated(*, runner_out: Callable[[list[str]], str] = ...) -> dict[str, str] | None`: runs `["brew", "outdated", "--json=v2"]` with env `HOMEBREW_NO_AUTO_UPDATE=1, HOMEBREW_NO_ENV_HINTS=1` merged into the current environment (never replacing it — `PATH` etc. must survive), parses the JSON's `formulae` array into `{name: current_version}`-keyed... actually `{name: latest_version}` (the field the caller compares the LOCALLY installed formula version against is `current_version` from brew's own JSON — read the exact field names from 12-RESEARCH.md section 4.1's quoted sample: `{"name": "ast-grep", "installed_versions": ["0.45.1"], "current_version": "0.45.3", ...}` — return `{name: current_version}` per entry, keyed by formula/cask name). A second call with `--cask` appended covers casks; return `None` (not `{}`) on any `CommandError`/`OSError`, per the "None means unknown" convention `pnpm_global_packages` already establishes.
    - `pnpm_outdated_global(*, resolve_pnpm: Callable[[], str | None] = real_pnpm, runner: Callable[[list[str]], tuple[int, str]] = ...) -> dict[str, str] | None`: runs `[pnpm, "outdated", "-g", "--json"]` (absolute-path `pnpm` from `resolve_pnpm()`; `None` pnpm -> `None` result) via a DEDICATED runner shape that returns `(returncode, stdout)` and treats returncode `0` OR `1` as a successful read (parse `stdout` as JSON either way — pnpm's own convention, live-verified in 12-RESEARCH.md section 4.2), any OTHER returncode or a JSON parse failure -> `None`. Parses `{"pkg": {"current": "...", "latest": "...", ...}, ...}` into `{pkg: latest}`.
    - `uv_tool_outdated(*, runner_out: Callable[[list[str]], str] = ...) -> dict[str, str] | None`: runs `["uv", "tool", "list", "--outdated"]`, parses plain text via a regex matching non-indented lines shaped `^(\S+) v(\S+) \[latest: (\S+)\]$` (skipping indented `- <command>` continuation lines), returns `{distribution: latest}` keyed by the PyPI distribution name (e.g. `"graphifyy"`, matching the registry's `pypi_pkg`, NOT the exposed command name `"graphify"`). `None` on `CommandError`/`OSError`.
  </behavior>
  <action>
    Create `installer/manager_versions.py` with the three functions above, each split into a pure parser (`parse_brew_outdated_json(raw: str) -> dict[str, str] | None`, `parse_pnpm_outdated_json(raw: str) -> dict[str, str] | None`, `parse_uv_tool_outdated(raw: str) -> dict[str, str] | None`) and a thin IO wrapper, mirroring `installer/pnpm_globals.py`'s own `parse_global_packages`/`pnpm_global_packages` split exactly (module docstring should name this precedent). Write `tests/test_manager_versions.py` using the LITERAL captured strings from 12-RESEARCH.md section 4 as fixtures for all three parsers, plus the pnpm exit-1 case (a fake runner returning `(1, '{"pnpm": {"current": "11.9.0", "latest": "12.3.4", ...}}')` must parse successfully, not raise) and a genuinely-empty/malformed-JSON case for each (returns `None`, never raises).
  </action>
  <verify>
    <automated>uv run pytest tests/test_manager_versions.py -q</automated>
    <fails_when>non-zero exit, or `parse_pnpm_outdated_json` raises or returns `None` on the literal exit-1 pnpm JSON fixture from 12-RESEARCH.md section 4.2, or `parse_uv_tool_outdated` fails to extract `{"graphifyy": "0.9.55", "pre-commit": "4.6.2"}` from the literal `uv tool list --outdated` fixture text in section 4.3</fails_when>
  </verify>
  <done>`installer/manager_versions.py` exists with three tested parsers matching 12-RESEARCH.md section 4's exact live-verified command shapes, exit codes, and env requirements; the pnpm exit-1-tolerant path is explicitly tested (not merely implemented).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire brew/cask/node/uv-tool tools into the shared version-status cache and the Ver column</name>
  <files>installer/version_status.py, installer/wizard_app.py, installer/catalog_tui.py, tests/test_version_status.py, tests/test_wizard_app.py, tests/test_catalog_tui.py</files>
  <read_first>
    - `installer/version_status.py` and `installer/wizard_app.py`'s `CatalogScreen`/refresh worker as left by Plan 12-01 (read the actual committed state)
    - `installer/resolve.py` in full (`resolve_methods`, `_RANK` — the priority ladder that decides which method kind is "the one currently in use" for a tool with multiple declared methods)
  </read_first>
  <action>
    Add `resolve_manager_status(tool: Tool, *, active_method: Method, cache: dict[str, VersionCacheEntry], probe: Callable[[list[str]], str | None], brew_outdated: Callable[..., dict[str, str] | None], pnpm_outdated: Callable[..., dict[str, str] | None], uv_outdated: Callable[..., dict[str, str] | None], now: datetime) -> tuple[VersionStatus, VersionCacheEntry | None]` to `installer/version_status.py`, branching on `active_method.kind`: `brew`/`cask` -> look up `active_method.params["formula"]`/`["cask"]` in a fresh `brew_outdated()` call (only when the cache entry `is_stale`); `node` -> look up `active_method.params["npm_pkg"]` in `pnpm_outdated_global()`; `uv-tool` -> look up `active_method.params["pypi_pkg"]` in `uv_tool_outdated()`; a lookup miss (package not in the manager's own outdated set) means up-to-date (`outdated=False`), NOT unknown — the manager already told us its full outdated set, silence about a package means it isn't in it. Extend the Task-1-of-12-01 worker loop in `installer/wizard_app.py::CatalogScreen` to also call `resolve_manager_status` for every tool whose top-ranked `resolve_methods(tool, platform)[0].kind` is `brew`/`cask`/`node`/`uv-tool`, writing into the SAME `version_cache.py` cache file and the SAME `_version_statuses` dict Plan 12-01 built, so the Ver column renders identically regardless of source.
  </action>
  <verify>
    <automated>uv run pytest tests/test_version_status.py tests/test_wizard_app.py tests/test_catalog_tui.py -k "manager or brew or pnpm_status or uv_tool_status" -q</automated>
    <fails_when>non-zero exit, or a fixture brew-installed tool whose `brew_outdated` fake returns its formula name renders anything other than "current → latest" in the Ver column, or a fixture tool absent from a manager's outdated set renders "unknown" instead of "up to date"</fails_when>
  </verify>
  <done>Every catalog tool whose active method is brew/cask/node/uv-tool shows a real version-status Ver-column entry sourced from `installer/manager_versions.py`, sharing the same 7-day cache and staleness rule as github_release tools from Plan 12-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brew/pnpm/uv-tool subprocess stdout -> installer | Each manager's own outdated-report output (JSON or plain text) crosses into this phase's parsing/comparison logic |
| Environment variable injection point | `HOMEBREW_NO_AUTO_UPDATE=1`/`HOMEBREW_NO_ENV_HINTS=1` are merged into the child process environment for the `brew outdated` call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Spoofing | brew/pnpm/uv-tool subprocess output | medium | mitigate | Every parser (`parse_brew_outdated_json`, `parse_pnpm_outdated_json`, `parse_uv_tool_outdated`) validates JSON structure/regex shape before trusting a field; a malformed or unexpected shape returns `None` (unknown), never a fabricated version. |
| T-12-06 | Denial of Service | Unbounded manager "outdated" query subprocess inside the version-refresh worker thread | medium | mitigate | Each manager call runs through this project's existing `Runner`/`OutputRunner` timeout convention (mirroring `installer/pnpm_globals.py::LIST_TIMEOUT_SECONDS`, 20s) — a query, unlike a mutating install/update, must never wait forever; the same bound applies to `brew outdated`/`pnpm outdated -g`/`uv tool list --outdated`. |

</threat_model>

<verification>
- `uv run pytest tests/test_manager_versions.py tests/test_version_status.py tests/test_wizard_app.py tests/test_catalog_tui.py -q` exits 0.
- `make validate` passes on the exact tree before commit.
</verification>

<success_criteria>
- brew/cask/node/uv-tool-installed catalog tools show a real current-vs-latest comparison in the Ver column, using the exact live-verified commands from 12-RESEARCH.md section 4.
- pnpm's exit-1-on-outdated behavior is explicitly handled and tested, never silently swallowed as a failure.
- One shared cache/staleness mechanism governs all four source kinds (github_release + brew/cask/node/uv-tool).
</success_criteria>

<output>
Create `.planning/phases/12-version-aware-status-update-action/12-02-SUMMARY.md` when done
</output>
