---
phase: 04-package-manager-redirect-policy
plan: 02
type: execute
wave: 1
cross_ai: true
depends_on: []
files_modified:
  - installer/registry.toml
  - tests/test_registry.py
autonomous: true
requirements:
  - REQ-npm-global-volta-redirect

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`volta` is a browsable system-tier catalog tool that resolves at least one install method on macOS and on every supported Linux distro (R-02)."
    - "`volta`'s catalog description states plainly that its global installs run npm's postinstall scripts unrestricted (D-07's finding, recorded not papered over)."
    - "`~/.volta/bin` is declared as a managed bin dir on every volta method, so a CLI installed through `volta install` is reachable on PATH."
  artifacts:
    - installer/registry.toml
    - tests/test_registry.py
  key_links:
    - "`installer/resolve.py::resolve_methods` picks the official install script on Linux and the Homebrew formula on macOS from volta's two declared methods."
    - "`installer/shellrc.py::collect_bin_dirs` reads each resolved method's `bin_dir`, so the declared `~/.volta/bin` is what makes volta-installed globals reachable."
---

<objective>
Add `volta` to `installer/registry.toml` as a `tier="system"` catalog tool, with the
Homebrew formula on macOS and the official install script on Linux, and with the
Volta-shells-out-to-npm security finding recorded on the entry itself.

Purpose: R-02. The `npm install -g` / `pnpm add -g` → `volta install` redirect that plan
04-03 ships is non-functional on a machine that cannot install volta, and volta is in no
phase of the roadmap today. This plan is independent of every guards.py change, so it
runs in parallel with plan 04-01.

Decisions implemented:
- D-06: `volta install <pkg>` is the redirect target for every global install, so volta has to be installable through this project before that redirect can work.
- D-07: the gating research question is answered on the artifact itself — `volta install` runs a real `npm install --global` with install scripts ungated, recorded with its source citation rather than assumed safe.
- D-08: the volta split is what resolves `REQ-pnpm-global-reinstall-mitigation`'s root cause for anything moved off pnpm, and that split needs volta present to exist at all.
- R-02: volta becomes a `tier="system"` registry entry, mirroring `uv`/`pnpm`/`brew`.

Output: one new `[[tool]]` block in `installer/registry.toml` plus the registry-authoring
assertions that keep it honest.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@.planning/phases/04-package-manager-redirect-policy/04-CONTEXT.md
@.planning/phases/04-package-manager-redirect-policy/04-RESEARCH.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@installer/registry.toml
@tests/test_registry.py

`.codegraph/` exists at the repo root. Use `codegraph_explore` (MCP tool) or
`codegraph explore "<symbols>"` (shell) BEFORE Grep/Read when locating code.
Run `codegraph index || codegraph init -i` once before starting.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: volta as a system-tier catalog entry</name>
  <files>installer/registry.toml, tests/test_registry.py</files>
  <read_first>
    - installer/registry.toml lines 1448-1471 (the `pnpm` entry — the exact shape to mirror: system tier, pkg-mgr category, os-scoped script method plus a brew method)
    - installer/registry.toml lines 60-76 (the `uv` entry — the simplest script+brew pair)
    - installer/registry.toml lines 138-160 (the `brew` entry — how a `bin_dir` and a `shell = "bash"` script method are declared)
    - installer/model.py lines 126-180 (`load_tools` validation: required `tier`, method-kind allowlist, `os` must be a list)
    - tests/test_registry.py lines 34-56 and 314-345 (the "requested entries" set and the per-platform resolution guard)
    - .planning/phases/04-package-manager-redirect-policy/04-RESEARCH.md "Pitfall 5" and the Package Legitimacy Audit section
  </read_first>
  <behavior>
    - `load_tools(REGISTRY)` yields a tool with `id == "volta"`, `tier == "system"`, `category == "pkg-mgr"`, `cmd == "volta"`, `audience == "both"`.
    - `resolve_methods(volta, Platform(os="macos", arch=<amd64|arm64>, immutable=False, has_brew=True))` returns a non-empty list whose first kind is `"brew"`.
    - `resolve_methods(volta, Platform(os=<debian|arch|fedora>, arch=<amd64|arm64>, immutable=False, has_brew=True))` returns a non-empty list whose first kind is `"script"`.
    - `test_every_tool_resolves_at_least_one_method_on_each_platform` still passes with volta present and volta is NOT added to `MACOS_ONLY`.
    - Both of volta's methods declare `bin_dir = "~/.volta/bin"`.
    - `volta` is a member of the `test_registry_includes_requested_installable_entries` id set.
  </behavior>
  <action>
    Append a new `[[tool]]` block to `installer/registry.toml`, placed immediately after
    the existing `pnpm` entry (they are the two Node-side package managers and this phase
    splits global installs between them). Exact field values:

    ```
    id = "volta"
    name = "Volta"
    category = "pkg-mgr"
    cmd = "volta"
    priority = "P2"
    audience = "both"
    tier = "system"
    desc = "Node toolchain manager; owns globally installed Node CLIs in its own shim dir"
    ```

    Two methods, in this declared order:

    1. `kind = "script"`, `os = ["debian", "arch", "fedora"]`,
       `url = "https://get.volta.sh"`, `shell = "bash"`, `bin_dir = "~/.volta/bin"`.
       This is the install path Volta's own getting-started guide publishes
       (`docs.volta.sh/guide/getting-started`); `installer/executors.py::_script` turns it
       into `curl -fsSL -- https://get.volta.sh | bash`.
    2. `kind = "brew"`, `formula = "volta"`, `bin_dir = "~/.volta/bin"`. Confirmed present
       in homebrew-core (`formulae.brew.sh/formula/volta`).

    Declare `bin_dir` on BOTH methods, not only the script one: volta places the shims for
    every CLI it installs in `~/.volta/bin` regardless of how volta itself was installed,
    so that directory is what makes a `volta install`-ed command reachable. Without it the
    macOS/brew path would install volta and leave everything volta installs off PATH.

    Do not add a `requires` list. Volta bundles its own Node toolchain and the brew method
    follows the same convention as `uv`/`pnpm`, neither of which declares `requires = ["brew"]`.

    Add the assertions from `<behavior>` to `tests/test_registry.py`: extend the existing
    `test_registry_includes_requested_installable_entries` id set with `"volta"` and add a
    dedicated `test_volta_...` case for the per-platform method order and the `bin_dir`
    declarations, following the shape of the existing
    `test_eza_resolves_to_download_on_linux_and_brew_only_on_macos` test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry.py -x -q` exits 0.
    - `uv run python3 -c "from pathlib import Path; from installer.model import load_tools; from installer.platform import Platform; from installer.resolve import resolve_methods; r = Path('installer/registry.toml'); v = next(t for t in load_tools(r) if t.id == 'volta'); assert v.tier == 'system' and v.category == 'pkg-mgr' and v.cmd == 'volta'; mac = resolve_methods(v, Platform(os='macos', arch='arm64', immutable=False, has_brew=True)); assert [m.kind for m in mac][0] == 'brew'; lin = resolve_methods(v, Platform(os='fedora', arch='amd64', immutable=False, has_brew=True)); assert [m.kind for m in lin][0] == 'script'; assert all(m.params.get('bin_dir') == '~/.volta/bin' for m in v.methods)"` exits 0.
    - `grep -c 'volta' tests/test_registry.py` is at least 2.
    - `uv run python3 -c "import tests.test_registry as t; assert 'volta' not in t.MACOS_ONLY"` exits 0 — volta is not on the macOS-only allowlist, so the per-platform resolution guard really covers it on Linux.
  </acceptance_criteria>
  <done>`volta` loads, validates, and resolves an install method on macOS and on every supported Linux distro, with `~/.volta/bin` declared as its managed bin dir.</done>
</task>

<task type="auto">
  <name>Task 2: Record the Volta-shells-out-to-npm finding on the entry</name>
  <files>installer/registry.toml, tests/test_registry.py</files>
  <read_first>
    - .planning/phases/04-package-manager-redirect-policy/04-RESEARCH.md "Volta-Internals Verdict" in the Summary, "Pitfall 2", and the "Confirmed Volta-shells-to-npm source" code example
    - .planning/phases/04-package-manager-redirect-policy/04-CONTEXT.md D-07
    - installer/registry.toml lines 146-147 (the precedent for a TOML comment recording a verified caveat on an entry)
  </read_first>
  <action>
    Add a TOML comment block directly above volta's `[[tool]]` header recording D-07's
    answer as verified fact, not as a guess. It must state, in English:

    - `volta install <pkg>` runs a real `npm install --global --loglevel=warn
      --no-update-notifier --no-audit <pkg>` subprocess, with no flag that disables
      install scripts — verified by reading Volta's own source,
      `crates/volta-core/src/tool/package/install.rs`, function `run_global_install`
      (github.com/volta-cli/volta, fetched 2026-09-05).
    - Consequence: anything installed through volta runs npm's preinstall/install/
      postinstall scripts unrestricted, which pnpm has gated behind an opt-in allowlist
      since pnpm v10 (`pnpm.io/supply-chain-security`). Routing global installs to volta
      is therefore a security-for-stability tradeoff the user accepted in D-06/D-08 to
      fix pnpm's global-package loss on self-update — it is not a clean win.
    - Version caveat: npm v12 flips install scripts to opt-in
      (`github.blog/changelog/2026-06-09-upcoming-breaking-changes-for-npm-v12/`). The
      finding above holds for the npm versions Volta pins today and should be
      re-verified with `volta run --node <pinned-version> npm --version` if this entry is
      revisited after 2026-10-05.

    Add one test to `tests/test_registry.py` asserting the finding is present in the
    committed registry text so it cannot be dropped silently: read
    `installer/registry.toml` as text and assert the substring `run_global_install`
    appears in it. Name the test so its purpose is obvious, e.g.
    `test_volta_entry_records_the_npm_postinstall_finding`.

    English only, per CLAUDE.md.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_registry.py -x -q` exits 0.
    - `grep -c 'run_global_install' installer/registry.toml` is 1.
    - `grep -c 'supply-chain-security' installer/registry.toml` is at least 1.
    - `grep -c 'run_global_install' tests/test_registry.py` is at least 1.
    - `make validate` exits 0.
    - `make test` exits 0.
  </acceptance_criteria>
  <done>The registry itself carries the source-verified reason the volta redirect is a documented tradeoff, and a test fails if that record is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → `curl \| bash` | A declared install URL is piped into a shell with the user's privileges. |
| volta → npm subprocess | Packages installed through volta execute their own lifecycle scripts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | `https://get.volta.sh` piped to bash | high | accept | Identical to every other `kind="script"` entry already in the registry (`uv`, `pnpm`, `brew`), and it is Volta's own published install path. The registry-authoring discipline (read the real install script, cite what was checked) is the existing control; introducing a different one for this single entry would fork the convention. |
| T-04-07 | Elevation of Privilege | npm lifecycle scripts run by `volta install` | high | accept | Explicitly accepted by the user in D-06/D-08 as a tradeoff against pnpm's global-package loss. Mitigation is disclosure, not prevention: the finding is recorded on the entry (Task 2) and repeated in the doctor copy in plan 04-04. |
| T-04-08 | Spoofing | typosquatted `volta` formula/package | low | mitigate | Not a package dependency of this codebase. Legitimacy established in 04-RESEARCH.md's Package Legitimacy Audit: official `volta-cli/volta` org, present in homebrew-core. |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — this plan installs no PyPI/npm/crates package into the project; it adds a catalog entry describing a system tool. 04-RESEARCH.md's Package Legitimacy Audit records this explicitly. |
</threat_model>

<verification>
- `uv run pytest tests/test_registry.py -x -q`.
- `make validate && make test` on the exact tree before commit (ONESHOT-RULES Rule 6).
- No install is executed against this machine — this plan only edits registry data and tests (ONESHOT-RULES Rule 5).
</verification>

<success_criteria>
- ROADMAP SC#4 (prerequisite): the machine the `volta install` redirect targets can install volta through this project's own catalog, on macOS and Linux alike.
- D-07 is answered in the committed artifact, with its source citation and its version caveat, and a test guards the record.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan's share)

| Artifact | Kind | Purpose |
|----------|------|---------|
| `installer/registry.toml` `[[tool]] id = "volta"` | data | Makes the D-06 redirect target installable (R-02) |
| `installer/registry.toml` volta comment block | doc | D-07's source-verified security finding, in the artifact it describes |
| `tests/test_registry.py` volta cases | test | Per-platform resolution, bin_dir declaration, finding-not-dropped guard |
</artifacts>

<output>
Create `.planning/phases/04-package-manager-redirect-policy/04-02-SUMMARY.md` when done.
Record in it: the exact methods declared, why `bin_dir` is on both, and the D-07 finding
text as committed.
</output>
