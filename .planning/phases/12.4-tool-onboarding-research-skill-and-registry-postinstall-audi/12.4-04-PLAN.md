---
phase: 12.4-tool-onboarding-research-skill-and-registry-postinstall-audi
plan: 04
type: execute
wave: 3
cross_ai: true
depends_on: ["12.4-02", "12.4-03"]
files_modified:
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-AUDIT.md
  - .claude/architecture.md
  - .planning/PROJECT.md
autonomous: true
requirements: [D-04]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every one of the 89 [[tool]] entries in registry.toml has been checked against the new onboarding checklist (tier correctness, dependency declaration, postinstall classification) and the result -- pass or a named finding -- is recorded in a reviewable artifact, not just asserted."
    - "Any additional postinstall/setup-mechanism gap found beyond rtk/graphify (a tool with real per-host agent-integration needs but no wiring through postinstall/skill_pack/host_setup) is either fixed in this phase or logged as a named .planning/todos/pending/ entry -- never silently dropped."
    - "The pre-existing, unrelated Tool.post_install (plural)/POST_INSTALL_ACTIONS dead-code finding 12.4-RESEARCH.md flagged is recorded in the audit artifact as a noted-but-intentionally-unfixed finding, with a pending todo so it is not lost, per 12.4-RESEARCH.md's own explicit 'not something this phase needs to fix' guidance."
    - "12.4-CONTEXT.md's 'graphify output-location unification' research requirement -- never addressed by 12.4-RESEARCH.md -- is closed by direct investigation and recorded in the audit artifact as a no-code-change finding: the currently-installed gsd-graphify skill already copies graphify's build output into .planning/graphs/, and the underlying question concerns a global, cross-project GSD skill and a different graphify subcommand than D-02's own per-host install registration -- not silently dropped, not papered over with unrelated code."
    - "D-01/D-01a/D-02/D-03/D-04's real decisions are consolidated into .planning/PROJECT.md's Key Decisions table and a new Phase 12.4 subsection in .claude/architecture.md, mirroring the pattern every prior multi-plan phase (9, 12.3) already established."
  artifacts:
    - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-AUDIT.md
  key_links:
    - "The audit's postinstall-classification pass -> the SAME three-mechanism decision tree Plan 12.4-03 wrote into .claude/architecture.md (one canonical checklist applied, not a fourth ad-hoc judgment call)"
    - "Any audit finding routed to a follow-up -> a new, named .planning/todos/pending/{date}-{slug}.md entry, matching this project's existing pending-todo convention (.planning/todos/pending/2026-09-10-unify-tmp-prune-tooling-into-an-installable-tweaks-module.md)"
---

<objective>
Close D-04: run the full-catalog audit this phase's checklist (Plan 12.4-03) exists to enable, against
every one of the 89 `[[tool]]` entries in `installer/registry.toml` — not just the confirmed rtk/graphify
gap Plans 12.4-01/12.4-02 already closed, and not just the `ai`-tier/agent-harness subset. Then consolidate
this phase's real decisions (D-01 through D-04) into `.planning/PROJECT.md`'s Key Decisions table and a new
Phase 12.4 subsection in `.claude/architecture.md`, mirroring the pattern Phase 9 and Phase 12.3 already
established, and close with a full `make validate && make test` gate.

Purpose: 12.4-RESEARCH.md's own grep-scan found no THIRD postinstall/per-host gap beyond codegraph/rtk/
graphify — but that was a cheap keyword grep, not the structured, checklist-driven per-entry audit D-04
actually requires. This plan is that real audit, so any gap it finds gets a name and an owner (fixed here,
or logged for a follow-up) instead of staying invisible the way rtk/graphify's gap did since Phase 8/9.
Output: `.planning/phases/12.4-.../12.4-AUDIT.md` (the full audit record), any new
`.planning/todos/pending/*.md` entries for gaps not fixed in this phase, Phase 12.4's decisions consolidated
into `.planning/PROJECT.md`/`.claude/architecture.md`, and a final green `make validate && make test`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-CONTEXT.md
@.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-RESEARCH.md
@.planning/todos/pending/2026-09-10-unify-tmp-prune-tooling-into-an-installable-tweaks-module.md
@CLAUDE.md
@.claude/architecture.md
@installer/registry.toml
@installer/model.py
@installer/postinstall.py
@installer/host_setup.py
@Makefile
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full-catalog audit against the onboarding checklist -- all 89 entries</name>
  <files>.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-AUDIT.md</files>
  <read_first>
    - `.claude/architecture.md`'s new "Tool onboarding: tier, dependencies, and postinstall classification"
      subsection (Plan 12.4-03) — the exact checklist this audit applies to every entry; do not invent a
      fourth, divergent judgment process.
    - `installer/registry.toml` in full (89 `[[tool]]` entries, ~2900 lines) — read the whole file; do not
      sample. 12.4-RESEARCH.md's own tier/mechanism inventory (reproduced below) is a starting index, not a
      substitute for reading each entry.
    - 12.4-RESEARCH.md's "Don't Hand-Roll" section's closing "Key insight" paragraph — the pre-existing,
      unrelated `Tool.post_install` (plural)/`POST_INSTALL_ACTIONS`/`SENSITIVE_POST_INSTALL_ACTIONS`
      dead-code finding: zero production callers, zero registry usage, zero tests anywhere in the tree
      (confirmed again this session: `grep -rn "\.post_install\b" installer/ tests/` and
      `grep -rn "default_enabled_sensitive_actions" installer/ tests/ installer/registry.toml` both return
      hits ONLY inside `installer/model.py`'s own declaration/parsing code, never a consumer). Research is
      explicit this is "not something this phase needs to fix" — record it as a NOTED finding, do not spend
      this task's own scope deleting it (Plan 12.2-01 already deliberately left these fields untouched for
      the same reason: out of that phase's decision scope).
    - `.planning/todos/pending/2026-09-10-unify-tmp-prune-tooling-into-an-installable-tweaks-module.md` — the
      existing convention for a logged, not-fixed-here finding: YAML frontmatter (`created`, `title`, `area`,
      `severity`, `files`), then `## Problem` / `## Solution` prose sections.
    - 12.4-CONTEXT.md's `canonical_refs` -> "Research NOT yet done — required before/during planning" ->
      "graphify output-location unification" — this item was never investigated by 12.4-RESEARCH.md
      (confirmed this session: `grep -in "graphify-out\|gsd-graphify\|output.location" 12.4-RESEARCH.md`
      returns nothing). This task closes that gap by direct investigation rather than leaving it silently
      unaddressed: reading the locally installed `~/.claude/skills/gsd-graphify/SKILL.md` (confirmed present
      this session) shows `gsd-graphify` already runs `graphify update .` (graphify's own knowledge-graph
      BUILD subcommand, which writes to its own default `graphify-out/` — a DIFFERENT graphify subcommand
      from the per-host `graphify <host> install` registration subcommand D-02's `_graphify_register` hook
      wires) and then explicitly `cp`'s `graph.json`/`graph.html`/`GRAPH_REPORT.md` into `.planning/graphs/`
      — an already-coordinated, intentional two-stage design (build once in graphify's own default location,
      then copy the specific artifacts GSD consumes), not "two separate, uncoordinated locations" as
      12.4-CONTEXT.md feared. Record this as a NOTED, no-code-change finding: (a) the underlying concern is
      already addressed by the currently-installed GSD version's own copy-based design; (b) even if it were
      not, `gsd-graphify`'s skill file lives at `~/.claude/skills/` — a GLOBAL, cross-project GSD asset, not
      a file this repository's git history tracks — so no change to `installer/*.py`/`registry.toml` could
      ever influence it either way; and (c) this question concerns graphify's knowledge-graph BUILD
      subcommand, entirely distinct from the per-host AGENT-INTEGRATION `install` subcommand D-02's own
      `_graphify_register` hook (Plan 12.4-02) actually wires. Do not add any code to `installer/*.py` for
      this — there is nothing in this repository for such code to do.
  </read_first>
  <action>
Perform the D-04 audit as four passes, all recorded in a new
`.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-AUDIT.md`:

**Pass 1 — postinstall/setup-mechanism classification (the checklist item this phase's own gap belongs to).**
This project's 89 entries break down as: 26 `system`-tier, 41 `user`-tier, 22 `ai`-tier (confirmed this
session via `python3 -c "import tomllib; ..."` against the committed registry). Apply the checklist's
three-mechanism decision tree to every `ai`-tier entry specifically (agent-facing tools are where a
postinstall-shaped gap is structurally possible — a `system`/`user`-tier entry with no agent-host
relationship cannot have this class of gap):
  - The 4 real agent hosts (`codex`, `claude`, `opencode`, `cursor-agent`) plus `antigravity` — confirm none
    of these five need to register INTO another host (they ARE the host), so "no postinstall wiring" is the
    correct, already-shipped classification for all five.
  - `codegraph`, `rtk`, `graphify` — confirm all three now correctly declare `postinstall = "<hook-name>"`
    (rtk/graphify closed by Plans 12.4-01/12.4-02 this same phase) and each hook is reachable through
    `installer/postinstall.py`'s `POSTINSTALL_HOOKS`.
  - `pi` — confirm `kind = "host_setup"`, `setup_id = "pi"` remains the correct classification (its own
    setup is genuinely interactive, unscriptable from a Textual worker thread — re-derive this from `pi`'s
    own current install docs, do not assume 12.4-RESEARCH.md's characterization is still accurate without a
    fresh check).
  - The 7 `skill_pack` entries (`ponytail`, `openspec`, `superpowers`, `softaworks-agent-toolkit`,
    `matt-pocock-skills`, `opengsd`, `spec-kit`) — for EACH, confirm its `[tool.skill_lifecycle]`
    `mode = "manual-required"`/`"command"` classification is still the right call under the NEW checklist's
    decision tree (interactive/trust-review-required, not merely "nobody wired it yet") — this is the one
    place a real gap analogous to rtk/graphify's could plausibly still hide: a `skill_pack` entry whose
    install step is ACTUALLY non-interactive today (upstream may have shipped a `--yes`/`--non-interactive`
    flag since its `skill_lifecycle` was last verified) is a genuine finding, not a rubber-stamp.
  - `rg`, `fd`, `bat`, `sd`, `eza`, `aichat` — confirm these six remain plain binaries with no agent-host
    registration concept (they are consumed BY an agent's shell environment, not registered INTO an agent
    host's config) — no postinstall wiring is correct, but state the reasoning explicitly in the audit
    rather than a bare pass/fail.

**Pass 2 — tier correctness and dependency declaration, full 89-entry sweep.** For every entry NOT covered
by Pass 1's ai-tier focus (the 26 system + 41 user entries, plus the ai-tier entries' tier/dependency halves
Pass 1 didn't already cover): confirm `tier` still matches the checklist's rationale (a `system`-tier entry
genuinely is a cross-cutting prerequisite; a `user`-tier entry genuinely has no agent-host relationship), and
confirm `requires` names every REAL install-time prerequisite the entry's own method(s) depend on (a
`node`/`uv-tool`/`sdkman`-kind method without the corresponding manager in `requires` is the specific,
checklist-item-2 gap shape to look for). This is a sweep for OBVIOUS misclassification/omission, not a
re-verification of already-tested invariants (`tests/test_registry.py`'s existing tier-distribution and
dependency-integrity tests already CI-pin the aggregate counts — this pass is about individual-entry
REASONING correctness a count-only test cannot catch).

**Pass 3 — the dead-code finding.** Record `Tool.post_install`/`POST_INSTALL_ACTIONS`/
`SENSITIVE_POST_INSTALL_ACTIONS` (`installer/model.py`) as a NOTED, intentionally-unfixed finding: zero
production callers, zero registry usage, zero test coverage, a violation of `.claude/architecture.md` rule 5
("No orphan helpers") that predates this phase and is unrelated to `Tool.postinstall` (singular, the
mechanism this phase actually extends) despite the confusingly similar name. Per 12.4-RESEARCH.md's own
explicit guidance, do NOT delete it in this task — log it as a new
`.planning/todos/pending/{today's date}-remove-orphaned-tool-post-install-plural-field.md` entry (frontmatter:
`created`, `title`, `area: tech-debt`, `severity: minor`, `files: [installer/model.py]`; body: `## Problem`
naming the exact dead symbols and the zero-caller evidence, `## Solution` — delete the field, the two
constants, and their `load_tools` parsing/validation block, confirmed safe by the same zero-caller grep this
task ran).

**Pass 4 — the graphify output-location question 12.4-CONTEXT.md flagged as required research.** Record, as
a NOTED, no-code-change finding: `12.4-RESEARCH.md` never investigated this item (confirmed by this task's
own re-check). Direct investigation this task performed (or re-confirms, if Plan 12.4-01/12.4-02's own
execution already surfaced it first) shows the currently-installed `~/.claude/skills/gsd-graphify/SKILL.md`
already runs graphify's own `update` (knowledge-graph BUILD) subcommand into its default `graphify-out/`,
then explicitly copies `graph.json`/`graph.html`/`GRAPH_REPORT.md` into `.planning/graphs/` — an
already-coordinated, intentional two-stage design, not the "two separate uncoordinated locations"
12.4-CONTEXT.md worried about. This question concerns graphify's BUILD subcommand and a GLOBAL,
cross-project GSD skill file outside this repository's own git history — entirely distinct from the
per-host `install` subcommand D-02's `_graphify_register` hook wires inside `installer/postinstall.py`. No
change to `installer/*.py`/`registry.toml` is warranted or possible for this finding; record the evidence
and the reasoning in `12.4-AUDIT.md` so a future reader does not re-raise it without this context.

Write `12.4-AUDIT.md` as: a one-paragraph summary of scope (89/89 entries checked), a findings table
(`Entry | Checklist item | Finding | Disposition (pass / fixed-here / logged-as-todo)`), and an explicit
closing statement of whether any NEW postinstall-mechanism gap analogous to rtk/graphify was found. If Pass 1
finds a genuine new gap (a `skill_pack` entry whose upstream now supports non-interactive install, or any
other checklist-item-3 miss): per 12.4-CONTEXT.md's Claude's Discretion clause, fix it in THIS task if it is
small and isolated (a registry field change plus a matching `installer/postinstall.py` hook mirroring
rtk/graphify's own shape), or log it as a new `.planning/todos/pending/` entry if it is substantial — do not
silently drop it either way. If Pass 2 finds a genuine tier/dependency miss, apply the SAME rule.
  </action>
  <verify>
    <automated>test -f .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-AUDIT.md && grep -c "89" .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-AUDIT.md</automated>
    <fails_when>the audit file does not exist, or it never states the full 89-entry scope was covered (no mention of the number 89 anywhere, suggesting a partial or sampled pass rather than the full sweep D-04 requires).</fails_when>
  </verify>
  <done>
`12.4-AUDIT.md` records a full, checklist-driven pass over all 89 `registry.toml` entries — postinstall
classification (ai-tier focus), tier/dependency correctness (full sweep), and the dead-code finding — with
every finding disposed of as pass / fixed-here / logged-as-todo, never silently dropped.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Consolidate Phase 12.4's decisions into PROJECT.md/architecture.md; final quality gate</name>
  <files>.planning/PROJECT.md, .claude/architecture.md</files>
  <read_first>
    - `.planning/PROJECT.md`'s "## Key Decisions" table (lines 88+) — the exact `| Decision | Rationale |
      Outcome |` row shape Phase 9's four rows and Phase 12.3's three rows already established; this task
      adds Phase 12.4's rows in the same shape, citing real plan/summary filenames as evidence, never vague
      "TBD".
    - `.claude/architecture.md`'s existing `## Phase 9: postinstall hooks` and `## Phase 12.3: container e2e
      verification of the reconciled branch` sections — the exact prose-with-file-references style this
      task's new `## Phase 12.4` section follows: what changed, why, and which file/line proves it, not
      marketing language.
    - Task 1's `12.4-AUDIT.md` (just written) — the closing statement this task's new architecture.md section
      must accurately summarize (audit scope, disposition of any findings).
    - `Makefile`'s `validate`/`test` targets and `pyproject.toml`'s `fail_under = 90`/`min_confidence = 80` —
      the exact gate this task's final step runs and the floors it must not weaken.
  </read_first>
  <action>
Add Phase 12.4's real decisions to `.planning/PROJECT.md`'s Key Decisions table, one row per locked
decision, citing the plan/SUMMARY files that actually implemented it:
- D-03: the shared `present_agent_hosts()` helper and why it exists (one host-presence computation reused by
  `_codegraph_mcp_register`/`_rtk_register`/`_graphify_register`, never reimplemented per-tool) — cite
  12.4-01-SUMMARY.md.
- D-02: `_rtk_register`/`_graphify_register` wired into the existing closed dispatch table, including the
  live-verified cursor-agent design decision (whichever of the two admissible branches Plan 12.4-01's Task 2
  actually shipped, stated concretely — not "TBD") — cite 12.4-01-SUMMARY.md/12.4-02-SUMMARY.md.
- D-01/D-01a: the checklist + skill deliverable and the detect-and-configure-never-overwrite principle — cite
  12.4-03-SUMMARY.md.
- D-04: the full-catalog audit's real outcome (89/89 checked, N findings fixed, M findings logged) — cite
  this plan's own 12.4-AUDIT.md and 12.4-04-SUMMARY.md.

Add a new `## Phase 12.4: tool onboarding and registry postinstall audit` subsection to
`.claude/architecture.md`, after the existing `## Phase 12.3: container e2e verification of the reconciled
branch` section, documenting: the D-03 shared-helper convention (so a FUTURE postinstall hook reuses
`present_agent_hosts()` rather than reintroducing a fourth inline loop); the rtk/graphify worked examples
as the second and third entries in the `POSTINSTALL_HOOKS` dispatch table (codegraph remains the first);
and a one-paragraph summary of the D-04 audit's real outcome, referencing `12.4-AUDIT.md` by path rather
than duplicating its findings table inline.

Run `make validate` (ruff check, ruff format --check, pyright, bandit, vulture, shellcheck) then `make test`
(`uv run pytest --cov`, 90% branch-coverage floor) on the tree as left by all four of this phase's plans.
Both must exit 0. If either fails, fix the root cause within this task per `CLAUDE.md`'s "never bypass a
quality gate" rule — do not weaken a floor or add a suppression to make it pass. Confirm no stray file
outside this phase's declared `files_modified` across all four plans was left uncommitted
(`git status --short` reviewed against the four plans' combined `files_modified` plus this plan's own
`12.4-AUDIT.md` and any `.planning/todos/pending/*.md` entries Task 1 logged).
  </action>
  <verify>
    <automated>make validate && make test</automated>
    <fails_when>`make validate` exits non-zero on any of ruff-check/ruff-format/pyright/bandit/vulture/shellcheck, OR `make test` exits non-zero, OR reported branch coverage is below 90%.</fails_when>
  </verify>
  <done>
Phase 12.4's D-01/D-01a/D-02/D-03/D-04 decisions are recorded in `.planning/PROJECT.md`'s Key Decisions table
and `.claude/architecture.md`'s new Phase 12.4 section, both citing real evidence (SUMMARY files, the audit
artifact) rather than placeholder text, and `make validate && make test` are green on the final tree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The audit's own findings <-> what actually ships | A finding recorded in `12.4-AUDIT.md` but silently not acted on (neither fixed nor logged as a todo) would defeat D-04's entire purpose — the same class of gap this whole phase exists to close for rtk/graphify. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.4-10 | Repudiation | A partial or sampled audit claiming full coverage | medium | mitigate | Task 1's `<verify>` gate greps for the literal "89" scope statement in the audit artifact, and `<read_first>` explicitly instructs reading the full registry file, not a sample. |
| T-12.4-11 | Information Disclosure | N/A — no new external-facing surface in this plan | low | accept | This plan only writes internal planning/documentation artifacts and consolidates existing decisions; no new runtime code path is introduced. |
| T-12.4-12 | Tampering | Weakening `make validate`/`make test`'s floors to force a green gate | high | mitigate | Task 2 explicitly forbids lowering the coverage floor or adding a suppression; any gate failure must be fixed at its root cause per `CLAUDE.md`. |

No package-manager install tasks exist in this plan (documentation, audit, and gate-verification only) — the
Package Legitimacy Gate does not apply.
</threat_model>

<verification>
Confirm `12.4-AUDIT.md` exists and states full 89-entry coverage with every finding disposed of. Confirm any
new `.planning/todos/pending/*.md` entries this plan logged follow the existing frontmatter convention.
Confirm `.planning/PROJECT.md`'s Key Decisions table and `.claude/architecture.md`'s new Phase 12.4 section
both cite real SUMMARY/audit evidence. Run `make validate && make test` and confirm both exit 0 on the final
tree across all four of this phase's plans.
</verification>

<success_criteria>
- All 89 `registry.toml` entries are checked against the onboarding checklist; every finding is disposed of
  (pass / fixed-here / logged-as-todo), never silently dropped.
- The pre-existing `Tool.post_install` (plural) dead-code finding is recorded and logged as a pending todo,
  not fixed in this phase (per 12.4-RESEARCH.md's own explicit guidance) and not lost.
- Phase 12.4's real decisions (D-01 through D-04) are consolidated into `.planning/PROJECT.md` and
  `.claude/architecture.md`.
- `make validate && make test` are green on the phase's final tree.
</success_criteria>

<output>
Create `.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-04-SUMMARY.md` when done
</output>
