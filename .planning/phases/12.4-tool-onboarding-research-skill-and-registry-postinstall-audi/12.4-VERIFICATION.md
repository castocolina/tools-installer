---
phase: 12.4-tool-onboarding-research-skill-and-registry-postinstall-audi
verified: 2026-09-11T15:20:00Z
status: passed
score: 9/9 must-haves verified
covered_files:
  - .claude/architecture.md
  - .claude/skills/tool-onboarding/SKILL.md
  - .planning/PROJECT.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-01-PLAN.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-01-SUMMARY.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-02-PLAN.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-02-SUMMARY.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-03-PLAN.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-03-SUMMARY.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-04-PLAN.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-04-SUMMARY.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-AUDIT.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-CONTEXT.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-DISCUSSION-LOG.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-RESEARCH.md
  - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-REVIEW.md
  - .planning/todos/pending/2026-09-11-remove-orphaned-tool-post-install-plural-field.md
  - installer/model.py
  - installer/postinstall.py
  - installer/registry.toml
  - tests/test_model.py
  - tests/test_postinstall.py
  - tests/test_registry.py
covered_digest: "v1:sha256:2f6e34eb0f36e218b037a8d60548aa3112368ccc8e7f7c24bc808da7ecba2d8b"
behavior_unverified: 0
overrides_applied: 0
---

# Phase 12.4: Tool Onboarding Research Skill and Registry Postinstall Audit — Verification Report

**Phase Goal:** A repeatable, checklist-and-skill-driven onboarding process exists for any future
`registry.toml` addition (tier classification, dependency-tree declaration, postinstall/setup-
per-agent-harness classification); the confirmed rtk/graphify postinstall gap (researched in Phase
8, never wired into `installer/postinstall.py`) is closed via the same non-interactive per-host
mechanism `codegraph` already proves; and every one of the ~89 other `registry.toml` entries has
been checked against that same checklist for a similar research-to-implementation gap.

**Verified:** 2026-09-11T15:20:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `present_agent_hosts()` is the single shared host-presence helper, used by `_codegraph_mcp_register`, `_rtk_register`, and `_graphify_register` — not a fourth reimplementation. | ✓ VERIFIED | `installer/postinstall.py:29-46` defines `present_agent_hosts()`; all three hooks (lines 119, 218, 257) call it. `git diff f0a1695 -- tests/test_postinstall.py` shows the pre-existing 10 `test_codegraph_*` function bodies are byte-for-byte unmodified (pure additions after them); ran `uv run pytest tests/test_postinstall.py tests/test_model.py tests/test_registry.py` — 269 passed. |
| 2 | `rtk` is fully wired end-to-end for all four agent hosts (registry field, model validation, dispatch table), never fabricating a `--target auto`-equivalent default. | ✓ VERIFIED | `installer/registry.toml:1603` declares `postinstall = "rtk-register"`; `installer/model.py:20` includes `"rtk-register"` in `POSTINSTALL_HOOK_NAMES`; `installer/postinstall.py:272-276` registers it in `POSTINSTALL_HOOKS`. `_rtk_register`'s loop (`postinstall.py:223-225`) only calls a host present in `present_agent_hosts()`'s returned frozenset — an absent host is `continue`d, never called. |
| 3 | `rtk`'s live-verification evidence (idempotency, adversarial merge-safety, cursor-implies-claude re-test against a fresh v0.49.0 binary) is actually documented in the SUMMARY, not just asserted. | ✓ VERIFIED | `12.4-01-SUMMARY.md` "Live Verification Transcript" section records binary version (`rtk 0.49.0`), sha256 cross-checked against `checksums.txt`, and per-step transcript (fresh init, idempotent rerun with `grep -c` == 1, adversarial-seed survival, Pitfall 1/2 falsification results) — not a bare claim. |
| 4 | `graphify` is fully wired end-to-end for all four agent hosts, reusing `present_agent_hosts()`, one invocation per host (never a composed CSV). | ✓ VERIFIED | `installer/registry.toml:1550` declares `postinstall = "graphify-register"`; `installer/model.py:20` includes `"graphify-register"`; `installer/postinstall.py:253-269` (`_graphify_register`) loops `_GRAPHIFY_HOST_SUBCOMMANDS` calling `runner([graphify_bin, subcommand, "install"])` once per present host. |
| 5 | `graphify`'s live adversarial-merge-safety evidence for opencode/codex/cursor-agent (closing Assumption A2) is documented in the SUMMARY. | ✓ VERIFIED | `12.4-02-SUMMARY.md` "Live Verification Transcript" records the seeded-content/idempotency transcript for all three previously-untested hosts (AGENTS.md, opencode.json, `.cursor/rules/`), byte-for-byte survival and zero duplicates on rerun. |
| 6 | `.claude/architecture.md` contains a real "Tool onboarding: tier, dependencies, and postinstall classification" checklist (tier / dependency-tree / postinstall three-mechanism decision tree) plus D-01a's "detect and configure, never blind-overwrite" principle. | ✓ VERIFIED | `.claude/architecture.md:236-320` — full subsection present with all three named checklist items and the "detect and configure, never blind-overwrite" sentence, citing the GSD-statusline precedent. |
| 7 | `.claude/skills/tool-onboarding/SKILL.md` exists, is discoverable, and references (not duplicates) the same decision tree. | ✓ VERIFIED | File exists with `name: tool-onboarding` and a trigger-phrase `description:` frontmatter; body (read in full) walks research → tier → dependency/postinstall (referencing `.claude/architecture.md`'s subsection by name) → registry-entry-writing steps. |
| 8 | `12.4-AUDIT.md` covers all 89 registry entries, not a sample. | ✓ VERIFIED | Independently counted via `python3 -c "import tomllib; ... len(tools)"` against the committed `installer/registry.toml`: 89 total, tier breakdown 26/41/22 — matches `12.4-AUDIT.md`'s stated breakdown exactly. `12.4-REVIEW.md` independently cross-checked the same count via `grep -c '^\[\[tool\]\]$'`. |
| 9 | The `Tool.post_install` (plural) dead-code finding is logged as a pending todo, not silently dropped. | ✓ VERIFIED | `.planning/todos/pending/2026-09-11-remove-orphaned-tool-post-install-plural-field.md` exists with the project's standard frontmatter (`created`, `title`, `area: tech-debt`, `severity: minor`, `files`) and `## Problem`/`## Solution` sections naming the exact dead symbols and zero-caller evidence. |

**Score:** 9/9 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `installer/postinstall.py` | `present_agent_hosts`, `_rtk_register`, `_resolve_rtk_binary`, `_graphify_register`, dispatch table entries | ✓ VERIFIED | All present, wired, and exercised by 269 passing tests. Also reflects `12.4-REVIEW.md`'s post-review fix pass (`_resolve_bin_override` shared helper, well-known brew-prefix disk-presence fallback, PATH-lag caveat in the docstring) — confirmed present in the current file, not just claimed in the review. |
| `installer/model.py` | `POSTINSTALL_HOOK_NAMES` gains `rtk-register`/`graphify-register` | ✓ VERIFIED | `installer/model.py:19-21` — frozenset contains all three names, matches `POSTINSTALL_HOOKS.keys()` (pinned by `test_postinstall_hook_names_matches_the_real_dispatch_table`, passing). |
| `installer/registry.toml` | `rtk`/`graphify` `[[tool]]` entries gain `postinstall = "..."` plus dated `# Verified` comments | ✓ VERIFIED | Both entries confirmed (lines ~1550, ~1603), each with dated 2026-09-11 comments citing the live-verification transcripts. |
| `.claude/architecture.md` | New onboarding checklist subsection | ✓ VERIFIED | Present, full content matches plan's specification. |
| `.claude/skills/tool-onboarding/SKILL.md` | New invocable skill | ✓ VERIFIED | Exists, valid frontmatter, correct body. |
| `12.4-AUDIT.md` | Full 89-entry audit record | ✓ VERIFIED | Exists, 89/89 independently confirmed, four passes recorded with dispositions. |
| `.planning/todos/pending/2026-09-11-remove-orphaned-tool-post-install-plural-field.md` | Logged dead-code finding | ✓ VERIFIED | Exists with correct convention. |
| `.planning/PROJECT.md` / `.claude/architecture.md` | Phase 12.4 decisions consolidated | ✓ VERIFIED | `.planning/PROJECT.md`'s Key Decisions table gains 4 rows (D-03, D-02, D-01/D-01a, D-04, lines ~119-122); `.claude/architecture.md:560` gains `## Phase 12.4: tool onboarding and registry postinstall audit`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `installer/engine.py::install_tool`'s postinstall dispatch | `installer.postinstall.run_postinstall` | unchanged this phase | ✓ WIRED | `run_postinstall` dispatches by name through `POSTINSTALL_HOOKS`, unchanged shape; confirmed by passing `test_run_postinstall_*` tests. |
| `_rtk_register`/`_graphify_register`/`_codegraph_mcp_register` | `present_agent_hosts(tools)` | direct function call | ✓ WIRED | All three call sites confirmed by direct code read (lines 119, 218, 257 of `installer/postinstall.py`). |
| `rtk` `[[tool.method]]` (github_release ×3 / brew ×1) | `_resolve_rtk_binary`'s `method.kind` branch | direct read | ✓ WIRED | `_resolve_rtk_binary` branches on `method.kind == "brew"` (with well-known-prefix disk-presence fallback, post-review fix) vs. the `bin_dir` default path for `github_release`. |
| SKILL.md's postinstall-mechanism decision step | `.claude/architecture.md`'s three-mechanism taxonomy | named reference, not duplication | ✓ WIRED | SKILL.md Step 3 explicitly references "`.claude/architecture.md`'s 'Tool onboarding...' subsection" by name rather than re-deriving the tree. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full postinstall/model/registry test suite passes | `uv run pytest tests/test_postinstall.py tests/test_model.py tests/test_registry.py` | `269 passed in 0.66s` | ✓ PASS |
| `present_agent_hosts` refactor is behavior-preserving (pre-existing codegraph tests unmodified) | `git diff f0a1695 -- tests/test_postinstall.py \| grep -E "^[-+]def test_codegraph"` | no output (no function signature added/removed/changed) | ✓ PASS |
| Registry entry count matches the audit's claimed scope | `python3 -c "import tomllib; ...len(tools)"` | `89`, tiers `{'system': 26, 'ai': 22, 'user': 41}` | ✓ PASS |
| Full quality gate (`make validate && make test`) | `make validate && make test` | `make validate`: ruff/pyright/bandit/vulture/shellcheck all clean. `make test`: `1875 passed, 1 skipped`, 95.35% branch coverage (floor 90%) | ✓ PASS |
| No debt markers left in modified files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER" installer/postinstall.py installer/model.py .claude/skills/tool-onboarding/SKILL.md` | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| D-01 | 12.4-03 | Checklist + invocable skill, both shipped | ✓ SATISFIED | `.claude/architecture.md` subsection + `.claude/skills/tool-onboarding/SKILL.md` |
| D-01a | 12.4-01/02/03 | "Detect and configure, never blind-overwrite" applied and documented | ✓ SATISFIED | `present_agent_hosts()`-gated hooks, `cursor-agent` claude-gating, `ensure_dir` guards, principle stated as a standing checklist rule |
| D-02 | 12.4-01/02 | `_rtk_register`/`_graphify_register` wired into the closed dispatch table | ✓ SATISFIED | Code + tests + registry fields, all verified above |
| D-03 | 12.4-01 | Single shared host-presence utility | ✓ SATISFIED | `present_agent_hosts()`, reused by all three hooks |
| D-04 | 12.4-04 | Full 89-entry catalog audit, findings disposed of | ✓ SATISFIED | `12.4-AUDIT.md`, independently-confirmed 89-count, pending todo for the one deferred finding |

No orphaned requirements — all five (D-01, D-01a, D-02, D-03, D-04) declared across the four plans' `requirements` frontmatter fields and all five are addressed.

### Anti-Patterns Found

None blocking. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers in any file this phase modified. The one prior code-review cycle (`12.4-REVIEW.md`) found and fixed two Warnings (a brew-binary-resolution logic bug, a triplicated `bin_dir`-override snippet) and one Info item before this verification ran; both fixes are confirmed present in the current source, not merely claimed in the review report.

### Decision Coverage

All five of `12.4-CONTEXT.md`'s locked decisions (D-01, D-01a, D-02, D-03, D-04) are traceable in the shipped artifacts: D-01/D-01a in `.claude/architecture.md` + SKILL.md, D-02/D-03 in `installer/postinstall.py` + the two registry entries, D-04 in `12.4-AUDIT.md` and `PROJECT.md`'s consolidated decision rows. No decision from CONTEXT.md is missing from the shipped code or docs.

### Human Verification Required

N/A — infrastructure/tooling phase (installer postinstall dispatch, registry catalog audit, internal documentation/skill authoring). No user-facing UI or interactive flow to exercise. All must-haves are either directly code-verifiable (presence + wiring + passing unit tests, including tests that exercise the conditional cursor-agent-gated-on-claude behavior and the per-host argv composition) or are live-verification claims whose evidentiary transcripts were checked for presence in the SUMMARY files per this verification's explicit scope, and were found present and specific (binary versions, checksums, exit codes, byte-for-byte survival confirmations) rather than bare assertions.

### Gaps Summary

None. All 9 derived must-haves (from the four plans' `must_haves.truths` frontmatter, cross-referenced against `12.4-CONTEXT.md`'s five locked decisions) are VERIFIED against the actual codebase — not merely claimed in SUMMARY.md. `make validate && make test` are green on the current tree (1875 passed, 1 skipped, 95.35% coverage, well above the 90% floor). The phase goal — a repeatable onboarding checklist/skill, the rtk/graphify postinstall gap closed, and a full 89-entry catalog audit — is observably achieved in the codebase.

One minor prose imprecision noted (not a gap): Plan 01's frontmatter and SUMMARY describe "all 11 existing codegraph tests" where the pre-refactor file in fact contains 10 `test_codegraph_*` functions (12 total test functions including two shared `run_postinstall` dispatch tests). `git diff` against the pre-Plan-01 commit confirms zero codegraph test functions were added, removed, or modified — the substantive claim (behavior-preserving refactor, all pre-existing tests pass unmodified) holds; only the headline count in the prose is off by one.

---

_Verified: 2026-09-11T15:20:00Z_
_Verifier: Claude (gsd-verifier)_
