---
phase: 12.4-tool-onboarding-research-skill-and-registry-postinstall-audi
plan: 02
type: execute
wave: 2
cross_ai: true
depends_on: ["12.4-01"]
files_modified:
  - installer/postinstall.py
  - installer/model.py
  - installer/registry.toml
  - tests/test_postinstall.py
autonomous: true
requirements: [D-02, D-03, D-01a]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "After graphify installs on a machine with any of claude/opencode/codex/cursor-agent present, graphify's postinstall hook registers per-host non-interactively via that host's own subcommand form (graphify <host> install), never a --platform CSV pattern that doesn't exist for these four hosts."
    - "graphify's postinstall hook reuses the same present_agent_hosts() helper _rtk_register and _codegraph_mcp_register use (D-03) -- no third, independently-reimplemented host-presence loop exists anywhere in installer/postinstall.py."
    - "graphify's writes to opencode/codex/cursor-agent's own config surfaces (AGENTS.md, .opencode/opencode.json, .cursor/rules/graphify.mdc) are merge-safe against pre-existing adversarial content on each of those three hosts, not only the claude host 12.4-RESEARCH.md's own Pitfall 5 already adversarially verified -- closing Assumption A2."
  artifacts:
    - installer/postinstall.py (_graphify_register, POSTINSTALL_HOOKS gains 'graphify-register')
    - installer/model.py (POSTINSTALL_HOOK_NAMES gains 'graphify-register')
    - "installer/registry.toml (graphify [[tool]] entry gains postinstall = \"graphify-register\" plus a dated # Verified comment recording the 4-host adversarial-merge-safety evidence)"
  key_links:
    - "installer/engine.py::install_tool's existing tool.postinstall dispatch (unchanged this plan) -> installer.postinstall.run_postinstall('graphify-register', ...) -> _graphify_register -> present_agent_hosts(tools) (the SAME D-03 helper 12.4-01 built)"
    - "graphify's single uv-tool [[tool.method]] (installer/registry.toml:1541) -> bin_dir(method.params.get('bin_dir')) / 'graphify' -- no method.kind branching needed, unlike rtk"
---

<objective>
Close the second half of D-02: wire `graphify`'s already-researched, non-interactive per-host registration
commands into `installer/postinstall.py`'s dispatch table, reusing the exact same `present_agent_hosts()`
D-03 helper Plan 12.4-01 built for `_codegraph_mcp_register`/`_rtk_register` (never a third, independently
re-derived host-presence loop). `graphify` is structurally simpler than `rtk` — one method kind (`uv-tool`),
one uniform per-host subcommand shape (`graphify <host> install`), and 12.4-RESEARCH.md's own live
adversarial test already proved the `claude` host's writes are merge-safe — so this plan's own live
verification work focuses on closing Assumption A2: whether that same merge-safety generalizes to
`opencode`/`codex`/`cursor-agent`, which were only smoke-tested for a clean exit code in the research
session, never adversarially seeded.

Purpose: `graphify` has been a fully installable catalog tool since Phase 8 (08-01-PLAN.md), but its
own per-host setup was explicitly deferred ("Phase 9" in 08-RESEARCH.md's own Open Question 2) and never
picked back up — this plan is that follow-through, completing the rtk/graphify gap this whole phase exists
to close.
Output: `installer/postinstall.py` gains `_graphify_register()`; `installer/model.py`'s
`POSTINSTALL_HOOK_NAMES` gains `"graphify-register"`; `installer/registry.toml`'s `graphify` entry declares
`postinstall = "graphify-register"`; a TDD test suite mirroring the codegraph/rtk pattern; live adversarial
evidence closing Assumption A2 for the three previously-untested hosts.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-CONTEXT.md
@.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-RESEARCH.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@installer/postinstall.py
@installer/model.py
@installer/locations.py
@tests/test_postinstall.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _graphify_register() for all four agent hosts, wired end-to-end</name>
  <files>installer/postinstall.py, installer/model.py, installer/registry.toml, tests/test_postinstall.py</files>
  <read_first>
    - `installer/postinstall.py` as left by Plan 12.4-01 — `present_agent_hosts()` (D-03 helper),
      `_rtk_register`/`_resolve_rtk_binary`, and `POSTINSTALL_HOOKS` (now containing `"codegraph-mcp-register"`
      and `"rtk-register"`) are the direct precedent this task extends, not re-derives.
    - `installer/registry.toml:1533-1541` (`graphify`'s `[[tool]]` block) — ONE method:
      `[[tool.method]] kind = "uv-tool", pypi_pkg = "graphifyy"`. No `bin_dir` param is declared, so
      resolution is always the default `~/.local/bin` — there is no `method.kind` branching to write here,
      unlike rtk's github_release-vs-brew split.
    - 12.4-RESEARCH.md's "Pattern: `_rtk_register()` / `_graphify_register()`" table and the
      `_graphify_register()` skeleton under "Code Examples" — the exact per-host subcommand form
      (`graphify <host> install`, NOT `graphify install --platform <host>` — that form exists for other
      hosts graphify supports but is not this project's catalog vocabulary) and the confirmed default
      global/user scope (no `--project` flag needed, unlike rtk's explicit `-g`).
    - "State of the Art" table's second row — graphify's own command reference is a MIX of two CLI shapes
      across all the hosts IT supports; this project's four catalog hosts (`claude`/`opencode`/`codex`/
      `cursor-agent`) all use the `graphify <host> install` subcommand form specifically (confirmed live
      this research session), so this task does not need to branch on shape — only the host->subcommand
      NAME differs (`cursor-agent` -> `cursor`).
  </read_first>
  <action>
Add to `installer/postinstall.py`:
```
_GRAPHIFY_HOST_SUBCOMMANDS: dict[str, str] = {
    "claude": "claude",
    "opencode": "opencode",
    "codex": "codex",
    "cursor-agent": "cursor",
}

def _graphify_register(method: Method, runner: Runner, tools: Mapping[str, Tool]) -> str | None:
    present = present_agent_hosts(tools)
    if not present:
        return None
    bin_dir_param = method.params.get("bin_dir")
    override = bin_dir_param if isinstance(bin_dir_param, str) and bin_dir_param else None
    graphify_bin = str(bin_dir(override) / "graphify")
    errors: list[str] = []
    for host_id, subcommand in _GRAPHIFY_HOST_SUBCOMMANDS.items():
        if host_id not in present:
            continue
        try:
            runner([graphify_bin, subcommand, "install"])
        except CommandError as exc:
            errors.append(f"{host_id}: {exc}")
    return "; ".join(errors) if errors else None
```
Declared dict order (`claude, opencode, codex, cursor-agent`) matches `_rtk_register`'s own declared order
and `_CODEGRAPH_TARGETS`'s — deterministic iteration, no sorting needed, matching this project's existing
convention across all three hooks. Register `"graphify-register": _graphify_register` in
`POSTINSTALL_HOOKS`. Add `"graphify-register"` to `installer/model.py`'s `POSTINSTALL_HOOK_NAMES` frozenset
(now three entries). Add `postinstall = "graphify-register"` inside `installer/registry.toml`'s `graphify`
`[[tool]]` block (after `desc`, before `[[tool.method]]`).

RED: add tests to `tests/test_postinstall.py` mirroring the rtk pattern — bin resolution via `bin_dir`
(graphify has only one method kind, so no kind-branching test is needed, unlike rtk's github_release/brew
split); a subset-matrix test over the 16 host-presence combinations asserting the exact composed argv per
host (`[graphify_bin, "cursor", "install"]` for `cursor-agent`, etc. — cite Pitfall 4's "never a CSV, one
invocation per host" guarantee explicitly in a test name or docstring, since a copy-paste from codegraph's
shape would wrongly try to compose one multi-host call); a `CommandError` on one host captured as a partial
warning while the remaining hosts still get attempted; an empty `present_agent_hosts()` result short-circuits
to `None` with zero `runner()` calls. GREEN: implement, run green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_postinstall.py tests/test_model.py -q</automated>
    <fails_when>non-zero exit; any pre-existing codegraph/rtk test regresses; a new `_graphify_register` test fails; or `test_postinstall_hook_names_matches_the_real_dispatch_table` fails because the frozenset and the dispatch table's keys disagree.</fails_when>
  </verify>
  <done>
`_graphify_register` is fully wired (registry field, model validation, dispatch table) for all four agent
hosts, reuses `present_agent_hosts()` (no reimplemented host-presence logic), and its full TDD test suite is
green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Live adversarial verification closing Assumption A2 (opencode/codex/cursor-agent merge-safety)</name>
  <files>installer/registry.toml</files>
  <read_first>
    - 12.4-RESEARCH.md's "Pitfall 5" section in full — the exact adversarial methodology already proven for
      the `claude` host: seed `CLAUDE.md` with marker content and `.claude/settings.json` with a pre-existing
      `permissions.allow`, an unrelated `PreToolUse` hook on a different matcher, and a custom `statusLine`;
      run `graphify claude install --project` twice; confirm every pre-existing element survives byte-for-byte
      and the second run adds zero duplicates.
    - 12.4-RESEARCH.md's "Assumption A2" row in the Assumptions Log — the exact gap this task closes: the
      SAME rigor was never applied to `opencode` (`AGENTS.md`, shared with `codex`), `codex` (also
      `AGENTS.md`), and `cursor-agent` (`.cursor/rules/graphify.mdc`) — those three were only smoke-tested
      for a clean exit code against a CLEAN scratch project, never against pre-existing conflicting content.
  </read_first>
  <action>
Using the locally installed `graphify` binary (`~/.local/bin/graphify`, confirmed this session at v0.9.54 —
close enough to the registry's unpinned "latest" resolution that no fresh download is required, unlike
rtk's version-sensitive Pitfall 1/2/3 findings): in a fresh scratch project directory (`mktemp -d`, never
this repository or the real `$HOME` — ONESHOT-RULES Rule 5), run the SAME adversarial-seed methodology
Pitfall 5 already proved for `claude`, once per remaining host:

1. **`opencode`/`codex` (share `AGENTS.md`):** seed the scratch project's `AGENTS.md` with marker content
   plus an unrelated existing section, and seed `.opencode/opencode.json` with a pre-existing config key
   unrelated to graphify. Run `graphify opencode install` (project scope, matching the `claude` test's own
   `--project` flag if graphify's `opencode` subcommand accepts an equivalent, else its own documented
   default scope). Confirm the seeded `AGENTS.md` content and `opencode.json` key both survive byte-for-byte
   with graphify's own section/key appended, not replacing. Then run `graphify codex install` against the
   SAME already-modified `AGENTS.md` (both hosts share the file per 12.4-RESEARCH.md's own "State of the
   Art" note) and confirm it remains additive/idempotent — no duplicate graphify section, no loss of either
   host's own addition.
2. **`cursor-agent`:** seed `.cursor/rules/` with a pre-existing unrelated `.mdc` rule file. Run `graphify
   cursor install`. Confirm the pre-existing rule file is untouched and graphify's own
   `.cursor/rules/graphify.mdc` (or equivalent, per graphify's actual output — confirm the real filename
   live rather than assuming it matches this exact name) is added alongside it, not overwriting.
3. Re-run all three commands a second time against the SAME already-modified scratch project (idempotency):
   confirm no duplicate entries, no data loss, exit 0 on every rerun.
4. Record the verbatim transcript (files before/after, byte-for-byte diff or explicit "unchanged" confirmation)
   in this task's commit message / the plan's SUMMARY.

Add a `# Verified {today's date}: ...` comment directly above `graphify`'s `[[tool]]` block in
`installer/registry.toml`, extending (not replacing) any comment Task 1 or a prior phase already left there,
recording that all four hosts' writes are now confirmed merge-safe and idempotent against adversarial
pre-existing content — not just the `claude` host 12.4-RESEARCH.md itself already proved.

If any host's write turns out NOT merge-safe (a genuine regression from the `claude` host's proven
behavior): do not silently accept it. Record the exact failure mode in the registry comment and in this
task's SUMMARY as an open finding, and flag it for Plan 12.4-04's audit to log as a follow-up — do not
weaken `_graphify_register`'s own contract (it still only decides WHICH hosts to call, not what graphify
itself does to those hosts' files) to paper over an upstream tool's own bug.
  </action>
  <verify>
    <automated>grep -c "graphify" installer/registry.toml | grep -v '^0$'</automated>
    <fails_when>the grep reports 0 (the registry entry or its verification comment went missing), or the task's own commit message / SUMMARY does not record a pass/fail transcript for all three previously-untested hosts (opencode, codex, cursor-agent).</fails_when>
  </verify>
  <done>
All four agent hosts' `graphify` writes are now live-verified merge-safe and idempotent against adversarial
pre-existing content, closing 12.4-RESEARCH.md's Assumption A2, with the evidence recorded as both a
commit-message transcript and a dated registry comment.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_graphify_register` <-> real per-host config surfaces (`AGENTS.md`, `.opencode/opencode.json`, `.cursor/rules/*.mdc`) | The hook decides which hosts to call; graphify's own write behavior inside each host's config is what Task 2's adversarial test actually exercises. |
| Task 2's live-verification scratch project <-> the real dev machine's real `$HOME`/this repository | Every adversarial-seed command in this plan runs against a throwaway scratch project directory, never this repository or the real `$HOME`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.4-05 | Tampering | `graphify`'s writes to `opencode`/`codex`/`cursor-agent` config surfaces | high | mitigate | Task 2's adversarial-seed test reproduces the SAME rigor already proven safe for `claude`, closing Assumption A2 before this hook ships for the three previously-untested hosts. |
| T-12.4-06 | Elevation of Privilege | Over-broad host registration | critical | mitigate | `_graphify_register` reuses the SAME `present_agent_hosts()` (D-03) gate `_rtk_register`/`_codegraph_mcp_register` use — no independent, potentially-inconsistent host-detection logic. |
| T-12.4-07 | Repudiation | Claiming Assumption A2 is closed without evidence | medium | mitigate | Task 2 requires a recorded pass/fail transcript per host plus a dated registry comment, not a bare assertion. |

No package-manager install tasks exist in this plan (`graphify` is already a legitimacy-verified Phase 8
catalog entry, installed via its existing `uv-tool` method — this plan only adds code that runs AFTER that
install succeeds) — the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
Run `uv run pytest tests/test_postinstall.py tests/test_model.py -q` — all pre-existing codegraph/rtk tests
plus every new `_graphify_register` test green. Confirm `installer/registry.toml`'s `graphify` entry declares
`postinstall = "graphify-register"` and carries a dated `# Verified` comment covering all four hosts. Confirm
Task 2's commit message / SUMMARY records the adversarial-merge-safety transcript for `opencode`/`codex`/
`cursor-agent`, and that no command touched this repository or the real `$HOME`.
</verification>

<success_criteria>
- `_graphify_register` is fully wired (registry field, model validation, dispatch table) for all four agent
  hosts, reusing the D-03 shared helper.
- Merge-safety/idempotency is live-evidenced for ALL FOUR hosts, not just the `claude` host research already
  proved — Assumption A2 is closed.
- `uv run pytest tests/test_postinstall.py tests/test_model.py -q` is green.
- No command run during this plan's execution mutated this repository's own source tree outside the declared
  `files_modified`, or this machine's real `$HOME`.
</success_criteria>

<output>
Create `.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-02-SUMMARY.md` when done
</output>
