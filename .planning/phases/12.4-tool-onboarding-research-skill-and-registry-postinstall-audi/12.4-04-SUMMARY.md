---
phase: 12.4-tool-onboarding-research-skill-and-registry-postinstall-audi
plan: 04
subsystem: documentation
tags: [audit, registry, tech-debt, decisions-consolidation]

requires:
  - phase: 12.4-02
    provides: rtk/graphify postinstall wiring, the concrete worked example this audit checks
  - phase: 12.4-03
    provides: the onboarding checklist this audit applies
provides:
  - "12.4-AUDIT.md: full 89-entry checklist-driven audit, zero new gaps found"
  - Phase 12.4 decisions consolidated into PROJECT.md and architecture.md
affects: []

actuals:
  tokens: 36000
  tasks: 2
  commits: 1

tech-stack:
  added: []
  patterns: []

key-files:
  created:
    - .planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-AUDIT.md
    - .planning/todos/pending/2026-09-11-remove-orphaned-tool-post-install-plural-field.md
  modified:
    - .planning/PROJECT.md
    - .claude/architecture.md

key-decisions:
  - "Full 89-entry audit found zero new postinstall-mechanism gaps beyond the already-closed rtk/graphify one. One live finding recorded (spec-kit's upstream now documents --non-interactive) that does not change its manual-required classification -- the real blocker is a user-approved project-directory target, not prompt interactivity."
  - "Tool.post_install (plural) dead-code finding logged as a pending todo rather than fixed in-phase, per 12.4-RESEARCH.md's explicit guidance and Plan 12.2-01's prior precedent of leaving it untouched."
  - "graphify output-location question closed as a no-code-change finding: gsd-graphify's already-installed skill already does the build-then-copy two-stage design 12.4-CONTEXT.md worried was missing, and the question concerns a global cross-project GSD asset and a different graphify subcommand than D-02's own per-host registration."

patterns-established: []

requirements-completed: [D-04]

coverage:
  - id: D1
    description: "Every one of the 89 registry.toml entries checked against the onboarding checklist, with every finding disposed of (pass/fixed-here/logged-as-todo)."
    requirement: D-04
    verification:
      - kind: unit
        ref: "test -f 12.4-AUDIT.md && grep -c '89' 12.4-AUDIT.md"
        status: pass
    human_judgment: false
  - id: D2
    description: "Phase 12.4's D-01/D-01a/D-02/D-03/D-04 decisions consolidated into PROJECT.md's Key Decisions table and a new Phase 12.4 section in architecture.md, both citing real evidence."
    requirement: D-04
    verification:
      - kind: unit
        ref: "grep for the new PROJECT.md rows and the '## Phase 12.4' architecture.md heading"
        status: pass
    human_judgment: false
  - id: D3
    description: "make validate && make test green on the phase's final tree."
    requirement: D-04
    verification:
      - kind: unit
        ref: "make validate && make test — 1874 passed, 1 skipped, 95.35% coverage"
        status: pass
    human_judgment: false

completed: 2026-09-11
status: complete
---

# Phase 12.4 Plan 04: Full-catalog onboarding audit + decisions consolidation — Summary

**The D-04 audit checked all 89 `registry.toml` entries against Phase 12.4's own new onboarding checklist and found zero new postinstall-mechanism gaps beyond the already-closed rtk/graphify one; Phase 12.4's five real decisions (D-01/D-01a/D-02/D-03/D-04) are now consolidated into `PROJECT.md` and `architecture.md`, and `make validate && make test` are green on the phase's final tree.**

## Cross-AI dispatch

`podman logs --tail 8 omniroute` unchanged since Plan 03's check — same failure signature persisting throughout this phase's entire execution window (grok-cli quota exhausted, deepseek-v4-flash connection expired, last log activity 13:39:36Z). Proceeded directly to direct execution for this final plan. Recorded in scratchpad `12.4-04-crossai-attempt-failure.md`.

## Accomplishments
- **Pass 1 (postinstall-mechanism classification, ai-tier focus):** manually checked all 22 `ai`-tier entries. Confirmed the 5 real agent hosts need no postinstall wiring (they ARE the host), codegraph/rtk/graphify are correctly wired, `pi`'s `host_setup` classification was re-derived fresh (not assumed) via a live `gh api` README check — still genuinely interactive. Live-checked all 7 `skill_pack` entries' current upstream install docs for a newly-non-interactive flag; found one (`spec-kit`'s `specify init --non-interactive`) but confirmed it doesn't change the classification since the real blocker is a user-approved project-directory target.
- **Pass 2 (tier/dependency correctness, full 89-entry sweep):** programmatic `tomllib`-based sweep of every `node`/`uv-tool`/`sdkman`-kind method against its `requires` declaration — zero missing manager-dependency declarations found across all 89 entries.
- **Pass 3:** confirmed and logged the pre-existing `Tool.post_install` (plural) dead-code finding as `.planning/todos/pending/2026-09-11-remove-orphaned-tool-post-install-plural-field.md`.
- **Pass 4:** closed 12.4-CONTEXT.md's open "graphify output-location unification" research question by reading the installed `gsd-graphify` skill directly — confirmed it already does the build-then-copy design the concern assumed was missing.
- Added Phase 12.4's 4 rows to `.planning/PROJECT.md`'s Key Decisions table and a new `## Phase 12.4: tool onboarding and registry postinstall audit` section to `.claude/architecture.md`.
- `make validate && make test` green: 1874 passed, 1 skipped, 95.35% branch coverage (floor 90%).

## Task Commits

Executed directly (cross-AI skipped per the recorded Rule 12 disposition above) as a single atomic commit covering both tasks.

## Files Created/Modified
- `.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-AUDIT.md` — new, full audit record.
- `.planning/todos/pending/2026-09-11-remove-orphaned-tool-post-install-plural-field.md` — new, logged finding.
- `.planning/PROJECT.md` — Key Decisions table gains 4 Phase 12.4 rows.
- `.claude/architecture.md` — new `## Phase 12.4` section.

## Decisions Made
- All recorded above under key-decisions.

## Deviations from Plan

None — plan executed exactly as written. `git status --short` after both tasks confirmed exactly the declared `files_modified` plus the audit/todo artifacts the plan itself directed, no stray files.

## Issues Encountered
None.
