---
phase: 12.4-tool-onboarding-research-skill-and-registry-postinstall-audi
plan: 03
type: execute
wave: 2
cross_ai: true
depends_on: ["12.4-01"]
files_modified:
  - .claude/architecture.md
  - .claude/skills/tool-onboarding/SKILL.md
autonomous: true
requirements: [D-01, D-01a]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A future contributor adding a new registry.toml tool can read one checklist section in .claude/architecture.md and correctly decide tier, dependency declaration, and which of the three existing postinstall/setup mechanisms (postinstall hook / skill_pack+skill_lifecycle / host_setup / none) applies, without re-deriving the decision tree from scratch or from tribal knowledge."
    - "An invocable skill at .claude/skills/tool-onboarding/SKILL.md walks a user or agent through researching a new tool, deciding tier/dependencies/postinstall mechanism, and writing the registry.toml entry -- reusable for any future tool addition, not a one-off for this phase's rtk/graphify work."
    - "The checklist states D-01a's 'detect and configure, never blind-overwrite' principle as a standing rule for any future postinstall hook, citing the GSD-statusline precedent that motivated it."
  artifacts:
    - .claude/architecture.md (new checklist content under Registry-authoring guidelines)
    - .claude/skills/tool-onboarding/SKILL.md
  key_links:
    - ".claude/architecture.md's existing 'Per-tool, per-OS verification checklist' subsection -> this plan's extension (tier/dependency/postinstall additions), not a competing new section"
    - "The new SKILL.md's postinstall-mechanism decision step -> the SAME three-mechanism taxonomy documented in .claude/architecture.md (one canonical decision tree, not two copies that can drift apart)"
---

<objective>
Ship D-01's second deliverable (the first — the concrete `rtk`/`graphify` postinstall wiring — is Plans
12.4-01/12.4-02): a reusable "tool onboarding" checklist extension in `.claude/architecture.md` and a new
invocable skill (`.claude/skills/tool-onboarding/SKILL.md`, this project's first self-authored skill) that
any future `registry.toml` addition can be walked through — tier classification, dependency-tree
declaration, and postinstall-per-harness classification — so the rtk/graphify-shaped gap (researched, never
wired) does not recur silently for the next tool added to the catalog.

Purpose: turn the ad-hoc judgment that let rtk/graphify's postinstall gap sit unnoticed since Phase 8/9 into
a documented, repeatable decision process — both a doc a code reviewer can hold a new entry against, and a
skill that walks the process interactively.
Output: `.claude/architecture.md`'s "Registry-authoring guidelines" section gains a new checklist
subsection; `.claude/skills/tool-onboarding/SKILL.md` exists, following this project's Claude Code skill
conventions via `superpowers:writing-skills`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-CONTEXT.md
@.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-RESEARCH.md
@CLAUDE.md
@.claude/architecture.md
@installer/registry.toml
@installer/model.py
@installer/postinstall.py
@installer/host_setup.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend .claude/architecture.md's Registry-authoring guidelines with the tier/dependency/postinstall onboarding checklist</name>
  <files>.claude/architecture.md</files>
  <read_first>
    - `.claude/architecture.md`'s existing "Registry-authoring guidelines" section (`### Per-tool, per-OS
      verification checklist`, `### Prefer brew`, `### Platform-unavailable catalog rows...`) — this task
      adds a new `###` subsection immediately after "Per-tool, per-OS verification checklist" (before
      "Prefer brew"), extending the SAME guideline family, not a competing top-level section.
    - `.claude/architecture.md`'s "## Phase 9: postinstall hooks" section — the existing prose describing
      `Tool.postinstall`'s closed dispatch-table mechanism; this task's new checklist subsection references
      it by name rather than re-explaining the mechanism.
    - 12.4-RESEARCH.md's "Architecture Patterns" -> "The three existing postinstall/setup mechanisms (the
      checklist's decision tree)" section in full — this is the canonical content this task transcribes
      into `.claude/architecture.md` as permanent project documentation (research artifacts are ephemeral;
      architecture.md is not). Also its "Architectural Responsibility Map" table row for "Onboarding
      checklist".
    - `installer/model.py`'s `POSTINSTALL_HOOK_NAMES`/`SMOKE_CHECK_NAMES` comment at lines 14-19 — the
      "registry can only select a NAME from a closed set, never a literal command" invariant this checklist
      must restate as a standing rule (Known Threat Patterns table, 12.4-RESEARCH.md's Security Domain
      section: "Registry-driven arbitrary command injection... this phase's two new entries follow the
      identical pattern; the new checklist should explicitly re-state this invariant").
  </read_first>
  <action>
Add a new `###` subsection titled `### Tool onboarding: tier, dependencies, and postinstall classification`
directly after the existing `### Per-tool, per-OS verification checklist` subsection (before `### Prefer
brew`), inside `.claude/architecture.md`'s `## Registry-authoring guidelines` section. Content (adapt prose
to this project's existing documentation voice — declarative, cites real file/line evidence, no marketing
language):

1. **Tier classification rationale.** Before assigning `tier` (`system`/`user`/`ai`), state explicitly which
   of the three it is and why, per the existing "Tier is a browsing label" section's own definition: `system`
   = a prerequisite other tools may depend on regardless of personal preference (uv/pnpm/brew/sdkman/zsh/
   podman-shaped); `user` = a personal-choice dev tool with no agent-host relationship (terminal emulators,
   CLI utilities, editors); `ai` = agent-facing tooling — either an agent host itself, a tool an agent host
   consumes (MCP servers, token-reduction proxies, knowledge-graph builders), or an agent-ecosystem skill
   pack. This is not automated — the same "no lint enforces tier correctness" caveat the existing checklist
   already states for brew-preference applies here.

2. **Dependency-tree declaration.** Read the tool's OWN install documentation for its real prerequisites —
   never assume. If it installs via `pnpm add -g`/`uv tool install`/an SDKMAN candidate, its `requires` MUST
   name the manager tool (`pnpm`, `uv`, `sdkman`) so `installer/deps.py::resolve_dependencies` drags it in
   automatically; a tool with an undeclared real dependency silently relies on the user having installed the
   prerequisite through some other path. Cross-check against a worked example already in the registry —
   `mmdc.requires = ["pnpm", "puppeteer"]` (Phase 5) is the standard this checklist item holds new entries to.

3. **Postinstall-per-harness classification.** Ask, in order, exactly the three questions 12.4-RESEARCH.md's
   "Architecture Patterns" section poses (transcribe its decision tree here verbatim, adapted to prose/a
   short ASCII tree matching this file's existing style — see the "Registry-authoring guidelines" section's
   own worked-example convention):
   - Is the tool a fully-installable, on-PATH binary/CLI after a normal `Method` succeeds, AND does it need a
     non-interactive, host-detection-aware follow-up (register an MCP server, patch a per-agent config
     file)? -> `Tool.postinstall = "<hook-name>"`, dispatched through `installer/postinstall.py`'s closed
     `POSTINSTALL_HOOKS` table. Worked examples: `codegraph` (Phase 9), `rtk`/`graphify` (Phase 12.4).
   - Is the tool delivered AS a skill into one or more AI-assistant hosts, where install/status/update/removal
     are each either fundamentally interactive or need a human trust-review step? -> `[[tool.method]] kind =
     "skill_pack"` with `[tool.skill_lifecycle]` (`mode = "manual-required"` or `mode = "command"` for a
     closed, read-only `operation_id` status probe). Worked examples: `ponytail`, `openspec`, `opengsd`, and
     the other five `skill_pack` entries already in `installer/registry.toml`.
   - Does the tool's own setup require a live terminal or user-selected project target that cannot be safely
     scripted from a Textual worker thread? -> `[[tool.method]] kind = "host_setup"`, `setup_id = "<id>"`
     resolved through `installer/host_setup.py::HOST_SETUPS` (a closed registry of reviewed console
     handoffs, displayed not executed). Worked example: `pi`.
   - None of the above -> no postinstall wiring; the tool is a plain binary with nothing further to do.

   Restate explicitly: **the registry can only select a hook NAME from a closed, code-owned set in every one
   of these three mechanisms — never a literal command string.** A registry edit alone can never introduce
   arbitrary post-install execution; this invariant must never be relaxed to give a future entry an
   inline-command escape hatch.

   State D-01a's principle as a standing rule for any future postinstall hook this checklist routes a tool
   into: **detect and configure, never blind-overwrite.** A hook must never pass a flag that overwrites
   existing host configuration unconditionally (the confirmed real-world precedent: GSD's own installer
   writes `gsd-statusline.js` on Claude Code automatically, with no flag to skip it — a genuine risk for any
   environment running a separate custom statusline). Every hook in this project's dispatch table gates on
   live host presence (`present_agent_hosts()`, `installer/postinstall.py`) before writing anything, and
   every hook's own per-host write behavior should be verified merge-safe against pre-existing config before
   shipping — not assumed.

   Do not confuse this NAMED, singular `Tool.postinstall` mechanism with `installer/model.py`'s similarly-
   named but semantically distinct `Tool.post_install` (plural) field and its `POST_INSTALL_ACTIONS` tuple —
   the latter has zero production callers as of this phase (see 12.4-AUDIT.md, Plan 12.4-04) and is a
   separate, unrelated concept despite the near-identical name.

Keep the new subsection's length proportionate to the existing "Per-tool, per-OS verification checklist"
subsection (concise, declarative, cites real files/examples) — this is a process document, not a tutorial.
  </action>
  <verify>
    <automated>grep -c "Tool onboarding: tier, dependencies, and postinstall classification" .claude/architecture.md</automated>
    <fails_when>grep reports 0 (the new subsection heading is missing), or the subsection does not contain all three of "Tier classification", "Dependency-tree declaration", and "Postinstall-per-harness classification" as findable substrings.</fails_when>
  </verify>
  <done>
`.claude/architecture.md`'s Registry-authoring guidelines section documents the tier/dependency/postinstall
onboarding checklist, citing the three existing mechanisms (`postinstall` hook / `skill_pack` / `host_setup`)
by their real worked examples and restating both the closed-name invariant and D-01a's
detect-and-configure-never-overwrite principle.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author .claude/skills/tool-onboarding/SKILL.md, the new invocable onboarding skill</name>
  <files>.claude/skills/tool-onboarding/SKILL.md</files>
  <read_first>
    - `~/.claude/skills/graphify/SKILL.md` (or any other locally installed skill) — the concrete YAML
      frontmatter shape this project's skills already follow: `name:`, `description:` (a trigger-phrase
      description, not a summary), then a Markdown body.
    - Invoke `superpowers:writing-skills` for the authoritative frontmatter/trigger-convention format before
      writing this file — 12.4-RESEARCH.md's own "Don't Hand-Roll" recommendation, since this is this
      project's FIRST self-authored skill and there is no existing project convention to copy from directly.
    - Task 1's new `.claude/architecture.md` checklist subsection (just written) — this skill's body walks
      the SAME three checklist items interactively; it must not invent a second, divergent decision tree.
  </read_first>
  <action>
Create `.claude/skills/tool-onboarding/SKILL.md` with YAML frontmatter (`name: tool-onboarding`,
`description:` a trigger-phrase description covering "adding a new tool to registry.toml", "onboarding a
new catalog tool", "deciding tier/postinstall for a new tools-installer entry" — confirm the exact
frontmatter field set via `superpowers:writing-skills` rather than guessing).

Body walks four steps, each referencing the concrete mechanism it routes into (not abstract advice):

1. **Research the tool.** What is it, how does it install (official docs, not a guess), what OS/arch
   variants exist, does it need a checksum-verifiable release asset or a script/package-manager install —
   mirrors this project's own "Per-tool, per-OS verification checklist" (`.claude/architecture.md`) bar:
   read the tool's actual install script or package metadata directly.

2. **Decide tier.** Apply the tier classification rationale from Task 1's new checklist subsection
   (`system`/`user`/`ai`, per `.claude/architecture.md`'s "Tier is a browsing label" section).

3. **Decide dependencies and postinstall mechanism.** Apply the SAME dependency-tree-declaration and
   postinstall-per-harness three-mechanism decision tree Task 1 wrote into `.claude/architecture.md` — this
   skill's body should reference that section by name (`.claude/architecture.md`'s "Tool onboarding: tier,
   dependencies, and postinstall classification" subsection) rather than duplicating the full decision tree
   text inline, so there is exactly ONE canonical copy and the two documents cannot silently drift apart.

4. **Write the registry.toml entry.** The `[[tool]]` block shape (`id`, `name`, `category`, `cmd`, `priority`,
   `audience`, `tier`, `desc`, `requires`, optionally `postinstall`), one or more `[[tool.method]]` blocks
   per the tool's real install path(s), and a dated `# Verified {date}: ...` comment recording what was
   checked — the existing, mandatory recording mechanism `.claude/architecture.md`'s
   "Per-tool, per-OS verification checklist" already requires for every new entry.

Explicitly note in the skill body: this skill is a PROCESS aid (a checklist walked interactively), not a
code generator — it does not itself write `installer/*.py` changes; a `postinstall` hook implementation
(if step 3 routes the tool there) still follows the existing `installer/postinstall.py`
`POSTINSTALL_HOOKS`/`installer/model.py` `POSTINSTALL_HOOK_NAMES` wiring pattern, exactly as `codegraph`/
`rtk`/`graphify` demonstrate.
  </action>
  <verify>
    <automated>test -f .claude/skills/tool-onboarding/SKILL.md && grep -q "^name:" .claude/skills/tool-onboarding/SKILL.md</automated>
    <fails_when>the file does not exist, or it has no `name:` frontmatter key (malformed skill file that Claude Code could not discover/invoke).</fails_when>
  </verify>
  <done>
`.claude/skills/tool-onboarding/SKILL.md` exists with valid frontmatter and a body walking the same
tier/dependency/postinstall decision process `.claude/architecture.md` now documents, reusable for any
future `registry.toml` addition.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This plan's documentation edits <-> future contributor behavior | The checklist and skill are process aids with no automated enforcement — their only "trust boundary" is whether a future registry addition actually gets held against them in code review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.4-08 | Tampering | A future registry entry's `postinstall` field | medium | mitigate | The new checklist explicitly restates the closed-name-only invariant (never a literal command string) so a future onboarding pass does not "helpfully" add an inline-command escape hatch — the same Known Threat Pattern 12.4-RESEARCH.md's Security Domain section names. |
| T-12.4-09 | Repudiation | Two divergent copies of the postinstall decision tree (architecture.md vs. SKILL.md) drifting apart over time | low | mitigate | Task 2's SKILL.md explicitly references Task 1's `.claude/architecture.md` subsection by name rather than duplicating its full text, keeping exactly one canonical copy. |

No package-manager install tasks exist in this plan (pure documentation/skill authoring) — the Package
Legitimacy Gate does not apply.
</threat_model>

<verification>
Confirm `.claude/architecture.md` contains the new "Tool onboarding: tier, dependencies, and postinstall
classification" subsection with all three checklist items. Confirm `.claude/skills/tool-onboarding/SKILL.md`
exists with valid `name:`/`description:` frontmatter and a body referencing (not duplicating) the
architecture.md decision tree. Run `make validate` to confirm the doc/skill additions introduce no lint
regression (markdown is not linted by this project's gate, but the edit must not break any existing
`.claude/architecture.md` content the gate's other checks might incidentally reference).
</verification>

<success_criteria>
- `.claude/architecture.md`'s Registry-authoring guidelines section documents tier classification,
  dependency-tree declaration, and the three-mechanism postinstall decision tree, with D-01a's
  never-blind-overwrite principle stated as a standing rule.
- `.claude/skills/tool-onboarding/SKILL.md` exists, is discoverable (valid frontmatter), and references the
  SAME canonical decision tree rather than duplicating it.
- Neither document contradicts the rtk/graphify worked examples Plans 12.4-01/12.4-02 just shipped.
</success_criteria>

<output>
Create `.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-03-SUMMARY.md` when done
</output>
