---
phase: 12.4-tool-onboarding-research-skill-and-registry-postinstall-audi
plan: 02
subsystem: installer
tags: [postinstall, graphify, registry, tdd]

requires:
  - phase: 12.4-01
    provides: present_agent_hosts() shared host-presence helper (D-03)
provides:
  - graphify-register postinstall hook, wired for all four agent hosts
affects: ["12.4-04"]

actuals:
  tokens: 30000
  tasks: 2
  commits: 1

tech-stack:
  added: []
  patterns:
    - "One graphify <host> install invocation per present host, never a composed CSV (Pitfall 4)"

key-files:
  created: []
  modified:
    - installer/postinstall.py
    - installer/model.py
    - installer/registry.toml
    - tests/test_postinstall.py
    - tests/test_registry.py

key-decisions:
  - "_graphify_register reuses present_agent_hosts() (D-03), no independently re-derived host-presence loop -- third hook, same pattern as codegraph/rtk."
  - "Confirmed live (contra an initial mis-test with `--help`) that graphify's real per-host subcommand form is `graphify <host> install`, matching 12.4-RESEARCH.md's table exactly -- not the `--platform` form used by other, non-catalog hosts."
  - "Closed Assumption A2: opencode/codex/cursor-agent's writes are live-verified merge-safe and idempotent against adversarial pre-existing content, the same rigor 12.4-RESEARCH.md's Pitfall 5 already proved for claude. No regression found on any of the three hosts -- all three survived pre-existing content byte-for-byte and were idempotent on rerun."

patterns-established: []

requirements-completed: [D-02, D-03, D-01a]

coverage:
  - id: D1
    description: "graphify's postinstall hook registers per-host via graphify <host> install for every present agent host, reusing present_agent_hosts()."
    requirement: D-02
    verification:
      - kind: unit
        ref: "tests/test_postinstall.py::test_graphify_hook_never_composes_a_multi_host_csv_call (16 subsets) + composition/error/no-duplicate tests"
        status: pass
    human_judgment: false
  - id: D2
    description: "graphify's writes to opencode/codex/cursor-agent config surfaces are merge-safe against adversarial pre-existing content and idempotent, closing Assumption A2."
    requirement: D-01a
    verification:
      - kind: manual_procedural
        ref: "Live adversarial transcript below: seeded AGENTS.md/opencode.json/.cursor/rules content, ran graphify opencode/codex/cursor install, confirmed byte-for-byte survival plus idempotent rerun with zero duplicates"
        status: pass
    human_judgment: false

completed: 2026-09-11
status: complete
---

# Phase 12.4 Plan 02: graphify postinstall wiring (all four hosts) + Assumption A2 closure — Summary

**graphify's per-host registration (`graphify <host> install`) is now wired into the same postinstall dispatch table rtk/codegraph use, reusing `present_agent_hosts()` (D-03); live adversarial testing on opencode/codex/cursor-agent found zero merge-safety regressions from the claude host's already-proven behavior, closing 12.4-RESEARCH.md's Assumption A2.**

## Cross-AI dispatch

Checked `podman logs --tail 15 omniroute` fresh before starting (2026-09-11T13:39Z): same failure signature as Plan 01's actual dispatch attempt at 13:26Z — grok-cli quota exhausted, deepseek-v4-flash connection expired, a "Combo loop safety timeout... force-terminating" entry seconds before the check. No specific reason to believe recovery happened in the intervening ~13 minutes. Per the standing instruction not to blindly re-hammer a backend already demonstrated down this session, proceeded directly to direct execution rather than spending another dispatch+timeout cycle. Recorded in scratchpad `12.4-02-crossai-attempt-failure.md`.

## Accomplishments
- `_graphify_register` wired end-to-end for all four agent hosts (`claude`, `opencode`, `codex`, `cursor-agent`), reusing `present_agent_hosts()`.
- Corrected an initial mis-test: `graphify claude install --help` printed a generic usage hint, suggesting the subcommand form might not exist — but `--help` isn't a recognized flag for that specific subcommand, not evidence the subcommand is invalid. Re-tested without `--help` and confirmed `graphify claude install` works exactly as 12.4-RESEARCH.md's table describes. `graphify --help`'s full command list also directly confirms `claude install`, `codex install`, `opencode install`, `cursor install` are all real, distinct subcommands.
- Live adversarial verification closed Assumption A2 for `opencode`/`codex` (shared `AGENTS.md`) and `cursor-agent` (`.cursor/rules/graphify.mdc`) — all three previously only smoke-tested for a clean exit code, never against pre-existing conflicting content.
- `tests/test_registry.py::test_only_codegraph_and_rtk_declare_a_postinstall_hook` renamed to `test_only_codegraph_rtk_and_graphify_declare_a_postinstall_hook` and updated.

## Live Verification Transcript

**Binary:** locally installed `graphify` 0.9.54 (`~/.local/bin/graphify`) — no fresh download needed (unlike rtk's version-sensitive findings, graphify's behavior is not version-gated per 12.4-RESEARCH.md).
**Scratch project:** a fresh `mktemp -d` directory, never this repository or the real `$HOME`.

1. **opencode/codex shared `AGENTS.md` + `opencode.json`:** seeded `AGENTS.md` with an unrelated section plus a `MARKER-AGENTS-SEED-CONTENT` line, and `.opencode/opencode.json` with a pre-existing unrelated key (`unrelatedExistingKey`) and a nested object. Ran `graphify opencode install` — exit 0, both survived byte-for-byte, graphify's own section/plugin entry appended (not replacing). Ran `graphify codex install` against the SAME modified `AGENTS.md` — exit 0, output: `graphify already configured ... (no change)`; confirmed by `grep -c "## graphify" AGENTS.md` == 1 (no duplicate section) and the seed marker still present.
2. **`cursor-agent`:** seeded `.cursor/rules/unrelated.mdc` with a marker line. Ran `graphify cursor install` — exit 0, `.cursor/rules/graphify.mdc` added alongside the untouched `unrelated.mdc`.
3. **Idempotency rerun (all three, same already-modified scratch project):** `graphify opencode install` → `plugin already registered (no change)`; `graphify codex install` → `AGENTS.md ... (no change)`, and `.codex/hooks.json` content-inspected to confirm zero duplicate hook entries despite reprinting "PreToolUse hook registered" both runs; `graphify cursor install` → `already configured (no change)`. All exit 0, zero duplicates on any surface.

No command in this task touched this repository's source tree or the real `$HOME`. All scratch directories were deleted after verification.

## Task Commits

Executed directly (cross-AI skipped per the recorded Rule 12 disposition above) as a single atomic commit covering both tasks.

## Files Created/Modified
- `installer/postinstall.py` — `_GRAPHIFY_HOST_SUBCOMMANDS`, `_graphify_register`; `POSTINSTALL_HOOKS` gains `"graphify-register"`.
- `installer/model.py` — `POSTINSTALL_HOOK_NAMES` gains `"graphify-register"`.
- `installer/registry.toml` — `graphify`'s `[[tool]]` block gains `postinstall = "graphify-register"` plus a dated `# Verified` comment recording the 4-host adversarial-merge-safety evidence.
- `tests/test_postinstall.py` — full graphify hook test suite mirroring the rtk/codegraph pattern (no-op, absolute-bin-path resolution, cursor-agent→cursor mapping, error handling, no-double-invoke, 16-subset CSV-never test explicitly naming Pitfall 4).
- `tests/test_registry.py` — `test_only_codegraph_and_rtk_declare_a_postinstall_hook` → `test_only_codegraph_rtk_and_graphify_declare_a_postinstall_hook`.

## Decisions Made
- No deviation from the plan's own `_graphify_register` skeleton — it matched the real CLI exactly once tested without the `--help` red herring.
- Assumption A2 closed with a clean result: no merge-safety regression found on any of the three previously-untested hosts. Not flagged as an open finding for Plan 04's audit since no failure occurred.

## Deviations from Plan

None — plan executed exactly as written, once the `--help` mis-test was self-corrected before any code was written (documented above for transparency, not counted as a deviation since no incorrect code shipped).

## Issues Encountered
None.
