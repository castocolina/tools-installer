---
phase: 12.4-tool-onboarding-research-skill-and-registry-postinstall-audi
plan: 01
type: execute
wave: 1
cross_ai: true
depends_on: []
files_modified:
  - installer/postinstall.py
  - installer/model.py
  - installer/registry.toml
  - tests/test_postinstall.py
  - tests/test_model.py
autonomous: true
requirements: [D-02, D-03, D-01a]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A shared present_agent_hosts() helper computes agent-host presence exactly once per call site, and _codegraph_mcp_register uses it (not its own inline loop) with zero change to codegraph's existing observable behavior — all 11 existing codegraph tests in tests/test_postinstall.py pass unmodified."
    - "After rtk installs on a machine where claude is present, rtk's postinstall hook writes claude's RTK.md/settings.json hook non-interactively, preserving any pre-existing permissions/hooks/statusLine byte-for-byte, and is idempotent on a second run — proven live against the exact rtk version (v0.49.0, released 2026-09-11) the registry actually resolves to at install time, in an isolated scratch $HOME, never the real machine's ~/.claude/."
    - "rtk's postinstall hook never fabricates a --target auto-equivalent default: a host absent from present_agent_hosts() never receives an rtk init call."
  artifacts:
    - installer/postinstall.py (present_agent_hosts, _rtk_register, _resolve_rtk_binary, POSTINSTALL_HOOKS gains 'rtk-register')
    - installer/model.py (POSTINSTALL_HOOK_NAMES gains 'rtk-register')
    - "installer/registry.toml (rtk [[tool]] entry gains postinstall = \"rtk-register\" plus a dated # Verified comment recording this session's live v0.49.0 evidence)"
  key_links:
    - "installer/engine.py::install_tool's existing tool.postinstall dispatch (unchanged this plan) -> installer.postinstall.run_postinstall('rtk-register', ...) -> _rtk_register -> present_agent_hosts(tools) -> installer.status.is_installed"
    - "rtk [[tool.method]] kind (github_release ×3 / brew ×1, installer/registry.toml:1582-1614) -> _resolve_rtk_binary's method.kind branch -> the exact absolute rtk binary path invoked"
---

<objective>
Close half of D-02: wire `rtk`'s already-researched, non-interactive per-host registration commands into
`installer/postinstall.py`'s existing closed dispatch table, exactly like `codegraph`'s hook already does,
and extract the host-presence-detection logic both hooks need into one shared, reusable helper (D-03) so
`_codegraph_mcp_register` and the new `_rtk_register` — and `graphify`'s hook in Plan 12.4-02 — all read
host presence from the same place instead of each re-deriving it.

This is the phase's tracer: Task 1 touches every layer the whole phase modifies (the shared D-03 helper,
the registry `postinstall` field, `installer/model.py`'s closed-name validation, and `installer/postinstall.py`'s
dispatch table) on the thinnest real slice — the single `claude` host — proven with one real, live
invocation of a freshly-downloaded rtk v0.49.0 binary in an isolated scratch `$HOME`, never the real
machine's own `~/.claude/` (ONESHOT-RULES Rule 5). Task 2 expands `_rtk_register` to the remaining three
hosts (`opencode`, `codex`, `cursor-agent`), resolving 12.4-RESEARCH.md's Open Question 1 by re-running
its exact falsification test against the real v0.49.0 binary rather than trusting the stale, locally-cached
v0.44.1 result — "do not ship a guess" is 12.4-RESEARCH.md's own instruction for this exact task.

Purpose: rtk has been a fully installable catalog tool since Phase 8, but its own per-host setup commands
(researched then, confirmed unchanged in shape but drifted in exact flags this session) were never wired
into the postinstall mechanism Phase 9 built — this plan closes that half of the gap this whole phase
exists to close.
Output: `installer/postinstall.py` gains `present_agent_hosts()`, `_rtk_register()`, `_resolve_rtk_binary()`;
`installer/model.py`'s `POSTINSTALL_HOOK_NAMES` gains `"rtk-register"`; `installer/registry.toml`'s `rtk`
entry declares `postinstall = "rtk-register"`; a full TDD test suite in `tests/test_postinstall.py` mirroring
the existing 11-test codegraph pattern.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ONESHOT-RULES.md
@.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-CONTEXT.md
@.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-RESEARCH.md
@CLAUDE.md
@.claude/architecture.md
@.claude/testing.md
@installer/postinstall.py
@installer/model.py
@installer/status.py
@installer/locations.py
@installer/run.py
@tests/test_postinstall.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): D-03 shared present_agent_hosts() helper + rtk's simplest host (claude) wired end-to-end, live-proven against a fresh v0.49.0 binary</name>
  <precondition>Network access to github.com (Releases API and asset downloads) is reachable and `gh` is authenticated on this host — confirmed this session (`gh auth status`: logged in as castocolina). Required to download the real rtk v0.49.0 asset for live verification rather than trusting the stale locally-cached v0.44.1 binary.</precondition>
  <reversibility rating="reversible">Pure additive code (a new helper, a new hook, one new registry field) fully revisable via a follow-up commit; the live-verification evidence is recorded, not baked into an irreversible external state.</reversibility>
  <files>installer/postinstall.py, installer/model.py, installer/registry.toml, tests/test_postinstall.py</files>
  <read_first>
    - `installer/postinstall.py` (full file, 135 lines) — `_codegraph_mcp_register`'s inline host-presence
      loop at lines 84-88 (`present = [target for tool_id, target in _CODEGRAPH_TARGETS.items() if (tool :=
      tools.get(tool_id)) is not None and is_installed(tool)]`) is exactly what D-03 extracts; `POSTINSTALL_HOOKS`
      dict (line 113) and `run_postinstall` (line 118) are the dispatch shape `_rtk_register` plugs into
      unchanged.
    - `tests/test_postinstall.py` (full file, 257 lines) — the 11 existing codegraph tests are the literal
      template for this task's new tests (subset-matrix parametrization at line 162, missing-tool-id
      robustness at line 239, CommandError-to-warning-string at line 120, bin_dir branching at line 90).
    - `installer/model.py` lines 12-19 (`SMOKE_CHECK_NAMES`, `POSTINSTALL_HOOK_NAMES` — the closed-set
      frozenset this task adds `"rtk-register"` to) and lines 612-631 (`load_tools`'s `postinstall` field
      validation — unchanged by this task, just needs the new name present in the frozenset it checks
      against).
    - `installer/locations.py::bin_dir` (line 28) — `Path(declared).expanduser()` if given, else
      `~/.local/bin`; this is what `_resolve_rtk_binary` calls for the `github_release` branch, exactly
      mirroring `_codegraph_mcp_register`'s own `bin_dir(bin_dir_override) / "codegraph"` line.
    - `installer/registry.toml:1582-1614` (the `rtk` `[[tool]]` block) — four methods: `github_release` on
      macOS (no arch restriction), `github_release` on Linux amd64 (musl), `github_release` on Linux arm64
      (gnu), and a `brew` fallback. `_resolve_rtk_binary` must branch on `method.kind`, not assume
      `github_release` like codegraph's single-method-kind hook does.
    - 12.4-RESEARCH.md's "Pattern: `_rtk_register()` / `_graphify_register()`" and "Pitfall 5" sections —
      the exact per-host argv table and the adversarial-seeded-content methodology this task's live
      verification reproduces against the CURRENT v0.49.0 binary, not the cached v0.44.1 research already
      tested.
  </read_first>
  <action>
Part A — D-03 shared helper (refactor, no behavior change):

Add to `installer/postinstall.py`:
```
_AGENT_HOST_IDS: tuple[str, ...] = ("claude", "codex", "opencode", "cursor-agent")

def present_agent_hosts(tools: Mapping[str, Tool]) -> frozenset[str]:
    ...
```
computing, in one place, which of the four canonical agent-host catalog ids are present: `host_id in
result` iff `tools.get(host_id) is not None and is_installed(tools[host_id])`. A missing key behaves
exactly like a present-but-not-installed key (excluded, never a `KeyError`) — this generalizes the
existing `test_codegraph_hook_treats_a_missing_tool_id_as_absent_not_a_crash` guarantee to the shared
helper itself.

Refactor `_codegraph_mcp_register` to call `present_agent_hosts(tools)` and then build ITS OWN target CSV
by iterating `_CODEGRAPH_TARGETS` (unchanged, stays local — codegraph's `cursor-agent` -> `"cursor"` id
mapping is codegraph-specific vocabulary, not shared) and checking membership in the returned frozenset,
in the same declared order as today. The composed CSV, the `--target`/`--location`/`--yes`/`--no-permissions`
argv, and every existing codegraph test's expected output must be byte-identical to before this refactor —
this is a pure extraction, not a behavior change. Do NOT rename or move `_CODEGRAPH_TARGETS`.

RED: add `test_present_agent_hosts_*` tests to `tests/test_postinstall.py` calling `pi.present_agent_hosts`
directly (it does not exist yet — this fails) covering: an empty `tools` mapping returns an empty
frozenset; a `tools` mapping with all four hosts installed (monkeypatched `is_installed` returning `True`)
returns all four; a mapping missing one host id entirely behaves identically to that host being present-but-
not-installed; the return type is `frozenset`, not `list` or `set` (contract: unordered, dedupable, matches
the existing `_ALL_HOSTS`/`_ALL_SUBSETS` fixture shape at the top of the test file). GREEN: implement the
helper and the codegraph refactor; run the FULL existing codegraph test class (all 11 pre-existing tests,
unmodified) plus the new helper tests — all must pass. This is the tracer's core proof: the refactor is
behavior-preserving.

Part B — rtk's simplest host (claude), wired end-to-end and live-proven:

Add to `installer/postinstall.py`:
```
_RTK_HOST_ARGS: dict[str, tuple[str, ...]] = {
    "claude": ("-g", "--auto-patch"),
}

def _resolve_rtk_binary(method: Method) -> str:
    if method.kind == "brew":
        found = shutil.which("rtk")
        if found:
            return found
    bin_dir_param = method.params.get("bin_dir")
    override = bin_dir_param if isinstance(bin_dir_param, str) and bin_dir_param else None
    return str(bin_dir(override) / "rtk")

def _rtk_register(method: Method, runner: Runner, tools: Mapping[str, Tool]) -> str | None:
    present = present_agent_hosts(tools)
    if not present:
        return None
    rtk_bin = _resolve_rtk_binary(method)
    errors: list[str] = []
    for host_id in _RTK_HOST_ARGS:
        if host_id not in present:
            continue
        try:
            runner([rtk_bin, "init", *_RTK_HOST_ARGS[host_id]])
        except CommandError as exc:
            errors.append(f"{host_id}: {exc}")
    return "; ".join(errors) if errors else None
```
`_RTK_HOST_ARGS` in this task has ONLY the `claude` entry — Task 2 expands it to all four hosts. `shutil`
needs a new import at the top of `installer/postinstall.py`. Register `"rtk-register": _rtk_register` in
`POSTINSTALL_HOOKS`. Add `"rtk-register"` to `installer/model.py`'s `POSTINSTALL_HOOK_NAMES` frozenset
(alongside the existing `"codegraph-mcp-register"`, comma-separated — this keeps
`test_postinstall_hook_names_matches_the_real_dispatch_table` passing since it asserts the frozenset equals
`POSTINSTALL_HOOKS.keys()`). Add `postinstall = "rtk-register"` inside `installer/registry.toml`'s `rtk`
`[[tool]]` block (after `desc`, before the first `[[tool.method]]`, matching codegraph's own field position).

RED (for `_rtk_register`): add tests mirroring codegraph's pattern — `_resolve_rtk_binary` for both
`github_release` (resolves via `bin_dir`) and `brew` (resolves via `shutil.which`, monkeypatched) method
kinds; `_rtk_register` composes `[rtk_bin, "init", "-g", "--auto-patch"]` when `claude` is present, no-ops
(`calls == []`) when absent, returns a warning string (not a raised exception) on `CommandError`. GREEN:
implement, run green.

Live verification (production evidence, not a pytest test — run directly via Bash, record the transcript
in this task's commit message / the plan's SUMMARY):
1. Download the REAL current release, not the locally-cached v0.44.1: `gh release download v0.49.0 --repo
   rtk-ai/rtk --pattern 'rtk-x86_64-unknown-linux-musl.tar.gz' --dir <scratch-download-dir>` (scratch dir via
   `mktemp -d`), extract to get the raw `rtk` binary, `chmod +x`. Cross-check the downloaded tarball's
   sha256 against that same release's `checksums.txt` asset (download it too) before running the binary —
   the same integrity bar `installer/checksums.py` already applies to a real install, applied here by hand
   since this is a scratch verification run, not a registry-mediated install.
2. Create an isolated scratch home: `SCRATCH_HOME=$(mktemp -d)`. **Never use the real `$HOME` for any of
   the following — ONESHOT-RULES Rule 5 is non-negotiable: no command in this task may write into this
   machine's real `~/.claude/`, `~/.myshellrc`, or any other real dotfile.**
3. Run `HOME="$SCRATCH_HOME" <scratch-rtk-binary> init -g --auto-patch`. Confirm exit 0. Confirm
   `$SCRATCH_HOME/.claude/RTK.md` and `$SCRATCH_HOME/.claude/settings.json` now exist with rtk's hook block.
4. Re-run the identical command a second time against the SAME `$SCRATCH_HOME` (idempotency, mirroring
   `REQ-postinstall-idempotency-live-check`'s existing convention): confirm exit 0 and that the hook string
   in `settings.json` appears exactly once (not duplicated).
5. Adversarial seeded-content re-run (D-01a's real proof, reproducing 12.4-RESEARCH.md's Pitfall 5
   methodology against the CURRENT v0.49.0 build rather than trusting the cached v0.44.1 result): seed a
   FRESH `$SCRATCH_HOME/.claude/settings.json` with a pre-existing `permissions.allow` entry, an unrelated
   `PreToolUse` hook on a different matcher, and a custom `statusLine`, then run step 3's command once more.
   Confirm all three pre-existing elements survive byte-for-byte alongside rtk's own added hook.
6. Record in the task's own commit message / SUMMARY: the exact rtk version tested (`<scratch-rtk-binary>
   --version` — expect `rtk 0.49.0`), the tarball's sha256 (cross-checked against `checksums.txt`), and the
   pass/fail result of each of steps 3-5. This is the evidence base for `must_haves.truths`'s live-proof
   claim — a bare assertion without this transcript does not satisfy it.
7. Add a `# Verified {today's date}: ...` comment directly above `rtk`'s `[[tool]]` block in
   `installer/registry.toml` (the project's own registry-authoring convention, `.claude/architecture.md`'s
   "Per-tool, per-OS verification checklist") recording that the `claude` host's registration was
   live-verified against v0.49.0 in an isolated scratch `$HOME`, merge-safe against pre-existing config,
   and idempotent on rerun.
  </action>
  <verify>
    <automated>uv run pytest tests/test_postinstall.py tests/test_model.py -q</automated>
    <fails_when>non-zero exit; any of the 11 pre-existing codegraph tests fails or is missing; a new `present_agent_hosts`/`_rtk_register`/`_resolve_rtk_binary` test fails; or `test_postinstall_hook_names_matches_the_real_dispatch_table` fails because `POSTINSTALL_HOOK_NAMES` and `POSTINSTALL_HOOKS.keys()` disagree.</fails_when>
  </verify>
  <done>
`present_agent_hosts()` exists, is used by `_codegraph_mcp_register` (zero behavior change, all 11 existing
codegraph tests pass unmodified), and `_rtk_register` is wired end-to-end for the `claude` host — registry
field, model validation, dispatch table — with a real, evidenced live run against a freshly downloaded rtk
v0.49.0 binary in an isolated scratch `$HOME` proving idempotency and adversarial merge-safety, recorded as
both a commit-message transcript and a dated registry comment.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand _rtk_register to opencode/codex/cursor-agent, resolving Pitfall 1 (cursor-implies-claude) against the real v0.49.0 binary</name>
  <precondition>Network access to github.com is reachable and `gh` is authenticated (same as Task 1) — required to re-download or reuse the v0.49.0 asset for this task's own live falsification tests.</precondition>
  <reversibility rating="reversible">The cursor-agent gating design (unconditional vs. claude-gated) is a code branch, backed by this task's own live evidence, fully revisable in a follow-up commit if the upstream behavior changes again.</reversibility>
  <files>installer/postinstall.py, tests/test_postinstall.py</files>
  <read_first>
    - 12.4-RESEARCH.md's "Pitfall 1", "Pitfall 2", "Pitfall 3", and "Open Question 1" sections in full —
      the exact reproduction steps this task re-runs against v0.49.0, and the two honest design options
      Pitfall 1 names. **Option (a) — "always ensure ~/.claude/ exists before calling the cursor
      branch" — is REJECTED outright by this task, regardless of what the live re-test shows**: it writes
      Claude-Code-specific scaffolding on a machine that may never have selected or installed Claude Code,
      which is exactly the D-01a violation ("detect and configure, never blind-overwrite... never
      configure an unselected host") this whole phase exists to prevent. The only two admissible outcomes
      are: the bug is gone in v0.49.0 (ship the cursor-agent branch unconditionally, like the other three
      hosts) or the bug persists (ship Option (b): the cursor-agent branch only fires when `claude` is
      ALSO in `present_agent_hosts(tools)`'s returned set).
    - `installer/locations.py::ensure_dir` (line 77) — `directory.mkdir(parents=True, exist_ok=True)`,
      already imported nowhere in `installer/postinstall.py` yet; Pitfall 3 recommends this exact call
      before the cursor-agent branch invokes rtk, since a fresh `~/.cursor/` may not exist yet even when
      `cursor-agent` itself is genuinely installed.
    - Task 1's `_RTK_HOST_ARGS`/`_rtk_register`/`_resolve_rtk_binary` (just written) — this task extends
      the dict and, if Option (b) applies, adds one gating check inside the existing per-host loop; it does
      not restructure the function.
  </read_first>
  <action>
Live re-verification against v0.49.0 (reuse Task 1's downloaded binary if the scratch dir survived this
session, otherwise re-download the same `rtk-x86_64-unknown-linux-musl.tar.gz` v0.49.0 asset the same way —
never the cached v0.44.1). All of the following run with `HOME` overridden to a fresh `mktemp -d` scratch
directory each time — never the real machine's `$HOME` (ONESHOT-RULES Rule 5):

1. **Pitfall 1 falsification, scenario A (no pre-existing `.claude/`):** in a scratch `$HOME` containing
   `.cursor/` (`mkdir -p "$SCRATCH_HOME/.cursor"`) but no `.claude/` at all, run
   `HOME="$SCRATCH_HOME" <rtk-binary> init -g --agent cursor --auto-patch`. Record the exit code and
   whether `$SCRATCH_HOME/.claude/` was created as a side effect (the exact reproduction 12.4-RESEARCH.md
   performed against v0.44.1).
2. **Pitfall 1 falsification, scenario B (pre-existing `.claude/`):** in a fresh scratch `$HOME` with BOTH
   `.claude/` (seeded via a prior `rtk init -g --auto-patch` run, or a minimal manual seed) and `.cursor/`
   present, run the same cursor command. Confirm whether rtk's own "RTK hook registered (global)... Claude
   ... settings.json: hook already present" block still prints before the Cursor-targeted block (the
   confirming half of the original v0.44.1 reproduction).
3. **Pitfall 2 falsification (codex + --auto-patch rejection):** in a fresh scratch `$HOME`, run
   `HOME="$SCRATCH_HOME" <rtk-binary> init -g --codex --auto-patch` and confirm it exits non-zero with the
   "cannot be combined with --auto-patch" error; then run `HOME="$SCRATCH_HOME" <rtk-binary> init -g
   --codex` (no auto-patch) and confirm exit 0.
4. Record the verbatim output of all three checks in this task's commit message / the plan's SUMMARY —
   this is the evidence `must_haves` and the phase's own `<verification>` depend on.

Based on step 1's result, implement exactly one of the two admissible branches in `_RTK_HOST_ARGS` /
`_rtk_register` (never Option (a) — see `<read_first>`):

- **If the bug is gone** (no `~/.claude/` side effect in scenario A): extend `_RTK_HOST_ARGS` with all
  three remaining hosts unconditionally:
  ```
  _RTK_HOST_ARGS: dict[str, tuple[str, ...]] = {
      "claude": ("-g", "--auto-patch"),
      "opencode": ("-g", "--opencode", "--auto-patch"),
      "codex": ("-g", "--codex"),
      "cursor-agent": ("-g", "--agent", "cursor", "--auto-patch"),
  }
  ```
  and add a `# Verified {date}: ...` line to the registry comment (from Task 1) recording that v0.49.0 no
  longer reproduces the cursor-implies-claude write.
- **If the bug persists:** extend `_RTK_HOST_ARGS` the same way, but add one explicit gate inside
  `_rtk_register`'s per-host loop: `if host_id == "cursor-agent" and "claude" not in present: continue` —
  skip the cursor-agent branch (no error, no warning — a documented, silent narrowing, exactly mirroring
  this project's own precedent for rtk's weaker Codex integration) whenever `claude` is absent from
  `present_agent_hosts(tools)`'s result, even if `cursor-agent` itself is present. Before the branch that
  DOES fire the cursor-agent call, call `ensure_dir(Path.home() / ".cursor")` (Pitfall 3's defensive fix —
  cheap, non-destructive, only runs when the branch is actually about to execute) to guard the
  `.cursor/`-must-pre-exist gap. Add a `# Verified {date}: ...` line to the registry comment recording the
  exact v0.49.0 finding and the claude-gated design it drove.

Either way, `codex`'s branch never receives `--auto-patch` (Pitfall 2, confirmed above) and `opencode`'s
branch keeps `--opencode --auto-patch` per 12.4-RESEARCH.md's table (untested by this task's own live
re-run since Pitfall 1/2 are the only two flagged-uncertain behaviors — `opencode`'s syntax is otherwise
confirmed stable since Phase 8).

RED: extend `tests/test_postinstall.py` with a full subset-matrix test mirroring
`test_codegraph_hook_matches_expected_argv_for_every_host_presence_subset` (parametrize over the 16 subsets
of the four hosts), asserting: `opencode`'s composed argv includes `--opencode --auto-patch`; `codex`'s
composed argv NEVER contains `--auto-patch`; `cursor-agent`'s composed argv matches whichever gating design
was chosen (either fires whenever `cursor-agent` is present, or only fires when `claude` is ALSO present —
add a dedicated test for the claude-absent-cursor-agent-present case proving the chosen behavior, whichever
it is); `_rtk_register` never invokes the same host twice; a `CommandError` on one host's call is captured
as a partial warning string while the loop continues to the remaining hosts (mirroring
`_codegraph_mcp_register`'s single-CSV-call shape does NOT apply here — Pitfall 4 — each host gets its own
`runner()` call). If the claude-gated design is used, add a test monkeypatching `installer.postinstall.ensure_dir`
to assert it is called exactly once, with `Path.home() / ".cursor"`, only on the branch that actually invokes
rtk for cursor-agent. GREEN: implement, run green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_postinstall.py -q && uv run pytest tests/test_model.py -q</automated>
    <fails_when>non-zero exit from either command; the subset-matrix test fails for any of the 16 host-presence combinations; a test asserts `--auto-patch` appears in the composed codex argv; or (when the claude-gated design applies) a test proves the cursor-agent branch fires with claude absent.</fails_when>
  </verify>
  <done>
`_rtk_register` handles all four hosts with the argv table 12.4-RESEARCH.md documents, `codex` never
receives `--auto-patch`, and the cursor-agent branch's design is driven by this task's OWN live
re-verification against the real v0.49.0 binary (not the stale v0.44.1 research result) — Option (a) is
never present in the shipped code. Full TDD test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer/postinstall.py hook <-> real agent-host config files (`~/.claude/*`, `~/.cursor/*`) | The hook decides WHICH hosts get written to; a wrong decision writes config for a host the user never selected. |
| This task's live-verification scratch runs <-> the real dev machine's real `$HOME` | Every live-verification command in this plan must run under an isolated `$HOME` override; a mistake here would mutate this machine's real Claude/Cursor config. |
| GitHub Releases <-> the freshly-downloaded rtk v0.49.0 binary used for verification | The binary is fetched and run outside the registry's own checksum-verified install path, for investigative purposes only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12.4-01 | Tampering | `_rtk_register`'s cursor-agent branch (Pitfall 1) | high | mitigate | Task 2 re-verifies the exact behavior against the real v0.49.0 binary before shipping; Option (a) — the blind-scaffold workaround — is explicitly rejected in the plan text regardless of the live result; the claude-gated Option (b) is the only fallback if the bug persists. |
| T-12.4-02 | Elevation of Privilege | Over-broad host registration (writing config for an absent host) | critical | mitigate | `present_agent_hosts()` (D-03) is the single point deciding which hosts get touched, gated on live `is_installed`, never a tool's own default-target behavior — the same discipline Phase 9 established for codegraph's `--target auto` risk. |
| T-12.4-03 | Tampering (supply chain) | Freshly downloaded rtk v0.49.0 binary used only for this plan's live verification | medium | mitigate | Downloaded tarball's sha256 is cross-checked against that same release's own `checksums.txt` before execution; the binary never touches the real `$HOME`; `rtk` itself was already legitimacy-verified in Phase 8 and is not a new package. |
| T-12.4-04 | Repudiation | Claiming live verification happened without evidence | medium | mitigate | Task 1/Task 2 both require recording the verbatim command transcript and exit codes in the commit message / SUMMARY, plus a dated `# Verified` registry comment — not a bare assertion. |

No package-manager install tasks exist in this plan (rtk is already a legitimacy-verified Phase 8 catalog
entry; the v0.49.0 download is a scratch-only verification artifact, never installed via `installer/`'s own
executors) — the Package Legitimacy Gate does not apply.
</threat_model>

<verification>
Run `uv run pytest tests/test_postinstall.py tests/test_model.py -q` — all pre-existing codegraph tests plus
every new `present_agent_hosts`/`_rtk_register`/`_resolve_rtk_binary` test green. Confirm
`installer/registry.toml`'s `rtk` entry declares `postinstall = "rtk-register"` and carries a dated
`# Verified` comment. Confirm the commit message / SUMMARY records the live-verification transcript
(exit codes, file-existence checks, idempotency and adversarial-merge-safety results) against the real
v0.49.0 binary, and that no command in either task's execution touched this machine's real `$HOME`.
</verification>

<success_criteria>
- `present_agent_hosts()` exists and is the single source of host-presence truth for `_codegraph_mcp_register`
  and `_rtk_register`.
- `_codegraph_mcp_register`'s observable behavior is unchanged (all 11 pre-existing tests pass unmodified).
- `_rtk_register` is fully wired (registry field, model validation, dispatch table) for all four agent hosts,
  with `codex` never receiving `--auto-patch` and the cursor-agent branch's design backed by fresh live
  evidence against v0.49.0.
- No command run during this plan's execution mutated this machine's real `$HOME`.
- `uv run pytest tests/test_postinstall.py tests/test_model.py -q` is green.
</success_criteria>

<output>
Create `.planning/phases/12.4-tool-onboarding-research-skill-and-registry-postinstall-audi/12.4-01-SUMMARY.md` when done
</output>
